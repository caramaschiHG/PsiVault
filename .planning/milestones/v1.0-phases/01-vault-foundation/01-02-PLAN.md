---
phase: 01-vault-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/app/(vault)/setup/page.tsx
  - src/app/(vault)/setup/actions.ts
  - src/app/(vault)/setup/components/setup-checklist.tsx
  - src/app/(vault)/setup/components/setup-step-card.tsx
  - src/app/(vault)/settings/profile/page.tsx
  - src/lib/setup/readiness.ts
  - src/lib/setup/profile.ts
  - src/lib/setup/constants.ts
  - prisma/schema.prisma
  - tests/setup-readiness.test.ts
autonomous: true
requirements:
  - PROF-01
  - PROF-02

must_haves:
  truths:
    - "The first authenticated destination is a dedicated setup hub, not the normal dashboard."
    - "The setup hub behaves like a guided checklist with visible readiness state."
    - "The vault is not marked ready until identity, CRP, and core practice data are completed."
    - "Signature assets can be uploaded, replaced, and stored in the professional profile in Phase 1, while remaining optional for vault readiness until document features arrive."
    - "Default appointment duration, session price, and core service preferences are captured in the professional setup model."
  artifacts:
    - path: "src/lib/setup/readiness.ts"
      provides: "Single source of truth for setup completion state"
    - path: "src/app/(vault)/setup/page.tsx"
      provides: "Dedicated setup hub experience and checklist shell"
    - path: "src/lib/setup/profile.ts"
      provides: "Professional profile persistence including signature-asset storage metadata and future document reuse hooks"
    - path: "tests/setup-readiness.test.ts"
      provides: "Automated verification for profile completeness and default settings logic"
  key_links:
    - from: "src/lib/setup/readiness.ts"
      to: "src/app/(vault)/setup/page.tsx"
      via: "checklist progress and block/unblock behavior"
      pattern: "vault_ready"
    - from: "src/lib/setup/profile.ts"
      to: "prisma/schema.prisma"
      via: "professional profile and practice defaults persistence"
      pattern: "practice_profile"
---

<objective>
Build the dedicated setup hub and professional profile/defaults flow that makes the vault feel intentionally prepared before later patient and document features exist.

Purpose: the user explicitly chose a guided checklist and a dedicated setup hub. This plan turns the authenticated shell from “bare account” into “practice identity readying the vault.”

Output:
- dedicated setup hub with checklist and progress state
- professional profile and practice defaults persistence
- signature-asset upload/edit persistence in the professional profile
- readiness logic for required vs optional setup items
- automated verification of profile completeness and defaults
</objective>

<execution_context>
@/home/caramaschi/.codex/get-shit-done/workflows/execute-plan.md
@/home/caramaschi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-vault-foundation/01-CONTEXT.md
@.planning/phases/01-vault-foundation/01-RESEARCH.md
@.planning/phases/01-vault-foundation/01-VALIDATION.md
@.planning/phases/01-vault-foundation/01-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define setup readiness rules and test them</name>
  <files>src/lib/setup/readiness.ts, src/lib/setup/constants.ts, tests/setup-readiness.test.ts</files>
  <action>
Implement the readiness contract for the setup hub:
- required now: full name, CRP, core contact data, and core practice defaults
- optional now but relevant later: signature assets
- dedicated progress states that the UI can consume directly

Write the tests first or alongside the implementation so the readiness logic stays explicit and reusable in later phases.
  </action>
  <verify>
    <automated>pnpm vitest run tests/setup-readiness.test.ts</automated>
  </verify>
  <done>The readiness test suite proves the difference between required setup, optional-later fields, and vault-ready status.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build the dedicated setup hub and guided checklist UI</name>
  <files>src/app/(vault)/setup/page.tsx, src/app/(vault)/setup/components/setup-checklist.tsx, src/app/(vault)/setup/components/setup-step-card.tsx</files>
  <action>
Create the setup hub the user lands on after secure access. It should read as “prepare your vault,” not “complete enterprise admin settings.” Use the readiness contract to drive visible progress, blocked/unblocked steps, and calm instructional copy.
  </action>
  <verify>
    <automated>pnpm vitest run tests/setup-readiness.test.ts -t "setup hub state"</automated>
  </verify>
  <done>The setup hub renders the checklist/progress model and clearly communicates required vs later-optional items.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Persist professional identity and practice defaults</name>
  <files>src/app/(vault)/settings/profile/page.tsx, src/app/(vault)/setup/actions.ts, src/lib/setup/profile.ts, prisma/schema.prisma, tests/setup-readiness.test.ts</files>
  <action>
Implement the actual profile/defaults persistence layer and form handling for:
- professional identity and CRP
- core contact information
- default appointment duration
- default session price
- service preferences needed by later phases

Implement real signature-asset handling in Phase 1:
- upload/store signature asset metadata and file reference
- allow replacing/editing the stored signature asset
- expose the stored asset through the professional profile service so later document plans can reuse it directly

Keep signature assets optional for overall vault readiness, but not merely “modeled.”
  </action>
  <verify>
    <automated>pnpm vitest run tests/setup-readiness.test.ts -t "profile completeness" -t "practice defaults" -t "signature asset"</automated>
  </verify>
  <done>Professional profile, defaults, and signature-asset storage/edit flows work and feed the setup readiness state correctly.</done>
</task>

</tasks>
