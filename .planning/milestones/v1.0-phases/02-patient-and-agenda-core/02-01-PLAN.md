---
phase: 02-patient-and-agenda-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/patients/model.ts
  - src/lib/patients/repository.ts
  - src/lib/patients/summary.ts
  - src/lib/patients/audit.ts
  - src/app/(vault)/patients/page.tsx
  - src/app/(vault)/patients/archive/page.tsx
  - src/app/(vault)/patients/[patientId]/page.tsx
  - src/app/(vault)/patients/actions.ts
  - src/app/(vault)/patients/components/patient-form.tsx
  - src/app/(vault)/patients/components/patient-profile-header.tsx
  - src/app/(vault)/patients/components/patient-summary-cards.tsx
  - tests/patient-domain.test.ts
  - tests/patient-summary.test.ts
autonomous: true
requirements:
  - PATI-01
  - PATI-02
  - PATI-03
  - PATI-04

must_haves:
  truths:
    - "A professional can create a patient from an essentials-first flow, with social name, guardian, emergency contact, and observations available only when relevant."
    - "Patients remain workspace-scoped and privacy-safe in secondary surfaces, with sensitive observations excluded from lists and other glanceable views."
    - "Archiving a patient removes them from active patient flows without deleting linked history, and recovery returns the professional directly to the restored profile."
    - "The patient profile opens with identity first and immediately below it exposes an operational summary for last session, next session, pending items, document count, and financial status."
  artifacts:
    - path: "src/lib/patients/model.ts"
      provides: "Canonical patient aggregate shape, optional-field policy, and archive semantics"
    - path: "src/lib/patients/summary.ts"
      provides: "Stable patient operational summary contract with fallback states that later scheduling work can hydrate"
    - path: "src/app/(vault)/patients/[patientId]/page.tsx"
      provides: "Identity-first patient profile surface with summary block in the first screenful"
    - path: "tests/patient-domain.test.ts"
      provides: "Automated coverage for patient creation, optional fields, archive, and recovery behavior"
    - path: "tests/patient-summary.test.ts"
      provides: "Automated coverage for operational summary derivation and fallback states"
  key_links:
    - from: "src/lib/patients/model.ts"
      to: "src/lib/patients/repository.ts"
      via: "workspace-scoped patient persistence and soft-archive queries"
      pattern: "active_vs_archived_patient"
    - from: "src/lib/patients/summary.ts"
      to: "src/app/(vault)/patients/components/patient-summary-cards.tsx"
      via: "stable summary view model consumed by the patient profile UI"
      pattern: "patient_operational_summary"
    - from: "tests/patient-domain.test.ts"
      to: "src/app/(vault)/patients/actions.ts"
      via: "patient create/archive/recover mutation expectations"
      pattern: "archive_and_restore_patient"
---

<objective>
Establish the patient aggregate and the first patient-facing vault surfaces: intake, archive/recover, and identity-first profile summary.

Purpose: Phase 2 begins by making patient records real and trustworthy before scheduling logic depends on them. This plan creates the patient domain contract, workspace-scoped persistence, archive semantics, privacy-safe selectors, and the summary surface that later agenda, clinical, document, and finance work will reuse.

Output:
- patient schema, repository, and audit helpers
- essentials-first patient create/edit/archive/recover flows
- active and archived patient views with privacy-safe list shapes
- identity-first patient profile with operational summary contract and fallback states
- automated tests for patient domain behavior and summary contract/fallback behavior
</objective>

<execution_context>
@/home/caramaschi/.codex/get-shit-done/workflows/execute-plan.md
@/home/caramaschi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-patient-and-agenda-core/02-CONTEXT.md
@.planning/phases/02-patient-and-agenda-core/02-RESEARCH.md
@.planning/phases/02-patient-and-agenda-core/02-VALIDATION.md
@.planning/phases/01-vault-foundation/01-02-PLAN.md
@.planning/phases/01-vault-foundation/01-03-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the patient aggregate, archive semantics, and persistence contract</name>
  <files>prisma/schema.prisma, src/lib/patients/model.ts, src/lib/patients/repository.ts, src/lib/patients/audit.ts, tests/patient-domain.test.ts</files>
  <action>
Add the Phase 2 patient model as a workspace-scoped aggregate with:
- essential identity and contact fields for the core intake flow
- optional social name, guardian, emergency contact, and important observations fields
- soft archive behavior through explicit archive metadata instead of deletion

Keep the repository contract explicit about active vs archived selectors so later scheduling logic can safely exclude archived patients without losing history. Wire patient lifecycle audit helpers using the existing Phase 1 audit contract instead of inventing a parallel event style.
  </action>
  <verify>
    <automated>pnpm vitest run tests/patient-domain.test.ts -t "create patient profile" -t "optional patient fields" -t "archive and recover"</automated>
  </verify>
  <done>The patient schema and repository exist, archive is soft and reversible, and the patient domain tests prove create/optional/archive/recover behavior.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build patient intake, active/archive views, and recovery actions</name>
  <files>src/app/(vault)/patients/page.tsx, src/app/(vault)/patients/archive/page.tsx, src/app/(vault)/patients/actions.ts, src/app/(vault)/patients/components/patient-form.tsx, src/lib/patients/repository.ts, tests/patient-domain.test.ts</files>
  <action>
Implement the actual patient-facing flows for:
- creating and editing a patient from an essentials-first form
- progressively revealing social name, guardian, emergency contact, and important observations only when relevant
- listing active patients in a privacy-safe way
- moving archived patients to a dedicated archive surface with explicit restore

Do not let archive behave like a destructive delete. The restore action must reactivate the patient and return the user to the recovered profile rather than leaving them in a generic list.
  </action>
  <verify>
    <automated>pnpm vitest run tests/patient-domain.test.ts -t "create patient profile" -t "optional patient fields" -t "archive and recover"</automated>
  </verify>
  <done>The patient intake and archive views work against the repository contract, and archived patients leave active flows while remaining recoverable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Define the patient operational summary contract and identity-first profile shell</name>
  <files>src/lib/patients/summary.ts, src/app/(vault)/patients/[patientId]/page.tsx, src/app/(vault)/patients/components/patient-profile-header.tsx, src/app/(vault)/patients/components/patient-summary-cards.tsx, tests/patient-summary.test.ts</files>
  <action>
Create the stable summary contract required by `PATI-03`:
- `lastSession`
- `nextSession`
- `pendingItemsCount`
- `documentCount`
- `financialStatus`

Make the patient profile lead with identity and place the operational summary immediately below it within the first screenful. In this plan, define and render the contract with safe fallback states so the summary surface is stable before the scheduling domain exists. Do not claim real appointment-derived `lastSession` or `nextSession` values yet; that hydration should land only after `02-02` establishes appointment occurrences.
  </action>
  <verify>
    <automated>pnpm vitest run tests/patient-summary.test.ts</automated>
  </verify>
  <done>The patient profile opens with identity first, the operational summary contract is defined and rendered, and the summary tests verify fallback behavior plus future-safe placeholders that later scheduling work can hydrate.</done>
</task>

</tasks>
