---
phase: 06-retrieval-recovery-and-launch-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/lib/dashboard/aggregation.ts
  - src/app/(vault)/inicio/page.tsx
  - src/app/(vault)/layout.tsx
  - src/app/(vault)/patients/[patientId]/page.tsx
  - src/app/(vault)/patients/[patientId]/components/reminders-section.tsx
  - src/app/(vault)/actions/reminders.ts
autonomous: false
requirements:
  - TASK-01
  - TASK-02
  - TASK-03
  - DASH-01
  - DASH-02

must_haves:
  truths:
    - "Professional lands on /inicio after login and sees today's sessions, active reminders, payment pendency count, and monthly snapshot numbers"
    - "Each section shows a friendly empty state with a quick-action shortcut when empty"
    - "No payment amounts appear on the dashboard — only a pending count badge linking to /financeiro (SECU-05 compliant)"
    - "Professional can create a reminder from the dashboard (Novo lembrete button)"
    - "Professional can create a reminder from the patient profile reminders section"
    - "Completing a reminder moves it to the Concluídas collapsed section — it is never deleted"
    - "Patient profile shows active reminders linked to that patient and a collapsible completed history"
  artifacts:
    - path: "src/lib/dashboard/aggregation.ts"
      provides: "filterTodayAppointments, countPendingCharges, deriveMonthlySnapshot utility functions"
      exports:
        - filterTodayAppointments
        - countPendingCharges
        - deriveMonthlySnapshot
    - path: "src/app/(vault)/inicio/page.tsx"
      provides: "InicioPage server component — aggregates 4 repos and renders dashboard"
    - path: "src/app/(vault)/actions/reminders.ts"
      provides: "createReminderAction, completeReminderAction server actions"
      exports:
        - createReminderAction
        - completeReminderAction
    - path: "src/app/(vault)/patients/[patientId]/components/reminders-section.tsx"
      provides: "RemindersSection client component with active list, completion toggle, and collapsed Concluídas"
  key_links:
    - from: "src/app/(vault)/inicio/page.tsx"
      to: "src/lib/dashboard/aggregation.ts"
      via: "filterTodayAppointments + countPendingCharges + deriveMonthlySnapshot called at page load"
      pattern: "filterTodayAppointments|countPendingCharges|deriveMonthlySnapshot"
    - from: "src/app/(vault)/patients/[patientId]/components/reminders-section.tsx"
      to: "src/app/(vault)/actions/reminders.ts"
      via: "createReminderAction and completeReminderAction passed as props"
      pattern: "createReminderAction|completeReminderAction"
    - from: "src/app/(vault)/layout.tsx"
      to: "/inicio"
      via: "Início nav link added as first nav item"
      pattern: "href=\"/inicio\""
---

<objective>
Build the /inicio dashboard page and the reminders domain UX surface on the patient profile.

Purpose: This is the primary orientation screen — the first thing the professional sees after login. It must surface today's sessions, active reminders, payment pendencies, and monthly snapshot numbers in a calm, actionable view. The patient profile gains a reminders section that completes TASK-01/TASK-03 at the UI layer.

Output: /inicio route, aggregation utilities (tested by Wave 0 scaffolds), reminder server actions, RemindersSection component on patient profile, /inicio added to main nav.
</objective>

<execution_context>
@/home/caramaschi/.claude/get-shit-done/workflows/execute-plan.md
@/home/caramaschi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-retrieval-recovery-and-launch-polish/06-CONTEXT.md
@.planning/phases/06-retrieval-recovery-and-launch-polish/06-RESEARCH.md
@.planning/phases/06-retrieval-recovery-and-launch-polish/06-01-SUMMARY.md

Key existing files:
- src/app/(vault)/layout.tsx — add /inicio to nav (first item)
- src/app/(vault)/agenda/page.tsx — today UTC range pattern + listByDateRange usage
- src/app/(vault)/patients/[patientId]/page.tsx — multi-repo load pattern for adding reminders
- src/lib/finance/model.ts — deriveFinancialStatus, deriveMonthlyFinancialSummary
- src/lib/finance/repository.ts — listByWorkspaceAndMonth (added in Plan 01)
- src/lib/patients/repository.ts — listActive, listArchived
- src/lib/reminders/store.ts — getReminderRepository (created in Plan 01)
- src/app/(vault)/appointments/actions.ts — revalidatePath pattern + server action structure
</context>

<interfaces>
<!-- Key contracts executor needs from Plan 01 output and existing codebase -->

From src/lib/reminders/model.ts (Plan 01 output):
```typescript
export interface Reminder {
  id: string; workspaceId: string; title: string;
  dueAt: Date | null; link: ReminderLink | null;
  completedAt: Date | null; createdAt: Date; updatedAt: Date;
}
export function createReminder(input: CreateReminderInput, deps): Reminder;
export function completeReminder(reminder: Reminder, deps: { now: Date }): Reminder;
```

From src/lib/reminders/repository.ts (Plan 01 output):
```typescript
export interface ReminderRepository {
  save(reminder: Reminder): void;
  findById(id: string, workspaceId: string): Reminder | null;
  listActive(workspaceId: string): Reminder[];
  listCompleted(workspaceId: string): Reminder[];
  listActiveByPatient(patientId: string, workspaceId: string): Reminder[];
  listCompletedByPatient(patientId: string, workspaceId: string): Reminder[];
}
```

From src/lib/finance/repository.ts (Plan 01 extended):
```typescript
listByWorkspaceAndMonth(workspaceId: string, year: number, month: number): SessionCharge[];
```

From src/lib/finance/model.ts (existing):
```typescript
export function deriveMonthlyFinancialSummary(charges: SessionCharge[]): MonthlyFinancialSummary;
// Returns: { totalSessions, completedSessions, totalAmountCents, receivedAmountCents, pendingAmountCents }
```

From src/app/(vault)/agenda/page.tsx (existing pattern):
```typescript
// UTC today range for dashboard today's sessions:
const today = new Date(); // server time
const from = new Date(Date.UTC(today.getUTCFullYear(), today.getUTCMonth(), today.getUTCDate()));
const to = new Date(from.getTime() + 24 * 60 * 60 * 1000);
// apptRepo.listByDateRange(WORKSPACE_ID, from, to)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dashboard aggregation utilities + /inicio page + nav link</name>
  <files>
    src/lib/dashboard/aggregation.ts,
    src/app/(vault)/inicio/page.tsx,
    src/app/(vault)/layout.tsx
  </files>
  <behavior>
    filterTodayAppointments(appointments, from, to): returns only appointments with startsAt in [from, to)
    countPendingCharges(charges): returns count of charges with status "pendente" or "atrasado" (NOT amounts — SECU-05)
    deriveMonthlySnapshot(allPatients, monthlyCharges, monthlyAppointments): returns { activePatientCount, completedSessionCount, receivedAmountCents, pendingAmountCents } — uses listActive length for active count, filters COMPLETED appointments for session count; NOTE: receivedAmountCents and pendingAmountCents are ONLY used in the Settings/export context — the dashboard ONLY exposes pendingChargeCount badge (no amounts displayed)
  </behavior>
  <action>
    Step 1 — Create src/lib/dashboard/aggregation.ts:
    Export three pure functions:
    - filterTodayAppointments(appointments: Appointment[], from: Date, to: Date): Appointment[] — filters by startsAt >= from && startsAt < to
    - countPendingCharges(charges: SessionCharge[]): number — counts status === "pendente" || status === "atrasado"
    - deriveMonthlySnapshot(input: { activePatients: Patient[]; monthlyCharges: SessionCharge[]; monthlyAppointments: Appointment[] }): MonthlySnapshot — MonthlySnapshot type: { activePatientCount: number; completedSessionCount: number; receivedAmountCents: number; pendingAmountCents: number }. completedSessionCount = input.monthlyAppointments.filter(a => a.status === "COMPLETED").length. receivedAmountCents and pendingAmountCents derived from deriveMonthlyFinancialSummary(monthlyCharges).

    These functions are the implementations for tests/dashboard-aggregation.test.ts — run `pnpm test -- --reporter=verbose tests/dashboard-aggregation.test.ts` after creating them. Tests must go GREEN.

    Step 2 — Create src/app/(vault)/inicio/page.tsx as a server component:
    - Stub: const WORKSPACE_ID = "ws_1"; // stub — replace with real session in production
    - Load today's appointments via getAppointmentRepository().listByDateRange(WORKSPACE_ID, from, to) where from/to = UTC today range
    - Load all patients via listActive + listArchived from getPatientRepository()
    - Build patientMap: Map<string, Patient> for name resolution (patientId -> patient)
    - Load active reminders via getReminderRepository().listActive(WORKSPACE_ID)
    - Load pending charge count: aggregate charges across ALL patients via getFinanceRepository().listByWorkspaceAndMonth(WORKSPACE_ID, year, month) — call countPendingCharges on result
    - Load monthly snapshot: derive from active patients count + monthlyCharges + this month's COMPLETED appointments
    - Render sections inline (no separate component files needed — page IS the server component):
      Section 1 — "Hoje" (today's sessions): list of { time, patientName, careMode } cards. Empty state: "Nenhuma sessão hoje — Agendar consulta" with link to /agenda.
      Section 2 — "Lembretes ativos" (active reminders): list of reminder cards with title, optional due date. "Novo lembrete" button (links to a modal or inline form — inline form below the list). Completed reminders trigger completeReminderAction. Empty state: "Nenhum lembrete ativo — Novo lembrete".
      Section 3 — "Resumo do mês" (monthly snapshot, DASH-02): active patients count, sessions completed this month, payment pendency count badge ("N cobranças em aberto") with link to /financeiro. NO amountInCents displayed — SECU-05. Show received/pending counts only as integers (e.g., "R$ ___" placeholder if amounts are needed — but per CONTEXT.md, no amounts on dashboard).
    - Use Intl.DateTimeFormat("pt-BR", { timeZone: "America/Sao_Paulo" }) for session time display
    - Inline form for "Novo lembrete": title input (required) + optional dueAt date input + hidden workspaceId. Action = createReminderAction. revalidatePath("/inicio") after creation.

    Step 3 — Update src/app/(vault)/layout.tsx:
    - Add <a href="/inicio" style={navLinkStyle}>Início</a> as the FIRST nav item (before Agenda)
    - Layout stays a server component — no "use client" added

    Step 4 — Create src/app/(vault)/actions/reminders.ts:
    "use server" file. Follows src/app/(vault)/appointments/actions.ts pattern.
    - createReminderAction(formData: FormData): creates reminder via createReminder factory, saves to getReminderRepository(), creates audit event, revalidatePath("/inicio") + revalidatePath for patient profile if patientId present
    - completeReminderAction(reminderId: string): finds reminder, calls completeReminder, saves, creates audit event, revalidatePath("/inicio")
    - Use crypto.getRandomValues buffer for ID generation (same pattern as all existing action files)

    Run `pnpm test -- --reporter=verbose tests/dashboard-aggregation.test.ts` — must be GREEN.
    Run `pnpm test` — full suite must pass.
  </action>
  <verify>
    <automated>pnpm test -- --reporter=verbose tests/dashboard-aggregation.test.ts</automated>
  </verify>
  <done>
    tests/dashboard-aggregation.test.ts all green. /inicio/page.tsx exists and renders all 3 sections. layout.tsx has "Início" as first nav link. actions/reminders.ts exports createReminderAction and completeReminderAction. No amountInCents exposed on dashboard. pnpm test full suite green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reminders section on patient profile</name>
  <files>
    src/app/(vault)/patients/[patientId]/components/reminders-section.tsx,
    src/app/(vault)/patients/[patientId]/page.tsx
  </files>
  <action>
    Step 1 — Create src/app/(vault)/patients/[patientId]/components/reminders-section.tsx:
    "use client" component. Receives props:
    - activeReminders: Reminder[]
    - completedReminders: Reminder[]
    - createReminderAction: (formData: FormData) => Promise<void>
    - completeReminderAction: (reminderId: string) => Promise<void>
    - patientId: string

    Renders:
    - Section heading: "Lembretes"
    - List of activeReminders: each card shows title + optional due date (formatted via Intl.DateTimeFormat pt-BR). A "Concluir" button calls completeReminderAction(reminder.id). Uses useTransition for optimistic loading state.
    - "Novo lembrete" inline form: title (required), optional dueAt date input. On submit, calls createReminderAction with FormData including patientId (as hidden input for link field) and workspaceId.
    - Collapsible "Concluídas" section: a disclosure/toggle button shows count of completed reminders. When expanded, renders completedReminders list (read-only, shows completedAt date). Never deleted — just collapsed.
    - Empty active state: "Nenhum lembrete para este paciente"

    Use React useState for the collapsed/expanded toggle of "Concluídas". No external libraries.

    Step 2 — Update src/app/(vault)/patients/[patientId]/page.tsx:
    - Import getReminderRepository from src/lib/reminders/store.ts
    - Add after existing repo loads:
      const reminderRepo = getReminderRepository();
      const activeReminders = reminderRepo.listActiveByPatient(patient.id, WORKSPACE_ID);
      const completedReminders = reminderRepo.listCompletedByPatient(patient.id, WORKSPACE_ID);
    - Import createReminderAction, completeReminderAction from src/app/(vault)/actions/reminders.ts
    - Render <RemindersSection> below DocumentsSection and above or below FinanceSection — use Claude's discretion on exact position (suggestion: below FinanceSection as the last section before clinical timeline)
    - Pass all required props
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <done>
    RemindersSection component exists with active list, inline creation form, completion action, and collapsible Concluídas. Patient profile page loads patient-scoped reminders from repository. pnpm test full suite green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify dashboard and reminders UX</name>
  <what-built>
    Dashboard /inicio page with today's sessions, active reminders, payment pendency count badge, and monthly snapshot numbers. Novo lembrete creation from dashboard. Reminders section on patient profile with completion flow and Concluídas history. "Início" nav link as first nav item.
  </what-built>
  <how-to-verify>
    1. Open the app and navigate to /inicio — confirm the page loads with all three sections visible.
    2. Each section should show an empty-state message if no data exists (e.g., "Nenhuma sessão hoje — Agendar consulta").
    3. Create a reminder from the dashboard — type a title and submit. The reminder should appear in "Lembretes ativos".
    4. Click "Concluir" on the reminder — it should disappear from the active list and appear under the collapsed "Concluídas" section.
    5. Navigate to a patient profile and confirm the "Lembretes" section appears. Create a patient-linked reminder — it should appear on both the patient profile AND the dashboard (since dashboard shows all active reminders).
    6. Confirm no payment amounts appear on the dashboard — only a count badge (e.g., "3 cobranças em aberto") with a link to /financeiro.
    7. Confirm "Início" appears as the first nav link in the navigation bar.
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe any issues to fix.</resume-signal>
  <action>Human verifies dashboard orientation screen and reminders UX in the browser.</action>
  <verify><automated>pnpm test</automated></verify>
  <done>Professional can see dashboard, create reminders, complete them, and view history. Approved by reviewer.</done>
</task>

</tasks>

<verification>
- pnpm test passes (full suite green)
- /inicio route exists and aggregates all 4 domains (appointments, reminders, finance, patients)
- SECU-05: no amountInCents on dashboard surface — only count badge
- Reminder completion uses completedAt timestamp — no delete calls
- Vault layout still a server component (no "use client" in layout.tsx)
- "Início" is the first nav item
- Intl.DateTimeFormat("pt-BR", { timeZone: "America/Sao_Paulo" }) used for session times
</verification>

<success_criteria>
Dashboard is the professional's morning orientation screen. Reminders can be created from two entry points (dashboard + patient profile) and completed without losing history. Monthly snapshot gives a quick practice health check. Payment pendency surfaces as a count only — vault posture preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/06-retrieval-recovery-and-launch-polish/06-02-SUMMARY.md`
</output>
