---
phase: 07-infrastructure-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env
autonomous: false
requirements:
  - INFRA-01

must_haves:
  truths:
    - "O arquivo .env contém DATABASE_URL apontando para o pooler Supavisor (porta 6543) com ?pgbouncer=true"
    - "O arquivo .env contém DIRECT_URL apontando para conexão direta (porta 5432)"
    - "O arquivo .env contém NEXT_PUBLIC_SUPABASE_URL e NEXT_PUBLIC_SUPABASE_ANON_KEY válidos"
  artifacts:
    - path: ".env"
      provides: "Credenciais reais do projeto Supabase para runtime e migração"
      contains: "DATABASE_URL, DIRECT_URL, NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY"
  key_links:
    - from: ".env"
      to: "prisma/schema.prisma"
      via: "datasource db { url = env(\"DATABASE_URL\"), directUrl = env(\"DIRECT_URL\") }"
      pattern: "dual connection strategy"
---

<objective>
Provisionar o projeto Supabase e preencher as credenciais reais no arquivo .env local.

Purpose: Sem credenciais reais, nenhum comando Prisma pode conectar ao banco. Esta etapa desbloqueia a migração e todos os planos de repositório das fases 8–11.

Output: Arquivo .env preenchido com DATABASE_URL (pooler), DIRECT_URL (direto) e chaves Supabase Auth.
</objective>

<execution_context>
@/home/caramaschi/.claude/get-shit-done/workflows/execute-plan.md
@/home/caramaschi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/milestones/v1.1-phases/07-infrastructure-foundation/07-CONTEXT.md
@.env.example
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Provisionar Supabase e preencher .env</name>
  <what-built>
O código de infraestrutura já está completo:
- prisma/schema.prisma: schema completo com todos os modelos, UUID PKs, snake_case mapping
- src/lib/db.ts: singleton globalThis correto para evitar connection exhaustion
- .env.example: template com todas as variáveis necessárias
- datasource db: configurado com url (pooler) e directUrl (direto)

O que falta é o projeto Supabase provisionado e as credenciais reais no .env.
  </what-built>
  <how-to-verify>
1. Acesse https://supabase.com/dashboard e crie um novo projeto (se ainda não existir)
   - Região: South America (São Paulo) — aws-0-sa-east-1
   - Anote a senha do banco de dados criada

2. No painel do projeto Supabase, vá em Settings > Database > Connection string
   - Copie a string do "Transaction pooler" (porta 6543) e adicione ?pgbouncer=true ao final
   - Copie a string do "Direct connection" (porta 5432)

3. No painel do projeto Supabase, vá em Settings > API
   - Copie "Project URL" (NEXT_PUBLIC_SUPABASE_URL)
   - Copie "anon public" key (NEXT_PUBLIC_SUPABASE_ANON_KEY)
   - Copie "service_role secret" key (SUPABASE_SERVICE_ROLE_KEY)

4. Preencha o arquivo .env na raiz do projeto (copie de .env.example e substitua os valores):

DATABASE_URL="postgres://postgres.[project-ref]:[senha]@aws-0-sa-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgres://postgres.[project-ref]:[senha]@aws-0-sa-east-1.pooler.supabase.com:5432/postgres"
NEXT_PUBLIC_SUPABASE_URL="https://[project-ref].supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="[anon key]"
SUPABASE_SERVICE_ROLE_KEY="[service role key]"
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="[qualquer string aleatória para dev]"
  </how-to-verify>
  <resume-signal>Digite "pronto" quando o .env estiver preenchido com credenciais reais do Supabase</resume-signal>
</task>

</tasks>

<verification>
Após o checkpoint, verificar manualmente que o arquivo .env não contém valores em branco:
grep -v "^#" .env | grep -v "^$" | grep "=$"  (deve retornar vazio — nenhuma variável vazia)
</verification>

<success_criteria>
O arquivo .env contém credenciais reais do Supabase:
- DATABASE_URL com host pooler (porta 6543) e ?pgbouncer=true
- DIRECT_URL com host direto (porta 5432)
- NEXT_PUBLIC_SUPABASE_URL e NEXT_PUBLIC_SUPABASE_ANON_KEY preenchidos
</success_criteria>

<output>
Após conclusão, criar `.planning/milestones/v1.1-phases/07-infrastructure-foundation/07-01-SUMMARY.md`
</output>
