---
phase: 08-authentication-workspaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - prisma/schema.prisma
  - src/lib/supabase/client.ts
  - src/lib/supabase/server.ts
  - src/lib/supabase/middleware.ts
  - src/lib/auth/session.ts
  - src/middleware.ts
  - src/app/(auth)/sign-in/page.tsx
  - src/app/(auth)/sign-up/page.tsx
  - tests/auth-session.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04

must_haves:
  truths:
    - "Supabase Auth SSR packages are installed and configured."
    - "Next.js middleware properly intercepts and refreshes Supabase sessions."
    - "The Account schema no longer contains passwordHash, Session, or VerificationToken tables."
    - "Sign-in and Sign-up forms communicate with Supabase Auth via Server Actions."
    - "User sign-up provisions a local Account and Workspace in the database."
  artifacts:
    - path: "src/lib/supabase/server.ts"
      provides: "Server-side Supabase client for Server Actions and React Server Components."
    - path: "src/lib/supabase/middleware.ts"
      provides: "Middleware logic to refresh Supabase cookies."
    - path: "src/middleware.ts"
      provides: "Protected route gating using the Supabase session."
    - path: "prisma/schema.prisma"
      provides: "Updated schema without local password storage."
  key_links:
    - from: "src/middleware.ts"
      to: "src/lib/supabase/middleware.ts"
      via: "session parsing and cookie refresh"
      pattern: "auth_guard"
---

<objective>
Replace the v1.0 in-memory/stub authentication with real Supabase Auth (via `@supabase/ssr`), mapping Supabase users to internal Workspace/Account models.

Purpose: Implement robust, cookie-based session management across the Next.js App Router, enabling persistent identity without hand-rolled password hashing or token generation.

Output:
- `@supabase/supabase-js` and `@supabase/ssr` installed.
- Prisma schema stripped of custom session/password fields.
- `src/lib/supabase` clients implemented (server, client, middleware).
- `src/middleware.ts` refactored to read Supabase cookies.
- Sign-in and Sign-up forms refactored to use Server Actions invoking Supabase Auth.
- Account and Workspace provisioning wired into the sign-up flow.
</objective>

<execution_context>
@/home/caramaschi/.codex/get-shit-done/workflows/execute-plan.md
@/home/caramaschi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.1-phases/08-authentication-workspaces/08-CONTEXT.md
@.planning/milestones/v1.1-phases/08-authentication-workspaces/08-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install Supabase SSR and configure clients</name>
  <files>package.json, src/lib/supabase/client.ts, src/lib/supabase/server.ts, src/lib/supabase/middleware.ts</files>
  <action>
Install `@supabase/supabase-js` and `@supabase/ssr`. 
Create the standard utility files (`client.ts`, `server.ts`, `middleware.ts`) in `src/lib/supabase` using the Next.js App Router patterns provided by the `@supabase/ssr` documentation. These files will handle cookie-based authentication.
  </action>
  <verify>
    <automated>pnpm list @supabase/ssr</automated>
  </verify>
  <done>Supabase dependencies are installed and the basic server/client/middleware utility functions exist.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Prune Prisma Schema</name>
  <files>prisma/schema.prisma</files>
  <action>
Remove `passwordHash`, `emailVerifiedAt` (now handled by Supabase) from the `Account` model. 
Delete the `Session`, `VerificationToken`, and `PasswordResetToken` models completely since Supabase Auth handles these concerns. 
Ensure the `Account` model's `id` field maps directly to the UUID generated by Supabase `auth.users`.
  </action>
  <verify>
    <automated>npx prisma format && npx prisma validate</automated>
  </verify>
  <done>Prisma schema is clean and delegates password/session management to Supabase.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Refactor Authentication Server Actions</name>
  <files>src/app/(auth)/sign-in/page.tsx, src/app/(auth)/sign-up/page.tsx, src/app/(auth)/actions.ts</files>
  <action>
Create Server Actions (e.g. in `src/app/(auth)/actions.ts`) to handle form submissions for sign-in and sign-up.
For sign-up: Use `supabase.auth.signUp()`. If successful, instantiate Prisma to create the `Account` and `Workspace` rows in the database transactionally.
For sign-in: Use `supabase.auth.signInWithPassword()`.
Wire the React UI components in `sign-in/page.tsx` and `sign-up/page.tsx` to use these actions.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>Authentication forms execute real Supabase requests and provision local Accounts and Workspaces upon registration.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Refactor Middleware to use Supabase</name>
  <files>src/middleware.ts, src/lib/auth/session.ts, tests/auth-session.test.ts</files>
  <action>
Update `src/middleware.ts` to call the new `src/lib/supabase/middleware.ts` client to refresh the session cookie.
Use the Supabase session data to evaluate vault access. If no session exists, redirect to `/sign-in`.
Remove the old stub logic from `src/lib/auth/session.ts` and refactor it to fetch the `Account` using Prisma based on the Supabase user ID, checking for MFA and workspace details.
Update `tests/auth-session.test.ts` to mock Supabase clients instead of testing the old opaque token logic.
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <done>Middleware correctly intercepts requests, refreshes Supabase cookies, and enforces route protection.</done>
</task>

</tasks>
