---
phase: 01-vault-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - tsconfig.json
  - next.config.ts
  - prisma/schema.prisma
  - src/lib/db.ts
  - src/lib/auth/config.ts
  - src/lib/auth/session.ts
  - src/lib/auth/mfa.ts
  - src/lib/auth/tokens.ts
  - src/middleware.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/(auth)/sign-up/page.tsx
  - src/app/(auth)/sign-in/page.tsx
  - src/app/(auth)/verify-email/page.tsx
  - src/app/(auth)/reset-password/page.tsx
  - src/app/(auth)/mfa-setup/page.tsx
  - src/app/(vault)/setup/page.tsx
  - tests/auth-session.test.ts
  - tests/setup-readiness.test.ts
  - tests/audit-events.test.ts
  - vitest.config.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - SECU-01

must_haves:
  truths:
    - "The repository boots as a Next.js + TypeScript app and `pnpm test` is wired for Wave 0 validation."
    - "A professional can create an account with email/password and is blocked behind email verification before full vault access."
    - "A professional can sign in, refresh the browser, and remain authenticated through secure session handling."
    - "MFA is enforced before entering vault routes — protected routes redirect incomplete users to MFA setup."
    - "Password recovery is represented by a secure token-based flow, not an ad hoc manual reset."
    - "Vault routes only resolve for the authenticated owning account context."
  artifacts:
    - path: "src/lib/auth/config.ts"
      provides: "Central auth configuration, providers, session policy, and verification hooks"
    - path: "src/middleware.ts"
      provides: "Protected-route gating for auth, verification, and MFA readiness"
    - path: "tests/auth-session.test.ts"
      provides: "Wave 0 auth/session/MFA/recovery verification baseline"
    - path: "prisma/schema.prisma"
      provides: "Initial account/workspace/session/auth token schema"
  key_links:
    - from: "src/lib/auth/config.ts"
      to: "src/middleware.ts"
      via: "shared auth/session policy and vault access guards"
      pattern: "verified_and_mfa_ready"
    - from: "tests/auth-session.test.ts"
      to: "src/lib/auth/session.ts"
      via: "session persistence and protected access assertions"
      pattern: "session persistence"
---

<objective>
Bootstrap the PsiVault application runtime and deliver the complete authentication/session baseline for the vault foundation.

Purpose: every other plan in Phase 1 depends on a stable app scaffold, typed persistence layer, secure account lifecycle, and Wave 0 tests. This plan establishes the account/workspace boundary, email verification, MFA gate, password recovery path, and protected vault routing.

Output:
- Next.js + TypeScript + Prisma + test baseline scaffolded
- account/workspace/session/auth token schema defined
- sign-up, sign-in, email verification, password recovery, and MFA setup flows wired
- vault route protection and session persistence working
- Wave 0 tests for auth/session/setup/audit created and runnable
</objective>

<execution_context>
@/home/caramaschi/.codex/get-shit-done/workflows/execute-plan.md
@/home/caramaschi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-vault-foundation/01-CONTEXT.md
@.planning/phases/01-vault-foundation/01-RESEARCH.md
@.planning/phases/01-vault-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scaffold the web app, workspace model, and Wave 0 test baseline</name>
  <files>package.json, pnpm-workspace.yaml, tsconfig.json, next.config.ts, prisma/schema.prisma, vitest.config.ts, tests/auth-session.test.ts, tests/setup-readiness.test.ts, tests/audit-events.test.ts</files>
  <action>
Create the greenfield application scaffold using Next.js App Router with TypeScript, Prisma, and pnpm. Add the initial persistence model for account identity, workspace ownership, sessions, verification tokens, password reset tokens, and MFA enrollment material.

At the same time, install and configure Vitest so that Phase 1 has Wave 0 coverage for auth/session behavior, setup readiness, and audit event creation. The test files can start with focused stubs/assertions that express the intended behavior contract before the later tasks implement every detail.
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <done>The app scaffold exists, Prisma schema covers auth/workspace primitives, and the Wave 0 test command runs successfully.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement secure account lifecycle and protected vault gating</name>
  <files>src/lib/auth/config.ts, src/lib/auth/session.ts, src/lib/auth/tokens.ts, src/lib/auth/mfa.ts, src/middleware.ts, src/app/(auth)/sign-up/page.tsx, src/app/(auth)/sign-in/page.tsx, src/app/(auth)/verify-email/page.tsx, src/app/(auth)/reset-password/page.tsx, src/app/(auth)/mfa-setup/page.tsx, src/app/(vault)/setup/page.tsx, tests/auth-session.test.ts</files>
  <action>
Implement the account lifecycle chosen in context:
- sign-up with email/password
- verified email required before full vault access
- MFA required before any vault route
- persistent session across refreshes
- secure password recovery flow

Choose one concrete MFA implementation path that satisfies the Phase 1 requirement and future-proofs the system. Keep the route guard logic explicit: unauthenticated users go to sign-in, unverified users go to verification, and verified-but-not-MFA-ready users go to MFA setup.
  </action>
  <verify>
    <automated>pnpm vitest run tests/auth-session.test.ts</automated>
  </verify>
  <done>The auth/session test suite proves signup, session persistence, MFA gating, password recovery, and vault-route protection.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Establish ownership boundary and initial app shell for later phases</name>
  <files>src/lib/db.ts, src/app/layout.tsx, src/app/page.tsx, src/app/(vault)/setup/page.tsx, prisma/schema.prisma, tests/auth-session.test.ts</files>
  <action>
Add the initial account-to-workspace ownership boundary so that all future patient, document, and finance records can hang off the owning professional account without redesign. Create the initial app shell and a placeholder setup-hub route that later plans will deepen, but keep the vault inaccessible until auth/verification/MFA requirements are met.
  </action>
  <verify>
    <automated>pnpm vitest run tests/auth-session.test.ts -t "account boundary"</automated>
  </verify>
  <done>The app shell is in place, ownership boundary is explicit in the schema/code, and the setup hub is the only post-auth landing route available at the end of this plan.</done>
</task>

</tasks>
