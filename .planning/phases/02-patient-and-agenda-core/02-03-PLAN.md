---
phase: 02-patient-and-agenda-core
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/lib/patients/summary.ts
  - src/lib/appointments/agenda.ts
  - src/lib/appointments/defaults.ts
  - src/app/(vault)/agenda/page.tsx
  - src/app/(vault)/agenda/components/agenda-toolbar.tsx
  - src/app/(vault)/agenda/components/agenda-day-view.tsx
  - src/app/(vault)/agenda/components/agenda-week-view.tsx
  - src/app/(vault)/agenda/components/appointment-card.tsx
  - src/app/(vault)/agenda/components/completed-appointment-next-session-action.tsx
  - src/app/(vault)/patients/[patientId]/page.tsx
  - src/app/(vault)/patients/[patientId]/components/quick-next-session-card.tsx
  - tests/patient-summary.test.ts
  - tests/agenda-view-model.test.ts
  - tests/appointment-defaults.test.ts
autonomous: true
requirements:
  - PATI-03
  - SCHD-05
  - SCHD-07

must_haves:
  truths:
    - "The agenda is daily-first but still exposes a trustworthy weekly layout built from the same derived occurrence data."
    - "Agenda cards make time, status, and care mode obvious at a glance while remaining privacy-safe and free of secondary sensitive details."
    - "Once the appointment domain exists, the patient profile summary shows real scheduling-derived next session, last session, and pending items values instead of placeholders."
    - "Quick next-session entry points from patient and completed-appointment context prefill patient, duration, care mode, and price without silently assuming a new date or time."
    - "Agenda and quick rebooking consume the patient summary and appointment domain contracts from earlier plans rather than inventing new scheduling rules in the UI layer."
  artifacts:
    - path: "src/lib/patients/summary.ts"
      provides: "Scheduling-backed patient summary hydration for next session, last session, and pending items after the appointment domain exists"
    - path: "src/lib/appointments/agenda.ts"
      provides: "Derived day/week agenda view models with ordering, grouping, and privacy-safe card inputs"
    - path: "src/lib/appointments/defaults.ts"
      provides: "Shared quick next-session defaults initializer from patient and appointment context"
    - path: "src/app/(vault)/agenda/page.tsx"
      provides: "Daily-first agenda route with day/week switching"
    - path: "src/app/(vault)/agenda/components/appointment-card.tsx"
      provides: "Reusable appointment block anatomy for both agenda layouts"
    - path: "src/app/(vault)/agenda/components/completed-appointment-next-session-action.tsx"
      provides: "Explicit completed-appointment entry point for quick next-session creation"
    - path: "tests/patient-summary.test.ts"
      provides: "Automated verification that patient summary next session, last session, and pending items become real after scheduling exists"
    - path: "tests/agenda-view-model.test.ts"
      provides: "Automated verification for day/week agenda derivation and privacy-safe card payloads"
    - path: "tests/appointment-defaults.test.ts"
      provides: "Automated verification for quick next-session default selection"
  key_links:
    - from: "src/lib/patients/summary.ts"
      to: "src/app/(vault)/patients/[patientId]/page.tsx"
      via: "patient profile summary hydration from appointment occurrences"
      pattern: "patient_summary_with_schedule"
    - from: "src/lib/appointments/agenda.ts"
      to: "src/app/(vault)/agenda/components/appointment-card.tsx"
      via: "privacy-safe appointment card data consumed by both views"
      pattern: "agenda_card_view_model"
    - from: "src/lib/appointments/defaults.ts"
      to: "src/app/(vault)/patients/[patientId]/components/quick-next-session-card.tsx"
      via: "shared next-session prefill contract"
      pattern: "quick_next_session_defaults"
    - from: "src/lib/appointments/defaults.ts"
      to: "src/app/(vault)/agenda/components/completed-appointment-next-session-action.tsx"
      via: "completed-appointment quick rebooking entry point using the same defaults contract"
      pattern: "completed_appointment_to_next_session"
    - from: "src/app/(vault)/patients/[patientId]/page.tsx"
      to: "src/app/(vault)/agenda/page.tsx"
      via: "patient-context quick scheduling entry into agenda flows"
      pattern: "patient_to_next_session"
---

<objective>
Turn the patient and appointment contracts into the operating surfaces of Phase 2: day/week agenda views and rapid next-session creation.

Purpose: once patient and scheduling domain rules exist, the app must expose the real consultory rhythm. This plan builds the daily-first agenda, required weekly layout, privacy-safe appointment cards, and quick rebooking workflow that carries forward useful defaults without hiding date/time decisions from the professional.

Output:
- scheduling-backed patient summary next session, last session, and pending items values
- derived day and week agenda view models
- daily-first agenda page with weekly switch
- shared appointment card component with status and care-mode clarity
- quick next-session defaults initializer
- patient and completed-appointment entry points for rapid rebooking
- automated tests for patient summary hydration, agenda derivation, and next-session defaults
</objective>

<execution_context>
@/home/caramaschi/.codex/get-shit-done/workflows/execute-plan.md
@/home/caramaschi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-patient-and-agenda-core/02-CONTEXT.md
@.planning/phases/02-patient-and-agenda-core/02-RESEARCH.md
@.planning/phases/02-patient-and-agenda-core/02-VALIDATION.md
@.planning/phases/02-patient-and-agenda-core/02-01-PLAN.md
@.planning/phases/02-patient-and-agenda-core/02-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derive day and week agenda view models from appointment occurrences</name>
  <files>src/lib/appointments/agenda.ts, tests/agenda-view-model.test.ts</files>
  <action>
Implement the agenda view-model layer that converts appointment occurrences into:
- day agenda ordering and grouping
- week agenda grouping by weekday
- privacy-safe appointment card inputs
- clear status and care-mode labels/chips for the UI layer

Keep this server-side derivation separate from presentation so both agenda views stay consistent and later dashboard work can reuse the same query/result shapes.
  </action>
  <verify>
    <automated>pnpm vitest run tests/agenda-view-model.test.ts -t "day and week agenda"</automated>
  </verify>
  <done>The agenda view-model layer exists, orders/groupings are stable, and the derived appointment cards stay privacy-safe.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build the daily-first agenda UI and reusable appointment cards</name>
  <files>src/app/(vault)/agenda/page.tsx, src/app/(vault)/agenda/components/agenda-toolbar.tsx, src/app/(vault)/agenda/components/agenda-day-view.tsx, src/app/(vault)/agenda/components/agenda-week-view.tsx, src/app/(vault)/agenda/components/appointment-card.tsx, tests/agenda-view-model.test.ts</files>
  <action>
Implement the Phase 2 agenda interface using the view-model contract from Task 1. Day view is the primary operating posture, but weekly layout must still be available and legible. Appointment cards should read as calm blocks with visible time, status, and care mode, not spreadsheet rows.

Do not introduce drag-and-drop or ad hoc scheduling rules here. The agenda must stay a consumer of the earlier appointment-domain contracts.
  </action>
  <verify>
    <automated>pnpm vitest run tests/agenda-view-model.test.ts -t "day and week agenda"</automated>
  </verify>
  <done>The daily-first agenda and weekly view render from one derived contract and present appointment status and care mode clearly at a glance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Hydrate the patient summary from scheduling data and wire both quick next-session entry points</name>
  <files>src/lib/patients/summary.ts, src/lib/appointments/defaults.ts, src/app/(vault)/patients/[patientId]/page.tsx, src/app/(vault)/patients/[patientId]/components/quick-next-session-card.tsx, src/app/(vault)/agenda/components/completed-appointment-next-session-action.tsx, tests/patient-summary.test.ts, tests/appointment-defaults.test.ts</files>
  <action>
Create the shared initializer for quick next-session creation with this precedence:
1. latest relevant appointment values for the patient
2. practice profile defaults from Phase 1

Prefill:
- patient
- duration
- care mode
- price

Now that `02-02` has established appointment occurrences, return to `src/lib/patients/summary.ts` and replace the placeholder scheduling fields with real `lastSession`, `nextSession`, and `pendingItemsCount` derivation. `pendingItemsCount` should become schedule-derived in this phase, starting from actionable scheduling pendencies such as upcoming appointments still needing confirmation. Then expose the quick next-session flow from both:
- patient context through the patient profile quick action
- completed-appointment context through an explicit completed-appointment action in agenda/appointment surfaces

Leave date and time explicit and empty so the professional chooses the new slot intentionally. Both entry points must reuse the same defaults initializer and must not bypass the appointment conflict and archive rules already defined in `02-02`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/patient-summary.test.ts tests/appointment-defaults.test.ts</automated>
  </verify>
  <done>The patient summary now shows real next session, last session, and pending items values from appointment data, and both patient-context and completed-appointment quick next-session entry points reuse the same defaults contract without auto-picking the new slot.</done>
</task>

</tasks>
