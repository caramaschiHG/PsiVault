---
phase: 06-retrieval-recovery-and-launch-polish
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(vault)/settings/layout.tsx
autonomous: true
gap_closure: true
requirements:
  - SECU-03
  - SECU-04

must_haves:
  truths:
    - "A user on /settings/profile sees navigation links to Perfil, Seguranca, and Dados e Privacidade"
    - "A user on /settings/security sees navigation links to Perfil, Seguranca, and Dados e Privacidade"
    - "A user on /settings/dados-e-privacidade sees navigation links to Perfil, Seguranca, and Dados e Privacidade"
    - "Clicking Dados e Privacidade from any settings page navigates to /settings/dados-e-privacidade"
  artifacts:
    - path: "src/app/(vault)/settings/layout.tsx"
      provides: "Settings sub-navigation shared across all three settings pages"
      exports: ["default (SettingsLayout)"]
  key_links:
    - from: "src/app/(vault)/settings/layout.tsx"
      to: "/settings/dados-e-privacidade"
      via: "anchor href"
      pattern: "href.*dados-e-privacidade"
    - from: "src/app/(vault)/settings/layout.tsx"
      to: "/settings/profile"
      via: "anchor href"
      pattern: "href.*settings/profile"
    - from: "src/app/(vault)/settings/layout.tsx"
      to: "/settings/security"
      via: "anchor href"
      pattern: "href.*settings/security"
---

<objective>
Add a settings sub-navigation layout so that all three settings pages (Perfil, Seguranca, Dados e Privacidade) are mutually discoverable.

Purpose: The /settings/dados-e-privacidade page is fully implemented but unreachable from the UI — a user on /settings/profile or /settings/security has no way to find it without typing the URL directly. This is the sole gap blocking phase 6 sign-off (truth #15 failed in VERIFICATION.md).

Output: A single new file — src/app/(vault)/settings/layout.tsx — that Next.js automatically applies to every route under /settings/. It renders a compact tab-style sub-nav with three links (Perfil, Seguranca, Dados e Privacidade) above the page content. No existing files need modification.
</objective>

<execution_context>
@/home/caramaschi/.claude/get-shit-done/workflows/execute-plan.md
@/home/caramaschi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing vault layout for style reference -->
<!-- From src/app/(vault)/layout.tsx — inline styles use satisfies React.CSSProperties pattern -->

Nav link style used throughout the vault:
```typescript
const navLinkStyle = {
  fontSize: "0.875rem",
  fontWeight: 500,
  color: "#57534e",
  textDecoration: "none",
  padding: "0.3rem 0.75rem",
  borderRadius: "999px",
  transition: "background 0.15s",
} satisfies React.CSSProperties;
```

Brand color palette in use:
- Amber/brown accent: #b45309
- Primary action: #9a3412
- Text secondary: #57534e
- Border: rgba(146, 64, 14, 0.12) or rgba(146, 64, 14, 0.14)
- Background warm white: rgba(255, 252, 247, 0.97) or #fffdfa

The settings pages use `<main>` as their root element with inline style objects. The layout wraps `{children}` — no conflict.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create settings sub-navigation layout</name>
  <files>src/app/(vault)/settings/layout.tsx</files>
  <action>
    Create src/app/(vault)/settings/layout.tsx as a server component (no "use client" needed — static links only).

    The layout renders a compact tab-strip sub-nav above {children}. Three links in order:
    - "Perfil" → href="/settings/profile"
    - "Segurança" → href="/settings/security"
    - "Dados e Privacidade" → href="/settings/dados-e-privacidade"

    Structure:
    ```
    <div style={shellStyle}>
      <nav style={subNavStyle}>
        <a href="/settings/profile" style={tabLinkStyle}>Perfil</a>
        <a href="/settings/security" style={tabLinkStyle}>Segurança</a>
        <a href="/settings/dados-e-privacidade" style={tabLinkStyle}>Dados e Privacidade</a>
      </nav>
      {children}
    </div>
    ```

    Style guidance (inline, using satisfies React.CSSProperties):
    - shellStyle: display grid, gap 0 — layout wrapper only, no padding (pages own their padding)
    - subNavStyle: display flex, gap 0.25rem, padding 1rem 2rem 0, borderBottom "1px solid rgba(146, 64, 14, 0.1)", background "rgba(255, 252, 247, 0.6)"
    - tabLinkStyle: fontSize 0.875rem, fontWeight 500, color #57534e, textDecoration none, padding "0.45rem 1rem", borderRadius "12px 12px 0 0", border "1px solid transparent", transition "background 0.15s" — tab appearance consistent with the vault nav pill style

    No active-state highlighting needed (this is a server component with no usePathname access). All three tabs render identically — the page heading already indicates which section is active.

    Export: `export default function SettingsLayout({ children }: { children: React.ReactNode })`.

    Do NOT add "use client". Do NOT import from next/navigation (no active tab logic needed — keeps this a server component with zero client JS).
  </action>
  <verify>
    <automated>grep -r "href=\"/settings/dados-e-privacidade\"" /home/caramaschi/Área\ de\ trabalho/Projetos/PsiLock/src/app/\(vault\)/settings/layout.tsx && grep -r "href=\"/settings/profile\"" /home/caramaschi/Área\ de\ trabalho/Projetos/PsiLock/src/app/\(vault\)/settings/layout.tsx && grep -r "href=\"/settings/security\"" /home/caramaschi/Área\ de\ trabalho/Projetos/PsiLock/src/app/\(vault\)/settings/layout.tsx && echo "All three nav links present"</automated>
  </verify>
  <done>
    File exists at src/app/(vault)/settings/layout.tsx with all three href links. No TypeScript errors (npx tsc --noEmit passes). Navigating to any of the three settings routes shows the sub-nav above the page content.
  </done>
</task>

</tasks>

<verification>
After the task completes, run the full test suite to confirm no regressions:

```
cd /home/caramaschi/Área\ de\ trabalho/Projetos/PsiLock && npm test
```

Expected: 289/289 tests pass (layout.tsx adds no logic, only HTML structure).

Structural check — all three links present in the new file:
```
grep -c "href=\"/settings/" src/app/\(vault\)/settings/layout.tsx
```
Expected output: 3

TypeScript check:
```
npx tsc --noEmit
```
Expected: exits 0 with no errors.
</verification>

<success_criteria>
- src/app/(vault)/settings/layout.tsx exists and exports a default SettingsLayout component
- The layout renders href links to /settings/profile, /settings/security, and /settings/dados-e-privacidade
- Any user navigating to /settings/profile or /settings/security now sees Dados e Privacidade as a visible sibling tab — truth #15 satisfied
- 289/289 tests remain green — no regressions
- TypeScript compiler reports no errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-retrieval-recovery-and-launch-polish/06-05-SUMMARY.md`
</output>
