# Plan: Phase 09 - Patient & Agenda Persistence (Part 1)

## Goal
Implement Prisma repositories for Patients and Appointments, ensuring all domain logic persists correctly to the PostgreSQL database.

## Tasks

### 1. Schema Updates
- [ ] Add missing fields to `Appointment` model in `prisma/schema.prisma`:
  - `priceInCents Int? @map("price_in_cents")`
  - `meetingLink String? @map("meeting_link")`
  - `remoteIssueNote String? @map("remote_issue_note")`
- [ ] Run `npx prisma generate` to update the client.
- [ ] (Optional) Run `npx prisma db push` if in development or create a migration if in production.

### 2. Prisma Patient Repository
- [ ] Create `src/lib/patients/repository.prisma.ts`.
- [ ] Implement `PrismaPatientRepository` mapping:
  - `archivedAt` (Domain) <-> `deletedAt` (DB)
  - `archivedByAccountId` (Domain) <-> `deletedByAccountId` (DB)
- [ ] Ensure `listActive` and `listArchived` use the correct `deletedAt` filters and sorting.

### 3. Prisma Appointment Repository
- [ ] Create `src/lib/appointments/repository.prisma.ts`.
- [ ] Implement `PrismaAppointmentRepository` mapping:
  - Domain Enums <-> Prisma Enums
  - Handle all list methods (`listByDateRange`, `listBySeries`, `listByPatient`).

### 4. Test Integration & Verification
- [ ] Create a test helper for cleaning the database between tests.
- [ ] Create/Update tests to run against `PrismaPatientRepository`.
- [ ] Create/Update tests to run against `PrismaAppointmentRepository`.
- [ ] Run the full suite of domain tests identified in RESEARCH.md.

## Success Criteria
1. `PrismaPatientRepository` and `PrismaAppointmentRepository` are fully implemented.
2. All domain tests pass against the new Prisma implementations.
3. No regressions in existing functionality.
