---
phase: 12-authentication-ux
plan: "02"
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - src/app/(auth)/sign-in/page.tsx
  - src/app/(auth)/sign-up/page.tsx
autonomous: true
requirements:
  - AUTHUX-01
  - AUTHUX-02
  - AUTHUX-04
must_haves:
  truths:
    - "Sign-in exibe erro inline quando ?error= está na URL"
    - "Sign-in exibe erro por campo quando ?field= está na URL"
    - "Sign-in tem link 'Esqueceu a senha?' apontando para /reset-password"
    - "Sign-in exibe mensagem de sucesso quando ?success= está na URL"
    - "Sign-up exibe erro inline quando ?error= está na URL"
    - "Sign-up exibe erro por campo quando ?field= está na URL"
    - "Botão de submit em ambas as páginas usa <SubmitButton> com spinner"
    - "Inputs ficam desabilitados durante pending via <AuthForm> wrapper"
  artifacts:
    - path: "src/app/(auth)/sign-in/page.tsx"
      provides: "Página sign-in com error display, field errors, success message, SubmitButton, AuthForm"
    - path: "src/app/(auth)/sign-up/page.tsx"
      provides: "Página sign-up com error display, field errors, SubmitButton, AuthForm"
  key_links:
    - from: "src/app/(auth)/sign-in/page.tsx"
      to: "src/app/(auth)/auth-errors.ts"
      via: "import translateAuthError"
      pattern: "translateAuthError"
    - from: "src/app/(auth)/sign-in/page.tsx"
      to: "src/app/(auth)/components/submit-button.tsx"
      via: "import SubmitButton"
      pattern: "SubmitButton"
    - from: "src/app/(auth)/sign-in/page.tsx"
      to: "src/app/(auth)/components/auth-form.tsx"
      via: "import AuthForm (wraps inputs para desabilitar durante pending)"
      pattern: "AuthForm"
---

<objective>
Atualizar as páginas sign-in e sign-up para exibir erros inline (bloco abaixo do botão), erros por campo (abaixo do input), mensagem de sucesso, usar o SubmitButton com spinner e envolver inputs com AuthForm para desabilitar durante pending.

Purpose: AUTHUX-01 e AUTHUX-02 — as páginas hoje não exibem nenhum feedback visual de erro para o usuário.
Output: sign-in/page.tsx e sign-up/page.tsx polished.
</objective>

<execution_context>
@/home/caramaschi/.claude/get-shit-done/workflows/execute-plan.md
@/home/caramaschi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-authentication-ux/12-CONTEXT.md
@.planning/phases/12-authentication-ux/12-01-SUMMARY.md

<interfaces>
<!-- Contratos criados no Plan 01 -->

De src/app/(auth)/auth-errors.ts:
```typescript
export function translateAuthError(message: string): string;
// Mapeia mensagens Supabase → pt-BR. Fallback: "Ocorreu um erro inesperado. Tente novamente."
```

De src/app/(auth)/components/submit-button.tsx:
```typescript
export function SubmitButton({ label }: { label: string }): JSX.Element;
// "use client" — usa useFormStatus para pending state + spinner SVG
```

De src/app/(auth)/components/auth-form.tsx:
```typescript
export function AuthForm({ children }: { children: React.ReactNode }): JSX.Element;
// "use client" — usa useFormStatus; renderiza <fieldset disabled={pending}> ao redor dos children
// Usar dentro do <form> para envolver inputs + SubmitButton e desabilitar todos durante pending
```

De src/app/(auth)/actions.ts (existente — não modificar):
```typescript
export async function signIn(formData: FormData): Promise<void>
// Redireciona para ?error=encodeURIComponent(error.message) em caso de erro
// Redireciona para /mfa-setup em sucesso

export async function signUp(formData: FormData): Promise<void>
// Redireciona para ?error=encodeURIComponent(error.message) em caso de erro
// Redireciona para /verify-email em sucesso
```

De src/lib/supabase/constants.ts:
```typescript
export const AUTH_ROUTE_PATHS = {
  signIn: "/sign-in",
  signUp: "/sign-up",
  resetPassword: "/reset-password",
  // ...
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar sign-in/page.tsx</name>
  <files>src/app/(auth)/sign-in/page.tsx</files>
  <action>
    Converter para `async` server component que lê `searchParams` (await — Next.js 15).

    Props: `{ searchParams: Promise<{ error?: string; field?: string; success?: string }> }`

    Lógica de display:
    - `errorMessage`: se `params.error` existe → `translateAuthError(decodeURIComponent(params.error))`
    - `errorField`: `params.field ?? null` (valores possíveis: `"email"` | `"password"`)
    - `successMessage`: `params.success ? decodeURIComponent(params.success) : null`

    Estrutura do form:
    1. Se `successMessage`: exibir bloco de sucesso (fundo `#dcfce7`, border `#86efac`, texto `#166534`) acima do form
    2. Adicionar link "Esqueceu a senha?" como `<a href="/reset-password">` com estilo sutil (fontSize 0.875rem, color "#78716c") — colocar abaixo do campo senha, alinhado à direita
    3. Dentro do `<form action={signIn}>`, envolver inputs + botão com `<AuthForm>`:
       ```tsx
       <form action={signIn}>
         <AuthForm>
           {/* inputs de email e senha */}
           {/* link Esqueceu a senha? */}
           <SubmitButton label="Entrar" />
           {/* bloco de erro geral abaixo do botão */}
         </AuthForm>
       </form>
       ```
    4. Se `errorMessage` (sem field): exibir bloco de erro abaixo do `<SubmitButton>` — fundo `rgba(239, 68, 68, 0.08)`, border `1px solid rgba(239, 68, 68, 0.3)`, borderRadius `12px`, padding `0.75rem 1rem`, color `#dc2626`, fontSize `0.875rem`
    5. Se `errorField === "email"`: exibir `<span style={{ color: "#dc2626", fontSize: "0.8rem" }}>{errorMessage}</span>` abaixo do input de email (bloco geral apenas quando sem `field`)
    6. Se `errorField === "password"`: exibir span de erro abaixo do input de senha

    Regra CONTEXT.md: erro persiste até novo envio — não sumir ao editar (sem estado cliente extra — correto, é server component lendo searchParams).

    Adicionar link "Criar conta" abaixo do form: `<p>Não tem conta? <a href="/sign-up">Criar conta</a></p>` — estilo sutil.

    Manter todos os estilos existentes (`satisfies React.CSSProperties`). Não remover nem alterar estilos do card, inputs, ou paleta de cores.
  </action>
  <verify>
    <automated>cd /home/caramaschi/Área\ de\ trabalho/Projetos/PsiLock && pnpm tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript sem erros. sign-in/page.tsx é async, lê searchParams, usa translateAuthError, AuthForm, SubmitButton, exibe erros inline e por campo, tem link "Esqueceu a senha?".</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar sign-up/page.tsx</name>
  <files>src/app/(auth)/sign-up/page.tsx</files>
  <action>
    Converter para `async` server component que lê `searchParams` (await — Next.js 15).

    Props: `{ searchParams: Promise<{ error?: string; field?: string }> }`

    Lógica de display:
    - `errorMessage`: se `params.error` → `translateAuthError(decodeURIComponent(params.error))`
    - `errorField`: `params.field ?? null` (valores possíveis: `"email"` | `"password"` | `"displayName"`)

    Estrutura do form — envolver inputs com `<AuthForm>` dentro do `<form>`:
    ```tsx
    <form action={signUp}>
      <AuthForm>
        {/* inputs de displayName, email, password */}
        <SubmitButton label="Continuar para verificação" />
        {/* bloco de erro geral */}
      </AuthForm>
    </form>
    ```

    1. Se `errorField === "displayName"`: exibir span de erro abaixo do input de displayName
    2. Se `errorField === "email"`: exibir span de erro abaixo do input de email
    3. Se `errorField === "password"`: exibir span de erro abaixo do input de password
    4. Se `errorMessage` (sem field, ou sempre): exibir bloco de erro abaixo do `<SubmitButton>` com mesmo estilo definido em Task 1
    5. Substituir `<button>` por `<SubmitButton label="Continuar para verificação" />`

    Adicionar link "Já tem conta? Entrar" abaixo do form: `<a href="/sign-in">`.

    Manter todos os estilos existentes.
  </action>
  <verify>
    <automated>cd /home/caramaschi/Área\ de\ trabalho/Projetos/PsiLock && pnpm tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript sem erros. sign-up/page.tsx é async, lê searchParams, usa translateAuthError, AuthForm e SubmitButton, exibe erros inline e por campo.</done>
</task>

</tasks>

<verification>
pnpm tsc --noEmit — sem erros
pnpm test --run — suite verde
</verification>

<success_criteria>
- sign-in/page.tsx: async, searchParams aguardado, erros inline + por campo, link "Esqueceu a senha?", AuthForm + SubmitButton (inputs desabilitados durante pending)
- sign-up/page.tsx: async, searchParams aguardado, erros inline + por campo, AuthForm + SubmitButton
- Nenhum erro de TypeScript
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/12-authentication-ux/12-02-SUMMARY.md`
</output>
