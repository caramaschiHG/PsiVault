---
phase: 12-authentication-ux
plan: "03"
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - src/app/(auth)/actions.ts
  - src/app/(auth)/reset-password/page.tsx
  - src/lib/supabase/constants.ts
  - .env.example
autonomous: true
requirements:
  - AUTHUX-03
  - AUTHUX-04
must_haves:
  truths:
    - "Sem token na URL: /reset-password exibe formulário de 'solicitar e-mail'"
    - "Com ?code= na URL: /reset-password exibe formulário 'nova senha + confirmar senha'"
    - "requestPasswordReset redireciona para success sem revelar se e-mail existe"
    - "updatePassword valida senhas iguais antes de chamar Supabase"
    - "Senha atualizada redireciona para /sign-in?success=... com mensagem pt-BR"
    - "Token expirado exibe 'Este link expirou ou já foi utilizado.' com link para solicitar novo"
  artifacts:
    - path: "src/app/(auth)/actions.ts"
      provides: "requestPasswordReset e updatePassword server actions"
      exports: ["requestPasswordReset", "updatePassword"]
    - path: "src/app/(auth)/reset-password/page.tsx"
      provides: "Página dual-state: request form ou update form baseado em ?code="
    - path: "src/lib/supabase/constants.ts"
      provides: "AUTH_ROUTE_PATHS atualizado (sem mudanças necessárias — /reset-password já existe)"
    - path: ".env.example"
      provides: "NEXT_PUBLIC_SITE_URL documentado"
  key_links:
    - from: "src/app/(auth)/reset-password/page.tsx"
      to: "src/app/(auth)/actions.ts"
      via: "form action={requestPasswordReset} ou action={updatePassword}"
      pattern: "requestPasswordReset|updatePassword"
    - from: "src/app/(auth)/actions.ts"
      to: "supabase.auth.exchangeCodeForSession"
      via: "troca code PKCE por sessão antes de updateUser"
      pattern: "exchangeCodeForSession"
---

<objective>
Implementar o fluxo completo de reset de senha: server actions requestPasswordReset e updatePassword, e página dual-state /reset-password que bifurca entre "solicitar e-mail" e "nova senha" baseado na presença de ?code= na URL.

Purpose: AUTHUX-03 — hoje reset-password é um stub não funcional.
Output: actions.ts atualizado, reset-password/page.tsx funcional, .env.example com NEXT_PUBLIC_SITE_URL.
</objective>

<execution_context>
@/home/caramaschi/.claude/get-shit-done/workflows/execute-plan.md
@/home/caramaschi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-authentication-ux/12-CONTEXT.md
@.planning/phases/12-authentication-ux/12-RESEARCH.md
@.planning/phases/12-authentication-ux/12-01-SUMMARY.md

<interfaces>
<!-- Contratos existentes e criados no Plan 01 -->

De src/app/(auth)/actions.ts (existente — adicionar ao arquivo, não substituir):
```typescript
"use server";
import { createClient } from "@/lib/supabase/server";
import { redirect } from "next/navigation";
import { AUTH_ROUTE_PATHS } from "@/lib/supabase/constants";
// signIn e signUp já existem
```

De src/app/(auth)/auth-errors.ts (criado no Plan 01):
```typescript
export function translateAuthError(message: string): string;
// Inclui: "Token has expired or is invalid" → "Este link expirou ou já foi utilizado."
```

De src/lib/supabase/constants.ts:
```typescript
export const AUTH_ROUTE_PATHS = {
  signIn: "/sign-in",
  resetPassword: "/reset-password",
  // ...
};
```

De src/app/(auth)/components/submit-button.tsx (criado no Plan 01):
```typescript
export function SubmitButton({ label }: { label: string }): JSX.Element;
```

Supabase PKCE API:
```typescript
// Solicitar reset (envia e-mail com ?code= callback)
supabase.auth.resetPasswordForEmail(email, { redirectTo: `${SITE_URL}/reset-password` })

// Na página com code: trocar code por sessão
supabase.auth.exchangeCodeForSession(code)  // deve ser chamado antes de updateUser

// Atualizar senha
supabase.auth.updateUser({ password: newPassword })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar requestPasswordReset e updatePassword em actions.ts</name>
  <files>src/app/(auth)/actions.ts, .env.example</files>
  <action>
    **A) Adicionar ao src/app/(auth)/actions.ts** (NÃO reescrever o arquivo — apenas adicionar as novas funções ao final):

    ```typescript
    export async function requestPasswordReset(formData: FormData): Promise<void> {
      const email = formData.get("email") as string;
      const supabase = await createClient();
      const siteUrl = process.env.NEXT_PUBLIC_SITE_URL ?? "http://localhost:3000";

      await supabase.auth.resetPasswordForEmail(email, {
        redirectTo: `${siteUrl}/reset-password`,
      });

      // Sempre redirecionar para success — não revelar se e-mail existe (prevenção de enumeração)
      redirect(
        `${AUTH_ROUTE_PATHS.resetPassword}?success=${encodeURIComponent(
          "Link de recuperação enviado. Verifique seu e-mail."
        )}`
      );
    }

    export async function updatePassword(formData: FormData): Promise<void> {
      const password = formData.get("password") as string;
      const confirmPassword = formData.get("confirmPassword") as string;
      const code = formData.get("code") as string;

      // Validar senhas iguais antes de chamar Supabase
      if (password !== confirmPassword) {
        redirect(
          `${AUTH_ROUTE_PATHS.resetPassword}?code=${encodeURIComponent(code)}&field=confirmPassword&error=${encodeURIComponent(
            "As senhas não coincidem."
          )}`
        );
      }

      const supabase = await createClient();

      // PKCE: trocar code por sessão
      const { error: exchangeError } = await supabase.auth.exchangeCodeForSession(code);
      if (exchangeError) {
        redirect(
          `${AUTH_ROUTE_PATHS.resetPassword}?error=${encodeURIComponent(exchangeError.message)}`
        );
      }

      const { error } = await supabase.auth.updateUser({ password });
      if (error) {
        redirect(
          `${AUTH_ROUTE_PATHS.resetPassword}?code=${encodeURIComponent(code)}&error=${encodeURIComponent(error.message)}`
        );
      }

      redirect(
        `${AUTH_ROUTE_PATHS.signIn}?success=${encodeURIComponent(
          "Senha atualizada. Faça login com a nova senha."
        )}`
      );
    }
    ```

    **B) Atualizar .env.example:**
    Adicionar entrada se não existir:
    ```
    NEXT_PUBLIC_SITE_URL=http://localhost:3000
    ```
  </action>
  <verify>
    <automated>cd /home/caramaschi/Área\ de\ trabalho/Projetos/PsiLock && pnpm tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript sem erros. actions.ts exporta requestPasswordReset e updatePassword. .env.example tem NEXT_PUBLIC_SITE_URL.</done>
</task>

<task type="auto">
  <name>Task 2: Implementar reset-password/page.tsx dual-state</name>
  <files>src/app/(auth)/reset-password/page.tsx</files>
  <action>
    Reescrever completamente o stub atual como async server component.

    Props: `{ searchParams: Promise<{ code?: string; error?: string; field?: string; success?: string }> }`

    Lógica de bifurcação:
    - `params.code` existe → estado "nova senha" (update form)
    - `params.code` não existe → estado "solicitar e-mail" (request form)

    **Estado "solicitar e-mail":**
    - Eyebrow: "Recuperar acesso"
    - Título: "Recupere o acesso ao seu vault."
    - Texto: "Enviaremos um link de recuperação para o seu e-mail."
    - Form com `action={requestPasswordReset}`:
      - Campo email
      - `<SubmitButton label="Enviar link de recuperação" />`
    - Se `successMessage`: exibir bloco de sucesso (verde) acima do form
    - Se `errorMessage` (sem field): exibir bloco de erro abaixo do botão
    - Link "Lembrou a senha? Entrar" → `/sign-in`

    **Estado "nova senha":**
    - Eyebrow: "Nova senha"
    - Título: "Crie uma nova senha para o vault."
    - Texto: "Escolha uma senha forte. Ela será sua única credencial de acesso."
    - Form com `action={updatePassword}`:
      - `<input type="hidden" name="code" value={params.code} />` — passar code como hidden field para a server action
      - Campo "Nova senha" (name="password", type="password")
      - Campo "Confirmar nova senha" (name="confirmPassword", type="password")
      - Se `errorField === "confirmPassword"`: exibir span de erro abaixo do campo confirmPassword
      - `<SubmitButton label="Atualizar senha" />`
    - Se `errorMessage` (e não é "Token has expired or is invalid"): bloco de erro abaixo do botão
    - Se erro de token expirado (`translateAuthError(errorMessage)` contém "expirou"): exibir mensagem especial + link para `/reset-password`

    **Detecção de token expirado:** verificar se o erro decodificado é "Token has expired or is invalid". Usar translateAuthError para exibir a mensagem pt-BR. Adicionar link: `<a href="/reset-password">Solicitar novo link</a>`.

    Estilos: manter o padrão da codebase — card 28px, warm palette, `satisfies React.CSSProperties`. Mesmo estilo de error block e success block definido nos planos anteriores.
  </action>
  <verify>
    <automated>cd /home/caramaschi/Área\ de\ trabalho/Projetos/PsiLock && pnpm tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript sem erros. reset-password/page.tsx é async, bifurca entre request/update baseado em params.code, usa SubmitButton, hidden code field, translateAuthError, exibe token expirado com link para solicitar novo.</done>
</task>

</tasks>

<verification>
pnpm tsc --noEmit — sem erros
pnpm test --run — suite verde
</verification>

<success_criteria>
- requestPasswordReset: não revela enumeração de e-mail, redireciona para success sempre
- updatePassword: valida senhas antes de chamar Supabase, usa exchangeCodeForSession, redireciona para /sign-in?success= em sucesso
- reset-password/page.tsx: bifurcação clara por params.code, hidden field code no update form, token expirado com link de re-solicitação
- .env.example tem NEXT_PUBLIC_SITE_URL
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/12-authentication-ux/12-03-SUMMARY.md`
</output>
