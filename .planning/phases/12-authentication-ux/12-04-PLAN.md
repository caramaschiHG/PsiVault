---
phase: 12-authentication-ux
plan: "04"
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - src/middleware.ts
  - src/lib/supabase/middleware.ts
autonomous: true
requirements:
  - AUTHUX-05
must_haves:
  truths:
    - "Usuário autenticado com MFA completo que visita /sign-in é redirecionado para /inicio"
    - "Usuário autenticado sem MFA que visita /sign-in é redirecionado para /mfa-setup"
    - "Usuário não autenticado que visita /sign-in continua na página sem redirecionamento"
    - "Middleware cobre /sign-in, /sign-up, /verify-email, /reset-password"
    - "updateSession é chamado em todas as rotas do matcher para refresh de sessão"
  artifacts:
    - path: "src/middleware.ts"
      provides: "Middleware integrado com updateSession + redirect de auth routes para autenticados"
    - path: "src/lib/supabase/middleware.ts"
      provides: "updateSession retorna { supabase, supabaseResponse, user } (modificado no Plan 01)"
  key_links:
    - from: "src/middleware.ts"
      to: "src/lib/supabase/middleware.ts"
      via: "import updateSession"
      pattern: "updateSession"
    - from: "src/middleware.ts"
      to: "supabase.auth.mfa.listFactors"
      via: "verificação de MFA para decidir redirectTo"
      pattern: "listFactors|mfa"
---

<objective>
Reescrever src/middleware.ts para chamar updateSession em todas as rotas, e redirecionar usuários já autenticados que acessam rotas de auth para /inicio (MFA completo) ou /mfa-setup (MFA pendente).

Purpose: AUTHUX-05 — hoje o middleware passa session=null e account=null hardcoded, tornando a proteção de vault ineficaz e não redirecionando autenticados fora das rotas protegidas.
Output: middleware.ts funcional com Supabase SSR.
</objective>

<execution_context>
@/home/caramaschi/.claude/get-shit-done/workflows/execute-plan.md
@/home/caramaschi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-authentication-ux/12-CONTEXT.md
@.planning/phases/12-authentication-ux/12-RESEARCH.md
@.planning/phases/12-authentication-ux/12-01-SUMMARY.md

<interfaces>
<!-- Contratos existentes e modificados no Plan 01 -->

De src/lib/supabase/middleware.ts (modificado no Plan 01 para retornar user):
```typescript
export async function updateSession(request: NextRequest): Promise<{
  supabase: SupabaseClient;
  supabaseResponse: NextResponse;
  user: User | null;
}>
// Chama getUser() internamente uma vez. Retorna user sem round-trip extra.
```

De src/lib/supabase/constants.ts:
```typescript
export const AUTH_ROUTE_PATHS = {
  signIn: "/sign-in",
  signUp: "/sign-up",
  verifyEmail: "/verify-email",
  resetPassword: "/reset-password",
  mfaSetup: "/mfa-setup",
};
```

De src/lib/auth/session.ts (manter compatibilidade — enforceVaultAccess ainda pode ser exportado mas não será mais chamado pelo middleware principal):
```typescript
export function decideVaultAccess(input: { pathname; session; account; now }): { allowed; redirectTo? }
```

Supabase MFA API:
```typescript
const { data: mfaData } = await supabase.auth.mfa.listFactors();
// mfaData.totp: array de fatores TOTP
// fator.status: "verified" | "unverified"
const hasMfa = mfaData?.totp?.some(f => f.status === "verified") ?? false;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescrever middleware.ts com updateSession e redirect de auth routes</name>
  <files>src/middleware.ts</files>
  <action>
    Reescrever `src/middleware.ts` completamente. A função `enforceVaultAccess` existente pode ser mantida como export para outros consumidores se existirem (verificar — se não for importada em nenhum lugar além do próprio middleware, remover).

    Nova lógica:

    ```typescript
    import type { NextRequest } from "next/server";
    import { NextResponse } from "next/server";
    import { updateSession } from "@/lib/supabase/middleware";

    const AUTH_ROUTES = ["/sign-in", "/sign-up", "/verify-email", "/reset-password"];

    export async function middleware(request: NextRequest) {
      const { supabase, supabaseResponse, user } = await updateSession(request);

      const pathname = request.nextUrl.pathname;
      const isAuthRoute = AUTH_ROUTES.some((r) => pathname === r || pathname.startsWith(r + "/"));

      if (isAuthRoute && user) {
        // Usuário autenticado acessando rota de auth — redirecionar
        const { data: mfaData } = await supabase.auth.mfa.listFactors();
        const hasMfa = mfaData?.totp?.some((f) => f.status === "verified") ?? false;
        const redirectTo = hasMfa ? "/inicio" : "/mfa-setup";
        return NextResponse.redirect(new URL(redirectTo, request.url));
      }

      return supabaseResponse;
    }

    export const config = {
      matcher: [
        "/vault/:path*",
        "/setup/:path*",
        "/sign-in",
        "/sign-up",
        "/verify-email",
        "/reset-password",
        "/inicio",
        "/patients/:path*",
        "/agenda/:path*",
        "/sessions/:path*",
        "/financeiro/:path*",
        "/settings/:path*",
      ],
    };
    ```

    Nota importante sobre o matcher: o middleware agora precisa cobrir as rotas de vault para que `updateSession` faça o refresh do token em TODAS as rotas protegidas. O matcher existente (`/vault/:path*`, `/setup/:path*`) provavelmente é legado — as rotas reais do vault são `/inicio`, `/patients/*`, `/agenda/*`, etc. Incluir todas.

    Nota sobre `enforceVaultAccess`: verificar se é importada em algum arquivo além do próprio middleware. Se não for, não precisa ser mantida. A proteção de vault (redirecionar não-autenticados) pode ser adicionada em um futuro plan da Phase 14 (QUAL hardening) — na Phase 12 o objetivo é apenas o redirect de autenticados fora de auth routes.
  </action>
  <verify>
    <automated>cd /home/caramaschi/Área\ de\ trabalho/Projetos/PsiLock && pnpm tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>TypeScript sem erros. middleware.ts é async, chama updateSession, redireciona autenticados de auth routes para /inicio ou /mfa-setup. Matcher cobre auth routes e vault routes.</done>
</task>

</tasks>

<verification>
pnpm tsc --noEmit — sem erros
pnpm test --run — suite verde (middleware não é coberto por unit tests — validação manual descrita abaixo)

Validação manual:
1. Estar logado → navegar para /sign-in → deve redirecionar para /inicio (MFA completo) ou /mfa-setup
2. Não estar logado → navegar para /sign-in → página de login renderiza normalmente
3. pnpm dev sobe sem erro de compilação
</verification>

<success_criteria>
- middleware.ts chama updateSession em vez de enforceVaultAccess com null hardcoded
- Autenticados em auth routes são redirecionados
- Matcher cobre auth routes E vault routes
- TypeScript compila sem erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/12-authentication-ux/12-04-SUMMARY.md`
</output>
