---
phase: 13-ui-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
autonomous: true
requirements:
  - UIUX-01
  - UIUX-04

must_haves:
  truths:
    - "Tokens tipográficos (--font-size-page-title, --font-size-body, --font-size-meta, --font-size-label, --font-size-section-title) estão definidos em :root"
    - "Tokens de fonte (--font-serif, --font-sans) estão definidos em :root"
    - "Tokens de espaçamento (--space-page-padding, --space-section-gap, --space-row-height) estão definidos em :root"
    - "Animação @keyframes skeleton-pulse está definida em globals.css"
    - "Classes responsivas base (.vault-sidebar, .vault-bottom-nav, .vault-content) com media queries @media (max-width: 767px) e (min-width: 768px) estão definidas"
    - "Classes .vault-sidebar .nav-link e .vault-sidebar .nav-link.active sobrescrevem cores para contexto dark"
    - "Classes .bottom-nav-item e .bottom-nav-item.active estão definidas"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Design tokens completos, animações, classes responsivas, estilos sidebar dark, bottom nav styles"
      contains: "--font-size-page-title"
  key_links:
    - from: "src/app/globals.css :root"
      to: "todos os page.tsx e componentes vault"
      via: "var(--token)"
      pattern: "--font-size-page-title"
---

<objective>
Estabelecer o design system completo em globals.css: tokens tipográficos, tokens de espaçamento, animação skeleton, classes responsivas com media queries, e estilos para sidebar dark e bottom nav mobile.

Purpose: Esta é a base que todos os planos subsequentes dependem. Sem esses tokens e classes, os outros planos não podem ser implementados corretamente.
Output: globals.css expandido com escala tipográfica, breakpoints responsivos, e estilos de navegação.
</objective>

<execution_context>
@/home/caramaschi/.claude/get-shit-done/workflows/execute-plan.md
@/home/caramaschi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-ui-ux-polish/13-CONTEXT.md
@.planning/phases/13-ui-ux-polish/13-RESEARCH.md
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar tokens tipográficos e de espaçamento em globals.css</name>
  <files>src/app/globals.css</files>
  <action>
Abrir globals.css e adicionar dentro do bloco `:root` existente os seguintes tokens novos (não substituir os existentes, apenas adicionar):

```css
/* Tipografia — escala fase 13 */
--font-serif: "IBM Plex Serif", Georgia, serif;
--font-sans:  "IBM Plex Sans", "Segoe UI", sans-serif;

--font-size-page-title:    1.5rem;    /* 24px — h1 de página */
--font-size-section-title: 1rem;      /* 16px — h2 de seção */
--font-size-body:          0.9375rem; /* 15px — texto corrido */
--font-size-meta:          0.8125rem; /* 13px — metadados, datas, status */
--font-size-label:         0.75rem;   /* 12px — eyebrow uppercase */

/* Espaçamento */
--space-page-padding-x: 2.5rem;
--space-page-padding-y: 2rem;
--space-section-gap:    1.5rem;
--space-row-height:     3.5rem; /* 56px mínimo */
```

A seguir, adicionar ao final do arquivo (fora do :root):

1. Animação skeleton:
```css
@keyframes skeleton-pulse {
  0%, 100% { opacity: 1; }
  50%       { opacity: 0.5; }
}
```

2. Classes para sidebar dark — sobrescrever .nav-link no contexto dark:
```css
/* Sidebar dark context — sobrescreve cores do .nav-link global */
.vault-sidebar .nav-link {
  color: rgba(255, 255, 255, 0.75);
}
.vault-sidebar .nav-link:hover {
  color: rgba(255, 255, 255, 0.95);
  background-color: rgba(255, 255, 255, 0.08);
}
.vault-sidebar .nav-link.active {
  color: rgba(255, 255, 255, 0.98);
  background-color: rgba(255, 255, 255, 0.12);
  font-weight: 500;
}
```

3. Classes de layout responsivo:
```css
/* Responsividade vault */
@media (max-width: 767px) {
  .vault-sidebar {
    display: none !important;
  }
  .vault-bottom-nav {
    display: flex !important;
  }
  .vault-content {
    padding-bottom: 5rem;
  }
}

@media (min-width: 768px) {
  .vault-bottom-nav {
    display: none !important;
  }
}
```

4. Estilos do bottom nav mobile:
```css
/* Bottom nav mobile */
.vault-bottom-nav {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  height: 4rem;
  background-color: var(--color-surface-0);
  border-top: 1px solid var(--color-border);
  display: none; /* oculto por padrão em desktop */
  align-items: center;
  justify-content: space-around;
  z-index: 100;
  padding: 0 0.5rem;
}

.bottom-nav-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0.25rem;
  font-size: var(--font-size-label);
  color: var(--color-text-2);
  text-decoration: none;
  padding: 0.5rem 0.75rem;
  border-radius: var(--radius-md);
  min-width: 3rem;
  transition: color 0.15s;
}

.bottom-nav-item.active {
  color: var(--color-accent);
  font-weight: 600;
}

.bottom-nav-item svg {
  width: 1.25rem;
  height: 1.25rem;
}
```

Regras importantes:
- NÃO remover nenhum token ou classe já existente
- NÃO alterar variáveis de cor, radius ou shadow existentes
- NÃO usar Tailwind — manter CSS nativo
- Usar os valores EXATOS da escala tipográfica decidida (1.5rem para page-title, 0.9375rem para body, 0.8125rem para meta)
  </action>
  <verify>
    <automated>cd /home/caramaschi/Área\ de\ trabalho/Projetos/PsiLock && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - globals.css contém --font-size-page-title: 1.5rem no :root
    - globals.css contém @keyframes skeleton-pulse
    - globals.css contém .vault-sidebar .nav-link com cor rgba(255,255,255,...)
    - globals.css contém @media (max-width: 767px) com .vault-sidebar { display: none }
    - globals.css contém .vault-bottom-nav com position: fixed; bottom: 0
    - pnpm build passa sem erros de CSS
  </done>
</task>

</tasks>

<verification>
pnpm build passa sem erros após as adições ao globals.css.
grep "font-size-page-title" src/app/globals.css retorna linha com "1.5rem".
grep "skeleton-pulse" src/app/globals.css retorna a keyframe.
grep "vault-sidebar .nav-link" src/app/globals.css retorna os seletores dark.
</verification>

<success_criteria>
- Tokens tipográficos definidos em :root com valores exatos da escala decidida
- Tokens de fonte --font-serif e --font-sans presentes
- @keyframes skeleton-pulse definida
- Classes responsivas .vault-sidebar / .vault-bottom-nav com media queries corretas
- Estilos .vault-sidebar .nav-link para contexto dark com contraste WCAG AA
- Estilos .bottom-nav-item e .bottom-nav-item.active definidos
- pnpm build verde
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/13-ui-ux-polish/13-01-SUMMARY.md`
</output>
