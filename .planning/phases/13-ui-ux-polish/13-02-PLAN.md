---
phase: 13-ui-ux-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/app/(vault)/layout.tsx
  - src/app/(vault)/components/vault-sidebar-nav.tsx
  - src/app/(vault)/components/bottom-nav.tsx
autonomous: true
requirements:
  - UIUX-03
  - UIUX-06

must_haves:
  truths:
    - "Sidebar usa fundo warm dark (~#2d1810) com texto claro e contraste WCAG AA"
    - "Sidebar tem footer com avatar (iniciais) + nome do profissional + link de sair"
    - "Sidebar tem grupos de navegação com separadores visuais"
    - "Em mobile (< 768px) a sidebar não é renderizada / fica oculta via classe CSS"
    - "BottomNav component é renderizado no layout com a classe vault-bottom-nav (oculto em desktop via CSS)"
    - "BottomNav exibe 5 ícones com label curto e indica a rota ativa"
    - "Layout aplica classe vault-content na área de conteúdo principal"
  artifacts:
    - path: "src/app/(vault)/components/bottom-nav.tsx"
      provides: "Componente client island para navegação mobile"
      exports: ["BottomNav"]
    - path: "src/app/(vault)/layout.tsx"
      provides: "Layout vault com sidebar dark e bottom nav"
      contains: "vault-sidebar"
    - path: "src/app/(vault)/components/vault-sidebar-nav.tsx"
      provides: "Sidebar redesenhada com dark scheme e avatar footer"
      contains: "vault-sidebar"
  key_links:
    - from: "src/app/(vault)/layout.tsx"
      to: "src/app/(vault)/components/vault-sidebar-nav.tsx"
      via: "import e renderização do aside"
      pattern: "VaultSidebarNav"
    - from: "src/app/(vault)/layout.tsx"
      to: "src/app/(vault)/components/bottom-nav.tsx"
      via: "import e renderização após conteúdo"
      pattern: "BottomNav"
    - from: "src/app/(vault)/components/bottom-nav.tsx"
      to: "usePathname"
      via: "next/navigation"
      pattern: "usePathname"
---

<objective>
Redesenhar a sidebar para o esquema dark profissional, adicionar footer com avatar, criar o componente BottomNav para mobile, e atualizar o layout vault para usar as classes CSS definidas no plano 01.

Purpose: Navegação é a espinha dorsal da UX. A sidebar dark e o bottom nav mobile definem o caráter visual do produto e garantem usabilidade em todos os viewports.
Output: layout.tsx atualizado, vault-sidebar-nav.tsx redesenhado, bottom-nav.tsx criado.
</objective>

<execution_context>
@/home/caramaschi/.claude/get-shit-done/workflows/execute-plan.md
@/home/caramaschi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-ui-ux-polish/13-CONTEXT.md
@.planning/phases/13-ui-ux-polish/13-RESEARCH.md
@.planning/phases/13-ui-ux-polish/13-01-SUMMARY.md
@src/app/(vault)/layout.tsx
@src/app/(vault)/components/vault-sidebar-nav.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar BottomNav component e redesenhar VaultSidebarNav</name>
  <files>
    src/app/(vault)/components/bottom-nav.tsx
    src/app/(vault)/components/vault-sidebar-nav.tsx
  </files>
  <action>
**1. Criar src/app/(vault)/components/bottom-nav.tsx**

Componente client island (precisa de "use client" para usePathname). Exibe 5 itens de navegação com ícone SVG + label curto. Usa a classe `.vault-bottom-nav` definida em globals.css (que é `display: none` em desktop e `display: flex` em mobile via media query).

```typescript
"use client";
import { usePathname } from "next/navigation";

const NAV_ITEMS = [
  {
    href: "/inicio",
    label: "Início",
    icon: (/* svg home 20x20 */) // SVG inline de casa/home simples
  },
  {
    href: "/agenda",
    label: "Agenda",
    icon: (/* svg calendar 20x20 */)
  },
  {
    href: "/patients",
    label: "Pacientes",
    icon: (/* svg user/people 20x20 */)
  },
  {
    href: "/financeiro",
    label: "Financeiro",
    icon: (/* svg wallet/currency 20x20 */)
  },
  {
    href: "/settings/profile",
    label: "Config",
    icon: (/* svg gear/settings 20x20 */)
  },
];
```

Ícones: usar SVGs simples inline (não importar biblioteca) — stroke icons estilo Linear. Exemplos de paths para cada ícone:
- Home: `<path d="M3 12L12 3l9 9"/><path d="M9 21V12h6v9"/>`
- Calendar: `<rect x="3" y="4" width="18" height="18" rx="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/>`
- People: `<path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/>`
- Wallet: `<rect x="1" y="4" width="22" height="16" rx="2"/><line x1="1" y1="10" x2="23" y2="10"/>`
- Settings: `<circle cx="12" cy="12" r="3"/><path d="M19.07 4.93l-1.41 1.41M5.34 5.34L3.93 6.75M21 12h-2M3 12H1M19.07 19.07l-1.41-1.41M5.34 18.66l-1.41-1.41M12 21v-2M12 3V1"/>`

SVG wrapper: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth={1.75} strokeLinecap="round" strokeLinejoin="round" width={20} height={20}>`

Lógica de active state:
```typescript
const pathname = usePathname();
const isActive = (href: string) => pathname === href || pathname.startsWith(href + "/");
```

Renderização:
```typescript
<nav className="vault-bottom-nav" aria-label="Navegação mobile">
  {NAV_ITEMS.map((item) => (
    <a
      key={item.href}
      href={item.href}
      className={`bottom-nav-item${isActive(item.href) ? " active" : ""}`}
      aria-current={isActive(item.href) ? "page" : undefined}
    >
      {item.icon}
      <span>{item.label}</span>
    </a>
  ))}
</nav>
```

**2. Redesenhar src/app/(vault)/components/vault-sidebar-nav.tsx**

Manter: `"use client"`, usePathname, links de navegação existentes.
Modificar: esquema de cores dark, adicionar grupos de navegação com separadores, adicionar footer com avatar.

Cor de fundo da sidebar: `#2d1810` (warm dark — derivado do accent terracota escurecido).
Texto principal: `rgba(255,255,255,0.9)`.
Texto de apoio: `rgba(255,255,255,0.55)`.

Estrutura do componente:
```
<nav className="vault-sidebar-nav" style={{ display: "flex", flexDirection: "column", height: "100%", gap: "0.5rem" }}>
  {/* Logo / workspace mark */}
  <div style={{ ... }}>PsiVault</div>

  {/* Grupo 1: principal */}
  <div style={{ flex: 1, display: "flex", flexDirection: "column", gap: "2px", paddingTop: "0.5rem" }}>
    {NAV_LINKS.map(link => (
      <a key={link.href} href={link.href} className={`nav-link${isActive(link.href) ? " active" : ""}`}>
        {link.icon} {link.label}
      </a>
    ))}
  </div>

  {/* Separador */}
  <div style={{ height: "1px", backgroundColor: "rgba(255,255,255,0.08)", margin: "0.5rem 0" }} />

  {/* Footer: avatar + nome + sair */}
  <div style={{ ... }}>
    <div style={{ /* avatar iniciais */ ... }}>PS</div>
    <span style={{ fontSize: "var(--font-size-meta)", color: "rgba(255,255,255,0.75)" }}>Profissional</span>
    <a href="/api/auth/signout" style={{ fontSize: "var(--font-size-meta)", color: "rgba(255,255,255,0.5)" }}>Sair</a>
  </div>
</nav>
```

Para o footer: o avatar exibe iniciais hardcodadas "PS" (PsiVault placeholder — integração real com sessão é fase 14). O nome exibe "Profissional" como fallback. O link de sair aponta para `/api/auth/signout`.

Avatar style: `width: 2rem; height: 2rem; borderRadius: "50%"; backgroundColor: "rgba(255,255,255,0.15)"; display: "flex"; alignItems: "center"; justifyContent: "center"; fontSize: "0.7rem"; fontWeight: 600; color: "rgba(255,255,255,0.9)"`.

NAV_LINKS deve manter os mesmos links que já existem no componente atual (ler o arquivo antes de modificar).

Os ícones nos nav links: adicionar SVG inline simples (mesmos ícones do bottom nav, 16x16) antes do label de cada item para consistência.

Classes: adicionar `className="vault-sidebar"` ao wrapper do aside em layout.tsx (próxima task).
  </action>
  <verify>
    <automated>cd /home/caramaschi/Área\ de\ trabalho/Projetos/PsiLock && pnpm build 2>&1 | tail -30</automated>
  </verify>
  <done>
    - bottom-nav.tsx criado com "use client", usePathname, 5 itens de nav com SVG + label
    - vault-sidebar-nav.tsx redesenhado com fundo dark, footer de avatar, grupos de nav
    - pnpm build passa sem erros TypeScript
  </done>
</task>

<task type="auto">
  <name>Task 2: Atualizar layout.tsx — aside dark + classes CSS + BottomNav</name>
  <files>src/app/(vault)/layout.tsx</files>
  <action>
Modificar src/app/(vault)/layout.tsx para:

1. Aplicar `className="vault-sidebar"` ao elemento `<aside>` existente — isso ativa as media queries definidas em globals.css (display: none em mobile)

2. Atualizar inline style do `<aside>` para o esquema dark:
```typescript
const sidebarStyle = {
  width: "240px",
  minHeight: "100vh",
  backgroundColor: "#2d1810",
  borderRight: "none",
  display: "flex",
  flexDirection: "column",
  flexShrink: 0,
} satisfies React.CSSProperties;
```

3. Aplicar `className="vault-content"` ao elemento que envolve o conteúdo principal (o `<main>` ou div do lado direito) — ativa o `padding-bottom: 5rem` em mobile

4. Importar e renderizar `<BottomNav />` no final do layout, fora do `<main>`, como sibling do `<aside>`:
```typescript
import { BottomNav } from "@/app/(vault)/components/bottom-nav";

// No JSX, depois do </main>:
<BottomNav />
```

5. O wrapper raiz do layout deve ser um flex container horizontal:
```typescript
const rootStyle = {
  display: "flex",
  minHeight: "100vh",
  backgroundColor: "var(--color-surface-0)",
} satisfies React.CSSProperties;
```

Ler o arquivo atual antes de modificar para preservar a estrutura existente (children rendering, SearchBar se houver, etc.).

NÃO modificar a lógica de autenticação/workspace existente no layout.
NÃO remover o VaultSidebarNav — apenas envolvê-lo com o aside dark.
  </action>
  <verify>
    <automated>cd /home/caramaschi/Área\ de\ trabalho/Projetos/PsiLock && pnpm build 2>&1 | tail -30</automated>
  </verify>
  <done>
    - layout.tsx tem aside com className="vault-sidebar" e backgroundColor: "#2d1810"
    - layout.tsx importa e renderiza BottomNav
    - main/conteúdo tem className="vault-content"
    - pnpm build passa sem erros
  </done>
</task>

</tasks>

<verification>
pnpm build passes.
grep "vault-sidebar" src/app/(vault)/layout.tsx retorna o className no aside.
grep "BottomNav" src/app/(vault)/layout.tsx retorna o import e uso.
grep "vault-content" src/app/(vault)/layout.tsx retorna o className no conteúdo.
src/app/(vault)/components/bottom-nav.tsx existe e tem "use client".
</verification>

<success_criteria>
- Sidebar com background #2d1810 e texto claro
- Footer da sidebar com avatar de iniciais + nome + link de sair
- BottomNav component criado com 5 itens + active state
- Layout usa classes vault-sidebar, vault-bottom-nav, vault-content para controle responsivo via CSS
- pnpm build verde
- pnpm test verde (sem regressões)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/13-ui-ux-polish/13-02-SUMMARY.md`
</output>
