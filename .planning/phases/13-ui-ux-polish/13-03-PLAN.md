---
phase: 13-ui-ux-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/app/(vault)/inicio/loading.tsx
  - src/app/(vault)/inicio/error.tsx
  - src/app/(vault)/agenda/loading.tsx
  - src/app/(vault)/agenda/error.tsx
  - src/app/(vault)/patients/loading.tsx
  - src/app/(vault)/patients/error.tsx
  - src/app/(vault)/patients/[patientId]/loading.tsx
  - src/app/(vault)/patients/[patientId]/error.tsx
  - src/app/(vault)/financeiro/loading.tsx
  - src/app/(vault)/financeiro/error.tsx
autonomous: true
requirements:
  - UIUX-05

must_haves:
  truths:
    - "loading.tsx existe em cada uma das 5 rotas principais (inicio, agenda, patients, patients/[patientId], financeiro)"
    - "error.tsx existe em cada uma das 5 rotas principais"
    - "Cada error.tsx tem 'use client' na linha 1"
    - "Cada loading.tsx renderiza skeleton com o mesmo shellStyle (padding, maxWidth) do page.tsx correspondente"
    - "Cada error.tsx exibe 'Algo deu errado' + botão 'Tentar novamente' com onClick={reset}"
    - "Skeleton usa animação skeleton-pulse via CSS"
  artifacts:
    - path: "src/app/(vault)/inicio/loading.tsx"
      provides: "Skeleton para /inicio"
      contains: "skeleton-pulse"
    - path: "src/app/(vault)/inicio/error.tsx"
      provides: "Error boundary para /inicio"
      contains: "use client"
    - path: "src/app/(vault)/agenda/loading.tsx"
      provides: "Skeleton para /agenda"
      contains: "skeleton-pulse"
    - path: "src/app/(vault)/agenda/error.tsx"
      provides: "Error boundary para /agenda"
      contains: "use client"
    - path: "src/app/(vault)/patients/loading.tsx"
      provides: "Skeleton para /patients"
      contains: "skeleton-pulse"
    - path: "src/app/(vault)/patients/error.tsx"
      provides: "Error boundary para /patients"
      contains: "use client"
    - path: "src/app/(vault)/patients/[patientId]/loading.tsx"
      provides: "Skeleton para /patients/[patientId]"
      contains: "skeleton-pulse"
    - path: "src/app/(vault)/patients/[patientId]/error.tsx"
      provides: "Error boundary para /patients/[patientId]"
      contains: "use client"
    - path: "src/app/(vault)/financeiro/loading.tsx"
      provides: "Skeleton para /financeiro"
      contains: "skeleton-pulse"
    - path: "src/app/(vault)/financeiro/error.tsx"
      provides: "Error boundary para /financeiro"
      contains: "use client"
  key_links:
    - from: "Next.js App Router"
      to: "loading.tsx"
      via: "Suspense automático pelo framework"
      pattern: "export default function.*Loading"
    - from: "Next.js App Router"
      to: "error.tsx"
      via: "Error boundary automático pelo framework"
      pattern: "use client"
---

<objective>
Criar loading.tsx e error.tsx para cada uma das 5 rotas data-dependent do vault: inicio, agenda, patients, patients/[patientId], e financeiro.

Purpose: Sem loading/error states, qualquer delay de rede ou falha de DB resulta em tela branca. Esses arquivos são a proteção mínima de UX para produção.
Output: 10 novos arquivos (5 loading + 5 error) nas rotas vault principais.
</objective>

<execution_context>
@/home/caramaschi/.claude/get-shit-done/workflows/execute-plan.md
@/home/caramaschi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-ui-ux-polish/13-CONTEXT.md
@.planning/phases/13-ui-ux-polish/13-RESEARCH.md
@.planning/phases/13-ui-ux-polish/13-01-SUMMARY.md
@src/app/(vault)/inicio/page.tsx
@src/app/(vault)/agenda/page.tsx
@src/app/(vault)/patients/page.tsx
@src/app/(vault)/financeiro/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar loading.tsx e error.tsx para inicio, agenda e patients</name>
  <files>
    src/app/(vault)/inicio/loading.tsx
    src/app/(vault)/inicio/error.tsx
    src/app/(vault)/agenda/loading.tsx
    src/app/(vault)/agenda/error.tsx
    src/app/(vault)/patients/loading.tsx
    src/app/(vault)/patients/error.tsx
  </files>
  <action>
Ler src/app/(vault)/inicio/page.tsx, src/app/(vault)/agenda/page.tsx, e src/app/(vault)/patients/page.tsx antes de criar os skeletons para extrair o shellStyle (padding, maxWidth, gap) de cada página e replicar nos loading.tsx correspondentes.

**Padrão para todos os loading.tsx:**

```typescript
// NÃO tem "use client" — é server component
import React from "react";

const shellStyle = {
  // REPLICAR o mesmo padding/maxWidth/gap do page.tsx correspondente
  padding: "2rem 2.5rem",
  maxWidth: 960,
  width: "100%",
  display: "grid",
  gap: "1.5rem",
  alignContent: "start",
} satisfies React.CSSProperties;

const skeletonBlockStyle = {
  height: "180px",
  borderRadius: "var(--radius-xl)",
  background: "var(--color-surface-1)",
  border: "1px solid var(--color-border)",
  animation: "skeleton-pulse 1.5s ease-in-out infinite",
} satisfies React.CSSProperties;

const skeletonTitleStyle = {
  height: "2rem",
  width: "40%",
  borderRadius: "var(--radius-md)",
  background: "var(--color-surface-1)",
  border: "1px solid var(--color-border)",
  animation: "skeleton-pulse 1.5s ease-in-out infinite",
} satisfies React.CSSProperties;

export default function [NomeRota]Loading() {
  return (
    <main style={shellStyle}>
      <div style={skeletonTitleStyle} />
      <div style={skeletonBlockStyle} />
      <div style={skeletonBlockStyle} />
    </main>
  );
}
```

Para /agenda: adicionar 4 skeleton blocks (1 header + 3 card slots) pois a agenda tem layout de cards de sessão.
Para /patients: adicionar 5 skeleton rows finos (height: 56px) pois a lista de pacientes é densa (linhas, não cards grandes).
Para /inicio: 3 skeleton blocks (seções do dashboard).

**Padrão para todos os error.tsx:**

```typescript
"use client";
import React from "react";

const shellStyle = {
  // REPLICAR o mesmo padding do page.tsx correspondente
  padding: "2rem 2.5rem",
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  minHeight: "60vh",
} satisfies React.CSSProperties;

const containerStyle = {
  textAlign: "center" as const,
  display: "grid",
  gap: "1rem",
  maxWidth: "360px",
} satisfies React.CSSProperties;

export default function [NomeRota]Error({
  error,
  reset,
}: {
  error: Error & { digest?: string };
  reset: () => void;
}) {
  return (
    <main style={shellStyle}>
      <div style={containerStyle}>
        <p style={{ fontSize: "var(--font-size-body)", fontWeight: 600, color: "var(--color-text-1)" }}>
          Algo deu errado
        </p>
        <p style={{ fontSize: "var(--font-size-meta)", color: "var(--color-text-2)" }}>
          Não foi possível carregar esta página. Tente novamente ou volte mais tarde.
        </p>
        <button onClick={reset} className="btn-secondary">
          Tentar novamente
        </button>
      </div>
    </main>
  );
}
```

CRÍTICO: todo error.tsx DEVE começar com `"use client"` na linha 1 — requisito Next.js. Sem isso, o build falha.
NÃO acessar repositórios, DB ou server actions em error.tsx — é exclusivamente client.
  </action>
  <verify>
    <automated>cd /home/caramaschi/Área\ de\ trabalho/Projetos/PsiLock && pnpm build 2>&1 | tail -30</automated>
  </verify>
  <done>
    - 6 arquivos criados (3 loading + 3 error) para inicio, agenda, patients
    - Todos os error.tsx começam com "use client"
    - Todos os loading.tsx têm animação skeleton-pulse
    - pnpm build passa
  </done>
</task>

<task type="auto">
  <name>Task 2: Criar loading.tsx e error.tsx para patients/[patientId] e financeiro</name>
  <files>
    src/app/(vault)/patients/[patientId]/loading.tsx
    src/app/(vault)/patients/[patientId]/error.tsx
    src/app/(vault)/financeiro/loading.tsx
    src/app/(vault)/financeiro/error.tsx
  </files>
  <action>
Ler src/app/(vault)/patients/[patientId]/page.tsx e src/app/(vault)/financeiro/page.tsx para extrair shellStyle antes de criar os skeletons.

Seguir os mesmos padrões da Task 1:
- loading.tsx: sem "use client", skeleton com shellStyle idêntico ao page.tsx, animation: "skeleton-pulse 1.5s ease-in-out infinite"
- error.tsx: "use client" linha 1, props `error` e `reset`, btn-secondary com onClick={reset}, mensagem "Algo deu errado"

Para patients/[patientId]/loading.tsx: página de perfil tem header de paciente + seções de timeline/documentos/reminders. Criar skeleton com:
- 1 bloco de header largo (height: 120px) para o cabeçalho do paciente
- 3 blocos de seção (height: 200px cada)

Para financeiro/loading.tsx: página tem listagem mensal de cobranças. Criar skeleton com:
- 1 bloco de header (height: 80px) para resumo do mês
- 5 skeleton rows (height: 56px) para a lista de cobranças

CRÍTICO: patients/[patientId]/error.tsx DEVE ter "use client" — mesma regra.

Naming convention dos exports:
- patients/[patientId]/loading.tsx → `export default function PatientProfileLoading()`
- patients/[patientId]/error.tsx → `export default function PatientProfileError(...)`
- financeiro/loading.tsx → `export default function FinanceiroLoading()`
- financeiro/error.tsx → `export default function FinanceiroError(...)`
  </action>
  <verify>
    <automated>cd /home/caramaschi/Área\ de\ trabalho/Projetos/PsiLock && pnpm build 2>&1 | tail -30</automated>
  </verify>
  <done>
    - 4 arquivos criados (2 loading + 2 error) para patients/[patientId] e financeiro
    - Todos os error.tsx começam com "use client"
    - pnpm build passa sem erros
    - Total: 10 arquivos criados neste plano (5 loading + 5 error)
  </done>
</task>

</tasks>

<verification>
ls src/app/(vault)/inicio/loading.tsx src/app/(vault)/inicio/error.tsx retorna os arquivos.
ls src/app/(vault)/agenda/loading.tsx src/app/(vault)/agenda/error.tsx retorna os arquivos.
ls src/app/(vault)/patients/loading.tsx src/app/(vault)/patients/error.tsx retorna os arquivos.
ls src/app/(vault)/patients/[patientId]/loading.tsx src/app/(vault)/patients/[patientId]/error.tsx retorna os arquivos.
ls src/app/(vault)/financeiro/loading.tsx src/app/(vault)/financeiro/error.tsx retorna os arquivos.
grep -l '"use client"' src/app/(vault)/*/error.tsx src/app/(vault)/patients/[patientId]/error.tsx retorna todos os error.tsx.
pnpm build passa.
</verification>

<success_criteria>
- 10 arquivos criados: 5 loading.tsx + 5 error.tsx nas rotas vault principais
- Todos os error.tsx têm "use client" como primeira diretiva
- Todos os loading.tsx usam skeleton-pulse animation (definida no plano 01)
- Skeletons replicam o shellStyle dos page.tsx correspondentes (sem layout shift)
- pnpm build verde
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/13-ui-ux-polish/13-03-SUMMARY.md`
</output>
