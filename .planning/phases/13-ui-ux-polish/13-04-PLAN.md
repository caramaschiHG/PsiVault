---
phase: 13-ui-ux-polish
plan: 04
type: execute
wave: 3
depends_on:
  - 13-01
  - 13-02
  - 13-03
files_modified:
  - src/app/(vault)/components/empty-state.tsx
  - src/app/(vault)/inicio/page.tsx
  - src/app/(vault)/agenda/page.tsx
  - src/app/(vault)/patients/page.tsx
  - src/app/(vault)/patients/[patientId]/page.tsx
  - src/app/(vault)/financeiro/page.tsx
autonomous: false
requirements:
  - UIUX-02
  - UIUX-03
  - UIUX-04
  - UIUX-05
  - UIUX-06

must_haves:
  truths:
    - "Títulos de página h1 usam fontSize: 'var(--font-size-page-title)' (1.5rem) e fontFamily: 'var(--font-serif)' em todas as views primárias"
    - "Body text usa fontSize: 'var(--font-size-body)' (0.9375rem)"
    - "Metadados, datas e status usam fontSize: 'var(--font-size-meta)' (0.8125rem)"
    - "Títulos de seção (HOJE, LEMBRETES, etc.) usam fontWeight 600 + textTransform uppercase + letterSpacing"
    - "Listas de pacientes e cobranças são rows com hover, NÃO cards em grid"
    - "EmptyState component existe e é usado quando array de dados está vazio"
    - "Empty states têm ícone + título humanizado + descrição + botão de ação"
    - "Em mobile, formulários são 1 coluna, campos full-width"
    - "FAB (floating action button) aparece em mobile nas páginas com ação primária"
    - "pnpm build passa sem erros TypeScript"
  artifacts:
    - path: "src/app/(vault)/components/empty-state.tsx"
      provides: "Componente reutilizável EmptyState"
      exports: ["EmptyState"]
    - path: "src/app/(vault)/inicio/page.tsx"
      provides: "Dashboard /inicio com tipografia correta e empty state"
      contains: "font-size-page-title"
    - path: "src/app/(vault)/patients/page.tsx"
      provides: "Lista de pacientes com rows + hover + empty state"
      contains: "EmptyState"
    - path: "src/app/(vault)/agenda/page.tsx"
      provides: "Agenda com tipografia e empty state para semana livre"
      contains: "EmptyState"
    - path: "src/app/(vault)/financeiro/page.tsx"
      provides: "Financeiro com rows + empty state"
      contains: "EmptyState"
  key_links:
    - from: "src/app/(vault)/patients/page.tsx"
      to: "src/app/(vault)/components/empty-state.tsx"
      via: "import EmptyState"
      pattern: "EmptyState"
    - from: "src/app/(vault)/inicio/page.tsx"
      to: "var(--font-size-page-title)"
      via: "fontSize inline style"
      pattern: "font-size-page-title"
---

<objective>
Criar o componente EmptyState reutilizável e aplicar o polish visual completo nas 5 views primárias: tipografia correta via tokens, hierarquia visual, rows densas em listas, empty states humanizados, e FAB mobile nas páginas de ação.

Purpose: Este é o plano de acabamento visual. Com os tokens (13-01), navegação (13-02) e loading/error states (13-03) prontos, este plano aplica o design system em cada página para o produto parecer produção-ready.
Output: EmptyState component + 5 páginas polidas com tipografia, hierarquia, e estados corretos.
</objective>

<execution_context>
@/home/caramaschi/.claude/get-shit-done/workflows/execute-plan.md
@/home/caramaschi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-ui-ux-polish/13-CONTEXT.md
@.planning/phases/13-ui-ux-polish/13-RESEARCH.md
@.planning/phases/13-ui-ux-polish/13-01-SUMMARY.md
@.planning/phases/13-ui-ux-polish/13-02-SUMMARY.md
@.planning/phases/13-ui-ux-polish/13-03-SUMMARY.md
@src/app/(vault)/inicio/page.tsx
@src/app/(vault)/agenda/page.tsx
@src/app/(vault)/patients/page.tsx
@src/app/(vault)/financeiro/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar EmptyState component e polir inicio e patients</name>
  <files>
    src/app/(vault)/components/empty-state.tsx
    src/app/(vault)/inicio/page.tsx
    src/app/(vault)/patients/page.tsx
  </files>
  <action>
**1. Criar src/app/(vault)/components/empty-state.tsx**

Componente server puro (sem "use client"):

```typescript
import React from "react";

interface EmptyStateProps {
  icon: React.ReactNode;
  title: string;
  description?: string;
  actionLabel?: string;
  actionHref?: string;
}

export function EmptyState({ icon, title, description, actionLabel, actionHref }: EmptyStateProps) {
  const containerStyle = {
    display: "flex",
    flexDirection: "column" as const,
    alignItems: "center",
    justifyContent: "center",
    gap: "0.75rem",
    padding: "3rem 1.5rem",
    textAlign: "center" as const,
    color: "var(--color-text-2)",
  } satisfies React.CSSProperties;

  const iconStyle = {
    width: "3rem",
    height: "3rem",
    opacity: 0.4,
    marginBottom: "0.25rem",
  } satisfies React.CSSProperties;

  const titleStyle = {
    fontSize: "var(--font-size-body)",
    fontWeight: 600,
    color: "var(--color-text-1)",
    margin: 0,
  } satisfies React.CSSProperties;

  const descStyle = {
    fontSize: "var(--font-size-meta)",
    color: "var(--color-text-2)",
    margin: 0,
    maxWidth: "280px",
  } satisfies React.CSSProperties;

  return (
    <div style={containerStyle}>
      <div style={iconStyle}>{icon}</div>
      <p style={titleStyle}>{title}</p>
      {description && <p style={descStyle}>{description}</p>}
      {actionLabel && actionHref && (
        <a href={actionHref} className="btn-primary" style={{ marginTop: "0.5rem" }}>
          {actionLabel}
        </a>
      )}
    </div>
  );
}
```

**2. Polir src/app/(vault)/inicio/page.tsx**

Ler o arquivo completo primeiro. Aplicar as seguintes mudanças:

a) Título de página h1: mudar fontSize para `"var(--font-size-page-title)"` e adicionar `fontFamily: "var(--font-serif)"`. Se não existe h1 explícito com fontSize, criar um com esses valores.

b) Títulos de seção (ex: "HOJE", "PRÓXIMAS SESSÕES", "LEMBRETES"): aplicar:
```typescript
{
  fontSize: "var(--font-size-label)",
  fontWeight: 600,
  textTransform: "uppercase" as const,
  letterSpacing: "0.06em",
  color: "var(--color-text-2)",
}
```

c) Body text e metadados: substituir fontSize hardcoded por tokens (0.9375rem → `var(--font-size-body)`, 0.875rem ou menores → `var(--font-size-meta)`).

d) Se o dashboard estiver vazio (sem sessões hoje, sem lembretes): usar EmptyState com mensagens humanizadas. Verificar as condições no page.tsx e adicionar `<EmptyState>` onde cabível (ex: lista de sessões do dia vazia → "Sua agenda está livre hoje").

e) shellStyle: garantir `padding: "var(--space-page-padding-y) var(--space-page-padding-x)"` ou equivalente `"2rem 2.5rem"`.

**3. Polir src/app/(vault)/patients/page.tsx**

Ler o arquivo completo primeiro. Aplicar:

a) Título h1: `fontSize: "var(--font-size-page-title)"`, `fontFamily: "var(--font-serif)"`.

b) Lista de pacientes: manter como rows (não cards em grid) — verificar se já é assim. Cada row deve ter:
   - `minHeight: "var(--space-row-height)"` (3.5rem = 56px)
   - hover state via className ou inline `:hover` (não disponível inline — usar classe CSS se necessário, ou deixar via CSS class existente)
   - `borderBottom: "1px solid var(--color-border)"`

c) Se lista vazia: adicionar `<EmptyState>`:
```typescript
<EmptyState
  icon={/* SVG de pessoa */}
  title="Nenhum paciente ainda"
  description="Comece adicionando o primeiro paciente ao sistema."
  actionLabel="Adicionar paciente"
  actionHref="/patients/new"
/>
```

d) FAB mobile para ação primária (adicionar paciente): adicionar no final do JSX um `<a>` com position fixed, só visível em mobile via classe CSS:
```typescript
<a
  href="/patients/new"
  className="fab-mobile"
  style={{
    position: "fixed",
    bottom: "5.5rem",
    right: "1.5rem",
    width: "3.5rem",
    height: "3.5rem",
    borderRadius: "50%",
    backgroundColor: "var(--color-accent)",
    color: "#fff",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    boxShadow: "var(--shadow-lg)",
    fontSize: "1.5rem",
    textDecoration: "none",
    zIndex: 90,
  } satisfies React.CSSProperties}
  aria-label="Adicionar paciente"
>
  +
</a>
```
Adicionar classe `.fab-mobile { display: none; }` no globals.css e `@media (max-width: 767px) { .fab-mobile { display: flex; } }` — ou verificar se já existe algum mecanismo. Como inline styles não suportam media queries, a visibilidade do FAB em desktop deve ser controlada por classe CSS.

Adicionar ao globals.css (se ainda não existir):
```css
.fab-mobile {
  display: none;
}
@media (max-width: 767px) {
  .fab-mobile {
    display: flex !important;
  }
}
```
  </action>
  <verify>
    <automated>cd /home/caramaschi/Área\ de\ trabalho/Projetos/PsiLock && pnpm build 2>&1 | tail -30</automated>
  </verify>
  <done>
    - empty-state.tsx criado com interface EmptyStateProps e export EmptyState
    - inicio/page.tsx usa var(--font-size-page-title) e var(--font-serif) no título
    - patients/page.tsx usa EmptyState para lista vazia com mensagem humanizada
    - patients/page.tsx tem FAB mobile (+) com posição fixa
    - pnpm build passa
  </done>
</task>

<task type="auto">
  <name>Task 2: Polir agenda, patients/[patientId] e financeiro</name>
  <files>
    src/app/(vault)/agenda/page.tsx
    src/app/(vault)/patients/[patientId]/page.tsx
    src/app/(vault)/financeiro/page.tsx
  </files>
  <action>
Ler os 3 arquivos completos antes de modificar. Aplicar para cada um:

**src/app/(vault)/agenda/page.tsx**

a) Título h1: `fontSize: "var(--font-size-page-title)"`, `fontFamily: "var(--font-serif)"`.

b) Títulos de seção de data (ex: "SEG, 18 MAR", "HOJE"): aplicar `fontWeight: 600`, `textTransform: "uppercase"`, `letterSpacing: "0.06em"`, `fontSize: "var(--font-size-label)"`.

c) Metadados de sessão (horário, duração, tipo): `fontSize: "var(--font-size-meta)"`.

d) Se semana sem sessões: usar EmptyState:
```typescript
<EmptyState
  icon={/* SVG calendar */}
  title="Sua semana está livre"
  description="Nenhuma sessão agendada para este período."
  actionLabel="Agendar sessão"
  actionHref="/appointments/new"
/>
```

e) FAB mobile para agendar:
```typescript
<a href="/appointments/new" className="fab-mobile" style={{ /* mesmo estilo do patients */ }} aria-label="Agendar sessão">+</a>
```

**src/app/(vault)/patients/[patientId]/page.tsx**

a) Nome do paciente (h1 ou equivalente): `fontSize: "var(--font-size-page-title)"`, `fontFamily: "var(--font-serif)"`.

b) Títulos de seção ("SESSÕES", "DOCUMENTOS", "LEMBRETES", "COBRANÇAS"): `fontWeight: 600`, `textTransform: "uppercase"`, `letterSpacing: "0.06em"`, `fontSize: "var(--font-size-label)"`.

c) Metadados (data, horário, status de cobrança): `fontSize: "var(--font-size-meta)"`.

d) Seções vazias: adicionar `<EmptyState>` inline para seção de sessões sem histórico, documentos vazios, lembretes vazios. Usar tom humanizado:
   - Sessões vazia: "Nenhuma sessão registrada ainda"
   - Documentos vazios: "Nenhum documento gerado ainda"
   - Lembretes vazios: "Tudo em dia!"

e) Garantir espaço entre seções: `gap: "var(--space-section-gap)"` ou `"1.5rem"` entre seções.

**src/app/(vault)/financeiro/page.tsx**

a) Título h1: `fontSize: "var(--font-size-page-title)"`, `fontFamily: "var(--font-serif)"`.

b) Lista de cobranças: manter como rows (table-like), não cards. Cada row:
   - `minHeight: "var(--space-row-height)"` (3.5rem)
   - `borderBottom: "1px solid var(--color-border)"`

c) Metadados financeiros (data, valor, status): `fontSize: "var(--font-size-meta)"`.

d) Se lista vazia: EmptyState com mensagem "Nenhuma cobrança este mês" + "Tudo em dia!".

**Regras gerais para todos:**
- NÃO alterar lógica de dados, server actions, ou formatação existente
- NÃO remover funcionalidades — apenas estilizar
- Substituir fontSize hardcoded (2rem, 1.5rem literais, etc.) por vars
- Usar `satisfies React.CSSProperties` em todos os style objects novos/modificados
- NÃO adicionar Tailwind ou classes que não existam em globals.css
  </action>
  <verify>
    <automated>cd /home/caramaschi/Área\ de\ trabalho/Projetos/PsiLock && pnpm build 2>&1 | tail -30</automated>
  </verify>
  <done>
    - agenda/page.tsx usa var(--font-size-page-title) e EmptyState para semana livre
    - patients/[patientId]/page.tsx usa var(--font-serif) no nome do paciente e EmptyState por seção
    - financeiro/page.tsx usa var(--font-size-page-title) e rows densas com minHeight
    - pnpm test passa (sem regressões em domain tests)
    - pnpm build passes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação visual do design system</name>
  <action>Verificar visualmente que todos os elementos de design foram aplicados corretamente: sidebar dark, tipografia, bottom nav mobile, empty states, e polish das views primárias.</action>
  <what-built>
    Design system completo aplicado: tokens tipográficos, sidebar dark, bottom nav mobile, loading/error states por rota, EmptyState component, e polish visual nas 5 views primárias (inicio, agenda, pacientes, perfil de paciente, financeiro).
  </what-built>
  <how-to-verify>
    1. Iniciar o servidor: pnpm dev
    2. Verificar sidebar: visitar /inicio — sidebar deve aparecer escura (fundo ~#2d1810) com texto claro, footer com iniciais "PS" + "Profissional" + "Sair"
    3. Verificar tipografia: título "Início" deve estar em IBM Plex Serif, 24px (menor que antes)
    4. Verificar responsivo: abrir DevTools, mudar viewport para 375px — sidebar deve sumir, bottom nav deve aparecer na parte de baixo com 5 ícones
    5. Verificar loading state: ir para /agenda — deve aparecer o skeleton enquanto carrega (difícil de ver em local; forçar via DevTools > Network > Throttle)
    6. Verificar empty states: se alguma seção estiver vazia, deve aparecer ícone + título humanizado + botão de ação
    7. Verificar /patients — lista deve ser rows densas (não cards em grid)
    8. Verificar /financeiro — lista de cobranças deve ser rows com linhas separadoras
    9. Em mobile (375px), verificar FAB "+" em /patients e /agenda
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva os problemas encontrados</resume-signal>
</task>

</tasks>

<verification>
pnpm build passes.
pnpm test passes (nenhum domain test regressou).
grep "font-size-page-title" src/app/(vault)/inicio/page.tsx retorna linha de estilo.
grep "EmptyState" src/app/(vault)/patients/page.tsx retorna uso do componente.
src/app/(vault)/components/empty-state.tsx existe com export EmptyState.
</verification>

<success_criteria>
- EmptyState component criado e usado nas 5 views primárias
- Todos os títulos de página usam var(--font-size-page-title) + var(--font-serif)
- Títulos de seção usam uppercase + font-weight 600 + letter-spacing
- Listas de pacientes e cobranças são rows densas (minHeight 56px) com separadores
- Empty states têm ícones SVG + mensagens humanizadas em pt-BR + botão de ação
- FAB mobile em patients e agenda
- pnpm build verde
- pnpm test verde
- Checkpoint humano aprovado: sidebar dark, bottom nav mobile, tipografia, empty states funcionando visualmente
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/13-ui-ux-polish/13-04-SUMMARY.md`
</output>
