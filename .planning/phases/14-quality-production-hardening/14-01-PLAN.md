---
phase: 14-quality-production-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(vault)/settings/dados-e-privacidade/actions.ts
  - src/app/api/backup/route.ts
  - src/app/api/export/patient/[patientId]/route.ts
autonomous: true
requirements:
  - QUAL-01

must_haves:
  truths:
    - "Clicar em 'Baixar backup' com senha errada retorna erro 'Senha incorreta.' e não emite cookie"
    - "Clicar em 'Baixar backup' com senha correta emite o cookie psivault_backup_auth e libera o download"
    - "Clicar em exportar paciente com senha errada retorna erro sem emitir cookie psivault_export_auth"
    - "O email usado na verificação vem da sessão Supabase ativa, nunca de input do usuário"
    - "As API routes verificam cookie válido antes de servir dados — stub substituído"
  artifacts:
    - path: "src/app/(vault)/settings/dados-e-privacidade/actions.ts"
      provides: "confirmBackupAuthAction e exportPatientAuthAction com signInWithPassword real"
      contains: "supabase.auth.signInWithPassword"
    - path: "src/app/api/backup/route.ts"
      provides: "GET /api/backup com verificação de cookie real (sem stub)"
      contains: "psivault_backup_auth"
    - path: "src/app/api/export/patient/[patientId]/route.ts"
      provides: "GET /api/export/patient/[id] com verificação de cookie real (sem stub)"
      contains: "psivault_export_auth"
  key_links:
    - from: "src/app/(vault)/settings/dados-e-privacidade/actions.ts"
      to: "supabase.auth.signInWithPassword"
      via: "createClient() do @supabase/ssr"
      pattern: "signInWithPassword"
    - from: "src/app/api/backup/route.ts"
      to: "cookies().get('psivault_backup_auth')"
      via: "verificação do cookie httpOnly antes de servir o arquivo"
      pattern: "psivault_backup_auth"
---

<objective>
Substituir os stubs de re-auth em `confirmBackupAuthAction` e `exportPatientAuthAction` por verificação real de senha via Supabase Auth, e substituir o stub de verificação de cookie nas duas API routes de backup e export.

Purpose: O gate de re-auth para backup e export é atualmente um stub que aceita qualquer senha não-vazia (nas actions) e verifica o cookie de forma permissiva (nas routes). Isso é uma falha de segurança que precisa ser fechada antes do lançamento (QUAL-01).
Output: `actions.ts` com verificação real de senha + as duas API routes com verificação real de cookie.
</objective>

<execution_context>
@/home/caramaschi/.claude/get-shit-done/workflows/execute-plan.md
@/home/caramaschi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-quality-production-hardening/14-CONTEXT.md

<interfaces>
<!-- Contratos existentes que o executor precisa. Não explorar o codebase. -->

De src/lib/security/sensitive-actions.ts:
```typescript
export const SENSITIVE_ACTION_REAUTH_WINDOW_MS = 1000 * 60 * 10;

export interface SensitiveActionChallenge {
  id: string;
  actionId: string;
  title: string;
  reason: string;
  confirmationLabel: string;
  requiresReauth: true;
  issuedAt: Date;
}

export interface SensitiveActionDecision {
  allowed: boolean;
  status: "approved" | "needs_confirmation" | "needs_reauth" | "expired";
  message: string;
}
// evaluateSensitiveAction e createSensitiveActionChallenge também exportados
```

De src/app/(vault)/settings/dados-e-privacidade/actions.ts (estado atual — stub):
```typescript
// v1 stub: accept any non-empty password
export async function confirmBackupAuthAction(input: { password: string }): Promise<ActionResult>
export async function exportPatientAuthAction(input: { password: string }): Promise<ActionResult>
// Ambas setam cookie httpOnly por 10 min sem verificar a senha
const REAUTH_COOKIE_MAX_AGE_SECONDS = 60 * 10;
```

Padrão de createClient Supabase SSR (já em uso no projeto):
```typescript
import { createClient } from "@/lib/supabase/server";
// createClient() retorna Promise<SupabaseClient>
// Chamar await createClient() no início da action
```

Cookies das API routes a verificar:
- GET /api/backup: verificar cookie `psivault_backup_auth` (httpOnly, gerado por confirmBackupAuthAction)
- GET /api/export/patient/[patientId]: verificar cookie `psivault_export_auth` (httpOnly, gerado por exportPatientAuthAction)
- Se cookie ausente ou expirado: retornar 401
- Se cookie presente e válido: servir o arquivo normalmente
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Substituir stub por signInWithPassword em confirmBackupAuthAction e exportPatientAuthAction</name>
  <files>src/app/(vault)/settings/dados-e-privacidade/actions.ts</files>
  <action>
    Reescrever as duas funções em `actions.ts` para verificar a senha real via Supabase Auth. Manter o contrato externo idêntico (`ActionResult`), `REAUTH_COOKIE_MAX_AGE_SECONDS`, os nomes dos cookies, e a lógica de set do cookie httpOnly — apenas adicionar a verificação de senha antes de setar o cookie.

    Para AMBAS as funções (`confirmBackupAuthAction` e `exportPatientAuthAction`):

    1. Importar `createClient` de `"../../../../lib/supabase/server"` (ajustar profundidade de caminho conforme localização).
    2. No início da função, após guard de senha vazia:
       - `const supabase = await createClient();`
       - `const { data: { user } } = await supabase.auth.getUser();`
       - Se `!user?.email` → return `{ ok: false, error: "Sessão inválida. Faça login novamente." }`
    3. Chamar `await supabase.auth.signInWithPassword({ email: user.email, password: input.password })`.
    4. Se `error` → return `{ ok: false, error: "Senha incorreta." }`.
    5. Só então setar o cookie (lógica já existente) e retornar `{ ok: true }`.

    Remover o comentário "v1 stub" e o comentário "Production: verify...". Não alterar mais nada no arquivo.

    Pitfall a evitar: NUNCA usar o email do input do usuário — sempre da sessão Supabase (`user.email`).
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <done>Ambas as funções chamam `supabase.auth.signInWithPassword` com email da sessão; senha incorreta retorna `{ ok: false, error: "Senha incorreta." }`; senha correta seta cookie e retorna `{ ok: true }`. Não há mais referência ao stub.</done>
</task>

<task type="auto">
  <name>Task 2: Substituir stub de cookie nas API routes de backup e export</name>
  <files>
    src/app/api/backup/route.ts,
    src/app/api/export/patient/[patientId]/route.ts
  </files>
  <action>
    Ler os dois arquivos de route e identificar onde o cookie de re-auth é verificado. Substituir qualquer verificação permissiva (stub que aceita cookie vazio ou qualquer valor) por verificação real: cookie deve existir e não estar expirado.

    Para AMBOS os arquivos:
    1. Importar `cookies` de `"next/headers"`.
    2. No início do handler GET, antes de qualquer acesso aos dados:
       - Ler o cookie correspondente: `psivault_backup_auth` ou `psivault_export_auth`
       - Se cookie ausente: retornar `NextResponse.json({ error: "Não autorizado." }, { status: 401 })`
       - O cookie já carrega sua própria expiração via `Max-Age` no httpOnly set — se presente, está válido.
    3. Manter toda a lógica de geração/streaming do arquivo inalterada após a verificação.

    Não alterar imports além do necessário para a verificação de cookie. Não alterar lógica de geração do backup ou export.

    Nota: O cookie é gerado pelas actions com `REAUTH_COOKIE_MAX_AGE_SECONDS = 60 * 10` (10 minutos). O browser não envia cookies expirados, então verificar presença é suficiente.
  </action>
  <verify>
    <automated>grep -n "psivault_backup_auth" src/app/api/backup/route.ts && grep -n "psivault_export_auth" src/app/api/export/patient/\[patientId\]/route.ts</automated>
  </verify>
  <done>Ambos os routes leem o cookie correspondente e retornam 401 se ausente. Não há stub que aceite requisições sem cookie. pnpm test passa.</done>
</task>

</tasks>

<verification>
- `grep -r "v1 stub" src/app/(vault)/settings/dados-e-privacidade/actions.ts` retorna vazio
- `grep "signInWithPassword" src/app/(vault)/settings/dados-e-privacidade/actions.ts` retorna 2 ocorrências (uma por função)
- `grep "getUser" src/app/(vault)/settings/dados-e-privacidade/actions.ts` retorna 2 ocorrências
- `grep "psivault_backup_auth" src/app/api/backup/route.ts` retorna pelo menos 1 ocorrência (verificação de cookie)
- `grep "psivault_export_auth" src/app/api/export/patient/\[patientId\]/route.ts` retorna pelo menos 1 ocorrência
- `pnpm test` passa sem erros
</verification>

<success_criteria>
- `confirmBackupAuthAction` e `exportPatientAuthAction` verificam senha via Supabase antes de emitir cookie
- Email de verificação vem exclusivamente da sessão ativa (`supabase.auth.getUser()`)
- Contrato externo `ActionResult` mantido idêntico
- Zero comentários stub restantes no arquivo de actions
- API routes de backup e export retornam 401 se cookie de re-auth estiver ausente
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/14-quality-production-hardening/14-01-SUMMARY.md`
</output>
