---
phase: 14-quality-production-hardening
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(vault)/sessions/[appointmentId]/error.tsx
  - src/app/(vault)/patients/[patientId]/documents/error.tsx
autonomous: true
requirements:
  - QUAL-02

must_haves:
  truths:
    - "Erros em server components de sessions/[appointmentId] mostram boundary em pt-BR com botão 'Tentar novamente' em vez de tela branca"
    - "Erros em server components de patients/[patientId]/documents mostram boundary em pt-BR com botão 'Tentar novamente' em vez de tela branca"
  artifacts:
    - path: "src/app/(vault)/sessions/[appointmentId]/error.tsx"
      provides: "Error boundary para rota de sessão clínica"
      contains: "use client"
    - path: "src/app/(vault)/patients/[patientId]/documents/error.tsx"
      provides: "Error boundary para rota de documentos do paciente"
      contains: "use client"
  key_links:
    - from: "src/app/(vault)/sessions/[appointmentId]/error.tsx"
      to: "Next.js App Router error.tsx convention"
      via: "export default function com props { error, reset }"
      pattern: "reset\\(\\)"
---

<objective>
Criar `error.tsx` nos 2 segmentos de rota vault que ainda não têm boundary: `sessions/[appointmentId]` e `patients/[patientId]/documents`.

Purpose: Sem `error.tsx`, qualquer exceção em server components dessas rotas resulta em tela branca. Os 5 existentes (inicio, agenda, patients, patients/[patientId], financeiro) definem o padrão exato a replicar (QUAL-02).
Output: 2 arquivos `error.tsx` novos, idênticos em estrutura e visual aos existentes.
</objective>

<execution_context>
@/home/caramaschi/.claude/get-shit-done/workflows/execute-plan.md
@/home/caramaschi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-quality-production-hardening/14-CONTEXT.md

<interfaces>
<!-- Padrão exato de src/app/(vault)/inicio/error.tsx — replicar sem variação -->

```typescript
"use client";
import React from "react";

const shellStyle = {
  padding: "2rem 2.5rem",
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  minHeight: "60vh",
} satisfies React.CSSProperties;

const containerStyle = {
  textAlign: "center" as const,
  display: "grid",
  gap: "1rem",
  maxWidth: "360px",
} satisfies React.CSSProperties;

export default function InicioError({
  error: _error,
  reset,
}: {
  error: Error & { digest?: string };
  reset: () => void;
}) {
  return (
    <main style={shellStyle}>
      <div style={containerStyle}>
        <p style={{ fontSize: "var(--font-size-body)", fontWeight: 600, color: "var(--color-text-1)" }}>
          Algo deu errado
        </p>
        <p style={{ fontSize: "var(--font-size-meta)", color: "var(--color-text-2)" }}>
          Não foi possível carregar esta página. Tente novamente ou volte mais tarde.
        </p>
        <button onClick={reset} className="btn-secondary">
          Tentar novamente
        </button>
      </div>
    </main>
  );
}
```

CSS vars disponíveis em globals.css: `--font-size-body`, `--font-size-meta`, `--color-text-1`, `--color-text-2`
CSS class disponível: `btn-secondary`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar error.tsx para sessions/[appointmentId] e patients/[patientId]/documents</name>
  <files>
    src/app/(vault)/sessions/[appointmentId]/error.tsx,
    src/app/(vault)/patients/[patientId]/documents/error.tsx
  </files>
  <action>
    Criar os 2 arquivos `error.tsx` seguindo o padrão EXATO de `src/app/(vault)/inicio/error.tsx`.

    Regras:
    - `"use client"` no topo (obrigatório para error.tsx no App Router)
    - `import React from "react"` para `satisfies React.CSSProperties`
    - Estilos inline com `satisfies React.CSSProperties` (padrão do projeto — sem Tailwind)
    - Props: `{ error: Error & { digest?: string }; reset: () => void }`
    - `error` prefixado com `_error` (não usado, mas tipado corretamente)
    - Texto pt-BR: "Algo deu errado" (heading) + "Não foi possível carregar esta página. Tente novamente ou volte mais tarde." (sub)
    - Botão "Tentar novamente" com `onClick={reset}` e `className="btn-secondary"`
    - Nome da função: `SessionError` para sessions, `DocumentsError` para documents

    Não criar nenhum outro arquivo. Não modificar arquivos existentes.
  </action>
  <verify>
    <automated>pnpm build 2>&1 | grep -E "error|Error" | grep -v "ErrorBoundary" | head -20</automated>
  </verify>
  <done>Os 2 arquivos existem em seus respectivos diretórios, seguem o padrão visual dos 5 existentes, e `pnpm build` não reporta erros relacionados a esses arquivos.</done>
</task>

</tasks>

<verification>
- `ls src/app/\(vault\)/sessions/\[appointmentId\]/error.tsx` retorna o arquivo
- `ls src/app/\(vault\)/patients/\[patientId\]/documents/error.tsx` retorna o arquivo
- Ambos contêm `"use client"` na primeira linha
- Ambos contêm `onClick={reset}`
- `pnpm test` passa
</verification>

<success_criteria>
- 2 arquivos `error.tsx` criados nos caminhos corretos
- Visual idêntico aos 5 boundaries existentes (shellStyle + containerStyle + btn-secondary)
- Texto em pt-BR sem detalhes técnicos
- TypeScript compila sem erros nesses arquivos
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/14-quality-production-hardening/14-02-SUMMARY.md`
</output>
