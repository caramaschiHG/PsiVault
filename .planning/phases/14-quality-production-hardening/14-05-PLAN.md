---
phase: 14-quality-production-hardening
plan: "05"
type: execute
wave: 2
depends_on:
  - "14-01"
  - "14-02"
  - "14-03"
  - "14-04"
files_modified:
  - .env.example
  - src/app/(vault)/patients/actions.ts
  - src/app/(vault)/appointments/actions.ts
  - src/app/(vault)/sessions/[appointmentId]/actions.ts
  - src/app/(vault)/patients/[patientId]/documents/new/actions.ts
  - src/app/(vault)/patients/[patientId]/documents/[documentId]/actions.ts
  - src/app/(vault)/patients/[patientId]/documents/[documentId]/edit/actions.ts
autonomous: false
requirements:
  - DEPLOY-01
  - DEPLOY-02

must_haves:
  truths:
    - "pnpm build completa sem erros de TypeScript ou build errors"
    - "As 5 variáveis de ambiente obrigatórias estão documentadas com fonte e propósito"
    - "O profissional sabe exatamente quais env vars configurar no painel da Vercel antes de fazer deploy"
  artifacts:
    - path: ".env.example"
      provides: "Template de env vars de produção para Vercel"
      contains: "NEXT_PUBLIC_SUPABASE_URL"
  key_links:
    - from: ".env.example"
      to: "Vercel dashboard Environment Variables"
      via: "Documentação de quais vars são necessárias"
      pattern: "NEXT_PUBLIC_SUPABASE"
---

<objective>
Verificar que `pnpm build` completa sem erros (DEPLOY-02) e criar `.env.example` documentando as env vars obrigatórias para deploy na Vercel (DEPLOY-01). Checkpoint humano para confirmar o build limpo.

Purpose: Antes do lançamento, o profissional precisa saber exatamente quais variáveis configurar na Vercel e o build precisa estar limpo (DEPLOY-01, DEPLOY-02).
Output: Build limpo verificado + `.env.example` com todas as vars documentadas.
</objective>

<execution_context>
@/home/caramaschi/.claude/get-shit-done/workflows/execute-plan.md
@/home/caramaschi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-quality-production-hardening/14-CONTEXT.md

<interfaces>
<!-- Env vars identificadas no CONTEXT.md -->
Obrigatórias para produção Vercel:
- NEXT_PUBLIC_SUPABASE_URL — Supabase Dashboard > Settings > API > Project URL
- NEXT_PUBLIC_SUPABASE_ANON_KEY — Supabase Dashboard > Settings > API > anon key
- SUPABASE_SERVICE_ROLE_KEY — Supabase Dashboard > Settings > API > service_role key
- DATABASE_URL — Supabase Dashboard > Settings > Database > Connection string (Transaction/Supavisor pooler)
- DIRECT_URL — Supabase Dashboard > Settings > Database > Connection string (Direct connection)

Verificar no codebase se há outras vars (NEXTAUTH_SECRET, etc.):
- grep -rn "process.env\." src/ | grep -v "NODE_ENV" | grep "process.env\.[A-Z]"

<!-- 6 arquivos de actions modificados pelo plano 14-03 — alvos previsíveis para correção de tipos -->
Arquivos que podem ter erros de TypeScript após adição de try/catch no plano 14-03:
- src/app/(vault)/patients/actions.ts
- src/app/(vault)/appointments/actions.ts
- src/app/(vault)/sessions/[appointmentId]/actions.ts
- src/app/(vault)/patients/[patientId]/documents/new/actions.ts
- src/app/(vault)/patients/[patientId]/documents/[documentId]/actions.ts
- src/app/(vault)/patients/[patientId]/documents/[documentId]/edit/actions.ts

Erro esperado: funções que agora podem retornar `{ ok: false, error: string }` além de `void` ou `{ ok: true }` podem ter tipo de retorno inferido como `Promise<{ ok: boolean; error?: string } | undefined>` ao invés de `Promise<{ ok: boolean; error?: string }>`. Corrigir adicionando anotação de tipo explícita no retorno se necessário — não alterar lógica.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Executar pnpm build e corrigir erros encontrados</name>
  <files>
    src/app/(vault)/patients/actions.ts,
    src/app/(vault)/appointments/actions.ts,
    src/app/(vault)/sessions/[appointmentId]/actions.ts,
    src/app/(vault)/patients/[patientId]/documents/new/actions.ts,
    src/app/(vault)/patients/[patientId]/documents/[documentId]/actions.ts,
    src/app/(vault)/patients/[patientId]/documents/[documentId]/edit/actions.ts
  </files>
  <action>
    Executar `pnpm build` e corrigir todos os erros encontrados.

    Erros esperados após as mudanças da fase (plano 14-03 adicionou try/catch nos 6 arquivos de actions):
    - TypeScript errors por mudança de tipo de retorno: funções que antes retornavam `void` implicitamente agora podem retornar `{ ok: false, error: string }` no catch. O TypeScript pode inferir `Promise<{ ok: boolean; error?: string } | undefined>` — corrigir adicionando tipo de retorno explícito `Promise<{ ok: boolean; error?: string } | void>` ou `Promise<ActionResult>` na assinatura da função.
    - Executor deve pular arquivos onde o build não gerou erros — apenas corrigir os que o build reportar.

    Para cada erro TypeScript reportado pelo build:
    1. Identificar o arquivo e linha do erro
    2. Adicionar/ajustar tipo de retorno na assinatura da função — NÃO alterar lógica de negócio
    3. Re-executar `pnpm build` até passar

    Também verificar se há outras env vars usadas além das 5 documentadas:
    ```
    grep -rn "process\.env\." src/ | grep -v "NODE_ENV" | grep -oP "process\.env\.\w+" | sort -u
    ```
    Adicionar qualquer var encontrada ao `.env.example` com comentário explicativo.
  </action>
  <verify>
    <automated>pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>pnpm build completa sem erros (exit code 0). Output final contém "✓ Compiled successfully" ou equivalente Next.js 15.</done>
</task>

<task type="auto">
  <name>Task 2: Criar .env.example com todas as env vars de produção documentadas</name>
  <files>.env.example</files>
  <action>
    Criar `.env.example` na raiz do projeto com todas as variáveis de ambiente necessárias para deploy na Vercel, com comentários em pt-BR explicando onde encontrar cada valor.

    Estrutura:
    ```
    # PsiVault — Variáveis de Ambiente de Produção
    # Configure estas variáveis no painel da Vercel: Settings > Environment Variables
    #
    # ATENÇÃO: Nunca commitar o arquivo .env com valores reais.
    # Este arquivo é apenas um template — copie para .env.local para desenvolvimento.

    # ── Supabase Auth ──────────────────────────────────────────────────────────
    # Supabase Dashboard > Settings > API > Project URL
    NEXT_PUBLIC_SUPABASE_URL=

    # Supabase Dashboard > Settings > API > Project API Keys > anon (public)
    NEXT_PUBLIC_SUPABASE_ANON_KEY=

    # Supabase Dashboard > Settings > API > Project API Keys > service_role (secret)
    # NUNCA expor no frontend — apenas server-side
    SUPABASE_SERVICE_ROLE_KEY=

    # ── Banco de Dados (Prisma) ────────────────────────────────────────────────
    # Supabase Dashboard > Settings > Database > Connection string > Transaction pooler
    # Usado para queries em runtime (via Supavisor connection pooling)
    DATABASE_URL=

    # Supabase Dashboard > Settings > Database > Connection string > Direct connection
    # Usado por prisma migrate deploy (requer conexão direta, não pooler)
    DIRECT_URL=

    # [outras vars encontradas no codebase via grep — adicionar aqui]
    ```

    Verificar via grep se há outras vars além dessas 5 e adicioná-las com comentário.
    Não criar `.env` com valores reais — apenas `.env.example` com valores vazios.
    Confirmar que `.env.example` está no `.gitignore` ou que é seguro commitar (é seguro — sem valores reais).
  </action>
  <verify>
    <automated>cat .env.example | grep "NEXT_PUBLIC_SUPABASE_URL=" | wc -l</automated>
  </verify>
  <done>.env.example existe na raiz com as 5+ vars documentadas e valores vazios. Comentários em pt-BR explicam onde encontrar cada valor no Supabase Dashboard.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Build de produção verificado + .env.example criado. Todos os planos 01-04 desta fase foram executados: re-auth gate real (QUAL-01), error boundaries (QUAL-02), try/catch em actions (QUAL-03), auditoria de segurança + postinstall (QUAL-04/05/06/DEPLOY-03).
  </what-built>
  <how-to-verify>
    1. Confirme que `pnpm build` passou sem erros (o executor deve ter reportado o resultado).
    2. Verifique `.env.example` na raiz — confirme que lista todas as vars necessárias para a Vercel.
    3. Opcional: abrir o arquivo `src/app/(vault)/sessions/[appointmentId]/error.tsx` e confirmar que existe.
    4. Opcional: abrir `src/app/(vault)/settings/dados-e-privacidade/actions.ts` e confirmar que `signInWithPassword` aparece.
    5. Se algum erro de build foi encontrado e não corrigido, descreva aqui.
  </how-to-verify>
  <resume-signal>
    Digite "aprovado" para concluir a fase, ou descreva os problemas encontrados para correção.
  </resume-signal>
</task>

</tasks>

<verification>
- `pnpm build` exit code 0
- `.env.example` existe com 5+ variáveis documentadas
- `pnpm test` passa
- `grep "signInWithPassword" src/app/(vault)/settings/dados-e-privacidade/actions.ts` retorna 2 ocorrências
</verification>

<success_criteria>
- pnpm build completa sem erros em modo produção
- .env.example documenta todas as env vars necessárias para Vercel com fontes em pt-BR
- Checkpoint humano confirma build limpo e documentação adequada
- Fase 14 completa: todos os 9 requirements (QUAL-01..06 + DEPLOY-01..03) endereçados
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/14-quality-production-hardening/14-05-SUMMARY.md`
</output>
