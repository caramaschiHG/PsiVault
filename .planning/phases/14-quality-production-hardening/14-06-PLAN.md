---
phase: 14-quality-production-hardening
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(vault)/patients/actions.ts
  - src/app/(vault)/appointments/actions.ts
  - src/app/(vault)/sessions/[appointmentId]/actions.ts
  - src/app/(vault)/patients/[patientId]/documents/new/actions.ts
  - src/app/(vault)/patients/[patientId]/documents/[documentId]/actions.ts
  - src/app/(vault)/patients/[patientId]/documents/[documentId]/edit/actions.ts
autonomous: true
gap_closure: true
requirements: [QUAL-03]

must_haves:
  truths:
    - "Catch blocks em todas as server actions de mutação retornam { ok: false, error: 'Algo deu errado. Tente novamente.' }"
    - "TypeScript compila sem erros — tipo de retorno é Promise<{ ok: boolean; error?: string } | void>"
    - "pnpm test passa (lógica de domínio não alterada)"
  artifacts:
    - path: "src/app/(vault)/patients/actions.ts"
      provides: "4 actions com tipo de retorno corrigido e catch retornando { ok: false }"
    - path: "src/app/(vault)/appointments/actions.ts"
      provides: "Actions com catch retornando { ok: false }"
    - path: "src/app/(vault)/sessions/[appointmentId]/actions.ts"
      provides: "createNoteAction + updateNoteAction com catch retornando { ok: false }"
    - path: "src/app/(vault)/patients/[patientId]/documents/new/actions.ts"
      provides: "createDocumentAction com catch retornando { ok: false }"
    - path: "src/app/(vault)/patients/[patientId]/documents/[documentId]/actions.ts"
      provides: "archiveDocumentAction com catch retornando { ok: false }"
    - path: "src/app/(vault)/patients/[patientId]/documents/[documentId]/edit/actions.ts"
      provides: "updateDocumentAction com catch retornando { ok: false }"
  key_links:
    - from: "catch blocks"
      to: "{ ok: false, error: 'Algo deu errado. Tente novamente.' }"
      via: "tipo de retorno Promise<{ ok: boolean; error?: string } | void>"
      pattern: "return \\{ ok: false"
---

<objective>
Fechar o gap QUAL-03: os catch blocks das server actions retornam `void` (silenciosamente) em vez de `{ ok: false, error: 'Algo deu errado. Tente novamente.' }`. A correção altera o tipo de retorno para `Promise<{ ok: boolean; error?: string } | void>` em todas as 6 funções afetadas, permitindo que o TypeScript aceite o retorno tipado sem conflito com `form action`.

Purpose: Satisfazer o contrato declarado no plano 14-03 e no CONTEXT.md — actions retornam resposta tipada de erro quando ocorre falha de infraestrutura.
Output: 6 arquivos de actions com catch blocks corrigidos e tipo de retorno adequado.
</objective>

<execution_context>
@/home/caramaschi/.claude/get-shit-done/workflows/execute-plan.md
@/home/caramaschi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-quality-production-hardening/14-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corrigir tipo de retorno e catch blocks em patients/actions.ts e sessions/actions.ts</name>
  <files>
    src/app/(vault)/patients/actions.ts,
    src/app/(vault)/sessions/[appointmentId]/actions.ts
  </files>
  <action>
Em AMBOS os arquivos, para cada função exportada (`createPatientAction`, `updatePatientAction`, `archivePatientAction`, `recoverPatientAction`, `createNoteAction`, `updateNoteAction`):

1. Mudar o tipo de retorno de `Promise<void>` para `Promise<{ ok: boolean; error?: string } | void>`

2. No bloco `catch`, substituir `return;` por:
   ```typescript
   return { ok: false, error: "Algo deu errado. Tente novamente." };
   ```

Nenhuma outra mudança — não alterar lógica, imports, guards, redirect, console.error, ou padrão flag-variable.

Resultado esperado em cada catch:
```typescript
} catch (err) {
  console.error("[actionName]", err);
  return { ok: false, error: "Algo deu errado. Tente novamente." };
}
```
  </action>
  <verify>
    <automated>pnpm --prefix /home/caramaschi/Área\ de\ trabalho/Projetos/PsiLock tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - patients/actions.ts: 4 catch blocks retornam { ok: false, error: '...' }
    - sessions/actions.ts: 2 catch blocks retornam { ok: false, error: '...' }
    - TypeScript não apresenta erros nesses arquivos
  </done>
</task>

<task type="auto">
  <name>Task 2: Corrigir tipo de retorno e catch blocks nos 4 arquivos de documents/actions.ts e appointments/actions.ts</name>
  <files>
    src/app/(vault)/appointments/actions.ts,
    src/app/(vault)/patients/[patientId]/documents/new/actions.ts,
    src/app/(vault)/patients/[patientId]/documents/[documentId]/actions.ts,
    src/app/(vault)/patients/[patientId]/documents/[documentId]/edit/actions.ts
  </files>
  <action>
Em TODOS os 4 arquivos, para cada função exportada:

1. Mudar o tipo de retorno de `Promise<void>` para `Promise<{ ok: boolean; error?: string } | void>`

2. No bloco `catch`, substituir `return;` por:
   ```typescript
   return { ok: false, error: "Algo deu errado. Tente novamente." };
   ```

Em `appointments/actions.ts` há múltiplas funções exportadas — aplicar a mesma mudança em todos os catch blocks de todas as funções exportadas.

Nenhuma outra mudança — não alterar lógica, imports, guards, redirect, console.error, ou padrão flag-variable.

Após as edições, rodar o suite de testes:
```bash
pnpm test
```

Se os testes passarem, a tarefa está concluída.
  </action>
  <verify>
    <automated>cd /home/caramaschi/Área\ de\ trabalho/Projetos/PsiLock && pnpm test 2>&1 | tail -20</automated>
  </verify>
  <done>
    - appointments/actions.ts: todos os catch blocks retornam { ok: false, error: '...' }
    - documents/new/actions.ts: catch block corrigido
    - documents/[documentId]/actions.ts: catch block corrigido
    - documents/[documentId]/edit/actions.ts: catch block corrigido
    - pnpm test: todos os testes passam
    - pnpm tsc --noEmit: sem erros de TypeScript
  </done>
</task>

</tasks>

<verification>
```bash
# Confirmar que nenhum catch retorna void silenciosamente
grep -rn "return;" src/app/\(vault\)/patients/actions.ts src/app/\(vault\)/appointments/actions.ts src/app/\(vault\)/sessions src/app/\(vault\)/patients/\[patientId\]/documents
# Deve retornar apenas returns de guards (not exist), não de catch blocks

# Confirmar padrão nos catch blocks
grep -A1 "console.error" src/app/\(vault\)/patients/actions.ts
# Deve mostrar: return { ok: false, error: "Algo deu errado. Tente novamente." };

# TypeScript limpo
pnpm tsc --noEmit

# Testes passando
pnpm test
```
</verification>

<success_criteria>
- Todos os 6 arquivos de actions têm catch blocks que retornam `{ ok: false, error: "Algo deu errado. Tente novamente." }`
- Tipo de retorno é `Promise<{ ok: boolean; error?: string } | void>` em todas as funções afetadas
- `pnpm tsc --noEmit` sem erros
- `pnpm test` verde
- QUAL-03 truth literal satisfeita: nenhum catch retorna void silenciosamente
</success_criteria>

<output>
Após a conclusão, criar `.planning/phases/14-quality-production-hardening/14-06-SUMMARY.md`
</output>
