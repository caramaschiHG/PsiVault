---
phase: 15-planning-metadata-realignment
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/v1.1-MILESTONE-AUDIT.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "The active launch milestone is labeled consistently as v1.2 across PROJECT, ROADMAP, and STATE"
    - "STATE no longer says milestone v1.1 or Ready to start Phase 10 after Phase 14 completion"
    - "The current launch scope is described as the unarchived phases 07–20, with Phase 15 as the current planning focus"
    - "The milestone audit remains useful historical evidence but no longer conflicts with the active metadata story"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Canonical current milestone declaration for the live launch scope"
      contains: "## Current Milestone: v1.2"
    - path: ".planning/ROADMAP.md"
      provides: "Roadmap with explicit active-scope wording that matches PROJECT and STATE"
      contains: "Phase 15: Planning Metadata Realignment"
    - path: ".planning/STATE.md"
      provides: "Accurate active milestone, current focus, and next actionable state"
      contains: "milestone: v1.2"
    - path: ".planning/v1.1-MILESTONE-AUDIT.md"
      provides: "Historical audit narrative annotated against the normalized active scope"
      contains: "current unarchived launch scope"
  key_links:
    - from: ".planning/PROJECT.md"
      to: ".planning/STATE.md"
      via: "shared current milestone label and launch-scope wording"
      pattern: "v1.2"
    - from: ".planning/ROADMAP.md"
      to: ".planning/STATE.md"
      via: "shared active-phase story after Phase 14 completion"
      pattern: "Phase 15"
---

<objective>
Canonicalize the active launch milestone metadata so the live planning documents tell one consistent story about the current scope and current position.

Purpose: Phase 15 exists because `PROJECT.md`, `ROADMAP.md`, and `STATE.md` currently disagree about the active milestone and current phase. This breaks planning continuity and makes milestone closeout ambiguous.
Output: Updated active planning documents (`PROJECT.md`, `ROADMAP.md`, `STATE.md`) plus an audit note adjustment so historical evidence no longer reads like active truth.
</objective>

<execution_context>
@/home/caramaschi/.codex/get-shit-done/workflows/execute-plan.md
@/home/caramaschi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/15-planning-metadata-realignment/15-RESEARCH.md
@.planning/phases/15-planning-metadata-realignment/15-VALIDATION.md

<specifics>
- Preserve historical context for `v1.1`, but make `v1.2 Lançamento` the unambiguous active milestone across live files
- Update `STATE.md` frontmatter and prose sections together; do not leave stale “Phase 10” references in current-position sections
- Keep the roadmap history if needed, but add active-scope wording so phases `07`–`20` are clearly the current unarchived launch scope
- Avoid inventing new milestone files unless absolutely necessary; prefer editing the active documents already used by GSD
</specifics>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align active milestone language across PROJECT and ROADMAP</name>
  <files>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/v1.1-MILESTONE-AUDIT.md</files>
  <action>
    Update `.planning/PROJECT.md` and `.planning/ROADMAP.md` so they describe the same active launch milestone and the same scope boundary. Keep historical references to `v1.1` only where they are clearly archival or explanatory.

    Required outcomes:
    1. `.planning/PROJECT.md` keeps `v1.2 Lançamento` as the active milestone and explicitly states that the live unarchived launch scope includes phases `07`–`20`
    2. `.planning/ROADMAP.md` makes the active scope legible without requiring readers to infer it from mixed section headings
    3. `.planning/v1.1-MILESTONE-AUDIT.md` remains historical evidence but is adjusted so it no longer reads like the present active milestone definition once the metadata is normalized

    Do not rewrite the entire roadmap. Make the minimum document edits required to remove ambiguity.
  </action>
  <verify>
    <automated>rg -n "Current Milestone: v1\\.2|current unarchived launch scope|Phase 15: Planning Metadata Realignment" .planning/PROJECT.md .planning/ROADMAP.md .planning/v1.1-MILESTONE-AUDIT.md</automated>
  </verify>
  <done>`PROJECT.md`, `ROADMAP.md`, and the milestone audit no longer disagree about what the active launch scope is.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite STATE to match the post-Phase-14 reality</name>
  <files>.planning/STATE.md</files>
  <action>
    Update `.planning/STATE.md` frontmatter and human-readable sections so the file reflects the actual current state after Phase 14 completion and before Phase 15 execution.

    Required outcomes:
    1. Frontmatter uses `milestone: v1.2`
    2. `status`, `Current focus`, `Current Position`, and `Pending Todos` no longer say “Ready to start Phase 10”
    3. The file names Phase 15 as the current planning focus and keeps future pending work scoped to phases `15`–`20`
    4. Completed plans/phase summaries already present remain historical evidence; only the “current state” narrative changes

    Keep the state file compact and truthful. Prefer correcting stale current-position text over rewriting historical decision logs.
  </action>
  <verify>
    <automated>rg -n "milestone: v1\\.2|Ready to start Phase 15|Current focus:\\*\\* Phase 15|Plan and execute Phase 15" .planning/STATE.md && ! rg -n "Ready to start Phase 10|Current focus:\\*\\* Phase 10" .planning/STATE.md</automated>
  </verify>
  <done>`STATE.md` describes the same milestone and current focus that `PROJECT.md` and `ROADMAP.md` describe.</done>
</task>

</tasks>

<verification>
- `rg -n "milestone: v1\\.2" .planning/STATE.md` returns exactly one active frontmatter match
- `rg -n "Current Milestone: v1\\.2" .planning/PROJECT.md` returns one match
- `rg -n "Phase 15: Planning Metadata Realignment" .planning/ROADMAP.md` returns the phase block
- `rg -n "Ready to start Phase 10|Current focus:\\*\\* Phase 10" .planning/STATE.md` returns no matches
</verification>

<success_criteria>
- Active milestone is consistently `v1.2` in live planning metadata
- Current state points at Phase 15 instead of stale Phase 10 startup language
- Launch-scope boundary is readable without cross-file guesswork
- Historical audit evidence no longer contradicts the normalized active metadata
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/15-planning-metadata-realignment/15-01-SUMMARY.md`
</output>
