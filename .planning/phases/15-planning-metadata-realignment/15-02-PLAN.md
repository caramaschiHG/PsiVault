---
phase: 15-planning-metadata-realignment
plan: "02"
type: execute
wave: 1
depends_on:
  - "01"
files_modified:
  - .planning/phases/07-infrastructure-foundation
  - .planning/phases/08-authentication-workspaces
  - .planning/milestones/v1.1-phases/07-infrastructure-foundation
  - .planning/milestones/v1.1-phases/08-authentication-workspaces
autonomous: true
requirements: []

must_haves:
  truths:
    - "Phases 07 and 08 are reachable from the active `.planning/phases/` tree"
    - "There is one canonical active location for each of phases 07 and 08"
    - "Internal references inside phase 07/08 artifacts no longer point at stale milestone-only paths if those phases are promoted into the active tree"
    - "Historical archive structure, if preserved, is clearly archival rather than a second active source of truth"
  artifacts:
    - path: ".planning/phases/07-infrastructure-foundation"
      provides: "Canonical active phase-07 artifact directory"
      contains: "07-RESEARCH.md"
    - path: ".planning/phases/08-authentication-workspaces"
      provides: "Canonical active phase-08 artifact directory"
      contains: "08-RESEARCH.md"
    - path: ".planning/phases/07-infrastructure-foundation/07-01-PLAN.md"
      provides: "Updated phase-07 plan references"
      contains: ".planning/phases/07-infrastructure-foundation"
    - path: ".planning/phases/08-authentication-workspaces/08-01-PLAN.md"
      provides: "Updated phase-08 plan references"
      contains: ".planning/phases/08-authentication-workspaces"
  key_links:
    - from: ".planning/phases/07-infrastructure-foundation/07-01-PLAN.md"
      to: ".planning/phases/07-infrastructure-foundation/07-CONTEXT.md"
      via: "active-tree path references"
      pattern: ".planning/phases/07-infrastructure-foundation"
    - from: ".planning/phases/08-authentication-workspaces/08-01-PLAN.md"
      to: ".planning/phases/08-authentication-workspaces/08-RESEARCH.md"
      via: "active-tree path references"
      pattern: ".planning/phases/08-authentication-workspaces"
---

<objective>
Realign the phase artifact topology so phases 07 and 08 live in the active planning tree and no longer sit outside the canonical scope used by current launch planning.

Purpose: GSD cannot reason cleanly about the current launch milestone while two early phases live only under `.planning/milestones/v1.1-phases/`. This plan makes the active phase tree complete.
Output: Active canonical phase directories for 07 and 08 plus rewritten internal references away from stale milestone-only paths.
</objective>

<execution_context>
@/home/caramaschi/.codex/get-shit-done/workflows/execute-plan.md
@/home/caramaschi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-planning-metadata-realignment/15-RESEARCH.md
@.planning/ROADMAP.md
@.planning/milestones/v1.1-phases/07-infrastructure-foundation/07-RESEARCH.md
@.planning/milestones/v1.1-phases/08-authentication-workspaces/08-RESEARCH.md

<specifics>
- Prefer one canonical active location under `.planning/phases/` for each of phases `07` and `08`
- If archival copies are preserved under `.planning/milestones/v1.1-phases/`, mark them clearly as archival or replace them with lightweight pointers; do not keep two active editable copies
- Rewrite all internal `@...` references inside moved/copied phase files so future execution reads the canonical active tree
- Preserve summaries, research, context, and validation artifacts already produced for phase `07`; preserve all extant phase `08` artifacts as well
</specifics>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote phases 07 and 08 into the active `.planning/phases` tree</name>
  <files>.planning/phases/07-infrastructure-foundation, .planning/phases/08-authentication-workspaces, .planning/milestones/v1.1-phases/07-infrastructure-foundation, .planning/milestones/v1.1-phases/08-authentication-workspaces</files>
  <action>
    Establish `.planning/phases/07-infrastructure-foundation` and `.planning/phases/08-authentication-workspaces` as the canonical active directories for those phases.

    Required outcomes:
    1. Both directories exist under `.planning/phases/`
    2. All existing research/context/plan/summary/validation artifacts from the old location are preserved in the canonical active location
    3. The old `.planning/milestones/v1.1-phases/...` copies no longer act like active working copies

    Use the least risky migration strategy. If you preserve the old location, make it clearly archival. Do not leave duplicated active sources of truth.
  </action>
  <verify>
    <automated>find .planning/phases -maxdepth 1 \( -name '07-*' -o -name '08-*' \) | sort</automated>
  </verify>
  <done>Phases 07 and 08 are present in the active tree and no longer depend on the milestone archive path for discovery.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite stale phase-internal references to the canonical paths</name>
  <files>.planning/phases/07-infrastructure-foundation/*, .planning/phases/08-authentication-workspaces/*</files>
  <action>
    Update moved/promoted phase artifacts so their internal references point to `.planning/phases/...` instead of `.planning/milestones/v1.1-phases/...`.

    Required outcomes:
    1. `@...` references in plan files follow the canonical active location
    2. Research/context files that mention the old active location are updated where necessary
    3. No active phase file under `.planning/phases/07-*` or `.planning/phases/08-*` still points at the stale milestone-only path
  </action>
  <verify>
    <automated>! rg -n "\\.planning/milestones/v1\\.1-phases/(07|08)-" .planning/phases/07-infrastructure-foundation .planning/phases/08-authentication-workspaces</automated>
  </verify>
  <done>The promoted phase artifacts are internally self-consistent and only reference the canonical active tree.</done>
</task>

</tasks>

<verification>
- `find .planning/phases -maxdepth 1 \( -name '07-*' -o -name '08-*' \) | sort` returns both phase directories
- `test -f .planning/phases/07-infrastructure-foundation/07-RESEARCH.md`
- `test -f .planning/phases/08-authentication-workspaces/08-RESEARCH.md`
- `rg -n "\\.planning/milestones/v1\\.1-phases/(07|08)-" .planning/phases/07-infrastructure-foundation .planning/phases/08-authentication-workspaces` returns no matches
</verification>

<success_criteria>
- The active phases tree includes 07 and 08
- Only one canonical active location exists for each promoted phase
- Promoted artifacts no longer carry stale internal path references
- The milestone archive path, if retained, is archival-only
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/15-planning-metadata-realignment/15-02-SUMMARY.md`
</output>
