---
phase: 16
plan: 3
title: "CSS Migration — inline styles to design token classes"
wave: 2
depends_on: [1]
files_modified:
  - src/app/globals.css
  - src/components/ui/notification-bell.tsx
requirements:
  - ARCH-04
autonomous: true
---

<objective>
Migrate all inline styles from notification-bell.tsx to CSS classes in globals.css using design tokens. Remove all React.CSSProperties objects from the component. The bell component uses className instead of style props.
</objective>

<must_haves>
- All 7 inline style objects removed from notification-bell.tsx
- CSS classes .notif-bell, .notif-badge, .notif-dropdown, .notif-dropdown-header, .notif-action, .notif-item, .notif-unread-dot added to globals.css
- All values use existing design tokens (--color-*, --radius-*, --shadow-*, --z-*)
- Zero inline styles remaining in the component
- Visual appearance unchanged
</must_haves>

## Task 1: Add notification CSS classes to globals.css

<read_first>
- src/app/globals.css (find the section with existing component classes — look for patterns like `.skip-link`, `.vault-sidebar`, `.external-link`)
- src/components/ui/notification-bell.tsx (lines 116-202 — all inline style objects to convert)
</read_first>

<action>
Add the following CSS classes to globals.css, at the end of the component classes section (before any @media queries):

```css
/* ─── Notification System ─── */

.notif-bell-wrap {
  position: relative;
}

.notif-bell {
  position: relative;
  width: 36px;
  height: 36px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: none;
  border: none;
  cursor: pointer;
  color: var(--color-text-2);
  border-radius: var(--radius-sm);
  transition: color var(--transition-fast), background-color var(--transition-fast);
}

.notif-bell:hover {
  color: var(--color-text-1);
  background-color: var(--color-surface-1);
}

.notif-badge {
  position: absolute;
  top: 2px;
  right: 2px;
  min-width: 16px;
  height: 16px;
  border-radius: var(--radius-pill);
  background: var(--color-rose);
  color: #fff;
  font-size: var(--font-size-2xs);
  font-weight: 700;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 0 3px;
  line-height: 1;
}

.notif-dropdown {
  position: absolute;
  top: calc(100% + 6px);
  right: 0;
  width: 360px;
  background: var(--color-surface-0);
  border-radius: var(--radius-lg);
  border: 1px solid var(--color-border);
  box-shadow: var(--shadow-xl);
  z-index: var(--z-dropdown);
  overflow: hidden;
}

.notif-dropdown-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: var(--space-3) var(--space-4);
  border-bottom: 1px solid var(--color-border);
}

.notif-dropdown-title {
  font-size: var(--font-size-sm);
  font-weight: 600;
  color: var(--color-text-1);
}

.notif-dropdown-actions {
  display: flex;
  gap: var(--space-1);
}

.notif-action {
  background: none;
  border: none;
  cursor: pointer;
  font-size: var(--font-size-xs);
  color: var(--color-accent);
  font-weight: 500;
  padding: var(--space-1) var(--space-2);
  border-radius: var(--radius-xs);
  transition: background-color var(--transition-fast);
}

.notif-action:hover {
  background-color: var(--color-surface-1);
}

.notif-action--danger {
  color: var(--color-rose);
}

.notif-list {
  max-height: 400px;
  overflow-y: auto;
}

.notif-empty {
  padding: var(--space-8);
  text-align: center;
  color: var(--color-text-3);
  font-size: var(--font-size-sm);
}

.notif-item {
  width: 100%;
  padding: var(--space-3) var(--space-4);
  border: none;
  cursor: pointer;
  text-align: left;
  display: flex;
  align-items: flex-start;
  gap: var(--space-3);
  border-bottom: 1px solid var(--color-border-light, rgba(0,0,0,0.04));
  background: transparent;
  transition: background-color var(--transition-fast);
}

.notif-item:hover {
  background-color: var(--color-surface-1);
}

.notif-item--unread {
  background-color: rgba(154, 52, 18, 0.04);
}

.notif-item-content {
  display: flex;
  gap: var(--space-3);
  align-items: flex-start;
}

.notif-item-body {
  flex: 1;
  min-width: 0;
}

.notif-item-header {
  display: flex;
  gap: var(--space-2);
  align-items: center;
}

.notif-item-icon {
  flex-shrink: 0;
}

.notif-item-title {
  font-size: var(--font-size-sm);
  font-weight: 500;
  color: var(--color-text-1);
  line-height: 1.3;
}

.notif-item-title--unread {
  font-weight: 600;
}

.notif-item-desc {
  margin: var(--space-1) 0 0;
  font-size: var(--font-size-xs);
  color: var(--color-text-3);
  line-height: 1.4;
}

.notif-unread-dot {
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: var(--color-accent);
  margin-top: 6px;
  flex-shrink: 0;
}
```
</action>

<acceptance_criteria>
- globals.css contains `.notif-bell-wrap` class
- globals.css contains `.notif-bell` class with `var(--color-text-2)` color
- globals.css contains `.notif-badge` class with `var(--color-rose)` background
- globals.css contains `.notif-dropdown` class with `var(--shadow-xl)` shadow
- globals.css contains `.notif-dropdown-header` class
- globals.css contains `.notif-action` class
- globals.css contains `.notif-item` class with `var(--transition-fast)` transition
- globals.css contains `.notif-unread-dot` class with `var(--color-accent)` background
- globals.css contains hover states for `.notif-bell:hover`, `.notif-item:hover`, `.notif-action:hover`
- globals.css contains `.notif-item--unread` modifier class
</acceptance_criteria>

## Task 2: Migrate notification-bell.tsx to use CSS classes

<read_first>
- src/components/ui/notification-bell.tsx (entire file — 203 lines)
- src/app/globals.css (the .notif-* classes just added in Task 1)
- src/components/ui/notification-context.tsx (updated API from Plan 2)
</read_first>

<action>
Rewrite `src/components/ui/notification-bell.tsx` to use CSS classes instead of inline styles:

1. **Remove** all 7 `React.CSSProperties` objects at bottom of file (lines 116-202):
   - `bellButtonStyle`
   - `badgeStyle`
   - `dropdownStyle`
   - `dropdownHeaderStyle`
   - `dropdownActionStyle`
   - `notifItemStyle`
   - `unreadDotStyle`

2. **Replace** all `style={...}` props with `className="notif-*"` equivalents:

```tsx
"use client";

import { useState, useRef, useEffect } from "react";
import { useNotifications } from "./notification-context";

export function NotificationBell() {
  const { notifications, unreadCount, markAsRead, markAllAsRead, clearAll } = useNotifications();
  const [open, setOpen] = useState(false);
  const ref = useRef<HTMLDivElement>(null);

  // Close on outside click
  useEffect(() => {
    if (!open) return;
    const handler = (e: MouseEvent) => {
      if (ref.current && !ref.current.contains(e.target as Node)) setOpen(false);
    };
    document.addEventListener("mousedown", handler);
    return () => document.removeEventListener("mousedown", handler);
  }, [open]);

  // Close on Escape
  useEffect(() => {
    if (!open) return;
    const handler = (e: KeyboardEvent) => {
      if (e.key === "Escape") setOpen(false);
    };
    window.addEventListener("keydown", handler);
    return () => window.removeEventListener("keydown", handler);
  }, [open]);

  return (
    <div ref={ref} className="notif-bell-wrap">
      <button
        onClick={() => setOpen((v) => !v)}
        className="notif-bell"
        aria-label={`Notificações${unreadCount > 0 ? `, ${unreadCount} não lidas` : ""}`}
        aria-expanded={open}
      >
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true">
          <path d="M6 8a6 6 0 0 1 12 0c0 7 3 9 3 9H3s3-2 3-9" />
          <path d="M10.3 21a1.94 1.94 0 0 0 3.4 0" />
        </svg>
        {unreadCount > 0 && (
          <span className="notif-badge" aria-hidden="true">
            {unreadCount > 9 ? "9+" : unreadCount}
          </span>
        )}
      </button>

      {open && (
        <div className="notif-dropdown" role="dialog" aria-label="Notificações">
          <div className="notif-dropdown-header">
            <span className="notif-dropdown-title">Notificações</span>
            <div className="notif-dropdown-actions">
              {unreadCount > 0 && (
                <button onClick={markAllAsRead} className="notif-action">
                  Marcar todas como lidas
                </button>
              )}
              {notifications.length > 0 && (
                <button onClick={clearAll} className="notif-action notif-action--danger">
                  Limpar
                </button>
              )}
            </div>
          </div>

          <div className="notif-list">
            {notifications.length === 0 ? (
              <div className="notif-empty">
                Nenhuma notificação
              </div>
            ) : (
              notifications.map((n) => (
                <button
                  key={n.id}
                  onClick={() => {
                    if (!n.read) markAsRead(n.id);
                  }}
                  className={`notif-item${!n.read ? " notif-item--unread" : ""}`}
                >
                  <div className="notif-item-content">
                    {!n.read && <span className="notif-unread-dot" />}
                    <div className="notif-item-body">
                      <div className="notif-item-header">
                        <span className={`notif-item-title${!n.read ? " notif-item-title--unread" : ""}`}>
                          {n.title}
                        </span>
                      </div>
                      {n.description && (
                        <p className="notif-item-desc">
                          {n.description}
                        </p>
                      )}
                    </div>
                  </div>
                </button>
              ))
            )}
          </div>
        </div>
      )}
    </div>
  );
}
```

3. **No inline styles remain** — all styling via CSS classes.
</action>

<acceptance_criteria>
- `notification-bell.tsx` contains zero `React.CSSProperties` type annotations
- `notification-bell.tsx` contains zero `style=` props (except possibly on SVG elements)
- `notification-bell.tsx` contains `className="notif-bell-wrap"` on wrapper div
- `notification-bell.tsx` contains `className="notif-bell"` on button
- `notification-bell.tsx` contains `className="notif-dropdown"` on dropdown div
- `notification-bell.tsx` contains `className="notif-item"` on notification buttons
- `notification-bell.tsx` contains no `bellButtonStyle`, `badgeStyle`, `dropdownStyle` etc. objects
- File is under 120 lines (was 203 lines with inline styles)
</acceptance_criteria>

<verification>
```bash
npx tsc --noEmit src/components/ui/notification-bell.tsx
# Verify zero inline styles:
grep -c "React.CSSProperties" src/components/ui/notification-bell.tsx
# Should output: 0
grep -c "style=" src/components/ui/notification-bell.tsx
# Should output: 0 (or 1-2 for SVG only)
```
Should compile without errors and have zero inline style objects.
</verification>
