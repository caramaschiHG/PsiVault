---
wave: 1
depends_on: []
files_modified:
  - "src/app/globals.css"
  - "src/components/ui/notification-item.tsx"
  - "src/components/ui/notification-bell.tsx"
autonomous: true
---

# Plan 1: Tipos de Notificação, Ícones e Ações

## Requirements
- NTYPE-01: Tipo "update": ícone raio, cor accent, expande detalhes inline
- NTYPE-02: Tipo "session_reminder": ícone calendário, navega para `/agenda`
- NTYPE-03: Tipo "payment_pending": ícone cifrão, navega para `/financeiro`
- NTYPE-04: Tipo "patient_noshow": ícone alerta, navega para `/patients/{id}`
- NTYPE-05: Tipo "birthday": ícone presente, navega para `/patients/{id}`
- NTYPE-06: SVG outline e cor de fundo sutil

## Dependencies
- None

## Tasks

```xml
<task id="18-01-01" type="execute">
  <read_first>
    <file>src/app/globals.css</file>
  </read_first>
  <action>
    Add notification type modifier classes in `src/app/globals.css` using existing tokens for a subtle background (`bg-opacity-10` equivalent).
    Add:
    - `.notif-item--update`: `background-color: var(--color-accent-light); color: var(--color-accent);`
    - `.notif-item--reminder`: `background-color: rgba(30, 64, 175, 0.08); color: var(--color-note-blue);` (use existing `--color-note-blue` or `#1e40af`)
    - `.notif-item--payment`: `background-color: var(--color-warning-bg); color: var(--color-warning-text);`
    - `.notif-item--noshow`: `background-color: var(--color-error-bg); color: var(--color-error-text);`
    - `.notif-item--birthday`: `background-color: var(--color-success-bg); color: var(--color-success-text);`
    Make sure these colors apply to the icon wrapper or the card background itself (use `.notif-icon-wrap` inside the item to hold the color).
  </action>
  <acceptance_criteria>
    - `src/app/globals.css` contains `.notif-item--update` and other type modifiers.
  </acceptance_criteria>
</task>

<task id="18-01-02" type="execute">
  <read_first>
    <file>src/lib/notifications/types.ts</file>
  </read_first>
  <action>
    Create a new file `src/components/ui/notification-item.tsx`.
    Implement the `NotificationItem` React component.
    - Props: `notification: AppNotification`, `onMarkAsRead: () => void`, `onCloseDropdown: () => void`.
    - Use `useRouter` from `next/navigation` for actions.
    - Inside the component, map `notification.type` to inline SVGs (outline style, 24x24 or 20x20):
      - `update`: Zap (lightning)
      - `session_reminder`: Calendar
      - `payment_pending`: DollarSign
      - `patient_noshow`: AlertCircle
      - `birthday`: Gift
    - Add accordion state: `const [expanded, setExpanded] = useState(false)` for `update` type.
    - Implement `handleClick`:
      - First, call `onMarkAsRead()`.
      - If `type === "update"`, toggle `setExpanded(!expanded)`.
      - If `type === "session_reminder"`, `router.push('/agenda')` and `onCloseDropdown()`.
      - If `type === "payment_pending"`, `router.push('/financeiro')` and `onCloseDropdown()`.
      - If `type === "patient_noshow"` or `"birthday"`, `router.push('/patients/' + notification.patientId)` and `onCloseDropdown()`.
    - Render the `<div className={"notif-item " + ...}>` with the unread dot, the icon wrapper (applying the specific CSS class from task 18-01-01), the title, and description. If `expanded` and `type === "update"`, render a `div` below with `notification.changelog`.
  </action>
  <acceptance_criteria>
    - `src/components/ui/notification-item.tsx` exports `NotificationItem`.
    - The file contains `router.push('/agenda')` for session_reminder.
    - The file uses `expanded` state to show `changelog`.
  </acceptance_criteria>
</task>

<task id="18-01-03" type="execute">
  <read_first>
    <file>src/components/ui/notification-bell.tsx</file>
  </read_first>
  <action>
    Refactor `src/components/ui/notification-bell.tsx` to use the new `NotificationItem`.
    - Import `NotificationItem` from `./notification-item`.
    - Replace the `<button key={n.id} className="notif-item"...>` block inside `.notif-list` with `<NotificationItem key={n.id} notification={n} onMarkAsRead={() => markAsRead(n.id)} onCloseDropdown={() => setOpen(false)} />`.
    - Ensure everything else stays identical.
  </action>
  <acceptance_criteria>
    - `src/components/ui/notification-bell.tsx` imports and uses `<NotificationItem />`.
    - The old `notif-item` button logic is removed from `notification-bell.tsx`.
  </acceptance_criteria>
</task>
```
