---
phase: 19
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(vault)/financeiro/page-client.tsx
autonomous: true
requirements:
  - UXFI-02
must_haves:
  truths:
    - "Usuário visualiza indicativos visuais em destaque para identificar pagamentos atrasados ou inadimplência."
    - "Usuário visualiza a lista principal no formato plano e cronológico de extrato, abandonando a UI antiga de sanfona por paciente."
  artifacts:
    - path: "src/app/(vault)/financeiro/page-client.tsx"
      provides: "Uso do componente Tabs e layout de extrato bancário"
      min_lines: 500
  key_links:
    - from: "src/app/(vault)/financeiro/page-client.tsx"
      to: "src/components/ui/tabs.tsx"
      via: "import and render Tabs"
      pattern: "import \\{ Tabs"
---

<objective>
Refatorar a lista de cobranças na aba Financeiro para usar o padrão de Extrato Bancário e destacar inadimplência em aba separada.

Purpose: Melhorar a escaneabilidade dos dados financeiros e dar destaque operacional a recebimentos atrasados.
Output: Interface atualizada com Tabs (Extrato e Atrasados) e lista simples sem sanfona.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-refatora-o-ux-ui-finan-as/19-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aplicar Tabs e separar visão de Extrato e Atrasos</name>
  <files>src/app/(vault)/financeiro/page-client.tsx</files>
  <action>Refatore a tela `page-client.tsx` para importar e utilizar o componente `Tabs` de `src/components/ui/tabs.tsx`. Envolva a lista principal com `Tabs`, criando duas abas principais: "Extrato" (defaultValue) e "Atrasados" (cujo TabPanel filtrará apenas as cobranças com `status === "atrasado"`). O estado dos filtros superiores (Status, Paciente) deve se aplicar a ambas as abas.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>As abas "Extrato" e "Atrasados" estão renderizando corretamente usando o componente Tabs com sync na URL (`searchParamKey="tab"`).</done>
</task>

<task type="auto">
  <name>Task 2: Refatorar listagem para Extrato (Flat List)</name>
  <files>src/app/(vault)/financeiro/page-client.tsx</files>
  <action>Substitua o atual padrão de agrupamento por paciente (estilo sanfona/accordion) por uma lista plana estilo Extrato. Remova a lógica de `patientGroupStyle` e `expandedPatient`, renderizando as cobranças `filteredCharges` diretamente mapeadas e ordenadas. Exiba em cada linha o nome do paciente, o status, o valor, a data e botões de ação atuais.</action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>A UI renderiza uma lista plana sequencial de todas as cobranças. Sem agrupamento recolhível por paciente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client UI | Client components re-organizing data already scoped to the authenticated professional. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Information Disclosure | page-client.tsx | accept | Data is already securely fetched server-side per workspace; frontend sorting/tabbing introduces no new data access surface. |
</threat_model>

<verification>
Verificar manualmente que as abas filtram as visualizações e que a lista em formato de extrato exibe os botões de ação ("Receber" / "Desfazer").
</verification>

<success_criteria>
O profissional visualiza todas as cobranças em lista sequencial sem cliques adicionais para expandir, e possui uma aba focada "Atrasados".
</success_criteria>

<output>
After completion, create `.planning/phases/19-refatora-o-ux-ui-finan-as/19-01-SUMMARY.md`
</output>