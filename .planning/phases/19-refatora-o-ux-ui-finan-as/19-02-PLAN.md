---
phase: 19
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - src/app/(vault)/financeiro/page.tsx
  - src/app/(vault)/financeiro/page-client.tsx
  - src/app/(vault)/financeiro/components/charge-side-panel.tsx
autonomous: true
requirements:
  - UXFI-01
must_haves:
  truths:
    - "Usuário visualiza e edita cobranças e detalhes em Drawers laterais"
    - "Drawer é ativado e persistido pela URL via ?drawer=id"
    - "Botões inline de 'Receber' na lista abrem o Drawer ao invés de popovers inline"
  artifacts:
    - path: "src/app/(vault)/financeiro/components/charge-side-panel.tsx"
      provides: "Componente Drawer para edição e visualização de cobrança"
    - path: "src/app/(vault)/financeiro/page.tsx"
      provides: "Repassa searchParams de drawer para o client"
  key_links:
    - from: "src/app/(vault)/financeiro/page-client.tsx"
      to: "src/app/(vault)/financeiro/components/charge-side-panel.tsx"
      via: "import ChargeSidePanel"
      pattern: "<ChargeSidePanel"
---

<objective>
Implementar a visualização e edição de cobranças via Drawers integrados por URL, eliminando popovers inline.

Purpose: Centralizar as ações de uma cobrança em um espaço dedicado na tela sem perder o contexto da lista principal.
Output: Componente `ChargeSidePanel` criado e integrado na listagem principal.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-refatora-o-ux-ui-finan-as/19-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar o componente Drawer para Finanças</name>
  <files>src/app/(vault)/financeiro/components/charge-side-panel.tsx</files>
  <action>Crie o componente `ChargeSidePanel` baseado no padrão de `AppointmentSidePanel`. Ele deve receber uma string de `drawerId` (ou null) da URL, e um callback `onClose` que limpa o param da URL. O drawer exibirá as informações completas da cobrança selecionada e seus botões de ação ("Receber", "Desfazer"). Se `drawerId === "nova"`, deve exibir o formulário de Adicionar Cobrança.</action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Arquivo criado, exportando o drawer com o wrapper de focus trap e `dialog` com CSS vars adequadas (`--z-modal`).</done>
</task>

<task type="auto">
  <name>Task 2: Integrar o Drawer à URL e Listagem</name>
  <files>src/app/(vault)/financeiro/page.tsx, src/app/(vault)/financeiro/page-client.tsx</files>
  <action>Atualize `page.tsx` para passar `drawer` (do `searchParams`) ao client. No `page-client.tsx`, renderize o `ChargeSidePanel`. Ao clicar em uma cobrança na lista, use `router.push` para adicionar `?drawer={charge.id}` na URL, mantendo os outros params (`month`, `year`). Remova o popover inline de métodos de pagamento e o formulário inline de adicionar cobrança; substitua essas interações pelos estados do drawer.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Clicar em uma linha de cobrança ou botão + abre o drawer. A URL sincroniza e o popover antigo não existe mais.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Drawer | Modifies the representation of client forms. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02 | Tampering | page-client.tsx | mitigate | Data modifications triggered from the Drawer still post to the secure Server Actions which validate workspace and authorization bounds. |
</threat_model>

<verification>
Clicar em "Nova Cobrança" e numa cobrança existente. Verificar que a URL altera para `?drawer=nova` ou `?drawer=[id]`, e o painel abre deslizando da direita.
</verification>

<success_criteria>
Drawers operacionais eliminando formulários e popovers inline da listagem de finanças. Deep linking no state.
</success_criteria>

<output>
After completion, create `.planning/phases/19-refatora-o-ux-ui-finan-as/19-02-SUMMARY.md`
</output>