---
phase: 20-m-dulo-de-gest-o-de-despesas
plan: "03"
title: "Storage de Comprovantes (Supabase)"
wave: 1
depends_on: ["20-01"]
autonomous: false
requirements: [DESP-03]
files_modified:
  - src/lib/expenses/upload-validation.ts
  - src/lib/expenses/storage.ts
  - tests/expenses/upload-validation.test.ts
  - tests/expenses/storage-key.test.ts
must_haves:
  - "Bucket privado expense-receipts criado no Supabase (manual)"
  - "Validação MIME (PDF/JPG/PNG) e size (≤10MB) executada antes de qualquer upload (D-19)"
  - "Storage key namespaced por workspaceId/expenseId/uuid-filename (isola tenants)"
  - "Signed URL com TTL 120s para preview/download de comprovante"
canonical_refs:
  - .planning/phases/20-m-dulo-de-gest-o-de-despesas/20-CONTEXT.md (D-19, D-21)
  - .planning/phases/20-m-dulo-de-gest-o-de-despesas/20-RESEARCH.md § "Architecture Patterns" 7
  - src/app/(vault)/setup/actions.ts (template de upload)
  - src/app/api/patients/[patientId]/documents/[documentId]/pdf/route.ts (template de signed URL)
---

# Plan 03 — Storage de Comprovantes

## Objective

Construir helpers para upload, validação e geração de signed URLs para comprovantes
de despesa. Bucket privado `expense-receipts` no Supabase. **Não** inclui server
actions ou UI — só os helpers puros e os testes. Server actions ficam no Plan 04,
UI no Plan 05.

## Tasks

### Task 20-03-01 — [CHECKPOINT HUMANO] Criar bucket no Supabase Dashboard

**Read first:**
- `.planning/phases/20-m-dulo-de-gest-o-de-despesas/20-CONTEXT.md` § D-19
- `.planning/phases/20-m-dulo-de-gest-o-de-despesas/20-RESEARCH.md` § "Architecture Patterns" 7

**Action (manual — não automatizável via CLI no projeto):**

1. Login Supabase Dashboard → Storage → "New bucket"
2. Name: `expense-receipts`
3. Public: **OFF** (privado)
4. File size limit: 10 MB
5. Allowed MIME types: `application/pdf, image/jpeg, image/png`
6. Confirmar criação
7. (Opcional, se RLS for aplicado): adicionar policy `deny all` por default — signed URLs server-side ignoram RLS

**Acceptance criteria (manual):**
- Bucket aparece em Storage
- Tentativa de download direto via URL pública retorna 403
- Confirmar com humano antes de prosseguir

**Status:** **CHECKPOINT** — bloqueia Plan 04 (server actions de upload).

---

### Task 20-03-02 — Validação de upload (MIME + size)

**Read first:**
- `src/app/(vault)/setup/actions.ts:1-30, 56-66` (constantes e checagens — template literal)

**Action:**

Criar `src/lib/expenses/upload-validation.ts`:

```ts
export const ALLOWED_RECEIPT_MIME_TYPES = [
  "application/pdf",
  "image/jpeg",
  "image/png",
] as const;

export const MAX_RECEIPT_SIZE_BYTES = 10 * 1024 * 1024; // 10 MB

export type ReceiptValidationResult =
  | { ok: true }
  | { ok: false; error: string };

export function validateReceiptFile(file: File): ReceiptValidationResult {
  if (file.size === 0) {
    return { ok: false, error: "Arquivo vazio." };
  }
  if (!ALLOWED_RECEIPT_MIME_TYPES.includes(file.type as (typeof ALLOWED_RECEIPT_MIME_TYPES)[number])) {
    return { ok: false, error: "Formato não suportado. Use PDF, JPG ou PNG." };
  }
  if (file.size > MAX_RECEIPT_SIZE_BYTES) {
    return { ok: false, error: "Arquivo muito grande. Máximo: 10MB." };
  }
  return { ok: true };
}
```

**Acceptance criteria:**
- `pnpm tsc --noEmit` limpo
- Mensagens de erro em pt-BR (CLAUDE.md)

---

### Task 20-03-03 — Storage helpers (key + signed URL)

**Read first:**
- `src/app/(vault)/setup/actions.ts:68-78` (upload pattern)
- `src/app/api/patients/[patientId]/documents/[documentId]/pdf/route.ts:82-97` (signed URL pattern)

**Action:**

Criar `src/lib/expenses/storage.ts`:

```ts
import type { SupabaseClient } from "@supabase/supabase-js";

export const RECEIPTS_BUCKET = "expense-receipts";
const SIGNED_URL_TTL_SECONDS = 120;

export function buildExpenseReceiptStorageKey(args: {
  workspaceId: string;
  expenseId: string;
  fileName: string;
}): string {
  const safeName = args.fileName.replace(/[^\w.\-]/g, "_");
  return `${args.workspaceId}/${args.expenseId}/${crypto.randomUUID()}-${safeName}`;
}

export async function uploadExpenseReceipt(
  supabase: SupabaseClient,
  args: { storageKey: string; buffer: ArrayBuffer; mimeType: string },
): Promise<{ ok: true } | { ok: false; error: string }> {
  const { error } = await supabase.storage
    .from(RECEIPTS_BUCKET)
    .upload(args.storageKey, args.buffer, { contentType: args.mimeType });
  if (error) return { ok: false, error: error.message };
  return { ok: true };
}

export async function deleteExpenseReceipt(
  supabase: SupabaseClient,
  storageKey: string,
): Promise<{ ok: true } | { ok: false; error: string }> {
  const { error } = await supabase.storage.from(RECEIPTS_BUCKET).remove([storageKey]);
  if (error) return { ok: false, error: error.message };
  return { ok: true };
}

export async function getExpenseReceiptSignedUrl(
  supabase: SupabaseClient,
  storageKey: string,
): Promise<string | null> {
  const { data } = await supabase.storage
    .from(RECEIPTS_BUCKET)
    .createSignedUrl(storageKey, SIGNED_URL_TTL_SECONDS);
  return data?.signedUrl ?? null;
}
```

**Acceptance criteria:**
- `pnpm tsc --noEmit` limpo
- `rg "createSignedUrl.*120" src/lib/expenses/storage.ts` retorna 1 match (TTL hardcoded como constante)
- `rg "expense-receipts" src/lib/expenses/storage.ts` retorna ≥1 match (bucket name como constante exportada)

---

### Task 20-03-04 — Tests

**Read first:**
- `src/lib/expenses/upload-validation.ts` e `storage.ts` (recém-criados)

**Action:**

Criar `tests/expenses/upload-validation.test.ts`:
- File válido (PDF, 1MB) → `{ ok: true }`
- File MIME inválido (`text/plain`) → `{ ok: false, error: "Formato não suportado..." }`
- File 10MB exato → `{ ok: true }`
- File 10MB + 1 byte → `{ ok: false, error: "Arquivo muito grande..." }`
- File vazio (0 bytes) → `{ ok: false, error: "Arquivo vazio." }`

Criar `tests/expenses/storage-key.test.ts`:
- `buildExpenseReceiptStorageKey({ workspaceId: "ws_a", expenseId: "exp_1", fileName: "nota.pdf" })`
  retorna string que **começa com** `ws_a/exp_1/` e **termina com** `-nota.pdf`
- Caracteres especiais no fileName são sanitizados (`"nota fiscal #1.pdf"` → não tem espaço/`#`)
- 1000 chamadas com mesmos args produzem 1000 chaves distintas (UUID no meio)

**Acceptance criteria:**
- `pnpm vitest run tests/expenses/upload-validation.test.ts tests/expenses/storage-key.test.ts` exit 0
- ≥7 testes passando

---

## Verification (full plan)

```bash
pnpm tsc --noEmit
pnpm vitest run tests/expenses/upload-validation.test.ts tests/expenses/storage-key.test.ts
```

Manual: confirmar bucket criado.

## Success Criteria

- ✅ Bucket `expense-receipts` privado existe no Supabase
- ✅ Helpers puros de validação e storage testados
- ✅ Storage key namespacing previne cross-tenant access
- ✅ Signed URL TTL = 120s (constante)
