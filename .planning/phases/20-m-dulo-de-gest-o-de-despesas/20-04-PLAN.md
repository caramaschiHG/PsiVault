---
phase: 20-m-dulo-de-gest-o-de-despesas
plan: "04"
title: "Server Actions + Integração com /financeiro"
wave: 2
depends_on: ["20-01", "20-02", "20-03"]
autonomous: true
requirements: [DESP-01, DESP-02, DESP-03]
files_modified:
  - src/app/(vault)/financeiro/actions.ts
  - src/app/(vault)/financeiro/page.tsx
  - src/app/api/expenses/[expenseId]/receipt/route.ts
  - tests/expenses/actions.test.ts
must_haves:
  - "Server actions cobrem CRUD de Categoria, Despesa, e Receipt (attach/replace/remove)"
  - "Toda action chama resolveSession ANTES de qualquer query (workspace scoping garantido)"
  - "Toda mutation emite audit event tipado e chama revalidatePath('/financeiro')"
  - "page.tsx carrega expenses + categories em paralelo no Promise.all existente"
  - "Rota /api/expenses/[expenseId]/receipt gera signed URL (120s) validando ownership"
canonical_refs:
  - .planning/phases/20-m-dulo-de-gest-o-de-despesas/20-CONTEXT.md (D-09, D-13, D-14)
  - .planning/phases/20-m-dulo-de-gest-o-de-despesas/20-PATTERNS.md § "Server actions" e "API routes"
  - src/app/(vault)/financeiro/actions.ts (extensão)
---

# Plan 04 — Server Actions + Integração

## Objective

Conectar o domínio de Despesas (Plans 02 e 03) à camada de aplicação: server actions
para mutations, rota API para download de comprovantes via signed URL, e
extensão do `page.tsx` para carregar dados em paralelo.

## Tasks

### Task 20-04-01 — Server actions de Categoria

**Read first:**
- `src/app/(vault)/financeiro/actions.ts` (arquivo inteiro — entender estrutura existente, especialmente `createId` linha 9, padrão de `resolveSession` e `revalidatePath`)
- `src/lib/expense-categories/store.ts` e `audit.ts`

**Action:**

Adicionar ao `src/app/(vault)/financeiro/actions.ts` (preservando todo o código existente):

```ts
// No topo, junto com createId existente (linha 9):
const createExpenseCategoryId = () => `excat_${crypto.randomUUID().slice(0, 12)}`;
const createAuditId = () => `audit_${crypto.randomUUID().slice(0, 12)}`;

// Novas actions:
export async function createExpenseCategoryAction(
  _prevState: { error?: string } | null,
  formData: FormData,
): Promise<{ error?: string; categoryId?: string }> {
  const { accountId, workspaceId } = await resolveSession();
  const name = String(formData.get("name") ?? "").trim();
  if (name.length < 1 || name.length > 60) {
    return { error: "Nome da categoria precisa ter entre 1 e 60 caracteres." };
  }

  const repo = getExpenseCategoryStore().repository;
  const category = createExpenseCategory(
    { workspaceId, name },
    { now: new Date(), createId: createExpenseCategoryId },
  );
  await repo.create(category);

  // audit
  await emitAudit(/* expense_category.created com metadata mínimo */);

  revalidatePath("/financeiro");
  return { categoryId: category.id };
}

export async function renameExpenseCategoryAction(
  categoryId: string,
  newName: string,
): Promise<{ error?: string }> {
  const { accountId, workspaceId } = await resolveSession();
  // validar, buscar, renomear, salvar, audit, revalidatePath
}

export async function archiveExpenseCategoryAction(
  categoryId: string,
): Promise<{ error?: string }> {
  // ...
}
```

**Acceptance criteria:**
- `pnpm tsc --noEmit` limpo
- `rg "resolveSession" src/app/\(vault\)/financeiro/actions.ts` count aumenta em ≥3 (uma por nova action)
- `rg "revalidatePath" src/app/\(vault\)/financeiro/actions.ts` count aumenta em ≥3
- Action retorna shape consistente com pattern de `saveSignatureAssetAction` (`{ error?: string, ... }`)

---

### Task 20-04-02 — Server actions de Despesa (CRUD + scope)

**Read first:**
- `src/lib/expenses/store.ts`, `model.ts`, `series.ts`, `audit.ts`
- Pattern de Phase 19 charges no mesmo arquivo de actions

**Action:**

Adicionar 3 actions ao `actions.ts`:

```ts
const createExpenseId = () => `exp_${crypto.randomUUID().slice(0, 12)}`;
const createSeriesId = () => `expser_${crypto.randomUUID().slice(0, 12)}`;

export async function createExpenseAction(
  _prevState: { error?: string } | null,
  formData: FormData,
): Promise<{ error?: string; expenseId?: string }> {
  const { accountId, workspaceId } = await resolveSession();

  // Validar campos
  const description = String(formData.get("description") ?? "").trim();
  const amountText = String(formData.get("amount") ?? "");
  const amountInCents = parseBRLToCents(amountText);
  const categoryId = String(formData.get("categoryId") ?? "");
  const dueDateStr = String(formData.get("dueDate") ?? "");
  const recurrencePattern = formData.get("recurrencePattern") as "MENSAL" | "QUINZENAL" | null;

  if (description.length < 1 || description.length > 200) return { error: "Descrição inválida." };
  if (!amountInCents || amountInCents <= 0) return { error: "Valor inválido." };
  if (!categoryId) return { error: "Categoria obrigatória." };
  const dueDate = new Date(dueDateStr);
  if (Number.isNaN(dueDate.getTime())) return { error: "Data inválida." };

  // Confirmar categoria pertence ao workspace
  const category = await getExpenseCategoryStore().repository.findById(workspaceId, categoryId);
  if (!category) return { error: "Categoria não encontrada." };

  const repo = getExpenseStore().repository;

  if (recurrencePattern) {
    const series = materializeSeries(
      { workspaceId, categoryId, description, amountInCents, dueDate, recurrencePattern, firstOccurrenceDate: dueDate, createdByAccountId: accountId },
      { now: new Date(), createId: createExpenseId, createSeriesId },
    );
    await repo.createMany(series);
    // audit: expense.created (uma vez para a série, com seriesId)
    revalidatePath("/financeiro");
    return { expenseId: series[0].id };
  } else {
    const expense = createExpense(
      { workspaceId, categoryId, description, amountInCents, dueDate, createdByAccountId: accountId },
      { now: new Date(), createId: createExpenseId },
    );
    await repo.create(expense);
    // audit: expense.created
    revalidatePath("/financeiro");
    return { expenseId: expense.id };
  }
}

export async function updateExpenseAction(
  expenseId: string,
  scope: SeriesScope,
  patch: { description?: string; amountInCents?: number; categoryId?: string; dueDate?: string },
): Promise<{ error?: string }> {
  // resolveSession → repo.findById → applySeriesEdit se for série → repo.bulkUpdate ou update → audit → revalidatePath
}

export async function deleteExpenseAction(
  expenseId: string,
  scope: SeriesScope,
): Promise<{ error?: string }> {
  const { accountId, workspaceId } = await resolveSession();
  const repo = getExpenseStore().repository;
  const target = await repo.findById(workspaceId, expenseId);
  if (!target) return { error: "Despesa não encontrada." };

  await repo.softDeleteWithScope(workspaceId, expenseId, scope, accountId, new Date());
  // audit: expense.deleted com scope
  revalidatePath("/financeiro");
  return {};
}
```

**Acceptance criteria:**
- `pnpm tsc --noEmit` limpo
- `rg "softDeleteWithScope" src/app/\(vault\)/financeiro/actions.ts` retorna ≥1 match
- `rg "materializeSeries" src/app/\(vault\)/financeiro/actions.ts` retorna ≥1 match
- Toda action começa com `await resolveSession()` antes de qualquer query (verificar manualmente)

---

### Task 20-04-03 — Server actions de Receipt (attach/replace/remove)

**Read first:**
- `src/app/(vault)/setup/actions.ts:50-94` (template completo de upload com validação)
- `src/lib/expenses/upload-validation.ts` e `storage.ts`

**Action:**

```ts
export async function attachReceiptAction(
  _prevState: { error?: string } | null,
  formData: FormData,
): Promise<{ error?: string }> {
  const { accountId, workspaceId } = await resolveSession();
  const expenseId = String(formData.get("expenseId") ?? "");
  const file = formData.get("file");
  if (!(file instanceof File)) return { error: "Nenhum arquivo selecionado." };

  const validation = validateReceiptFile(file);
  if (!validation.ok) return { error: validation.error };

  const repo = getExpenseStore().repository;
  const expense = await repo.findById(workspaceId, expenseId);
  if (!expense) return { error: "Despesa não encontrada." };
  if (expense.receiptStorageKey) return { error: "Despesa já possui comprovante. Use substituir." };

  const storageKey = buildExpenseReceiptStorageKey({ workspaceId, expenseId, fileName: file.name });
  const supabase = await createClient();
  const upload = await uploadExpenseReceipt(supabase, {
    storageKey,
    buffer: await file.arrayBuffer(),
    mimeType: file.type,
  });
  if (!upload.ok) return { error: `Erro no upload: ${upload.error}` };

  await repo.update(workspaceId, expenseId, {
    receiptStorageKey: storageKey,
    receiptFileName: file.name,
    receiptMimeType: file.type,
  });
  // audit: expense.receipt_attached
  revalidatePath("/financeiro");
  return {};
}

export async function replaceReceiptAction(/* ... */): Promise<{ error?: string }> {
  // Como attach, mas: se já existe, deleteExpenseReceipt do antigo antes de upload do novo
  // audit: expense.receipt_replaced
}

export async function removeReceiptAction(expenseId: string): Promise<{ error?: string }> {
  const { accountId, workspaceId } = await resolveSession();
  const repo = getExpenseStore().repository;
  const expense = await repo.findById(workspaceId, expenseId);
  if (!expense || !expense.receiptStorageKey) return { error: "Sem comprovante para remover." };

  const supabase = await createClient();
  await deleteExpenseReceipt(supabase, expense.receiptStorageKey);
  await repo.update(workspaceId, expenseId, {
    receiptStorageKey: null,
    receiptFileName: null,
    receiptMimeType: null,
  });
  // audit: expense.receipt_removed
  revalidatePath("/financeiro");
  return {};
}
```

**Acceptance criteria:**
- `pnpm tsc --noEmit` limpo
- `rg "validateReceiptFile" src/app/\(vault\)/financeiro/actions.ts` retorna ≥1 match
- `rg "expense.receipt_" src/app/\(vault\)/financeiro/actions.ts` retorna ≥3 matches (attach/replace/remove)

---

### Task 20-04-04 — Rota API para signed URL de comprovante

**Read first:**
- `src/app/api/patients/[patientId]/documents/[documentId]/pdf/route.ts` (template completo)

**Action:**

Criar `src/app/api/expenses/[expenseId]/receipt/route.ts`:

```ts
import { NextRequest, NextResponse } from "next/server";
import { resolveSession } from "@/lib/auth/session-resolve";
import { createClient } from "@/lib/supabase/server";
import { getExpenseStore } from "@/lib/expenses/store";
import { getExpenseReceiptSignedUrl } from "@/lib/expenses/storage";

export async function GET(
  _req: NextRequest,
  { params }: { params: Promise<{ expenseId: string }> },
) {
  const { workspaceId } = await resolveSession();
  const { expenseId } = await params;

  const expense = await getExpenseStore().repository.findById(workspaceId, expenseId);
  if (!expense || !expense.receiptStorageKey) {
    return NextResponse.json({ error: "Não encontrado" }, { status: 404 });
  }

  const supabase = await createClient();
  const url = await getExpenseReceiptSignedUrl(supabase, expense.receiptStorageKey);
  if (!url) return NextResponse.json({ error: "Falha ao gerar URL" }, { status: 500 });

  return NextResponse.redirect(url, 302);
}
```

**Acceptance criteria:**
- Arquivo existe
- `pnpm tsc --noEmit` limpo
- `rg "resolveSession|workspaceId" src/app/api/expenses/\[expenseId\]/receipt/route.ts` retorna ≥2 matches (ownership check)

---

### Task 20-04-05 — Integrar carga em page.tsx

**Read first:**
- `src/app/(vault)/financeiro/page.tsx` (arquivo inteiro — entender o `Promise.all` existente)

**Action:**

Modificar `page.tsx`:
- Adicionar imports: `getExpenseStore`, `getExpenseCategoryStore`
- Estender o `Promise.all` existente para incluir `expenses` e `categories`:
  ```ts
  const [charges, payments, expenses, categories] = await Promise.all([
    getChargeStore().repository.findByWorkspace(workspaceId),
    getPaymentStore().repository.findByWorkspace(workspaceId),
    getExpenseStore().repository.findByWorkspace(workspaceId),
    getExpenseCategoryStore().repository.findActiveByWorkspace(workspaceId),
  ]);
  ```
- Passar `expenses` e `categories` como props para `<FinanceiroPageClient>`

**Acceptance criteria:**
- `pnpm tsc --noEmit` limpo
- `rg "getExpenseStore" src/app/\(vault\)/financeiro/page.tsx` retorna 1 match
- `rg "Promise.all" src/app/\(vault\)/financeiro/page.tsx` ainda retorna 1 match (não foi quebrado em await sequencial)

---

### Task 20-04-06 — Tests de actions (workspace scoping)

**Read first:**
- Actions criadas

**Action:**

Criar `tests/expenses/actions.test.ts`:
- Mock `resolveSession` para retornar `{ accountId: "acct_1", workspaceId: "ws_a" }`
- `setExpenseRepositoryForTest(new InMemoryExpenseRepository())`
- Asserir: `createExpenseAction` chama `resolveSession` **antes** de qualquer call no repo
- Asserir: expense criado tem `workspaceId === "ws_a"`
- Asserir: tentar criar com categoria de outro workspace → erro

**Acceptance criteria:**
- `pnpm vitest run tests/expenses/actions.test.ts` exit 0
- ≥3 testes passando

---

## Verification (full plan)

```bash
pnpm tsc --noEmit
pnpm vitest run tests/expenses/
pnpm next build  # smoke check de que rotas compilam
```

## Success Criteria

- ✅ 9 server actions novas (3 categoria + 3 despesa + 3 receipt) funcionando
- ✅ Workspace scoping garantido em toda action (resolveSession primeiro)
- ✅ Audit events emitidos em toda mutation
- ✅ Rota API valida ownership antes de gerar signed URL
- ✅ page.tsx carrega expenses + categories em paralelo (sem regredir Phase 19)
