---
phase: 21-brand-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/(vault)/layout.tsx
  - src/app/(vault)/components/vault-sidebar-nav.tsx
autonomous: false
requirements:
  - BRAND-02

must_haves:
  truths:
    - "globals.css define tokens de espaçamento generosos (2rem section-gap, 4rem row-height) como variáveis nomeadas"
    - "O body tem line-height 1.6 — texto corrido mais legível e editorial"
    - "A sidebar demonstra os tokens refinados: mais respiro no brand block e entre itens de navegação"
    - "IBM Plex Serif está disponível via var(--font-serif) mas não aplicado globalmente — somente onde explicitado"
    - "Nenhuma cor da paleta atual foi alterada"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Tokens de espaçamento e tipografia refinados"
      contains: "--space-section-gap"
    - path: "src/app/(vault)/layout.tsx"
      provides: "Brand block com espaçamento generoso"
    - path: "src/app/(vault)/components/vault-sidebar-nav.tsx"
      provides: "Nav com gap e padding aumentados"
  key_links:
    - from: "globals.css :root"
      to: "todas as views do vault"
      via: "var(--space-section-gap) e var(--space-row-height)"
      pattern: "var\\(--space-section-gap\\)"
    - from: "globals.css .vault-sidebar .nav-link"
      to: "vault-sidebar-nav.tsx"
      via: "classe CSS nav-link"
      pattern: "nav-link"
---

<objective>
Refinar tokens de espaçamento e tipografia em globals.css e aplicar os refinamentos na sidebar como demonstração visual da identidade de marca.

Purpose: BRAND-02 exige que os tokens visuais estejam definidos como variáveis CSS nomeadas e aplicados de forma coerente em pelo menos uma superfície. A sidebar é visível em toda sessão autenticada — é a demonstração ideal.
Output: globals.css com tokens atualizados; sidebar com mais respiro tipográfico e de espaçamento.
</objective>

<execution_context>
@/home/caramaschi/.claude/get-shit-done/workflows/execute-plan.md
@/home/caramaschi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-brand-foundation/21-CONTEXT.md
@.planning/phases/21-brand-foundation/21-RESEARCH.md
</context>

<interfaces>
<!-- Estado atual dos arquivos. Extraído da pesquisa (21-RESEARCH.md). -->

De src/app/globals.css (tokens atuais a modificar):
```css
/* Tokens de espaçamento — linha ~51 */
--space-section-gap:    1.5rem;   /* → alterar para 2rem */
--space-row-height:     3.5rem;   /* → alterar para 4rem */

/* Body — linha ~127 */
body {
  line-height: 1.5;               /* → alterar para 1.6 */
}

/* Sidebar nav-link — acrescentar line-height e letter-spacing */
.vault-sidebar .nav-link {
  color: rgba(255, 255, 255, 0.75);
  transition: background-color 150ms ease, color 150ms ease;
  /* → adicionar: line-height: 1.4; letter-spacing: 0.005em; */
}
```

De src/app/(vault)/layout.tsx (brandStyle inline atual):
```typescript
const brandStyle = {
  display: "flex",
  alignItems: "center",
  gap: "0.625rem",
  padding: "1.25rem 1rem 1rem",   /* → alterar para "1.5rem 1.25rem 1rem" */
} satisfies React.CSSProperties;
```

De src/app/(vault)/components/vault-sidebar-nav.tsx (navStyle inline atual):
```typescript
const navStyle: React.CSSProperties = {
  flex: 1,
  padding: "0.5rem 0.75rem",      /* → alterar para "0.75rem 0.875rem" */
  display: "flex",
  flexDirection: "column",
  gap: "0.25rem",                  /* → alterar para "0.375rem" */
};
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Refinar tokens de espaçamento e tipografia em globals.css</name>
  <files>src/app/globals.css</files>
  <action>
Fazer três ajustes pontuais em globals.css:

**1. Tokens de espaçamento (no bloco :root):**
- `--space-section-gap`: de `1.5rem` para `2rem`
- `--space-row-height`: de `3.5rem` para `4rem`

**2. line-height do body:**
- No seletor `body {}`, alterar `line-height: 1.5` para `line-height: 1.6`

**3. Refinamento tipográfico do nav-link da sidebar:**
- Na regra `.vault-sidebar .nav-link {}`, adicionar as propriedades:
  ```css
  line-height: 1.4;
  letter-spacing: 0.005em;
  ```

**Não alterar:**
- Nenhum `--font-size-*` token
- Nenhuma cor (`--color-*`)
- Nenhuma sombra (`--shadow-*`)
- Nenhum radius
- Nenhum outro token ou seletor não mencionado acima
  </action>
  <verify>
    <automated>grep -n "space-section-gap" /home/caramaschi/Área\ de\ trabalho/Projetos/PsiLock/src/app/globals.css && grep -n "space-row-height" /home/caramaschi/Área\ de\ trabalho/Projetos/PsiLock/src/app/globals.css && grep -n "line-height: 1.6" /home/caramaschi/Área\ de\ trabalho/Projetos/PsiLock/src/app/globals.css && grep -n "letter-spacing" /home/caramaschi/Área\ de\ trabalho/Projetos/PsiLock/src/app/globals.css</automated>
  </verify>
  <done>
globals.css contém: `--space-section-gap: 2rem`, `--space-row-height: 4rem`, `line-height: 1.6` no body, e `letter-spacing: 0.005em` no `.vault-sidebar .nav-link`. Nenhuma cor ou tamanho de fonte alterado.
  </done>
</task>

<task type="auto">
  <name>Task 2: Aplicar espaçamento refinado na sidebar (layout.tsx + vault-sidebar-nav.tsx)</name>
  <files>src/app/(vault)/layout.tsx, src/app/(vault)/components/vault-sidebar-nav.tsx</files>
  <action>
**Em src/app/(vault)/layout.tsx:**

Localizar o objeto `brandStyle` (inline style do brand block da sidebar). Alterar apenas o valor de `padding`:
- De: `padding: "1.25rem 1rem 1rem"`
- Para: `padding: "1.5rem 1.25rem 1rem"`

O objeto deve continuar usando `satisfies React.CSSProperties`. Não alterar `display`, `alignItems`, `gap` nem qualquer outro campo.

**Em src/app/(vault)/components/vault-sidebar-nav.tsx:**

Localizar o objeto `navStyle`. Alterar dois valores:
- `padding`: de `"0.5rem 0.75rem"` para `"0.75rem 0.875rem"`
- `gap`: de `"0.25rem"` para `"0.375rem"`

Não alterar `flex`, `display`, `flexDirection` nem nenhum outro campo. Não alterar a estrutura JSX da sidebar.
  </action>
  <verify>
    <automated>grep -n "1.5rem 1.25rem" /home/caramaschi/Área\ de\ trabalho/Projetos/PsiLock/src/app/\(vault\)/layout.tsx && grep -n "0.75rem 0.875rem" /home/caramaschi/Área\ de\ trabalho/Projetos/PsiLock/src/app/\(vault\)/components/vault-sidebar-nav.tsx && grep -n "0.375rem" /home/caramaschi/Área\ de\ trabalho/Projetos/PsiLock/src/app/\(vault\)/components/vault-sidebar-nav.tsx</automated>
  </verify>
  <done>
layout.tsx tem `padding: "1.5rem 1.25rem 1rem"` no brandStyle. vault-sidebar-nav.tsx tem `padding: "0.75rem 0.875rem"` e `gap: "0.375rem"` no navStyle. A estrutura JSX e todas as demais propriedades permanecem inalteradas.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificar sidebar com tokens refinados</name>
  <what-built>
Tokens de espaçamento e tipografia atualizados em globals.css (--space-section-gap: 2rem, --space-row-height: 4rem, line-height: 1.6 no body, letter-spacing na nav-link da sidebar). Sidebar com brand block e nav items com mais respiro (padding e gap aumentados).
  </what-built>
  <how-to-verify>
1. Iniciar o servidor: `pnpm dev` (se não estiver rodando)
2. Acessar qualquer rota do vault (ex: /inicio)
3. Verificar sidebar à esquerda:
   - O brand block (logo + "PsiVault") tem mais espaço interno no topo
   - Os itens de navegação estão mais espaçados entre si (gap maior)
   - O texto dos itens de nav parece ligeiramente mais legível (letter-spacing sutil)
4. Verificar em qualquer view com lista de dados: o espaçamento entre seções parece mais generoso
5. Confirmar: nenhuma cor mudou, paleta terracota intacta, estrutura da sidebar igual
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva problemas visuais encontrados</resume-signal>
  <action>Verificar visualmente a sidebar no browser após as Tasks 1 e 2.</action>
  <verify><automated>pnpm test</automated></verify>
  <done>Sidebar exibe espaçamento generoso e tipografia refinada; pnpm test passa sem regressões.</done>
</task>

</tasks>

<verification>
- `pnpm test` passa sem regressões
- globals.css: `grep "space-section-gap" src/app/globals.css` retorna `2rem`
- globals.css: `grep "space-row-height" src/app/globals.css` retorna `4rem`
- globals.css: `grep "line-height" src/app/globals.css` inclui `1.6` no body
- Sidebar renderiza com espaçamento visualmente mais generoso (verificação visual no checkpoint)
</verification>

<success_criteria>
globals.css define os tokens de espaçamento refinados como variáveis CSS nomeadas. A sidebar demonstra a aplicação desses tokens com mais respiro tipográfico e de espaçamento — visível em toda sessão autenticada. Nenhuma cor, tamanho de fonte ou estrutura de layout foi alterada.
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/21-brand-foundation/21-02-SUMMARY.md`
</output>
