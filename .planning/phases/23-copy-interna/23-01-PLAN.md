---
phase: 23-copy-interna
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(vault)/components/vault-sidebar-nav.tsx
  - src/app/(vault)/components/bottom-nav.tsx
  - src/app/(vault)/inicio/page.tsx
  - src/app/(auth)/complete-profile/page.tsx
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03

must_haves:
  truths:
    - "Sidebar exibe 'Prontuário' entre 'Pacientes' e 'Financeiro', com ícone de clipboard clínico"
    - "Bottom-nav exibe 'Prontuário' entre 'Pacientes' e 'Financeiro' — 'Config' removido do mobile"
    - "Dashboard mostra 'Sem atendimentos hoje.' (não 'Nenhuma sessão agendada para hoje.')"
    - "Dashboard mostra 'Atendimentos realizados' (não 'Sessões realizadas')"
    - "Dashboard mostra 'A receber' (não 'cobranças em aberto')"
    - "Mensagem contextual usa 'Seu próximo atendimento começa em X min' (não 'sessão')"
    - "Onboarding mostra 'Configure seu consultório.' como h1 (não 'Complete seu cadastro.')"
  artifacts:
    - path: "src/app/(vault)/components/vault-sidebar-nav.tsx"
      provides: "Sidebar nav com item Prontuário adicionado"
    - path: "src/app/(vault)/components/bottom-nav.tsx"
      provides: "Bottom nav mobile com Prontuário e sem Config"
    - path: "src/app/(vault)/inicio/page.tsx"
      provides: "Dashboard com vocabulário clínico correto"
    - path: "src/app/(auth)/complete-profile/page.tsx"
      provides: "Onboarding com framing de consultório"
  key_links:
    - from: "vault-sidebar-nav.tsx NAV_ITEMS"
      to: "/prontuario"
      via: "href + isActive guard"
      pattern: "prontuario"
    - from: "inicio/page.tsx contextualMessage"
      to: "string interpolada linha 106"
      via: "template literal"
      pattern: "próximo atendimento"
---

<objective>
Audit cirúrgico de copy nos quatro arquivos de UI existentes. Substitui strings que violam o vocabulário psicanalítico obrigatório (Phase 21) por linguagem clínica correta. Adiciona "Prontuário" à sidebar e ao bottom-nav como entrada de navegação de topo.

Purpose: O psicólogo que entra no app deve sentir a mesma coerência que viu na landing. Labels como "Sessões realizadas" e "cobranças em aberto" contradizem o posicionamento clínico do produto.

Output: Quatro arquivos editados — nav sidebar, nav mobile, dashboard e onboarding — com vocabulário alinhado ao CLAUDE.md.
</objective>

<execution_context>
@/home/caramaschi/.claude/get-shit-done/workflows/execute-plan.md
@/home/caramaschi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Contratos extraídos dos arquivos a editar. Executor usa diretamente — sem necessidade de explorar o codebase. -->

De src/app/(vault)/components/vault-sidebar-nav.tsx:
- NAV_ITEMS é um array `as const` com objetos `{ href, label, icon }`
- Ordem atual: /inicio, /agenda, /patients, /financeiro
- SETTINGS_ITEM separado: /settings/profile → "Configurações"
- isActive() tem guard especial para "/patients" (pathname.startsWith)
- Ícones: SVG inline, viewBox="0 0 24 24", fill="none", stroke="currentColor", strokeWidth={1.75}, width={16}, height={16}

De src/app/(vault)/components/bottom-nav.tsx:
- Mesma estrutura de NAV_ITEMS `as const`
- Itens atuais: /inicio, /agenda, /patients, /financeiro, /settings/profile (label "Config")
- Ícones: mesmo padrão SVG mas width={20}, height={20}
- isActive() tem guard especial para "/patients"

De src/app/(vault)/inicio/page.tsx:
- linha 106: `\`Sua próxima sessão começa em ${minutesUntilNext} min\`` → corrigir para "atendimento"
- linha 155: `"Nenhuma sessão agendada para hoje."` → "Sem atendimentos hoje."
- linha 211: `"Sessões realizadas"` → "Atendimentos realizados"
- linhas 218-219: `"cobrança em aberto"` / `"cobranças em aberto"` → "A receber" (singular e plural)

De src/app/(auth)/complete-profile/page.tsx:
- linha 39: `<h1 className="auth-title">Complete seu cadastro.</h1>` → "Configure seu consultório."
- Eyebrow "Perfil profissional" — MANTER (já correto)
- Subcopy "Esses dados identificam você nos documentos clínicos e configuram sua agenda." — MANTER
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar "Prontuário" à sidebar e ao bottom-nav</name>
  <files>src/app/(vault)/components/vault-sidebar-nav.tsx, src/app/(vault)/components/bottom-nav.tsx</files>
  <action>
Em vault-sidebar-nav.tsx:
1. Adicionar o objeto abaixo ao array NAV_ITEMS entre o item "/patients" e o item "/financeiro":
```tsx
{
  href: "/prontuario",
  label: "Prontuário",
  icon: (
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth={1.75} strokeLinecap="round" strokeLinejoin="round" width={16} height={16} aria-hidden="true">
      <path d="M9 5H7a2 2 0 0 0-2 2v12a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2V7a2 2 0 0 0-2-2h-2" />
      <rect x="9" y="3" width="6" height="4" rx="2" />
      <line x1="9" y1="12" x2="15" y2="12" />
      <line x1="9" y1="16" x2="13" y2="16" />
    </svg>
  ),
},
```
2. Atualizar a função isActive() adicionando guard para "/prontuario":
```tsx
function isActive(href: string) {
  if (href === "/patients") {
    return pathname.startsWith("/patients");
  }
  if (href === "/prontuario") {
    return pathname.startsWith("/prontuario");
  }
  return pathname === href || pathname.startsWith(href + "/");
}
```

Em bottom-nav.tsx:
1. Adicionar o item "Prontuário" entre "/patients" e "/financeiro" (mesmo ícone SVG, width={20} height={20}).
2. REMOVER o item "/settings/profile" (label "Config") do array NAV_ITEMS — Configurações fica acessível apenas via sidebar desktop. Resultado final: 5 itens (Início, Agenda, Pacientes, Prontuário, Financeiro).
3. Atualizar isActive() com o mesmo guard para "/prontuario" adicionado na sidebar.

Vocabulário — never usar: "sessão", "usuário", "cliente", "ficha".
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <done>NAV_ITEMS da sidebar contém "/prontuario" entre "/patients" e "/financeiro"; bottom-nav tem 5 itens sem "/settings/profile"; TypeScript compila sem erros.</done>
</task>

<task type="auto">
  <name>Task 2: Corrigir strings de vocabulário no dashboard e onboarding</name>
  <files>src/app/(vault)/inicio/page.tsx, src/app/(auth)/complete-profile/page.tsx</files>
  <action>
Em inicio/page.tsx, fazer as seguintes substituições exatas (todas strings inline no JSX ou template literals):

1. linha ~106 — template literal do contextualMessage:
   DE: `\`Sua próxima sessão começa em ${minutesUntilNext} min\``
   PARA: `\`Seu próximo atendimento começa em ${minutesUntilNext} min\``

2. linha ~155 — empty state da seção "Hoje":
   DE: `"Nenhuma sessão agendada para hoje."`
   PARA: `"Sem atendimentos hoje."`

3. linha ~211 — label do snapshot card "Sessões realizadas":
   DE: `"Sessões realizadas"`
   PARA: `"Atendimentos realizados"`

4. linhas ~218-219 — labels do badge de cobranças pendentes (há singular e plural):
   DE: `"cobrança em aberto"` / `"cobranças em aberto"`
   PARA: `"A receber"` (usar a mesma string para singular e plural — "A receber" funciona para ambos os casos)

Em complete-profile/page.tsx, fazer a seguinte substituição:

5. linha ~39 — h1 do onboarding:
   DE: `Complete seu cadastro.`
   PARA: `Configure seu consultório.`

Manter intactos:
- Eyebrow "Perfil profissional" — correto
- Subcopy "Esses dados identificam você nos documentos clínicos e configuram sua agenda." — correto
- Qualquer outra string não listada acima
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <done>"Sessões" não aparece mais em nenhuma string de UI no dashboard; "cobranças em aberto" substituído por "A receber"; h1 do onboarding é "Configure seu consultório."; testes continuam passando.</done>
</task>

</tasks>

<verification>
Após completar as duas tasks:
1. `pnpm test` — suite completa verde (sem modificações nos arquivos de teste)
2. Inspeção visual: sidebar exibe Início / Agenda / Pacientes / Prontuário / Financeiro / Configurações
3. Inspeção visual: bottom-nav mobile exibe Início / Agenda / Pacientes / Prontuário / Financeiro (sem Config)
4. Inspeção visual: /inicio mostra "Sem atendimentos hoje.", "Atendimentos realizados", "A receber"
5. Inspeção visual: /complete-profile mostra h1 "Configure seu consultório."
</verification>

<success_criteria>
- Nenhum dos strings proibidos aparece nas superfícies de navegação, dashboard ou onboarding: "sessão" (em contexto de UI), "cobranças em aberto", "Complete seu cadastro"
- Sidebar tem exatamente 5 itens principais + Configurações separado: Início, Agenda, Pacientes, Prontuário, Financeiro
- Bottom-nav tem exatamente 5 itens: Início, Agenda, Pacientes, Prontuário, Financeiro
- `pnpm test` passa sem falhas
</success_criteria>

<output>
Após completar este plano, criar `.planning/phases/23-copy-interna/23-01-SUMMARY.md` com o resumo dos artefatos modificados, decisões tomadas e estado das verificações.
</output>
