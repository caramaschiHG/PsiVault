---
phase: 23-copy-interna
plan: "02"
type: execute
wave: 2
depends_on:
  - "23-01"
files_modified:
  - src/app/(vault)/prontuario/page.tsx
  - src/app/(vault)/prontuario/loading.tsx
autonomous: true
requirements:
  - NAV-01

must_haves:
  truths:
    - "Rota /prontuario existe e renderiza lista de pacientes ativos com atividade clínica recente"
    - "Todo texto na rota usa vocabulário clínico correto: 'paciente', 'prontuário', 'atendimento', 'registro'"
    - "Empty state usa 'Nenhum registro clínico ainda.' — nunca 'sessão', 'usuário' ou 'ficha'"
    - "Página segue padrão do projeto: Server Component async, resolveSession(), workspaceId scope"
    - "loading.tsx existe com skeleton-pulse consistente com outras rotas vault"
  artifacts:
    - path: "src/app/(vault)/prontuario/page.tsx"
      provides: "Lista de pacientes ordenada por atividade clínica recente"
    - path: "src/app/(vault)/prontuario/loading.tsx"
      provides: "Skeleton de loading consistente com vault"
  key_links:
    - from: "prontuario/page.tsx"
      to: "PatientRepository.listActive(workspaceId)"
      via: "getPatientRepository() do store"
      pattern: "listActive"
    - from: "prontuario/page.tsx"
      to: "ClinicalRepository.listByPatient(workspaceId, patientId)"
      via: "getClinicalRepository() + Promise.all"
      pattern: "listByPatient"
---

<objective>
Criar a rota /prontuario como entrada clínica direta na navegação — lista de pacientes ativos ordenada por atividade clínica recente (última evolução). Esta rota é conceitualmente diferente de /patients: não é cadastro, é registro. O psicólogo acessa para retomar o fio de um acompanhamento.

Purpose: Prontuário como item de topo na sidebar é o sinal mais forte de que o produto entende a prática psicanalítica. A rota precisa existir para a navegação funcionar.

Output: page.tsx com lista clínica + loading.tsx com skeleton.
</objective>

<execution_context>
@/home/caramaschi/.claude/get-shit-done/workflows/execute-plan.md
@/home/caramaschi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-copy-interna/23-01-SUMMARY.md

<interfaces>
<!-- Contratos extraídos do codebase. Executor usa diretamente. -->

De src/lib/patients/store.ts (padrão verificado em patients/page.tsx):
```typescript
import { getPatientRepository } from "@/lib/patients/store";
// getPatientRepository().listActive(workspaceId) → Promise<Patient[]>
// Patient: { id, fullName, socialName, ... }
```

De src/lib/clinical/store.ts (padrão verificado em patients/[patientId]/page.tsx):
```typescript
import { getClinicalRepository } from "@/lib/clinical/store";
// getClinicalRepository().listByPatient(workspaceId, patientId) → Promise<ClinicalNote[]>
// ClinicalNote: { id, patientId, createdAt, appointmentId, ... }
```

De src/lib/supabase/session.ts:
```typescript
import { resolveSession } from "@/lib/supabase/session";
// const { workspaceId } = await resolveSession();
```

Padrão de loading.tsx (Phase 13):
```tsx
// Server Component puro — sem "use client"
// Usa className="skeleton-pulse" — classe CSS global definida em globals.css
export default function Loading() {
  return (
    <main style={shellStyle}>
      {/* skeleton rows */}
    </main>
  );
}
```

Vocabulário obrigatório (CLAUDE.md):
- USE: paciente, prontuário, atendimento, acompanhamento, registro clínico, evolução
- NUNCA: sessão (em UI), usuário, cliente, ficha, suporte
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar src/app/(vault)/prontuario/page.tsx</name>
  <files>src/app/(vault)/prontuario/page.tsx</files>
  <action>
Criar Server Component async seguindo exatamente o padrão de patients/page.tsx e inicio/page.tsx.

Estrutura da página:
1. Eyebrow: "Registros clínicos" (uppercase, letra menor — padrão eyebrowStyle)
2. H1: "Prontuário" (titleStyle com font-serif)
3. Copy contextual: número de pacientes ativos (ex: "3 pacientes em acompanhamento." / "1 paciente em acompanhamento." / "Nenhum registro clínico ainda.")

Dados:
- Buscar `patients = await patientRepo.listActive(workspaceId)`
- Para cada paciente, buscar notas em paralelo: `const notesPerPatient = await Promise.all(patients.map(p => clinicalRepo.listByPatient(workspaceId, p.id)))`
- Derivar "última evolução" por paciente: `latestNote = notes.sort((a,b) => b.createdAt - a.createdAt)[0]`
- Ordenar pacientes por data da última nota, mais recentes primeiro. Pacientes sem notas ficam ao final.

Lista de pacientes (ul/li):
- Card por paciente mostrando:
  - Nome do paciente (fullName + socialName entre parênteses se existir)
  - "Último registro: {data formatada}" — usar `Intl.DateTimeFormat("pt-BR", { day: "2-digit", month: "long", year: "numeric" })` — se não houver nota: "Sem registros"
  - Link para `/patients/{id}` com texto "Ver prontuário"
- Estilos inline `satisfies React.CSSProperties` — seguir padrão visual dos cards do dashboard (background: var(--color-surface-1), border, borderRadius, boxShadow var(--shadow-sm))

Empty state (quando `patients.length === 0`):
```tsx
<div style={emptyStateStyle}>
  <p style={emptyStateTextStyle}>Nenhum registro clínico ainda.</p>
  <p style={emptyStateTextStyle}>O histórico dos pacientes aparecerá aqui.</p>
</div>
```

Verificar cada string nova:
- "Registros clínicos" ✓
- "Prontuário" ✓
- "paciente em acompanhamento" ✓
- "Último registro" ✓
- "Sem registros" ✓
- "Ver prontuário" ✓
- "Nenhum registro clínico ainda." ✓
- "O histórico dos pacientes aparecerá aqui." ✓
- NUNCA escrever "sessão", "usuário", "cliente", "ficha"

Imports: usar `@/` alias para todos os imports internos.
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <done>Arquivo existe em src/app/(vault)/prontuario/page.tsx; TypeScript compila sem erros; nenhuma string de vocabulário proibido presente; testes passam.</done>
</task>

<task type="auto">
  <name>Task 2: Criar src/app/(vault)/prontuario/loading.tsx</name>
  <files>src/app/(vault)/prontuario/loading.tsx</files>
  <action>
Criar loading skeleton seguindo exatamente o padrão das outras rotas vault (Phase 13).

Regras:
- Server Component puro — sem "use client"
- Replicar shellStyle do page.tsx para evitar layout shift
- Usar className="skeleton-pulse" nos elementos placeholder (classe global já definida em globals.css)
- Estrutura: heading skeleton + 3 card skeletons (simula lista de 3 pacientes)
- Estilos inline `satisfies React.CSSProperties`

Exemplo de estrutura:
```tsx
export default function Loading() {
  return (
    <main style={shellStyle}>
      <div style={headingSkeletonStyle}>
        <div className="skeleton-pulse" style={{ height: "0.7rem", width: "8rem", borderRadius: "var(--radius-sm)" }} />
        <div className="skeleton-pulse" style={{ height: "2rem", width: "14rem", borderRadius: "var(--radius-sm)", marginTop: "0.3rem" }} />
      </div>
      {[0, 1, 2].map((i) => (
        <div key={i} className="skeleton-pulse" style={cardSkeletonStyle} />
      ))}
    </main>
  );
}
```

shellStyle deve ser idêntico ao do page.tsx para evitar layout shift.
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <done>Arquivo existe em src/app/(vault)/prontuario/loading.tsx; Server Component sem "use client"; usa skeleton-pulse; testes passam.</done>
</task>

</tasks>

<verification>
1. `pnpm test` — suite completa verde
2. Visitar /prontuario no browser:
   - Página carrega sem erro 404
   - Lista de pacientes aparece (ou empty state se sem pacientes)
   - Todos os textos usam vocabulário clínico correto
   - Loading state exibe skeleton enquanto dados carregam
3. Clicar "Ver prontuário" de um paciente navega para /patients/{id}
4. Sidebar mostra "Prontuário" como active ao estar em /prontuario
</verification>

<success_criteria>
- /prontuario renderiza sem erro de build ou runtime
- Nenhum string de vocabulário proibido ("sessão", "usuário", "cliente", "ficha") presente nos dois arquivos criados
- Pacientes ordenados por atividade clínica recente (com última nota mais recente primeiro)
- loading.tsx existe e usa skeleton-pulse
- `pnpm test` passa
</success_criteria>

<output>
Após completar este plano, criar `.planning/phases/23-copy-interna/23-02-SUMMARY.md` com o resumo dos artefatos criados, abordagem de dados utilizada e estado das verificações.
</output>
