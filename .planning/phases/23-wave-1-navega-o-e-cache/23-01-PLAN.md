---
phase: 23-wave-1-navega-o-e-cache
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(vault)/components/vault-sidebar-nav.tsx
  - src/app/(vault)/components/bottom-nav.tsx
autonomous: true
requirements:
  - NAV-01

must_haves:
  truths:
    - "Clicar em item de nav não provoca full page reload — navegação é client-side"
    - "active state continua funcionando corretamente via usePathname()"
    - "Link de signout (/api/auth/signout) permanece como <a href> (API route, não nav interna)"
    - "407 testes continuam passando sem regressão"
  artifacts:
    - path: "src/app/(vault)/components/vault-sidebar-nav.tsx"
      provides: "sidebar nav com <Link> para rotas internas"
      contains: "import Link from"
    - path: "src/app/(vault)/components/bottom-nav.tsx"
      provides: "bottom nav com <Link> para rotas internas"
      contains: "import Link from"
  key_links:
    - from: "vault-sidebar-nav.tsx"
      to: "next/link"
      via: "import Link from 'next/link'"
      pattern: "import Link from"
    - from: "bottom-nav.tsx"
      to: "next/link"
      via: "import Link from 'next/link'"
      pattern: "import Link from"
---

<objective>
Substituir `<a href>` por `<Link>` nos componentes de navegação vault para eliminar full page reloads a cada clique de nav.

Purpose: Cada clique na sidebar ou bottom nav atualmente destrói e remonta toda a árvore React (full page reload). `<Link>` do Next.js faz navegação client-side preservando estado e sem flash.

Output: vault-sidebar-nav.tsx e bottom-nav.tsx usando `<Link>` para todos os itens de nav internos. O link `/api/auth/signout` e `/settings/profile` no footer permanecem como `<a href>` — o signout é uma API route (não nav interna), e o link de perfil no footer também deve usar `<Link>`.
</objective>

<execution_context>
@C:/Users/SylvianAliceCaramasc/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/SylvianAliceCaramasc/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrar vault-sidebar-nav.tsx para Link</name>
  <read_first>
    - src/app/(vault)/components/vault-sidebar-nav.tsx (arquivo completo — ler antes de editar)
    - src/app/(vault)/settings/components/settings-nav.tsx (referência de padrão correto com Link)
  </read_first>
  <files>src/app/(vault)/components/vault-sidebar-nav.tsx</files>
  <action>
1. Adicionar `import Link from "next/link"` no topo do arquivo, após os imports existentes de react/next.

2. Substituir TODOS os `<a href={...}>` de nav interna por `<Link href={...}>`, mantendo todas as props existentes (className, aria-current). Isso inclui:
   - O map de NAV_ITEMS (linhas ~103-111): `<a href={item.href} className=...>` → `<Link href={item.href} className=...>`
   - O item SETTINGS_ITEM (linhas ~121-128): `<a href={SETTINGS_ITEM.href} className=...>` → `<Link href={SETTINGS_ITEM.href} className=...>`
   - O link "Meu perfil" no footer (linha ~142): `<a href="/settings/profile" style={settingsLinkStyle}>` → `<Link href="/settings/profile" style={settingsLinkStyle}>`

3. MANTER como `<a href>` sem alteração:
   - `<a href="/api/auth/signout" style={signoutStyle}>Sair</a>` — é uma API route, não nav interna. Não converter para Link.

4. NÃO adicionar prop `prefetch` explícita ao Link — App Router faz prefetch on-viewport por padrão.

5. NÃO alterar nenhuma outra lógica, estilos ou estrutura do componente.
  </action>
  <verify>
    <automated>rtk pnpm tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "import Link from" src/app/\(vault\)/components/vault-sidebar-nav.tsx` retorna linha com `import Link from "next/link"`
    - `grep -n "<Link href" src/app/\(vault\)/components/vault-sidebar-nav.tsx` retorna pelo menos 3 matches (NAV_ITEMS map + SETTINGS_ITEM + Meu perfil)
    - `grep -n 'href="/api/auth/signout"' src/app/\(vault\)/components/vault-sidebar-nav.tsx` ainda existe como `<a href=` (não `<Link href=`)
    - `grep -c "<a href" src/app/\(vault\)/components/vault-sidebar-nav.tsx` retorna 1 (apenas o signout)
    - `pnpm tsc --noEmit` passa sem erros relacionados a vault-sidebar-nav.tsx
  </acceptance_criteria>
  <done>vault-sidebar-nav.tsx usa Link para todos os itens de nav internos; único `<a href>` remanescente é o signout</done>
</task>

<task type="auto">
  <name>Task 2: Migrar bottom-nav.tsx para Link</name>
  <read_first>
    - src/app/(vault)/components/bottom-nav.tsx (arquivo completo — ler antes de editar)
  </read_first>
  <files>src/app/(vault)/components/bottom-nav.tsx</files>
  <action>
1. Adicionar `import Link from "next/link"` no topo do arquivo, após os imports existentes.

2. Substituir o `<a>` dentro do map de NAV_ITEMS (linhas ~89-97) por `<Link>`, mantendo todas as props existentes:

De:
```tsx
<a
  key={item.href}
  href={item.href}
  className={`bottom-nav-item${isActive(item.href) ? " active" : ""}`}
  aria-current={isActive(item.href) ? "page" : undefined}
>
```

Para:
```tsx
<Link
  key={item.href}
  href={item.href}
  className={`bottom-nav-item${isActive(item.href) ? " active" : ""}`}
  aria-current={isActive(item.href) ? "page" : undefined}
>
```

E fechar com `</Link>` em vez de `</a>`.

3. NÃO adicionar prop `prefetch` explícita.

4. NÃO alterar nenhuma outra lógica, estilos ou estrutura do componente. bottom-nav.tsx não tem links de API route — converter todos.
  </action>
  <verify>
    <automated>rtk pnpm tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "import Link from" src/app/\(vault\)/components/bottom-nav.tsx` retorna linha com `import Link from "next/link"`
    - `grep -n "<Link href" src/app/\(vault\)/components/bottom-nav.tsx` retorna pelo menos 1 match
    - `grep -c "<a" src/app/\(vault\)/components/bottom-nav.tsx` retorna 0 (nenhum `<a>` remanescente)
    - `pnpm tsc --noEmit` passa sem erros relacionados a bottom-nav.tsx
  </acceptance_criteria>
  <done>bottom-nav.tsx usa Link para todos os itens; nenhum `<a href>` remanescente</done>
</task>

<task type="auto">
  <name>Task 3: Verificar suite de testes</name>
  <read_first>
    - (nenhum arquivo de código — apenas execução de testes)
  </read_first>
  <files></files>
  <action>
Rodar a suite de testes completa para confirmar que as substituições de `<a>` por `<Link>` não introduziram regressões.

Comando: `pnpm test`

Se algum teste falhar, ler o output de erro com atenção. Falhas esperadas são zero — as mudanças são mecânicas e os testes não deveriam depender do tipo de elemento de âncora renderizado nos componentes de nav.
  </action>
  <verify>
    <automated>rtk pnpm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` termina com "X passed" onde X ≥ 407
    - Zero testes falhando ("0 failed" ou ausência de "failed" na linha de sumário)
  </acceptance_criteria>
  <done>Suite completa passa (≥407 testes) sem regressão</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→Next.js router | Navegação client-side — Link não expõe novos endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01-01 | Spoofing | vault-sidebar-nav signout link | accept | Link de signout permanece como `<a href>` — sem mudança de comportamento; API route valida sessão server-side |
</threat_model>

<verification>
- `grep -rn "import Link from" src/app/\(vault\)/components/vault-sidebar-nav.tsx` → presente
- `grep -rn "import Link from" src/app/\(vault\)/components/bottom-nav.tsx` → presente
- `grep -n 'href="/api/auth/signout"' src/app/\(vault\)/components/vault-sidebar-nav.tsx` → ainda como `<a`, não `<Link`
- `pnpm tsc --noEmit` → sem erros
- `pnpm test` → ≥407 testes passando
</verification>

<success_criteria>
- vault-sidebar-nav.tsx e bottom-nav.tsx importam e usam `Link` de `next/link` para todos os itens de navegação interna
- O único `<a href>` remanescente em vault-sidebar-nav.tsx é o link `/api/auth/signout`
- TypeScript compila sem erros
- ≥407 testes passando
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/23-wave-1-navega-o-e-cache/23-01-SUMMARY.md`
</output>
