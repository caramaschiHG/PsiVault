---
phase: 23-wave-1-navega-o-e-cache
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(vault)/layout.tsx
  - src/app/(vault)/patients/archive/page.tsx
  - src/lib/supabase/server.ts
  - src/lib/supabase/session.ts
autonomous: true
requirements:
  - CACHE-01
  - CACHE-02

must_haves:
  truths:
    - "Páginas vault são servidas com caching padrão do Next.js — force-dynamic removido do layout raiz"
    - "resolveSession() executa chamada Supabase/DB no máximo uma vez por render tree (React.cache)"
    - "createClient() Supabase é instanciado no máximo uma vez por request (React.cache)"
    - "force-dynamic redundante em patients/archive/page.tsx removido"
    - "407 testes continuam passando sem regressão"
  artifacts:
    - path: "src/app/(vault)/layout.tsx"
      provides: "vault layout sem force-dynamic"
      not_contains: "export const dynamic"
    - path: "src/lib/supabase/session.ts"
      provides: "resolveSession envolvida com React.cache()"
      contains: "cache("
    - path: "src/lib/supabase/server.ts"
      provides: "createClient envolvida com React.cache()"
      contains: "cache("
  key_links:
    - from: "src/lib/supabase/session.ts"
      to: "react"
      via: "import { cache } from 'react'"
      pattern: "import.*cache.*from 'react'"
    - from: "src/lib/supabase/server.ts"
      to: "react"
      via: "import { cache } from 'react'"
      pattern: "import.*cache.*from 'react'"
---

<objective>
Remover `export const dynamic = "force-dynamic"` do vault layout raiz e de patients/archive, e envolver `resolveSession()` e `createClient()` com `React.cache()` para deduplicar chamadas Supabase/DB por render tree.

Purpose: `force-dynamic` no layout raiz vault desabilita todo o caching do Next.js para todas as páginas vault. `resolveSession()` sem cache executa getUser() + DB query a cada Server Component que a chame num mesmo request. `createClient()` sem cache pode criar múltiplos clientes Supabase SSR desnecessários.

Output: layout.tsx sem `force-dynamic`, patients/archive/page.tsx sem `force-dynamic` redundante, session.ts e server.ts com `React.cache()`.
</objective>

<execution_context>
@C:/Users/SylvianAliceCaramasc/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/SylvianAliceCaramasc/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remover force-dynamic do vault layout e de patients/archive</name>
  <read_first>
    - src/app/(vault)/layout.tsx (arquivo completo — verificar linha 9 com force-dynamic)
    - src/app/(vault)/patients/archive/page.tsx (verificar linha de force-dynamic)
  </read_first>
  <files>
    src/app/(vault)/layout.tsx,
    src/app/(vault)/patients/archive/page.tsx
  </files>
  <action>
**Em src/app/(vault)/layout.tsx:**

Remover a linha 9 inteira:
```ts
export const dynamic = "force-dynamic";
```
Não substituir por nada — simplesmente deletar a linha. Não alterar nenhuma outra parte do arquivo.

**Em src/app/(vault)/patients/archive/page.tsx:**

Localizar a declaração `export const dynamic = "force-dynamic"` (está em torno da linha 8) e removê-la. Não substituir por nada. Não alterar nenhuma outra parte do arquivo.

**Raciocínio:** `cookies()` chamado dentro de `resolveSession()` (que é chamado por Server Components) já garante dynamic rendering por página automaticamente. O `force-dynamic` no layout era desnecessário e penalizava todas as páginas vault de uma vez. O do patients/archive é redundante pelo mesmo motivo.
  </action>
  <verify>
    <automated>rtk pnpm tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "force-dynamic" src/app/\(vault\)/layout.tsx` retorna vazio (nenhuma linha encontrada)
    - `grep -n "force-dynamic" src/app/\(vault\)/patients/archive/page.tsx` retorna vazio
    - `pnpm tsc --noEmit` passa sem erros
  </acceptance_criteria>
  <done>Ambos os arquivos sem declaração force-dynamic; TypeScript sem erros</done>
</task>

<task type="auto">
  <name>Task 2: Envolver createClient com React.cache()</name>
  <read_first>
    - src/lib/supabase/server.ts (arquivo completo — 29 linhas)
  </read_first>
  <files>src/lib/supabase/server.ts</files>
  <action>
O arquivo atual (29 linhas) exporta `createClient` como função async pura:

```ts
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'

export async function createClient() {
  const cookieStore = await cookies()
  ...
}
```

Fazer as seguintes alterações:

1. Adicionar import de `cache` do react no topo:
```ts
import { cache } from 'react'
```

2. Converter a exportação para usar `cache()`. A função interna continua async. O resultado de `cache()` é exportado como `createClient`:

```ts
export const createClient = cache(async () => {
  const cookieStore = await cookies()

  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() {
          return cookieStore.getAll()
        },
        setAll(cookiesToSet) {
          try {
            cookiesToSet.forEach(({ name, value, options }) => {
              cookieStore.set(name, value, options)
            })
          } catch (error) {
            // The `setAll` method was called from a Server Component.
            // This can be ignored if you have middleware refreshing
            // user sessions.
          }
        },
      },
    }
  )
})
```

O tipo de retorno (Promise<SupabaseClient>) é inferido automaticamente — não precisa de anotação explícita.

**Importante:** `React.cache()` deduplica chamadas dentro de um único render tree por request. Como `cookies()` é chamado dentro, o Next.js ainda trata a página como dinâmica. Não há quebra de comportamento.
  </action>
  <verify>
    <automated>rtk pnpm tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "import.*cache.*from 'react'" src/lib/supabase/server.ts` retorna match
    - `grep -n "export const createClient = cache(" src/lib/supabase/server.ts` retorna match
    - `grep -n "export async function createClient" src/lib/supabase/server.ts` retorna vazio (forma antiga removida)
    - `pnpm tsc --noEmit` passa sem erros relacionados a server.ts
  </acceptance_criteria>
  <done>createClient exportada como cache(async () => {...}); TypeScript sem erros</done>
</task>

<task type="auto">
  <name>Task 3: Envolver resolveSession com React.cache()</name>
  <read_first>
    - src/lib/supabase/session.ts (arquivo completo — 90 linhas)
  </read_first>
  <files>src/lib/supabase/session.ts</files>
  <action>
O arquivo atual (90 linhas) exporta `resolveSession` como função async pura (linha 15):

```ts
export async function resolveSession(): Promise<ResolvedSession> {
```

Fazer as seguintes alterações:

1. Adicionar import de `cache` do react nos imports do topo do arquivo. O bloco de imports atual começa com:
```ts
import { createClient } from "./server";
import { db } from "@/lib/db";
import { logServerRenderError, logServerRenderInfo, observeServerStage } from "@/lib/observability/server-render";
import { redirect } from "next/navigation";
```

Adicionar após esses imports:
```ts
import { cache } from 'react'
```

2. Converter a declaração da função para usar `cache()`. Trocar:

```ts
export async function resolveSession(): Promise<ResolvedSession> {
```

Por:

```ts
export const resolveSession = cache(async (): Promise<ResolvedSession> => {
```

3. Fechar o wrapper `cache()` no final do arquivo. A função atualmente termina na linha 89 com `}` (fecha o try/catch) e linha 90 com `}` (fecha a função). Alterar para:

```ts
    throw error;
  }
})
```

Ou seja, o `}` final que fechava a função `resolveSession` vira `})` para fechar o `cache(async () => {`.

**Atenção:** A função tem um bloco try/catch e múltiplos níveis de aninhamento. Certificar que apenas o `}` mais externo (que fecha a função) seja alterado para `})`. Os `}` internos (try, catch, if blocks) permanecem inalterados.

**O comportamento de `redirect()` com React.cache():** `redirect()` lança uma exceção especial do Next.js que não é cacheada — cada chamada ao resolveSession que termina em redirect() vai executar normalmente. Apenas o caminho de sucesso é cacheado.
  </action>
  <verify>
    <automated>rtk pnpm tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "import.*cache.*from 'react'" src/lib/supabase/session.ts` retorna match
    - `grep -n "export const resolveSession = cache(" src/lib/supabase/session.ts` retorna match
    - `grep -n "export async function resolveSession" src/lib/supabase/session.ts` retorna vazio (forma antiga removida)
    - `pnpm tsc --noEmit` passa sem erros relacionados a session.ts
  </acceptance_criteria>
  <done>resolveSession exportada como cache(async () => {...}); TypeScript sem erros</done>
</task>

<task type="auto">
  <name>Task 4: Verificar suite de testes</name>
  <read_first>
    - (nenhum arquivo de código — apenas execução de testes)
  </read_first>
  <files></files>
  <action>
Rodar a suite de testes completa para confirmar que a remoção de force-dynamic e os wraps com React.cache() não introduziram regressões.

Comando: `pnpm test`

Se algum teste falhar relacionado a session.ts ou server.ts, verificar se o mock de `React.cache` nos testes precisa ser atualizado. Em ambiente de teste (Vitest com node env), `cache` do React se comporta como identity function — os testes não devem precisar de ajuste, mas se falharem, inspecionar o output.
  </action>
  <verify>
    <automated>rtk pnpm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` termina com "X passed" onde X ≥ 407
    - Zero testes falhando ("0 failed" ou ausência de "failed" na linha de sumário)
  </acceptance_criteria>
  <done>Suite completa passa (≥407 testes) sem regressão</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server component→resolveSession | Chamadas múltiplas por render tree deduplicadas via cache |
| Next.js cache→Supabase | cache() não persiste entre requests — escopo por render tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02-01 | Information Disclosure | React.cache() em resolveSession | accept | React.cache() tem escopo por request/render tree — não vaza dados entre usuários; Supabase session ainda validada em cada request |
| T-23-02-02 | Information Disclosure | Remoção de force-dynamic | accept | cookies() em resolveSession já garante dynamic rendering por página; nenhuma página serve dados estáticos indevidamente |
</threat_model>

<verification>
- `grep -n "force-dynamic" src/app/\(vault\)/layout.tsx` → vazio
- `grep -n "force-dynamic" src/app/\(vault\)/patients/archive/page.tsx` → vazio
- `grep -n "cache(" src/lib/supabase/session.ts` → presente
- `grep -n "cache(" src/lib/supabase/server.ts` → presente
- `grep -n "import.*cache.*from 'react'" src/lib/supabase/session.ts` → presente
- `grep -n "import.*cache.*from 'react'" src/lib/supabase/server.ts` → presente
- `pnpm tsc --noEmit` → sem erros
- `pnpm test` → ≥407 testes passando
</verification>

<success_criteria>
- vault layout.tsx sem `export const dynamic = "force-dynamic"`
- patients/archive/page.tsx sem `export const dynamic = "force-dynamic"`
- session.ts: `resolveSession` exportada via `cache(async () => {...})`
- server.ts: `createClient` exportada via `cache(async () => {...})`
- TypeScript compila sem erros
- ≥407 testes passando
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/23-wave-1-navega-o-e-cache/23-02-SUMMARY.md`
</output>
