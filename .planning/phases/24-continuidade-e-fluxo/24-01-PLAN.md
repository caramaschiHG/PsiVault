---
phase: 24-continuidade-e-fluxo
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/prontuario-timeline.test.ts
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-04

must_haves:
  truths:
    - "O arquivo de testes existe e todos os casos falham antes da implementação (RED)"
    - "Testes cobrem ordenação cronológica da timeline (CONT-01)"
    - "Testes cobrem lookup note por appointmentId (CONT-02)"
    - "Testes cobrem empty state sem atendimentos (CONT-04)"
    - "Testes cobrem badge 'Novo acompanhamento' quando latestNote é null (CONT-04)"
  artifacts:
    - path: "tests/prontuario-timeline.test.ts"
      provides: "Contratos de teste para lógica de montagem da timeline"
      exports: []
  key_links:
    - from: "tests/prontuario-timeline.test.ts"
      to: "src/app/(vault)/prontuario/[patientId]/page.tsx"
      via: "funções puras extraídas ou lógica inline testável"
      pattern: "buildTimeline|sortAppointments|notesByAppointment"
---

<objective>
Criar o scaffold de testes (Wave 0) para a lógica de montagem da timeline cronológica do prontuário.

Purpose: Estabelecer contratos testáveis antes da implementação — os testes devem falhar (RED) e servir de guia para o executor do Plan 02.
Output: `tests/prontuario-timeline.test.ts` com fixtures e casos de teste cobrindo CONT-01, CONT-02 e CONT-04.
</objective>

<execution_context>
@/home/caramaschi/.claude/get-shit-done/workflows/execute-plan.md
@/home/caramaschi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-continuidade-e-fluxo/24-CONTEXT.md
@.planning/phases/24-continuidade-e-fluxo/24-RESEARCH.md
@.planning/phases/24-continuidade-e-fluxo/24-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar scaffold de testes prontuario-timeline</name>
  <files>tests/prontuario-timeline.test.ts</files>
  <action>
Criar `tests/prontuario-timeline.test.ts` com fixtures e casos de teste que descrevem a lógica de montagem da timeline cronológica. O arquivo de testes deve falhar completamente antes da implementação (RED state).

A lógica testável a ser extraída da page.tsx em funções puras (que o executor do Plan 02 irá criar) é:

1. `buildTimeline(appointments, now)` — retorna `{ upcoming, completedVisible, completedHidden, dismissedAll }`:
   - `upcoming`: único atendimento com status SCHEDULED ou CONFIRMED e `startsAt > now`, mais próximo primeiro; `null` se não houver
   - `completedVisible`: até 5 COMPLETED mais recentes (startsAt decrescente)
   - `completedHidden`: COMPLETED além dos 5 primeiros
   - `dismissedAll`: CANCELED + NO_SHOW em ordem cronológica decrescente

2. `buildNotesByAppointment(notes)` — retorna `Map<string, ClinicalNote>`:
   - Mapeia `note.appointmentId → note`
   - Lookup O(1) por appointmentId

3. `hasNotes(latestNote)` — retorna boolean:
   - `true` quando `latestNote !== null`
   - `false` quando `latestNote === null` (aciona badge "Novo acompanhamento")

Fixtures a criar:
```typescript
const makeAppt = (overrides: Partial<Appointment>): Appointment => ({ ... })
const makeNote = (overrides: Partial<ClinicalNote>): ClinicalNote => ({ ... })
```

Cenários obrigatórios:
- CONT-01: upcoming aparece como primeiro item mesmo sendo futuro
- CONT-01: completed ordenados por startsAt decrescente (mais recente primeiro)
- CONT-01: 5+ completed: só 5 visíveis, resto em hidden
- CONT-01: dismissed separados de completed, não misturados
- CONT-02: buildNotesByAppointment retorna nota correta para appointmentId existente
- CONT-02: buildNotesByAppointment retorna undefined para appointmentId sem nota
- CONT-04: sem atendimentos: upcoming=null, completedVisible=[], completedHidden=[], dismissedAll=[]
- CONT-04: hasNotes retorna false quando latestNote === null
- CONT-04: hasNotes retorna true quando latestNote existe

Usar tipos existentes do projeto: `import type { Appointment } from "@/lib/appointments/model"` e `import type { ClinicalNote } from "@/lib/clinical/model"`. As funções testadas serão importadas de `@/app/(vault)/prontuario/[patientId]/timeline"` — esse módulo não existe ainda, então os imports vão falhar e os testes ficarão em RED.

Formato de test: Vitest com `describe/it/expect` e globals ativados (`vitest.config.ts` já configura `globals: true`).
  </action>
  <verify>
    <automated>pnpm test tests/prontuario-timeline.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Arquivo existe. Testes falham por import não resolvido (RED state confirmado). Nenhum teste passa — isso é esperado e correto.</done>
</task>

</tasks>

<verification>
- `tests/prontuario-timeline.test.ts` existe
- `pnpm test tests/prontuario-timeline.test.ts` executa sem erro de sintaxe TypeScript (somente falha de import/resolução)
- Testes cobrem todos os cenários: CONT-01, CONT-02, CONT-04
</verification>

<success_criteria>
Scaffold de testes em RED state — falha por módulo de implementação ausente, não por erro de configuração ou sintaxe. O executor do Plan 02 usará este arquivo como contrato de implementação.
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/24-continuidade-e-fluxo/24-01-SUMMARY.md`
</output>
