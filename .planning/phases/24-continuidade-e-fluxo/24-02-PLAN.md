---
phase: 24-continuidade-e-fluxo
plan: 02
type: execute
wave: 1
depends_on:
  - 24-01
files_modified:
  - src/app/(vault)/prontuario/[patientId]/page.tsx
  - src/app/(vault)/prontuario/[patientId]/loading.tsx
  - src/app/(vault)/prontuario/[patientId]/timeline.ts
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04

must_haves:
  truths:
    - "Psicólogo navega para /prontuario/[id] e vê o nome do paciente em H1 serif"
    - "Timeline exibe upcoming como primeiro item marcado 'Próxima'"
    - "Completed aparece em ordem cronológica decrescente (mais recente primeiro)"
    - "Dismissed recolhidos por padrão com contagem discreta e opção de expandir"
    - "Cada entrada da timeline linka para /sessions/[appointmentId] se tiver nota clínica"
    - "Seção 'Documentos clínicos' sempre visível — texto discreto se vazia"
    - "Breadcrumb 'Prontuário → [Nome]' com link funcional para /prontuario"
    - "Link 'Dados cadastrais' aponta para /patients/[patientId]"
    - "Empty state com CTA quando não há atendimentos"
    - "pnpm test tests/prontuario-timeline.test.ts passa (GREEN)"
  artifacts:
    - path: "src/app/(vault)/prontuario/[patientId]/timeline.ts"
      provides: "Funções puras: buildTimeline, buildNotesByAppointment, hasNotes"
      exports: ["buildTimeline", "buildNotesByAppointment", "hasNotes"]
    - path: "src/app/(vault)/prontuario/[patientId]/page.tsx"
      provides: "View clínica focada — Server Component puro"
      exports: ["default (page component)"]
    - path: "src/app/(vault)/prontuario/[patientId]/loading.tsx"
      provides: "Skeleton da view clínica"
      exports: ["default (loading component)"]
  key_links:
    - from: "src/app/(vault)/prontuario/[patientId]/page.tsx"
      to: "src/app/(vault)/prontuario/[patientId]/timeline.ts"
      via: "import { buildTimeline, buildNotesByAppointment }"
      pattern: "buildTimeline|buildNotesByAppointment"
    - from: "src/app/(vault)/prontuario/[patientId]/page.tsx"
      to: "Promise.all de 4 repositórios"
      via: "getPatientRepository, getAppointmentRepository, getClinicalNoteRepository, getDocumentRepository"
      pattern: "Promise\\.all"
    - from: "src/app/(vault)/prontuario/[patientId]/page.tsx"
      to: "/sessions/[appointmentId]"
      via: "Link href na entrada da timeline quando nota existe"
      pattern: "href.*sessions"
---

<objective>
Criar a rota `/prontuario/[patientId]` como view clínica focada — separada da página operacional `/patients/[id]`.

Purpose: Comunicar acompanhamento longitudinal — o psicólogo chega aqui para retomar o trabalho analítico, não para editar cadastro ou gerenciar finanças.
Output: `timeline.ts` com funções puras (testáveis), `page.tsx` como Server Component puro, `loading.tsx` com skeleton.
</objective>

<execution_context>
@/home/caramaschi/.claude/get-shit-done/workflows/execute-plan.md
@/home/caramaschi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-continuidade-e-fluxo/24-CONTEXT.md
@.planning/phases/24-continuidade-e-fluxo/24-RESEARCH.md
@.planning/phases/24-continuidade-e-fluxo/24-01-SUMMARY.md

<interfaces>
<!-- Contratos extraídos do codebase. O executor deve usar diretamente — sem exploração adicional. -->

De src/lib/patients/store.ts:
```typescript
export function getPatientRepository(): PatientRepository;
// PatientRepository.findById(id: string, workspaceId: string): Promise<Patient | null>
```

De src/lib/appointments/store.ts:
```typescript
export function getAppointmentRepository(): AppointmentRepository;
// AppointmentRepository.listByPatient(patientId: string, workspaceId: string): Promise<Appointment[]>
```

De src/lib/clinical/store.ts:
```typescript
export function getClinicalNoteRepository(): ClinicalNoteRepository;
// ClinicalNoteRepository.listByPatient(patientId: string, workspaceId: string): Promise<ClinicalNote[]>
```

De src/lib/documents/store.ts:
```typescript
export function getDocumentRepository(): DocumentRepository;
// DocumentRepository.listActiveByPatient(patientId: string, workspaceId: string): Promise<PracticeDocument[]>
```

De src/lib/supabase/session.ts:
```typescript
export async function resolveSession(): Promise<{ accountId: string; workspaceId: string }>;
```

De src/app/(vault)/components/empty-state.tsx:
```typescript
interface EmptyStateProps {
  icon: React.ReactNode;
  title: string;
  description?: string;
  actionLabel?: string;
  actionHref?: string;
}
export function EmptyState(props: EmptyStateProps): JSX.Element;
```

Appointment model (src/lib/appointments/model.ts):
```typescript
// status: "SCHEDULED" | "CONFIRMED" | "COMPLETED" | "CANCELED" | "NO_SHOW"
// startsAt: Date
// id: string
// patientId: string
// workspaceId: string
// serviceMode: "IN_PERSON" | "ONLINE"
```

ClinicalNote model (src/lib/clinical/model.ts):
```typescript
// id: string
// appointmentId: string
// patientId: string
// createdAt: Date
// editedAt: Date | null
```

PracticeDocument model (src/lib/documents/model.ts):
```typescript
// id: string
// patientId: string
// type: string  (sem appointmentId — documentos listados separadamente)
// createdAt: Date
// title: string
```

globals.css tokens disponíveis:
- --font-serif: fonte serif editorial (Phase 21)
- --font-size-page-title: tamanho do H1
- --color-text-1, --color-text-2, --color-text-3
- --color-surface-1, --color-border
- --color-accent, --color-accent-light
- --radius-lg, --radius-xl
- --shadow-sm, --shadow-md
- skeleton-pulse: classe CSS para loading (keyframe em globals.css)
- --color-brown-mid: eyebrow color
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar timeline.ts com funções puras (GREEN os testes do Plan 01)</name>
  <files>src/app/(vault)/prontuario/[patientId]/timeline.ts</files>
  <behavior>
    - buildTimeline(appointments, now): upcoming é único, null se não houver SCHEDULED/CONFIRMED futuro
    - buildTimeline: completedVisible são os 5 COMPLETED mais recentes (startsAt desc)
    - buildTimeline: completedHidden são COMPLETED além dos 5 primeiros
    - buildTimeline: dismissedAll são CANCELED + NO_SHOW em startsAt desc
    - buildNotesByAppointment(notes): Map appointmentId → note
    - buildNotesByAppointment: retorna undefined para appointmentId sem nota
    - hasNotes(latestNote): false quando null, true quando ClinicalNote
    - Com lista vazia: upcoming=null, completedVisible=[], completedHidden=[], dismissedAll=[]
  </behavior>
  <action>
Criar `src/app/(vault)/prontuario/[patientId]/timeline.ts` com as funções puras que fazem os testes de `tests/prontuario-timeline.test.ts` passarem (GREEN).

```typescript
import type { Appointment } from "@/lib/appointments/model";
import type { ClinicalNote } from "@/lib/clinical/model";

export interface TimelineData {
  upcoming: Appointment | null;
  completedVisible: Appointment[];
  completedHidden: Appointment[];
  dismissedAll: Appointment[];
}

export function buildTimeline(appointments: Appointment[], now: Date): TimelineData {
  const upcoming = appointments
    .filter(a =>
      (a.status === "SCHEDULED" || a.status === "CONFIRMED") &&
      a.startsAt > now
    )
    .sort((a, b) => a.startsAt.getTime() - b.startsAt.getTime())
    .at(0) ?? null;

  const completedSorted = appointments
    .filter(a => a.status === "COMPLETED")
    .sort((a, b) => b.startsAt.getTime() - a.startsAt.getTime());

  const dismissedAll = appointments
    .filter(a => a.status === "CANCELED" || a.status === "NO_SHOW")
    .sort((a, b) => b.startsAt.getTime() - a.startsAt.getTime());

  return {
    upcoming,
    completedVisible: completedSorted.slice(0, 5),
    completedHidden: completedSorted.slice(5),
    dismissedAll,
  };
}

export function buildNotesByAppointment(notes: ClinicalNote[]): Map<string, ClinicalNote> {
  return new Map(notes.map(n => [n.appointmentId, n]));
}

export function hasNotes(latestNote: ClinicalNote | null): boolean {
  return latestNote !== null;
}
```

Após criar o arquivo, rodar os testes — devem passar (GREEN).
  </action>
  <verify>
    <automated>cd /home/caramaschi/Área\ de\ trabalho/Projetos/PsiLock && pnpm test tests/prontuario-timeline.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Todos os testes de prontuario-timeline.test.ts passam (GREEN). TypeScript sem erros de tipo.</done>
</task>

<task type="auto">
  <name>Task 2: Criar page.tsx e loading.tsx da view clínica /prontuario/[patientId]</name>
  <files>
    src/app/(vault)/prontuario/[patientId]/page.tsx
    src/app/(vault)/prontuario/[patientId]/loading.tsx
  </files>
  <action>
Criar `src/app/(vault)/prontuario/[patientId]/page.tsx` como Server Component puro (sem `use client`).

**Estrutura da página:**

```
<main>
  [breadcrumb] "Prontuário → [Nome]" com link para /prontuario
  <h1> nome do paciente — serif, editorial
  <p> nome social se existir, em tom discreto
  <a> "Dados cadastrais" → /patients/[patientId]   (link discreto)

  <section> "Histórico de atendimentos"
  <hr>
    [se upcoming]
      entrada da timeline com tag "Próxima", data formatada, modo de atendimento
    [se completedVisible vazio e upcoming null]
      EmptyState com CTA para agendar
    [completed visíveis]
      entradas da timeline: data + "Atendimento N" + modo + link para nota se existir
    [se completedHidden.length > 0]
      <details><summary>Ver mais ({n} atendimentos)</summary>
        entradas ocultas
      </details>
    [se dismissedAll.length > 0]
      <details><summary>{n} atendimento(s) dispensado(s)</summary>
        entradas: só data + status (sem número de sessão)
      </details>

  <section> "Documentos clínicos"
  <hr>
    [se documents vazio]
      <p> "Nenhum documento gerado neste acompanhamento."
    [senão]
      lista de documentos: tipo em pt-BR + data + link para visualizar
</main>
```

**Detalhes de implementação:**

1. `resolveSession()` para workspaceId
2. `Promise.all([patientRepo.findById, appointmentRepo.listByPatient, clinicalRepo.listByPatient, documentRepo.listActiveByPatient])`
3. `if (!patient) notFound()` — importar de `next/navigation`
4. `buildTimeline(appointments, new Date())` e `buildNotesByAppointment(notes)` de `./timeline`
5. Sessão número: usar `buildTimeline` para contar — `completedVisible.indexOf(appt) + 1` dentro da lista visível. Para completedHidden: índice contínuo. Não aplicar número a dismissed.
6. Modo de atendimento: `serviceMode === "ONLINE" ? "Online" : "Presencial"`
7. Documentos ordenados por `createdAt` decrescente. Mapeamento de tipo:
   ```typescript
   const DOC_LABELS: Record<string, string> = {
     session_note: "Nota de sessão",
     session_record: "Registro de sessão",
     receipt: "Recibo",
     declaration: "Declaração",
   };
   ```
8. Datas formatadas com `Intl.DateTimeFormat("pt-BR", { day: "2-digit", month: "long", year: "numeric", timeZone: "UTC" })`
9. Link para nota clínica: `href={/sessions/${appt.id}/note}` — somente se `notesByAppointment.get(appt.id)` existir
10. EmptyState quando `!upcoming && completedVisible.length === 0`: usar componente de `@/app/(vault)/components/empty-state` com `actionHref={/agenda/new?patientId=${patientId}}`

**Regras visuais (Phase 21 + CLAUDE.md anti-padrões):**
- H1: `fontFamily: "var(--font-serif)"`, `fontSize: "var(--font-size-page-title)"`, `fontWeight: 700`
- Breadcrumb: markup estático, `fontSize: "0.8125rem"`, `color: "var(--color-text-3)"`, Link em `--color-accent`
- Seções: h2 + `<hr>` separador — sem cards individuais, page flow linear
- Nenhum gradiente, nenhum badge colorido, sombras apenas `--shadow-sm` se necessário
- Tag "Próxima": texto pequeno discreto `color: var(--color-brown-mid)`, sem background colorido
- `satisfies React.CSSProperties` em todos os objetos de estilo

**loading.tsx:**
Criar `src/app/(vault)/prontuario/[patientId]/loading.tsx` como Server Component puro (sem `use client`). Replicar shell do page.tsx com blocos `className="skeleton-pulse"` para breadcrumb, H1, e 3-4 entradas de timeline simuladas. Copiar o padrão de `src/app/(vault)/prontuario/loading.tsx` adaptando para a estrutura da view clínica.
  </action>
  <verify>
    <automated>cd /home/caramaschi/Área\ de\ trabalho/Projetos/PsiLock && pnpm build 2>&1 | grep -E "error|Error|✓ Compiled|Route" | tail -20</automated>
  </verify>
  <done>
- `pnpm build` sem erros TypeScript
- Rota `/prontuario/[patientId]` aparece na saída do build
- `loading.tsx` sem `use client`
- `pnpm test` full suite verde
  </done>
</task>

</tasks>

<verification>
- `pnpm test tests/prontuario-timeline.test.ts` — todos passam (GREEN)
- `pnpm test` — suite completa verde
- `pnpm build` — sem erros, rota `/prontuario/[patientId]` compilada
- Nenhum `use client` em page.tsx ou loading.tsx
- Arquivo `timeline.ts` exporta `buildTimeline`, `buildNotesByAppointment`, `hasNotes`
</verification>

<success_criteria>
View clínica `/prontuario/[patientId]` existe, compila, e serve a timeline cronológica com hierarquia visual clara (breadcrumb, H1 serif, seções com h2+hr, flow linear sem cards soltos). Empty states e linguagem de acompanhamento corretos. Testes verdes.
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/24-continuidade-e-fluxo/24-02-SUMMARY.md`
</output>
