---
phase: 24-continuidade-e-fluxo
plan: 03
type: execute
wave: 1
depends_on:
  - 24-01
files_modified:
  - src/app/(vault)/prontuario/page.tsx
autonomous: true
requirements:
  - CONT-01
  - CONT-04

must_haves:
  truths:
    - "Link 'Ver prontuário' nos cards aponta para /prontuario/[patientId] (não mais /patients/[id])"
    - "Card sem notas clínicas exibe tag discreta 'Novo acompanhamento'"
    - "Tag usa estilo discreto: texto pequeno, itálico, cor text-3 — sem background colorido"
    - "Pacientes com notas não exibem a tag"
    - "pnpm test passa — sem regressão na suite completa"
  artifacts:
    - path: "src/app/(vault)/prontuario/page.tsx"
      provides: "Lista de prontuários com link correto e badge 'Novo acompanhamento'"
      exports: ["default (page component)"]
  key_links:
    - from: "src/app/(vault)/prontuario/page.tsx"
      to: "/prontuario/[patientId]"
      via: "Link href no card"
      pattern: "href.*prontuario.*patient\\.id"
---

<objective>
Ajustar `/prontuario/page.tsx` para conectar a lista existente à nova view clínica e sinalizar novos acompanhamentos.

Purpose: Fechar o loop de navegação — o link "Ver prontuário" deve levar à view clínica, não à página operacional. Badge "Novo acompanhamento" orienta o psicólogo sobre pacientes sem histórico.
Output: `prontuario/page.tsx` editado com 2 mudanças cirúrgicas: `href` e badge condicional.
</objective>

<execution_context>
@/home/caramaschi/.claude/get-shit-done/workflows/execute-plan.md
@/home/caramaschi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-continuidade-e-fluxo/24-CONTEXT.md
@.planning/phases/24-continuidade-e-fluxo/24-RESEARCH.md

<interfaces>
<!-- Estado atual de prontuario/page.tsx (lido diretamente). -->

Linha 76: `<Link href={`/patients/${patient.id}`} style={linkStyle}>`
  → mudar para: `<Link href={`/prontuario/${patient.id}`} style={linkStyle}>`

Linha 72-74: bloco de "Último registro"
  → adicionar badge condicional quando `latestNote === null`:
  ```typescript
  {!latestNote && (
    <span style={newBadgeStyle}>Novo acompanhamento</span>
  )}
  ```

Estilo do badge (adicionar ao arquivo):
```typescript
const newBadgeStyle = {
  fontSize: "0.75rem",
  color: "var(--color-text-3)",
  fontStyle: "italic",
} satisfies React.CSSProperties;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajustar link e adicionar badge na lista de prontuários</name>
  <files>src/app/(vault)/prontuario/page.tsx</files>
  <action>
Fazer 2 edições cirúrgicas em `src/app/(vault)/prontuario/page.tsx`:

**Edição 1 — Corrigir link "Ver prontuário":**
Na linha que contém `href={/patients/${patient.id}}`, mudar o href para `/prontuario/${patient.id}`.

Antes:
```typescript
<Link href={`/patients/${patient.id}`} style={linkStyle}>
  Ver prontuário
</Link>
```

Depois:
```typescript
<Link href={`/prontuario/${patient.id}`} style={linkStyle}>
  Ver prontuário
</Link>
```

**Edição 2 — Badge "Novo acompanhamento" condicional:**
Dentro do `<li>` de cada card, no bloco de `cardContentStyle`, após o `<p>` de "Último registro", adicionar:

```typescript
{!latestNote && (
  <span style={newBadgeStyle}>Novo acompanhamento</span>
)}
```

**Adicionar estilo** no final do arquivo (junto aos outros objetos de estilo):
```typescript
const newBadgeStyle = {
  fontSize: "0.75rem",
  color: "var(--color-text-3)",
  fontStyle: "italic",
} satisfies React.CSSProperties;
```

**Regras:**
- Não alterar nenhuma outra linha — edições cirúrgicas apenas
- Não modificar a lógica de ordenação por atividade
- Não alterar estilos existentes
- Badge não usa background colorido, não usa border, não é um chip — é texto simples e discreto (anti-padrão visual do CLAUDE.md)
  </action>
  <verify>
    <automated>cd /home/caramaschi/Área\ de\ trabalho/Projetos/PsiLock && pnpm build 2>&1 | grep -E "error|Error|✓ Compiled" | tail -10</automated>
  </verify>
  <done>
- `pnpm build` sem erros
- Link "Ver prontuário" aponta para `/prontuario/[id]` (verificar no código: `grep "prontuario" src/app/\(vault\)/prontuario/page.tsx`)
- Badge "Novo acompanhamento" presente no código com `newBadgeStyle` discreto
- `pnpm test` suite completa sem regressão
  </done>
</task>

</tasks>

<verification>
- `grep "prontuario.*patient.id" src/app/\(vault\)/prontuario/page.tsx` — confirma href correto
- `grep "Novo acompanhamento" src/app/\(vault\)/prontuario/page.tsx` — confirma badge
- `pnpm build` sem erros
- `pnpm test` verde
</verification>

<success_criteria>
A lista `/prontuario` conecta corretamente à view clínica. Pacientes sem nota clínica exibem "Novo acompanhamento" de forma discreta e não intrusiva.
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/24-continuidade-e-fluxo/24-03-SUMMARY.md`
</output>
