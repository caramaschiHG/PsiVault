# Plan 25-03 — actions.ts: revalidatePath type fix

## Goal

Add `"page"` as second argument to all 13 `revalidatePath("/financeiro")` calls in `actions.ts`.
This prevents the vault layout from being unnecessarily revalidated on every finance mutation.

## Tasks

### T1 — Replace all occurrences
Replace `revalidatePath("/financeiro")` → `revalidatePath("/financeiro", "page")` (13 occurrences).

## Verification

- TypeScript compiles
- 407 tests pass
