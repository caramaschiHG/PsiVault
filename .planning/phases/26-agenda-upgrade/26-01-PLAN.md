---
phase: 26-agenda-upgrade
plan: 01
type: execute
wave: 1
requirements:
  - AGENDA-01
must_haves:
  truths:
    - "deriveMonthAgenda() retorna { year, month, days: MonthDaySlot[] } com 35 ou 42 dias"
    - "AgendaMonthView é grid 7×5-6 com barras de status coloridas"
    - "Click em barra abre side panel; click no dia navega para day view"
    - "AgendaToolbar tem terceira tab 'Mês'"
  artifacts:
    - path: "src/lib/appointments/agenda.ts"
      provides: "deriveMonthAgenda() + tipos MonthDaySlot, MonthAgendaResult"
    - path: "src/app/(vault)/agenda/components/agenda-month-view.tsx"
      provides: "Componente Month View completo"
    - path: "src/app/(vault)/agenda/components/agenda-toolbar.tsx"
      provides: "Tab Mês adicionada"
---

<objective>
Implementar Month View na agenda — grid mensal com barras compactas de status por dia.

Purpose: Psicólogo pode ver o mês inteiro de sessões de relance, identificar padrões de frequência, gaps e densidade de atendimentos.
Output: `agenda-month-view.tsx` + `deriveMonthAgenda()` + tab "Mês" no toolbar
</objective>

<tasks>

<task type="auto">
  <name>Task 1: deriveMonthAgenda() + tipos em agenda.ts</name>
  <files>src/lib/appointments/agenda.ts</files>
  <action>
**Adicionar em `src/lib/appointments/agenda.ts`:**

```ts
export interface MonthDaySlot {
  date: Date;
  cards: AgendaCard[];
  isCurrentMonth: boolean;
}

export interface MonthAgendaResult {
  year: number;
  month: number; // 1-based
  days: MonthDaySlot[]; // 35 ou 42 days (5-6 weeks)
}
```

Implementar `deriveMonthAgenda()`:
- Input: `appointments: Appointment[]`, `anchorDate: Date`, `timezone: string`
- Calcular primeiro dia da primeira semana do mês (segunda-feira)
- Gerar 35 ou 42 dias (sempre múltiplo de 7)
- Para cada dia, filtrar appointments que caem naquele dia
- Converter para `AgendaCard[]` via `deriveAgendaCard()` existente
- Marcar `isCurrentMonth` para dias dentro do mês vs dias de meses adjacentes
- Função pura, sem I/O, testável isoladamente

Regras:
- Semana começa na segunda-feira (ISO 8601)
- Se o último dia cair numa segunda ou terça, incluir +7 dias para completar 6 semanas visuais
- Usar `deriveAgendaCard()` existente para converter appointments → cards
</action>
  <verify>
    <automated>cd "/home/caramaschi/Área de trabalho/Projetos/PsiLock" && pnpm tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>deriveMonthAgenda() compilando sem erros, retorna estrutura com 35-42 dias, days agrupados por semana, isCurrentMonth correto</done>
</task>

<task type="auto">
  <name>Task 2: AgendaMonthView component</name>
  <files>src/app/(vault)/agenda/components/agenda-month-view.tsx</files>
  <action>
**Criar `src/app/(vault)/agenda/components/agenda-month-view.tsx`:**

Props:
```ts
interface AgendaMonthViewProps {
  month: MonthAgendaResult;
  patientNames: Record<string, string>;
  onDayClick: (date: Date) => void;
  onAppointmentClick: (appointmentId: string) => void;
}
```

Renderização:
- Grid CSS: `grid-template-columns: repeat(7, 1fr)`
- Header row: Seg, Ter, Qua, Qui, Sex, Sáb, Dom (labels em pt-BR)
- Cada célula (day cell):
  - Número do dia no topo (opaco para current month, muted para adjacent)
  - Até 3 barras compactas por appointment
  - "+N mais" se mais que 3
- Barra compacta por appointment:
  - `height: 4px`, `border-radius: 2px`
  - `background`: cor do status (usar STATUS_COLORS existente)
  - `title`: nome do paciente truncado (tooltip nativo)
  - `onClick`: `onAppointmentClick(appointmentId)`
- Click no número do dia → `onDayClick(date)`
- Dias do mês adjacente: opacity 0.3
- Hover em cell: background sutil

Cores de status (reusar de appointment-block.tsx):
- SCHEDULED: `--color-brown-mid`
- CONFIRMED: verde sage
- COMPLETED: charcoal
- CANCELED: vermelho muted
- NO_SHOW: cinza

Estilo geral:
- Cell: min-height 80px, border 1px var(--color-border), padding 4px
- Header: font-weight 600, font-size 0.7rem, text-transform uppercase
- Grid: border-radius var(--radius-xl), overflow hidden
</action>
  <verify>
    <automated>cd "/home/caramaschi/Área de trabalho/Projetos/PsiLock" && pnpm tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>AgendaMonthView compilando, renderiza grid 7 colunas, barras coloridas, dias adjacentes muted</done>
</task>

<task type="auto">
  <name>Task 3: Toolbar — adicionar tab "Mês"</name>
  <files>src/app/(vault)/agenda/components/agenda-toolbar.tsx</files>
  <action>
**Modificar `src/app/(vault)/agenda/components/agenda-toolbar.tsx`:**

- Adicionar terceira opção no view switcher: "Dia", "Semana", **"Mês"**
- `activeView` agora aceita: `"day" | "week" | "month"`
- Month view href: `/agenda?view=month&date=${dateToParam(monthStart)}`
- Manter layout e estilo existente do toolbar
- Link ativo destacado da mesma forma que day/week
</action>
  <verify>
    <automated>cd "/home/caramaschi/Área de trabalho/Projetos/PsiLock" && pnpm tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Toolbar com 3 tabs: Dia, Semana, Mês. Todas funcionais com active state.</done>
</task>

<task type="auto">
  <name>Task 4: Integrar Month View no page.tsx</name>
  <files>src/app/(vault)/agenda/page.tsx</files>
  <action>
**Modificar `src/app/(vault)/agenda/page.tsx`:**

1. Importar `deriveMonthAgenda` de `agenda.ts`
2. Importar `AgendaMonthView` do componente novo
3. Adicionar caso `activeView === "month"`:
   - Calcular `monthStart` (primeiro dia útil do mês do anchorDate)
   - `deriveMonthAgenda(appointments, anchorDate, TIMEZONE)`
   - Renderizar `<AgendaMonthView>` com callbacks:
     - `onDayClick`: navega para `?view=day&date=YYYY-MM-DD`
     - `onAppointmentClick`: seleciona appointment (reusa panels existente)
4. `activeView` aceita `"month"` além de `"day"` e `"week"`
5. Prev/Next navigation para month view: ±1 mês
6. Period label: "Abril 2026" (formatMonthLabel)
7. Manter todo o código existente intacto — só adicionar branch novo

NÃO remover nada existente. Adicionar apenas o branch de month view.
</action>
  <verify>
    <automated>cd "/home/caramaschi/Área de trabalho/Projetos/PsiLock" && pnpm tsc --noEmit 2>&1 | tail -5 && pnpm test -- --run 2>&1 | tail -10</automated>
  </verify>
  <done>Month view funcional via `?view=month`. pnpm tsc green. Tests passando.</done>
</task>

</tasks>

<verification>
1. `pnpm tsc --noEmit`: zero erros
2. `pnpm test -- --run`: zero regressões
3. `pnpm dev` → `/agenda?view=month`: grid 7×6 renderiza
4. Barras de status coloridas visíveis por appointment
5. Click na barra abre side panel (se implementado) ou nada (OK para esta fase)
6. Click no dia navega para day view daquele dia
7. Dias de meses adjacentes com opacidade reduzida
8. Toolbar mostra "Mês" como terceira tab ativa
9. Navegação prev/next muda o mês corretamente
</verification>

<success_criteria>
- deriveMonthAgenda() é função pura testável
- AgendaMonthView renderiza grid correto com 35-42 cells
- Toolbar tem 3 tabs: Dia, Semana, Mês
- page.tsx integra month view sem quebrar day/week
- TypeScript compila sem erros
- Tests passam sem regressão
</success_criteria>

<output>
After completion, create `.planning/phases/26-agenda-upgrade/26-01-SUMMARY.md`
</output>
