---
phase: 26-agenda-upgrade
plan: 02
type: execute
wave: 1
requirements:
  - AGENDA-02
must_haves:
  truths:
    - "MiniCalendar é client component com grid 7×6 de dias"
    - "Dots nos dias com appointments (max 3 dots)"
    - "Click num dia navega via router.push"
    - "QuickCreatePopover abre ao clicar em slot vazio do grid"
    - "Quick create faz pre-fill de data/hora e usa createAppointmentAction"
  artifacts:
    - path: "src/app/(vault)/agenda/components/mini-calendar.tsx"
      provides: "Mini calendar component com dots e navegação"
    - path: "src/app/(vault)/agenda/components/quick-create-popover.tsx"
      provides: "Popover de criação rápida de appointment"
---

<objective>
Implementar Mini Calendar (sidebar) e Quick Create (click em slot vazio).

Purpose: Navegação visual por mês + criação rápida de sessões sem abrir formulário completo.
Output: `mini-calendar.tsx` + `quick-create-popover.tsx` + integração no page.tsx/CalendarGrid
</objective>

<tasks>

<task type="auto">
  <name>Task 1: MiniCalendar component</name>
  <files>src/app/(vault)/agenda/components/mini-calendar.tsx</files>
  <action>
**Criar `src/app/(vault)/agenda/components/mini-calendar.tsx`:**

`"use client"`

Props:
```ts
interface MiniCalendarProps {
  currentDate: Date;
  appointmentDates: Set<string>; // ISO date strings YYYY-MM-DD
  onDateSelect: (date: Date) => void;
  onViewChange?: (view: "day" | "week" | "month") => void;
}
```

Renderização:
- Header: `← Abril 2026 →` (mês/ano com setas)
  - Click ← →: muda mês (state local)
- Grid 7 colunas × 6 linhas
- Header row: S, T, Q, Q, S, S, D (iniciais em pt-BR)
- Células:
  - Número do dia
  - Hoje: círculo accent (`border: 2px solid var(--color-accent)`, sem background)
  - Dia selecionado: background `var(--color-accent-light)`, texto `var(--color-accent)`
  - Dia com appointments: 1-3 dots pequenos abaixo do número (`width: 3px, height: 3px, border-radius: 50%`)
    - Cores: 1 dot = qualquer appointment, 2 dots = 2-3 appointments, 3 dots = 4+
  - Dia de outro mês: color `var(--color-text-3)` (muted)
- Click no dia: `onDateSelect(date)` → navega via `router.push`

Estilo:
- Largura total do container (sidebar ~240px)
- Font-size: 0.75rem para números
- Gap entre células: 2px
- Padding: 0.5rem
- Background: var(--color-surface-1)
- Border: 1px solid var(--color-border)
- Border-radius: var(--radius-md)

Lógica interna:
- State: `displayMonth: Date` (mês sendo exibido, pode diferir de currentDate)
- Gerar days array: primeira segunda-feira antes do dia 1 do displayMonth, +42 dias
- Para cada dia, contar appointments via `appointmentDates.has(dateStr)`
</action>
  <verify>
    <automated>cd "/home/caramaschi/Área de trabalho/Projetos/PsiLock" && pnpm tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>MiniCalendar compilando, grid 7×6, dots em dias com appointments, navegação ← → funciona</done>
</task>

<task type="auto">
  <name>Task 2: QuickCreatePopover component</name>
  <files>src/app/(vault)/agenda/components/quick-create-popover.tsx</files>
  <action>
**Criar `src/app/(vault)/agenda/components/quick-create-popover.tsx`:**

`"use client"`

Props:
```ts
interface QuickCreatePopoverProps {
  patients: Array<{ id: string; fullName: string; socialName?: string }>;
  defaultStartsAt: string; // ISO
  defaultDurationMinutes: number;
  defaultCareMode: "IN_PERSON" | "ONLINE";
  onCreate: (formData: FormData) => Promise<{ ok: boolean; error?: string }>;
  fullFormHref: string; // "/appointments/new?startsAt=..."
  onClose: () => void;
}
```

Renderização:
- Popover posicionado próximo ao slot clicado (position: fixed ou absolute)
- Campos:
  1. **Paciente**: `<select>` com lista de pacientes ativos (ordenado por fullName)
  2. **Horário**: input text pre-filled (readonly, mostra o horário clicado)
  3. **Duração**: select com opções: 30min, 50min, 60min (50min default)
  4. **Modalidade**: toggle IN_PERSON / ONLINE (default do profile)
- Botão "Agendar" → submit do form
- Link "Formulário completo" → `fullFormHref`
- Botão fechar (×) ou Escape → `onClose()`
- `useActionState` para feedback de sucesso/erro

Estilo:
- Width: 280px
- Background: var(--color-surface-0)
- Border: 1px solid var(--color-border)
- Border-radius: var(--radius-lg)
- Box-shadow: var(--shadow-md)
- Padding: 1rem
- Font-size: 0.85rem

Server Action integration:
- Usar `createAppointmentAction` existente de `../../appointments/actions`
- Construir FormData: patientId, startsAt, durationMinutes, careMode
- On success: toast + router.refresh() + onClose()
- On error: mostrar erro inline
</action>
  <verify>
    <automated>cd "/home/caramaschi/Área de trabalho/Projetos/PsiLock" && pnpm tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>QuickCreatePopover compilando, form com 4 campos, usa createAppointmentAction, fecha com Escape</done>
</task>

<task type="auto">
  <name>Task 3: Integrar MiniCalendar + QuickCreate no page.tsx</name>
  <files>src/app/(vault)/agenda/page.tsx, src/app/(vault)/agenda/components/calendar-grid.tsx</files>
  <action>
**Modificar `page.tsx`:**

Desktop layout: `grid-template-columns: 240px 1fr`
- Sidebar (esquerda): MiniCalendar
  - `appointmentDates`: Set de dates ISO dos appointments do mês
  - `onDateSelect`: navega para day view daquele date
- Main (direita): Agenda content existente (toolbar + grid)

Mobile: MiniCalendar escondido (media query ou hidden em < 768px)

Query extra para MiniCalendar:
- Load appointments para o mês inteiro (só `startsAt` necessário)
- `listByDateRange(workspaceId, monthStart, monthEnd)`
- Extrair Set<string> de dates: `appointments.map(a => a.startsAt.toISOString().slice(0,10))`

**Modificar `CalendarGrid` (ou page.tsx):**

Adicionar `onSlotClick` prop:
- Click em área vazia do grid (não num appointment block) → abre QuickCreatePopover
- Resolve horário do slot clicado (arredondar para 15min)
- Popover posicionado próximo ao click (event.clientX/Y ou slot element ref)

Se modificar CalendarGrid:
- Adicionar prop opcional `onSlotClick?: (date: Date, slotEl: HTMLElement) => void`
- Nos droppable slots, adicionar `onClick` handler que chama onSlotClick
- Manter comportamento de drag intacto
</action>
  <verify>
    <automated>cd "/home/caramaschi/Área de trabalho/Projetos/PsiLock" && pnpm tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>MiniCalendar visível na sidebar desktop. QuickCreate abre ao clicar em slot vazio. Mobile sem mini-calendar.</done>
</task>

</tasks>

<verification>
1. `pnpm tsc --noEmit`: zero erros
2. `pnpm test -- --run`: zero regressões
3. MiniCalendar renderiza na sidebar desktop com dots nos dias corretos
4. Click no MiniCalendar navega para day view
5. QuickCreatePopover abre ao clicar em slot vazio do grid
6. Quick create cria appointment corretamente via server action
7. Form "Formulário completo" link navega para /appointments/new com startsAt pre-filled
8. Mobile: MiniCalendar escondido
</verification>

<success_criteria>
- MiniCalendar é client component com grid 7×6 funcional
- Dots indicam dias com appointments
- QuickCreatePopover cria appointments via action existente
- Desktop: sidebar com MiniCalendar + main content
- Mobile: MiniCalendar hidden
- TypeScript compila, tests passam
</success_criteria>

<output>
After completion, create `.planning/phases/26-agenda-upgrade/26-02-SUMMARY.md`
</output>
