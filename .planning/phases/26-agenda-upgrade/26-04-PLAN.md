---
phase: 26-agenda-upgrade
plan: 04
type: execute
wave: 1
requirements:
  - AGENDA-04
must_haves:
  truths:
    - "createAppointmentQuickAction cria appointment sem redirect, retorna { success, error }"
    - "Clique em slot vazio do CalendarGrid abre QuickCreatePopover posicionado no click"
    - "Clique em slot vazio do WeekCalendarGrid abre QuickCreatePopover posicionado no click"
    - "Popover fecha com Escape, clique fora, ou criação com sucesso"
    - "Clique em appointment block NAO abre popover (e.stopPropagation)"
  artifacts:
    - path: "src/app/(vault)/appointments/actions.ts"
      provides: "createAppointmentQuickAction — wrapper sem redirect"
    - path: "src/app/(vault)/agenda/components/quick-create-integration.tsx"
      provides: "Client wrapper que orquestra CalendarGrid/WeekCalendarGrid + QuickCreatePopover"
---

<objective>
Integrar QuickCreatePopover aos grids da agenda — clique em slot vazio abre popover de criação rápida.

Purpose: Psicólogo pode criar sessões rapidamente clicando em horários vazios do grid, sem navegar para formulário completo.
Output: `createAppointmentQuickAction` + integração nos dois grids + wrapper client na page.tsx
</objective>

<context>

## Estado Atual

- `QuickCreatePopover` existe mas não é importado em lugar nenhum
- `CalendarGrid` e `WeekCalendarGrid` têm `DroppableSlot` sem onClick
- `createAppointmentAction` faz redirect("/agenda") — incompatível com popover
- `AppointmentBlock` já tem onClick com e.stopPropagation() — sem conflito

## Fluxo de Dados

```
Clique no slot vazio → DroppableSlot.onClick
  → parse slotId → isoStartsAt + clientX/clientY
  → onSlotClick(isoStartsAt, position)
    → setPopoverState no wrapper client
      → renderiza QuickCreatePopover
        → onCreate(formData) = createAppointmentQuickAction
          → revalidatePath("/agenda")
          → retorna { success: true }
        → router.refresh() + onClose()
```

## Decisões de Design

- Nova action `createAppointmentQuickAction` (sem redirect, revalidatePath + {success})
- Wrapper client `QuickCreateWrapper` que gerencia popover state
- Grids recebem `onSlotClick` prop opcional
- DroppableSlot ganha onClick handler que parseia o slot datetime
- WeekCalendarGrid recebe as mesmas mudanças que CalendarGrid
- Profile defaults: duration = profile.defaultAppointmentDurationMinutes ?? 50, careMode = derivado do profile

</context>

<tasks>

<task type="auto">
  <name>Task 1: createAppointmentQuickAction — wrapper sem redirect</name>
  <files>src/app/(vault)/appointments/actions.ts</files>
  <action>
**Adicionar em `src/app/(vault)/appointments/actions.ts`:**

Criar `createAppointmentQuickAction(formData): Promise<{ success: boolean; error?: string }>`

Extrair a lógica de validação/criação da `createAppointmentAction` existente para uma função interna `handleCreateAppointment(formData, shouldRedirect: boolean)` e ter duas actions finas:

1. `createAppointmentAction` — existente, usa redirect (para formulário completo)
2. `createAppointmentQuickAction` — nova, usa revalidatePath + retorna { success, error }

Lógica da nova action:
1. `resolveSession()` → accountId, workspaceId
2. Validar FormData: patientId, startsAt, durationMinutes, careMode
3. Validar conflito via `checkAppointmentConflict()`
4. Criar appointment via repo
5. `revalidatePath("/agenda")`
6. Retornar `{ success: true }` ou `{ error: "mensagem" }`

Manter compatibilidade total com a action existente — não quebrar o formulário completo.
  </action>
  <verify>
    <automated>cd "/home/caramaschi/Área de trabalho/Projetos/PsiLock" && pnpm tsc --noEmit 2>&1 | grep -v tests/ | tail -5</automated>
  </verify>
  <done>Nova action compilando, existente intacta, testes passando</done>
</task>

<task type="auto">
  <name>Task 2: QuickCreateWrapper — client component orquestrador</name>
  <files>src/app/(vault)/agenda/components/quick-create-wrapper.tsx</files>
  <action>
**Criar `src/app/(vault)/agenda/components/quick-create-wrapper.tsx`:**

`"use client"`

```ts
interface QuickCreateWrapperProps {
  patients: Array<{ id: string; fullName: string; socialName?: string }>;
  defaultDurationMinutes: number;
  defaultCareMode: "IN_PERSON" | "ONLINE";
  onCreate: (formData: FormData) => Promise<{ success: boolean; error?: string }>;
  grid: React.ReactNode; // CalendarGrid ou WeekCalendarGrid já configurado
  onGridWithSlotClick: (onSlotClick: SlotClickHandler) => React.ReactNode;
  /** ISO date string para day view, ou weekStart para week view */
  viewDate: string;
}

type SlotClickHandler = (slotStartsAt: string, position: { top: number; left: number }) => void;
```

O wrapper:
1. Mantém state `popoverState: { startsAt, position } | null`
2. Renderiza o grid via `onGridWithSlotClick(handleSlotClick)` — injeta o callback
3. Quando `popoverState` não é null, renderiza `QuickCreatePopover`
4. `handleSlotClick`: `setPopoverState({ startsAt: slotStartsAt, position })`
5. `handleClose`: `setPopoverState(null)`
6. No `onCreate` callback: chama a prop, se success → `router.refresh()` + `handleClose()`

Usar `startTransition` para a criação (evita UI blocking).

O grid (CalendarGrid/WeekCalendarGrid) será renderizado com a prop `onSlotClick` injetada.
  </action>
  <verify>
    <automated>cd "/home/caramaschi/Área de trabalho/Projetos/PsiLock" && pnpm tsc --noEmit 2>&1 | grep -v tests/ | tail -5</automated>
  </verify>
  <done>Wrapper compilando, gerencia popover state, injeta onSlotClick no grid</done>
</task>

<task type="auto">
  <name>Task 3: CalendarGrid — adicionar onSlotClick no DroppableSlot</name>
  <files>src/app/(vault)/agenda/components/calendar-grid.tsx</files>
  <action>
**Modificar `CalendarGrid`:**

1. Adicionar tipo exportado:
```ts
export type SlotClickHandler = (
  slotStartsAt: string,
  position: { top: number; left: number },
) => void;
```

2. Adicionar prop opcional em `CalendarGridProps`:
```ts
onSlotClick?: SlotClickHandler;
```

3. Modificar `DroppableSlot` para aceitar e usar onClick:
```ts
function DroppableSlot({ id, dayStartHour, pixelsPerMinute, onClick }: DroppableSlotProps) {
  const { setNodeRef, isOver } = useDroppable({ id });

  const handleClick = (e: React.MouseEvent) => {
    if (!onClick) return;
    // Parse slot datetime from id: "slot-2026-03-20T14:00"
    const dateTimePart = id.replace(/^slot-/, "");
    const isoStartsAt = `${dateTimePart}:00.000Z`;

    onClick(isoStartsAt, {
      top: e.clientY,
      left: e.clientX,
    });
  };

  return (
    <div
      ref={setNodeRef}
      onClick={handleClick}
      style={{
        position: "absolute",
        top: topPx,
        left: 0,
        right: 0,
        height: slotHeightPx,
        background: isOver ? "rgba(154, 52, 18, 0.06)" : "transparent",
        transition: "background 80ms",
        cursor: onClick ? "pointer" : "default",
      }}
    />
  );
}
```

4. Passar `onSlotClick` para cada `DroppableSlot` no map:
```tsx
<DroppableSlot
  key={slotId}
  id={slotId}
  dayStartHour={dayStartHour}
  totalMinutes={totalMinutes}
  pixelsPerMinute={pixelsPerMinute}
  containerHeight={containerHeight}
  onClick={onSlotClick}
/>
```

5. Ajustar hover do slot: quando `onClick` existe, hover sutil indica clicabilidade.
  </action>
  <verify>
    <automated>cd "/home/caramaschi/Área de trabalho/Projetos/PsiLock" && pnpm tsc --noEmit 2>&1 | grep -v tests/ | tail -5</automated>
  </verify>
  <done>CalendarGrid com onSlotClick prop, DroppableSlot com onClick handler, compilando sem erros</done>
</task>

<task type="auto">
  <name>Task 4: WeekCalendarGrid — adicionar onSlotClick no DroppableSlot</name>
  <files>src/app/(vault)/agenda/components/week-calendar-grid.tsx</files>
  <action>
**Modificar `WeekCalendarGrid`:**

Mesmas mudanças que CalendarGrid, adaptadas para o grid semanal:

1. Importar `SlotClickHandler` de `calendar-grid.tsx` (tipo compartilhado)
2. Adicionar prop `onSlotClick?: SlotClickHandler` em `WeekCalendarGridProps`
3. Modificar o `DroppableSlot` local (que é separado) para aceitar onClick
4. Passar `onSlotClick` no map de slots (7 colunas × slots por dia)

O DroppableSlot do WeekCalendarGrid é mais complexo porque tem 7 colunas de dias. Cada slot pertence a um dia específico. O slot ID inclui a data do dia:
```
"slot-{dayIndex}-{date}T{hour}:{minute}"
```

Parse do slot:
```ts
const dateTimePart = id.replace(/^slot-\d+-/, "");
const isoStartsAt = `${dateTimePart}:00.000Z`;
```
  </action>
  <verify>
    <automated>cd "/home/caramaschi/Área de trabalho/Projetos/PsiLock" && pnpm tsc --noEmit 2>&1 | grep -v tests/ | tail -5</automated>
  </verify>
  <done>WeekCalendarGrid com onSlotClick prop, mesmo pattern que CalendarGrid</done>
</task>

<task type="auto">
  <name>Task 5: Integrar na page.tsx — substituir grids por wrapper com QuickCreate</name>
  <files>src/app/(vault)/agenda/page.tsx</files>
  <action>
**Modificar `page.tsx`:**

1. Importar:
```ts
import { createAppointmentQuickAction } from "../appointments/actions";
import { QuickCreateWrapper } from "./components/quick-create-wrapper";
```

2. Substituir a renderização do day view:
```tsx
<QuickCreateWrapper
  patients={allPatients}
  defaultDurationMinutes={profile.defaultAppointmentDurationMinutes ?? 50}
  defaultCareMode={profileCareMode}
  onCreate={createAppointmentQuickAction}
  viewDate={dateToParam(anchorDate)}
  renderGrid={(onSlotClick) => (
    dayResult.cards.length > 0 ? (
      <CalendarGrid
        blocks={appointments
          .filter((a) => dayResult.cards.some((c) => c.appointmentId === a.id))
          .map(toGridBlock)}
        panels={panels}
        patientNames={patientNames}
        date={dateToParam(anchorDate)}
        options={{ dayStartHour: 7, dayEndHour: 21 }}
        onSlotClick={onSlotClick}
      />
    ) : (
      <EmptyState ... />
    )
  )}
/>
```

3. Substituir a renderização do week view similarmente:
```tsx
<QuickCreateWrapper
  patients={allPatients}
  defaultDurationMinutes={profile.defaultAppointmentDurationMinutes ?? 50}
  defaultCareMode={profileCareMode}
  onCreate={createAppointmentQuickAction}
  viewDate={dateToParam(weekStart)}
  renderGrid={(onSlotClick) => (
    <WeekCalendarGrid
      blocks={appointments.map(toGridBlock)}
      panels={panels}
      patientNames={patientNames}
      weekStart={dateToParam(weekStart)}
      options={{ dayStartHour: 7, dayEndHour: 21 }}
      onSlotClick={onSlotClick}
    />
  )}
/>
```

4. Month view NÃO recebe QuickCreate (não faz sentido criar sessão num mês view — sem slot temporal específico).
  </action>
  <verify>
    <automated>cd "/home/caramaschi/Área de trabalho/Projetos/PsiLock" && pnpm tsc --noEmit 2>&1 | grep -v tests/ | tail -5 && pnpm test -- --run 2>&1 | tail -10</automated>
  </verify>
  <done>Day view e week view com QuickCreate integrado. Month view sem popover. TypeScript green. Tests green.</done>
</task>

</tasks>

<verification>
1. `pnpm tsc --noEmit`: zero erros
2. `pnpm test -- --run`: zero regressões
3. `pnpm dev` → `/agenda?view=day`:
   - Clicar em slot vazio do grid abre QuickCreatePopover posicionado no click
   - Popover pré-preenche horário do slot
   - Selecionar paciente e clicar "Agendar" cria sessão sem navegar
   - Toast "Sessão agendada" aparece
   - Grid atualiza com nova sessão
   - Clicar em appointment block NÃO abre popover
   - Escape fecha popover
   - Clique fora fecha popover
4. `pnpm dev` → `/agenda?view=week`:
   - Mesma UX em qualquer dia do grid semanal
5. `pnpm dev` → `/agenda?view=month`:
   - Sem QuickCreate (apenas navegação)
6. `/appointments/new` (formulário completo) ainda funciona com redirect
</verification>

<success_criteria>
- createAppointmentQuickAction cria appointments sem redirect, revalidatePath funciona
- QuickCreateWrapper gerencia popover state corretamente
- CalendarGrid e WeekCalendarGrid aceitam onSlotClick prop
- DroppableSlot em ambos grids tem onClick handler funcional
- page.tsx integra wrapper sem quebrar existing functionality
- Month view sem QuickCreate (comportamento preservado)
- TypeScript compila, tests passam, build green
</success_criteria>

<output>
After completion, create `.planning/phases/26-agenda-upgrade/26-04-SUMMARY.md`
</output>
