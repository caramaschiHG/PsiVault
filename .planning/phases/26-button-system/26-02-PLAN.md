# Phase 26-02: Remover SubmitButton de Auth e Unificar

## Goal
Remover o `SubmitButton` duplicado de `(auth)/components/` e usar o componente unificado de UI.

## Tasks

### Task 1: Identificar todos os usos do SubmitButton de auth
- `src/app/(auth)/components/submit-button.tsx` — componente duplicado
- Usado em: sign-in, sign-up, reset-password pages

### Task 2: Atualizar imports nas páginas de auth
- Mudar de `./submit-button` para `@/components/ui/submit-button`
- Ajustar props se necessário (auth usava spinner SMIL vs CSS — garantir compatibilidade)

### Task 3: Adaptar SubmitButton para contexto de auth
- Auth precisa de `width: 100%` — garantir que `className` suporte isso
- Auth precisa de spinner visível — garantir que o spinner CSS funcione em fundo escuro e claro
- Se necessário, adicionar prop `fullWidth` ao SubmitButton

### Task 4: Remover `src/app/(auth)/components/submit-button.tsx`
- Deletar arquivo duplicado
- Verificar que nenhum outro arquivo importa dele

### Task 5: Testar
- `pnpm build` — verificar build sem erros
- Testar fluxos de auth visualmente (sign-in, sign-up, reset)

## Files Changed
- `src/app/(auth)/components/sign-in/page.tsx` (edit)
- `src/app/(auth)/components/sign-up/page.tsx` (edit)
- `src/app/(auth)/components/reset-password/page.tsx` (edit)
- `src/app/(auth)/components/submit-button.tsx` (delete)

## Files
- 4 files (3 edits + 1 delete)
