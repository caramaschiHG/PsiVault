---
phase: 27-diagn-stico-e-funda-o-de-dados
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - .env.example
autonomous: true
requirements:
  - PERF-02
  - PERF-03
  - PERF-04

must_haves:
  truths:
    - PerformanceMetric model exists in schema.prisma with correct columns and indexes
    - Appointment model has composite index [workspaceId, status, startsAt]
    - SessionCharge model has composite index [workspaceId, status, createdAt]
    - Schema push completes successfully against the live database
    - .env.example documents Supavisor transaction pooler with prepareThreshold=0
  artifacts:
    - path: prisma/schema.prisma
      provides: PerformanceMetric model + composite indexes on Appointment and SessionCharge
      contains:
        - "model PerformanceMetric"
        - "@@index([workspaceId, status, startsAt])"
        - "@@index([workspaceId, status, createdAt])"
    - path: .env.example
      provides: Supavisor configuration documentation
      contains: "prepareThreshold=0"
  key_links:
    - from: prisma/schema.prisma
      to: database
      via: npx prisma db push --accept-data-loss
---

<objective>
Estabelecer a fundação de schema para Phase 27: criar tabela de métricas de performance, adicionar índices compostos críticos em Appointment e SessionCharge, e documentar configuração de connection pooling. Esta é a base sobre a qual todos os outros plans desta fase dependem.

Purpose: Sem o schema atualizado e pushado, os plans subsequentes não conseguem gerar tipos Prisma nem assumir que os índices existem no banco.
Output: Schema.prisma modificado, .env.example atualizado, schema push executado.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-diagn-stico-e-funda-o-de-dados/27-CONTEXT.md

## Interfaces relevantes do codebase

From prisma/schema.prisma (Appointment modelo existente):
```prisma
model Appointment {
  id          String @id @default(uuid())
  workspaceId String @map("workspace_id")
  patientId   String @map("patient_id")
  startsAt        DateTime @map("starts_at")
  endsAt          DateTime @map("ends_at")
  durationMinutes Int      @map("duration_minutes")
  careMode AppointmentCareMode @map("care_mode")
  status   AppointmentStatus   @default(SCHEDULED)
  // ... outros campos
  @@index([workspaceId, startsAt])
  @@index([workspaceId, patientId])
  @@index([seriesId])
  @@map("appointments")
}
```

From prisma/schema.prisma (SessionCharge modelo existente):
```prisma
model SessionCharge {
  id            String @id @default(uuid())
  workspaceId   String @map("workspace_id")
  patientId     String @map("patient_id")
  appointmentId String? @unique @map("appointment_id")
  status        String
  amountInCents Int?      @map("amount_in_cents")
  paymentMethod String?   @map("payment_method")
  paidAt        DateTime? @map("paid_at")
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")
  @@index([workspaceId, createdAt])
  @@index([workspaceId, patientId])
  @@map("session_charges")
}
```

From .env.example (seção Database existente):
```
# Supabase Dashboard > Settings > Database > Connection string > Transaction pooler (porta 6543)
# Usado para queries em runtime (via Supavisor connection pooling)
# Adicionar ?pgbouncer=true ao final da string
DATABASE_URL=

# Supabase Dashboard > Settings > Database > Connection string > Direct connection (porta 5432)
# Usado por prisma migrate deploy (requer conexão direta, não pooler)
DIRECT_URL=
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar PerformanceMetric model e índices compostos ao schema.prisma</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (ver state atual dos modelos Appointment, SessionCharge, e final do arquivo)
  </read_first>
  <action>
    Modificar prisma/schema.prisma em três lugares exatos:

    1. No modelo Appointment, APÓS a linha `@@index([seriesId])`, adicionar:
       `@@index([workspaceId, status, startsAt])`

    2. No modelo SessionCharge, APÓS a linha `@@index([workspaceId, patientId])`, adicionar:
       `@@index([workspaceId, status, createdAt])`

    3. No final do arquivo (após o último modelo existente), adicionar o modelo PerformanceMetric:

    ```prisma
    model PerformanceMetric {
      id          String   @id @default(uuid())
      metricName  String   @map("metric_name")
      value       Float
      rating      String?
      pagePath    String   @map("page_path")
      workspaceId String?  @map("workspace_id")
      sessionId   String?  @map("session_id")
      createdAt   DateTime @default(now()) @map("created_at")

      @@index([workspaceId, metricName, createdAt])
      @@index([createdAt])
      @@map("performance_metrics")
    }
    ```

    Importante: NÃO remover índices existentes — apenas adicionar os novos.
  </action>
  <verify>
    <automated>grep -n "model PerformanceMetric" prisma/schema.prisma && grep -n "@@index(\[workspaceId, status, startsAt\])" prisma/schema.prisma && grep -n "@@index(\[workspaceId, status, createdAt\])" prisma/schema.prisma</automated>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma contém `model PerformanceMetric` com campos: id, metricName, value, rating, pagePath, workspaceId, sessionId, createdAt
    - prisma/schema.prisma contém `@@index([workspaceId, status, startsAt])` dentro do modelo Appointment
    - prisma/schema.prisma contém `@@index([workspaceId, status, createdAt])` dentro do modelo SessionCharge
    - Índices existentes (`@@index([workspaceId, startsAt])`, `@@index([workspaceId, createdAt])`, etc.) permanecem inalterados
  </acceptance_criteria>
  <done>Schema.prisma atualizado com PerformanceMetric model e dois índices compostos adicionais.</done>
</task>

<task type="auto">
  <name>Task 2: Documentar Supavisor transaction pooler em .env.example</name>
  <files>.env.example</files>
  <read_first>
    - .env.example (seção Database existente)
  </read_first>
  <action>
    Modificar .env.example na seção Database (comentários apenas, não valores):

    Substituir:
    ```
    # Supabase Dashboard > Settings > Database > Connection string > Transaction pooler (porta 6543)
    # Usado para queries em runtime (via Supavisor connection pooling)
    # Adicionar ?pgbouncer=true ao final da string
    DATABASE_URL=
    ```

    Por:
    ```
    # Supabase Dashboard > Settings > Database > Connection string > Transaction pooler (porta 6543)
    # Usado para queries em runtime (via Supavisor connection pooling)
    # Formato: postgresql://...:6543/...?pgbouncer=true&prepareThreshold=0
    # prepareThreshold=0 é obrigatório para Prisma + Supavisor transaction mode
    DATABASE_URL=
    ```

    NÃO modificar DIRECT_URL nem outras seções.
  </action>
  <verify>
    <automated>grep -n "prepareThreshold=0" .env.example && grep -n "pgbouncer=true" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - .env.example contém o texto `prepareThreshold=0` na seção DATABASE_URL
    - .env.example contém explicação de que prepareThreshold=0 é obrigatório para Prisma + Supavisor
    - DIRECT_URL permanece inalterado
  </acceptance_criteria>
  <done>.env.example documenta corretamente a configuração de Supavisor transaction pooler.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Gerar Prisma client e pushar schema para o banco</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (verificar modificações do Task 1)
    - .env.local (verificar se DATABASE_URL e DIRECT_URL estão configurados)
  </read_first>
  <action>
    Executar em sequência:

    1. `npx prisma generate` — gerar tipos TypeScript atualizados com PerformanceMetric
    2. `npx prisma db push --accept-data-loss` — sincronizar schema com o banco de dados

    Se `prisma db push` falhar com erro de conexão, verificar se DATABASE_URL aponta para porta 6543 (transaction pooler) e DIRECT_URL para porta 5432 (direct). O comando `db push` requer DIRECT_URL para modificar schema.

    Se houver erro de permissão ou migration lock, reportar o erro exato e parar.
  </action>
  <verify>
    <automated>npx prisma generate && npx prisma db push --accept-data-loss</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma generate` completa sem erros
    - `npx prisma db push --accept-data-loss` completa sem erros
    - Tabela `performance_metrics` é criada no banco (confirmar via `npx prisma db pull --print` ou query SQL)
    - Índices adicionais em `appointments` e `session_charges` são criados
  </acceptance_criteria>
  <done>Schema push completado com sucesso. Banco sincronizado com novos modelos e índices.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema.prisma → database | Schema changes affect live data structures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Denial of Service | prisma db push | mitigate | Use --accept-data-loss only after confirming schema changes are additive (no destructive drops). Run on dev/staging first. |
| T-27-02 | Information Disclosure | .env.example | accept | .env.example contains no secrets — only documentation comments and empty values. |
</threat_model>

<verification>
- `npx prisma generate` executa sem erros
- `npx prisma db push --accept-data-loss` executa sem erros
- `grep "model PerformanceMetric" prisma/schema.prisma` retorna match
- `grep "@@index(\[workspaceId, status, startsAt\])" prisma/schema.prisma` retorna match
- `grep "@@index(\[workspaceId, status, createdAt\])" prisma/schema.prisma` retorna match
- `grep "prepareThreshold=0" .env.example` retorna match
</verification>

<success_criteria>
- Schema.prisma contém PerformanceMetric model com 7 campos e 2 índices
- Appointment tem índice composto [workspaceId, status, startsAt]
- SessionCharge tem índice composto [workspaceId, status, createdAt]
- Banco de dados reflete todas as mudanças de schema
- .env.example documenta Supavisor com prepareThreshold=0
</success_criteria>

<output>
After completion, create `.planning/phases/27-diagn-stico-e-funda-o-de-dados/27-01-SUMMARY.md`
</output>
