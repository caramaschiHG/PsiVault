---
phase: 27-diagn-stico-e-funda-o-de-dados
plan: 02
type: execute
wave: 2
depends_on:
  - 27-01-PLAN.md
files_modified:
  - package.json
  - next.config.ts
  - src/lib/metrics/model.ts
  - src/lib/metrics/repository.ts
  - src/lib/metrics/repository.prisma.ts
  - src/lib/metrics/store.ts
  - src/app/api/metrics/route.ts
  - src/components/cwv-collector.tsx
  - src/app/(vault)/layout.tsx
autonomous: true
requirements:
  - PERF-01
  - PERF-02

must_haves:
  truths:
    - `ANALYZE=true pnpm build` gera relatório HTML de bundle analysis
    - Endpoint POST /api/metrics recebe métricas CWV e persiste no PostgreSQL
    - Componente CwvCollector montado no layout do vault coleta métricas de todas as páginas protegidas
    - Métricas incluem LCP, INP, CLS, TTFB, FCP com pagePath e rating
  artifacts:
    - path: next.config.ts
      provides: Bundle analyzer configuration
      contains: "@next/bundle-analyzer"
    - path: src/lib/metrics/repository.prisma.ts
      provides: Persistência de métricas via Prisma
      exports: ["createPrismaMetricsRepository"]
    - path: src/app/api/metrics/route.ts
      provides: Endpoint de coleta CWV
      exports: ["POST"]
    - path: src/components/cwv-collector.tsx
      provides: Client component para coleta web-vitals
      exports: ["CwvCollector"]
  key_links:
    - from: src/components/cwv-collector.tsx
      to: /api/metrics
      via: fetch/sendBeacon POST
      pattern: "/api/metrics"
    - from: src/app/api/metrics/route.ts
      to: src/lib/metrics/repository.prisma.ts
      via: getMetricsRepository()
      pattern: "getMetricsRepository"
    - from: src/app/(vault)/layout.tsx
      to: src/components/cwv-collector.tsx
      via: JSX import
      pattern: "<CwvCollector"
---

<objective>
Implementar ferramentas de diagnóstico de performance: bundle analyzer para baseline de bundle size e instrumentação de Core Web Vitals (CWV) com coleta no browser, endpoint backend e persistência em PostgreSQL.

Purpose: Estabelecer medição objetiva de performance para validar melhorias nas fases subsequentes.
Output: Bundle analyzer configurado, domínio de métricas criado, endpoint /api/metrics funcional, coletor CWV ativo em todas as páginas do vault.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/27-diagn-stico-e-funda-o-de-dados/27-01-SUMMARY.md (Schema foundation — PerformanceMetric model exists)
@.planning/phases/27-diagn-stico-e-funda-o-de-dados/27-CONTEXT.md

## Interfaces relevantes do codebase

From next.config.ts (configuração existente):
```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  experimental: {
    serverActions: {
      bodySizeLimit: "3mb",
    },
    optimizePackageImports: [
      "date-fns",
      "react-day-picker",
      "@dnd-kit/core",
      "@dnd-kit/utilities",
    ],
  },
};

export default nextConfig;
```

From src/lib/patients/repository.prisma.ts (pattern para replicar):
```typescript
import { db } from "../db";
import type { Patient } from "./model";
import type { PatientRepository } from "./repository";
import { Prisma } from "@prisma/client";

const LIST_SELECT = Prisma.validator<Prisma.PatientSelect>()({...});

export function createPrismaPatientRepository(): PatientRepository {
  return { async save(...) {...}, ... };
}
```

From src/lib/db.ts:
```typescript
export const db = globalThis.__psivaultPrisma__ ?? new PrismaClient({...});
globalThis.__psivaultPrisma__ = db;
```

From src/app/(vault)/layout.tsx (server component):
```tsx
export default function VaultLayout({ children }: { children: React.ReactNode }) {
  return (
    <KeyboardShortcutsProvider>
      <NotificationProvider>
        <div style={shellStyle}>...</div>
      </NotificationProvider>
    </KeyboardShortcutsProvider>
  );
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar e configurar @next/bundle-analyzer</name>
  <files>package.json, next.config.ts</files>
  <read_first>
    - package.json (ver dependências atuais e scripts)
    - next.config.ts (configuração atual)
  </read_first>
  <action>
    1. Instalar bundle analyzer como devDependency:
       `pnpm add -D @next/bundle-analyzer`

    2. Modificar next.config.ts para incluir bundle analyzer condicional:

       Substituir o conteúdo de next.config.ts por:
       ```typescript
       import type { NextConfig } from "next";
       import withBundleAnalyzer from "@next/bundle-analyzer";

       const nextConfig: NextConfig = {
         reactStrictMode: true,
         experimental: {
           serverActions: {
             bodySizeLimit: "3mb",
           },
           optimizePackageImports: [
             "date-fns",
             "react-day-picker",
             "@dnd-kit/core",
             "@dnd-kit/utilities",
           ],
         },
       };

       const bundleAnalyzer = withBundleAnalyzer({
         enabled: process.env.ANALYZE === "true",
       });

       export default bundleAnalyzer(nextConfig);
       ```

       IMPORTANTE: Preservar TODAS as configurações existentes (reactStrictMode, serverActions bodySizeLimit, optimizePackageImports).
  </action>
  <verify>
    <automated>pnpm install && grep -n "@next/bundle-analyzer" package.json && grep -n "withBundleAnalyzer" next.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - package.json contém `@next/bundle-analyzer` em `devDependencies`
    - next.config.ts importa `withBundleAnalyzer` de `@next/bundle-analyzer`
    - next.config.ts exporta `bundleAnalyzer(nextConfig)` onde bundleAnalyzer é configurado com `enabled: process.env.ANALYZE === "true"`
    - Configurações existentes (reactStrictMode, serverActions, optimizePackageImports) permanecem inalteradas
  </acceptance_criteria>
  <done>Bundle analyzer instalado e configurado condicionalmente via ANALYZE=true.</done>
</task>

<task type="auto">
  <name>Task 2: Criar domínio de métricas e endpoint /api/metrics</name>
  <files>
    src/lib/metrics/model.ts
    src/lib/metrics/repository.ts
    src/lib/metrics/repository.prisma.ts
    src/lib/metrics/store.ts
    src/app/api/metrics/route.ts
    package.json
  </files>
  <read_first>
    - src/lib/patients/model.ts (pattern de domain model)
    - src/lib/patients/repository.ts (pattern de repository interface)
    - src/lib/patients/repository.prisma.ts (pattern de implementação Prisma)
    - src/lib/patients/store.ts (pattern de singleton store)
    - src/lib/db.ts (PrismaClient singleton)
  </read_first>
  <action>
    1. Instalar web-vitals:
       `pnpm add web-vitals`

    2. Criar src/lib/metrics/model.ts:
       ```typescript
       export interface PerformanceMetric {
         id: string;
         metricName: string;
         value: number;
         rating: string | null;
         pagePath: string;
         workspaceId: string | null;
         sessionId: string | null;
         createdAt: Date;
       }
       ```

    3. Criar src/lib/metrics/repository.ts:
       ```typescript
       import type { PerformanceMetric } from "./model";

       export interface MetricsRepository {
         save(metric: PerformanceMetric): Promise<PerformanceMetric>;
       }
       ```

    4. Criar src/lib/metrics/repository.prisma.ts:
       ```typescript
       import { db } from "../db";
       import type { PerformanceMetric } from "./model";
       import type { MetricsRepository } from "./repository";

       export function createPrismaMetricsRepository(): MetricsRepository {
         return {
           async save(metric: PerformanceMetric): Promise<PerformanceMetric> {
             await db.performanceMetric.create({
               data: {
                 id: metric.id,
                 metricName: metric.metricName,
                 value: metric.value,
                 rating: metric.rating,
                 pagePath: metric.pagePath,
                 workspaceId: metric.workspaceId,
                 sessionId: metric.sessionId,
                 createdAt: metric.createdAt,
               },
             });
             return metric;
           },
         };
       }
       ```

    5. Criar src/lib/metrics/store.ts:
       ```typescript
       import { createPrismaMetricsRepository } from "./repository.prisma";
       import type { MetricsRepository } from "./repository";

       declare global {
         // eslint-disable-next-line no-var
         var __psivaultMetricsRepo__: MetricsRepository | undefined;
       }

       export function getMetricsRepository(): MetricsRepository {
         return (
           globalThis.__psivaultMetricsRepo__ ?? createPrismaMetricsRepository()
         );
       }

       globalThis.__psivaultMetricsRepo__ = getMetricsRepository();
       ```

    6. Criar src/app/api/metrics/route.ts:
       ```typescript
       import { NextRequest, NextResponse } from "next/server";
       import { getMetricsRepository } from "@/lib/metrics/store";
       import { resolveSession } from "@/lib/supabase/session";

       interface MetricPayload {
         metricName: string;
         value: number;
         rating: string;
         pagePath: string;
       }

       export async function POST(request: NextRequest) {
         try {
           const body = await request.json();
           const payloads: MetricPayload[] = Array.isArray(body) ? body : [body];

           let workspaceId: string | null = null;
           try {
             const session = await resolveSession();
             workspaceId = session.workspaceId;
           } catch {
             // anonymous session — store with null workspaceId
           }

           const repo = getMetricsRepository();
           const now = new Date();

           await Promise.all(
             payloads.map((payload) =>
               repo.save({
                 id: crypto.randomUUID(),
                 metricName: payload.metricName,
                 value: payload.value,
                 rating: payload.rating ?? null,
                 pagePath: payload.pagePath,
                 workspaceId,
                 sessionId: null,
                 createdAt: now,
               })
             )
           );

           return NextResponse.json({ ok: true }, { status: 202 });
         } catch (error) {
           return NextResponse.json(
             { error: "Invalid payload" },
             { status: 400 }
           );
         }
       }
       ```
  </action>
  <verify>
    <automated>pnpm install && pnpm test && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - package.json contém `web-vitals` em `dependencies`
    - src/lib/metrics/model.ts exporta interface `PerformanceMetric` com 7 campos
    - src/lib/metrics/repository.ts exporta interface `MetricsRepository` com método `save`
    - src/lib/metrics/repository.prisma.ts exporta `createPrismaMetricsRepository` que usa `db.performanceMetric.create`
    - src/lib/metrics/store.ts exporta `getMetricsRepository` com padrão globalThis singleton
    - src/app/api/metrics/route.ts exporta `POST` handler que aceita array de métricas e persiste via repository
    - `pnpm build` completa sem erros de TypeScript
  </acceptance_criteria>
  <done>Domínio de métricas criado com model, repository, store e endpoint API funcional.</done>
</task>

<task type="auto">
  <name>Task 3: Criar e montar coletor CWV no vault layout</name>
  <files>src/components/cwv-collector.tsx, src/app/(vault)/layout.tsx</files>
  <read_first>
    - src/app/(vault)/layout.tsx (server component — ver onde importar client component)
    - src/components/ui/toast-provider.tsx (exemplo de client component importado em layout)
  </read_first>
  <action>
    1. Criar src/components/cwv-collector.tsx:
       ```tsx
       "use client";

       import { useEffect } from "react";
       import { onLCP, onINP, onCLS, onTTFB, onFCP } from "web-vitals";
       import type { Metric } from "web-vitals";

       interface CwvPayload {
         metricName: string;
         value: number;
         rating: string;
         pagePath: string;
       }

       const buffer: CwvPayload[] = [];
       let flushTimeout: ReturnType<typeof setTimeout> | null = null;

       function flush() {
         if (buffer.length === 0) return;
         const batch = buffer.splice(0, buffer.length);
         const body = JSON.stringify(batch);

         if (navigator.sendBeacon) {
           navigator.sendBeacon("/api/metrics", body);
         } else {
           fetch("/api/metrics", {
             method: "POST",
             body,
             keepalive: true,
             headers: { "Content-Type": "application/json" },
           }).catch(() => {});
         }
       }

       function queueMetric(metric: Metric) {
         buffer.push({
           metricName: metric.name,
           value: metric.value,
           rating: metric.rating,
           pagePath: window.location.pathname,
         });

         if (flushTimeout) clearTimeout(flushTimeout);
         flushTimeout = setTimeout(flush, 5000);

         if (buffer.length >= 5) flush();
       }

       export function CwvCollector() {
         useEffect(() => {
           onLCP(queueMetric);
           onINP(queueMetric);
           onCLS(queueMetric);
           onTTFB(queueMetric);
           onFCP(queueMetric);

           const handleBeforeUnload = () => flush();
           window.addEventListener("beforeunload", handleBeforeUnload);

           return () => {
             window.removeEventListener("beforeunload", handleBeforeUnload);
             if (flushTimeout) clearTimeout(flushTimeout);
             flush();
           };
         }, []);

         return null;
       }
       ```

    2. Modificar src/app/(vault)/layout.tsx:
       - Adicionar import: `import { CwvCollector } from "@/components/cwv-collector";`
       - Inserir `<CwvCollector />` como primeiro filho dentro do `<NotificationProvider>` (antes do div shellStyle)

       O layout deve ficar assim:
       ```tsx
       import { CwvCollector } from "@/components/cwv-collector";
       // ... outros imports

       export default function VaultLayout({ children }: { children: React.ReactNode }) {
         return (
           <KeyboardShortcutsProvider>
             <NotificationProvider>
               <CwvCollector />
               <div style={shellStyle}>...</div>
             </NotificationProvider>
           </KeyboardShortcutsProvider>
         );
       }
       ```
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/cwv-collector.tsx existe e exporta `CwvCollector` com `"use client"`
    - CwvCollector usa `onLCP`, `onINP`, `onCLS`, `onTTFB`, `onFCP` de `web-vitals`
    - Métricas são bufferizadas e enviadas em batch para `/api/metrics` via `sendBeacon` ou `fetch`
    - Flush ocorre a cada 5 segundos de inatividade ou quando buffer atinge 5 itens
    - src/app/(vault)/layout.tsx importa e renderiza `<CwvCollector />`
    - `pnpm build` completa sem erros
  </acceptance_criteria>
  <done>Coletor CWV ativo em todas as páginas do vault, enviando métricas para /api/metrics em batch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/metrics | Untrusted CWV data from any client |
| /api/metrics → PostgreSQL | Metrics persisted without PII validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02-01 | Tampering | /api/metrics POST | mitigate | Endpoint accepts only numeric metrics with bounded fields; no authorization required (anonymous CWV is valid). workspaceId resolved from session when available. |
| T-27-02-02 | Denial of Service | /api/metrics POST | mitigate | Batch endpoint (array) reduces request count; no heavy computation. Rate limiting can be added in Phase 31 if needed. |
| T-27-02-03 | Information Disclosure | CwvCollector | accept | CWV metrics contain no PII — only timing data and page path. |
</threat_model>

<verification>
- `ANALYZE=true pnpm build` gera arquivos em `.next/analyze/` (client.html e server.html)
- `pnpm build` completa sem erros
- `pnpm test` passa (407 tests)
- TypeScript compilation sem erros
- Endpoint /api/metrics aceita POST com array de métricas e retorna 202
</verification>

<success_criteria>
- Bundle analyzer gera relatório HTML quando ANALYZE=true
- Endpoint /api/metrics persiste métricas CWV no banco PostgreSQL
- CwvCollector coleta LCP, INP, CLS, TTFB, FCP de todas as páginas do vault
- Métricas são enviadas em batch com pagePath e rating
- Domínio de métricas segue padrão repository (model → repository → repository.prisma → store)
</success_criteria>

<output>
After completion, create `.planning/phases/27-diagn-stico-e-funda-o-de-dados/27-02-SUMMARY.md`
</output>
