---
phase: 27-diagn-stico-e-funda-o-de-dados
plan: 03
type: execute
wave: 2
depends_on:
  - 27-01-PLAN.md
files_modified:
  - src/lib/db.ts
  - src/lib/patients/repository.ts
  - src/lib/patients/repository.prisma.ts
  - src/app/(vault)/actions/search.ts
  - src/lib/appointments/repository.ts
  - src/lib/appointments/repository.prisma.ts
  - src/lib/documents/repository.ts
  - src/lib/documents/repository.prisma.ts
  - src/lib/finance/repository.ts
  - src/lib/finance/repository.prisma.ts
autonomous: true
requirements:
  - PERF-05
  - PERF-06

must_haves:
  truths:
    - Queries Prisma com duração >500ms são logadas no console em desenvolvimento
    - Endpoint de search de pacientes usa database-level search com LIST_SELECT (não carrega todos os pacientes em memória)
    - importantObservations permanece excluído de todas as queries de listagem/search
    - EXPLAIN ANALYZE documenta o impacto dos novos índices nas queries críticas
  artifacts:
    - path: src/lib/db.ts
      provides: Prisma query logging extension
      contains: "$allOperations"
    - path: src/lib/patients/repository.ts
      provides: Contrato searchByName
      contains: "searchByName"
    - path: src/lib/patients/repository.prisma.ts
      provides: Implementação searchByName com LIST_SELECT
      contains: "searchByName"
  key_links:
    - from: src/lib/db.ts
      to: console.warn
      via: middleware $extends
      pattern: "duration > 500"
    - from: src/app/(vault)/actions/search.ts
      to: src/lib/patients/repository.prisma.ts
      via: patientRepo.searchByName
      pattern: "searchByName"
    - from: src/lib/patients/repository.prisma.ts
      to: LIST_SELECT
      via: select: LIST_SELECT
      pattern: "LIST_SELECT"
---

<objective>
Implementar observabilidade de queries lentas via Prisma extension e otimizar o endpoint de search de pacientes com database-level search usando o padrão LIST_SELECT. Documentar impacto dos índices via EXPLAIN ANALYZE.

Purpose: Identificar queries lentas durante desenvolvimento e reduzir carga de memória no search ao delegar filtragem para o banco.
Output: PrismaClient com query logging, método searchByName no repositório de pacientes, search action otimizado, documentação de query plans.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/27-diagn-stico-e-funda-o-de-dados/27-01-SUMMARY.md (Schema foundation — índices pushados)
@.planning/phases/27-diagn-stico-e-funda-o-de-dados/27-CONTEXT.md

## Interfaces relevantes do codebase

From src/lib/db.ts (PrismaClient singleton atual):
```typescript
import { PrismaClient, type Workspace } from "@prisma/client";

declare global {
  var __psivaultPrisma__: PrismaClient | undefined;
}

export const db =
  globalThis.__psivaultPrisma__ ??
  new PrismaClient({
    log: process.env.NODE_ENV === "development" ? ["warn", "error"] : ["error"],
  });

globalThis.__psivaultPrisma__ = db;
```

From src/lib/patients/repository.prisma.ts (LIST_SELECT pattern):
```typescript
const LIST_SELECT = Prisma.validator<Prisma.PatientSelect>()({
  id: true,
  workspaceId: true,
  fullName: true,
  socialName: true,
  email: true,
  phone: true,
  sessionPriceInCents: true,
  guardianName: true,
  guardianPhone: true,
  emergencyContactName: true,
  emergencyContactPhone: true,
  deletedAt: true,
  deletedByAccountId: true,
  createdAt: true,
  updatedAt: true,
});
```

From src/lib/patients/repository.ts (interface atual):
```typescript
export interface PatientRepository {
  save(patient: Patient): Promise<Patient>;
  findById(id: string, workspaceId: string): Promise<Patient | null>;
  listActive(workspaceId: string): Promise<Patient[]>;
  listArchived(workspaceId: string): Promise<Patient[]>;
  listAllByWorkspace(workspaceId: string): Promise<Patient[]>;
}
```

From src/app/(vault)/actions/search.ts (search action atual):
```typescript
export async function searchAllAction(query: string): Promise<SearchResultItem[]> {
  const { workspaceId } = await resolveSession();
  const patientRepo = getPatientRepository();
  // ...
  const [activePatients, archivedPatients] = await Promise.all([
    patientRepo.listActive(workspaceId),
    patientRepo.listArchived(workspaceId),
  ]);
  const allPatients = [...activePatients, ...archivedPatients];
  // ... carrega appointments, documents, charges
  return searchAll({ query, workspaceId, patients: allPatients, ... });
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar Prisma query logging extension em src/lib/db.ts</name>
  <files>src/lib/db.ts</files>
  <read_first>
    - src/lib/db.ts (state atual do PrismaClient)
    - src/lib/patients/repository.prisma.ts (exemplo de uso de db)
  </read_first>
  <action>
    Modificar src/lib/db.ts para estender o PrismaClient com middleware de query logging:

    ```typescript
    import { PrismaClient, type Workspace } from "@prisma/client";

    declare global {
      // eslint-disable-next-line no-var
      var __psivaultPrisma__: PrismaClient | undefined;
    }

    const prismaClient = new PrismaClient({
      log: process.env.NODE_ENV === "development" ? ["warn", "error"] : ["error"],
    });

    const extendedClient = prismaClient.$extends({
      query: {
        async $allOperations({ operation, model, args, query }) {
          const start = performance.now();
          const result = await query(args);
          const duration = performance.now() - start;

          if (process.env.NODE_ENV === "development" && duration > 500) {
            // eslint-disable-next-line no-console
            console.warn(
              `[Slow Query] ${model ?? "raw"}.${operation} took ${duration.toFixed(2)}ms`
            );
          }

          return result;
        },
      },
    });

    export const db =
      globalThis.__psivaultPrisma__ ?? extendedClient;

    globalThis.__psivaultPrisma__ = db;

    export function buildOwnedWorkspaceSelector(accountId: string, workspaceId: string) {
      return {
        id_ownerAccountId: {
          id: workspaceId,
          ownerAccountId: accountId,
        },
      } as const;
    }

    export function ownsWorkspace(
      workspace: Pick<Workspace, "id" | "ownerAccountId">,
      accountId: string,
      workspaceId: string,
    ) {
      return workspace.id === workspaceId && workspace.ownerAccountId === accountId;
    }
    ```

    IMPORTANTE: Preservar as funções `buildOwnedWorkspaceSelector` e `ownsWorkspace`. O `globalThis.__psivaultPrisma__` deve armazenar o client estendido, não o original.

    Nota sobre tipos: Em Prisma 6, `$extends` retorna um tipo compatível. Se TypeScript reportar erro de tipo no `export const db`, ajustar a declaração global para `var __psivaultPrisma__: typeof extendedClient | undefined`.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/db.ts cria `prismaClient` com `new PrismaClient(...)`
    - src/lib/db.ts chama `prismaClient.$extends({ query: { $allOperations(...) } })`
    - O middleware mede `performance.now()` antes e depois de `query(args)`
    - Queries com `duration > 500` em `NODE_ENV === "development"` logam `[Slow Query] model.operation took Xms`
    - `buildOwnedWorkspaceSelector` e `ownsWorkspace` permanecem exportados
    - `pnpm build` completa sem erros de TypeScript
  </acceptance_criteria>
  <done>PrismaClient estendido com middleware de query logging que identifica queries >500ms em desenvolvimento.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar searchByName ao PatientRepository com LIST_SELECT</name>
  <files>
    src/lib/patients/repository.ts
    src/lib/patients/repository.prisma.ts
  </files>
  <read_first>
    - src/lib/patients/repository.ts (interface atual)
    - src/lib/patients/repository.prisma.ts (implementação atual com LIST_SELECT)
    - src/lib/patients/store.ts (singleton pattern)
  </read_first>
  <action>
    1. Modificar src/lib/patients/repository.ts — adicionar método à interface:
       ```typescript
       export interface PatientRepository {
         save(patient: Patient): Promise<Patient>;
         findById(id: string, workspaceId: string): Promise<Patient | null>;
         listActive(workspaceId: string): Promise<Patient[]>;
         listArchived(workspaceId: string): Promise<Patient[]>;
         listAllByWorkspace(workspaceId: string): Promise<Patient[]>;
         /** Search patients by name, social name, email, or phone. Excludes sensitive fields. */
         searchByName(workspaceId: string, query: string): Promise<Patient[]>;
       }
       ```

    2. Modificar src/lib/patients/repository.prisma.ts — adicionar implementação:
       No objeto retornado por `createPrismaPatientRepository()`, após `listAllByWorkspace`, adicionar:
       ```typescript
       async searchByName(workspaceId: string, query: string): Promise<Patient[]> {
         const q = query.trim();
         if (!q) return [];

         const patients = await db.patient.findMany({
           where: {
             workspaceId,
             deletedAt: null,
             OR: [
               { fullName: { contains: q, mode: "insensitive" } },
               { socialName: { contains: q, mode: "insensitive" } },
               { email: { contains: q, mode: "insensitive" } },
               { phone: { contains: q, mode: "insensitive" } },
             ],
           },
           orderBy: { fullName: "asc" },
           select: LIST_SELECT,
         });

         return patients.map(mapListToDomain);
       }
       ```

    3. Adicionar `searchByName` ao repositório in-memory em src/lib/patients/repository.ts:
       No objeto retornado por `createInMemoryPatientRepository()`, adicionar:
       ```typescript
       async searchByName(workspaceId, query) {
         const q = query.trim().toLowerCase();
         if (!q) return [];
         return [...store.values()]
           .filter((p) =>
             p.workspaceId === workspaceId &&
             p.archivedAt === null &&
             (
               p.fullName.toLowerCase().includes(q) ||
               p.socialName?.toLowerCase().includes(q) ||
               p.email?.toLowerCase().includes(q) ||
               p.phone?.toLowerCase().includes(q)
             )
           )
           .sort((a, b) => a.fullName.localeCompare(b.fullName, "pt-BR"))
           .map((p) => ({ ...p, importantObservations: null }));
       }
       ```
  </action>
  <verify>
    <automated>pnpm test -- --filter=patients</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/patients/repository.ts contém `searchByName(workspaceId: string, query: string): Promise<Patient[]>` na interface
    - src/lib/patients/repository.ts contém implementação `searchByName` no repositório in-memory
    - src/lib/patients/repository.prisma.ts contém implementação `searchByName` que usa `db.patient.findMany` com `select: LIST_SELECT`
    - A query Prisma usa `mode: "insensitive"` em todos os campos OR
    - `importantObservations` não aparece no select (confirmar via `LIST_SELECT` não contendo `importantObservations`)
    - `pnpm test` passa
  </acceptance_criteria>
  <done>PatientRepository estendido com searchByName que executa database-level search excluindo campos sensíveis.</done>
</task>

<task type="auto">
  <name>Task 3: Otimizar search action e documentar query plans com EXPLAIN ANALYZE</name>
  <files>src/app/(vault)/actions/search.ts</files>
  <read_first>
    - src/app/(vault)/actions/search.ts (implementação atual)
    - src/lib/patients/repository.ts (nova interface com searchByName)
  </read_first>
  <action>
    1. Modificar src/app/(vault)/actions/search.ts para usar `searchByName` em vez de carregar todos os pacientes:

       Substituir:
       ```typescript
       // Load all patients (active + archived)
       const [activePatients, archivedPatients] = await Promise.all([
         patientRepo.listActive(workspaceId),
         patientRepo.listArchived(workspaceId),
       ]);
       const allPatients = [...activePatients, ...archivedPatients];
       ```

       Por:
       ```typescript
       // Load matching patients via database search (PERF-06)
       const allPatients = await patientRepo.searchByName(workspaceId, query);
       ```

       IMPORTANTE: Preservar o restante da função (carregamento de appointments, documents, charges e chamada a `searchAll`).

    2. Documentar query plans com EXPLAIN ANALYZE:
       Criar arquivo `src/lib/db/QUERY_PLANS.md` com a análise das queries críticas:

       ```markdown
       # Query Plans — Phase 27 Index Impact

       Run these queries in Prisma Studio or psql to verify index usage:

       ## 1. Appointment list by workspace + status + date
       ```sql
       EXPLAIN ANALYZE
       SELECT * FROM appointments
       WHERE workspace_id = '<workspace-id>'
         AND status = 'SCHEDULED'
         AND starts_at > NOW()
       ORDER BY starts_at;
       ```
       Expected: Index Scan using `appointments_workspaceId_status_startsAt_idx`

       ## 2. SessionCharge list by workspace + status + createdAt
       ```sql
       EXPLAIN ANALYZE
       SELECT * FROM session_charges
       WHERE workspace_id = '<workspace-id>'
         AND status = 'pendente'
         AND created_at > NOW() - INTERVAL '30 days'
       ORDER BY created_at;
       ```
       Expected: Index Scan using `session_charges_workspaceId_status_createdAt_idx`

       ## 3. Patient search by name (case-insensitive)
       ```sql
       EXPLAIN ANALYZE
       SELECT id, workspace_id, full_name, social_name, email, phone,
              session_price_in_cents, guardian_name, guardian_phone,
              emergency_contact_name, emergency_contact_phone,
              deleted_at, deleted_by_account_id, created_at, updated_at
       FROM patients
       WHERE workspace_id = '<workspace-id>'
         AND deleted_at IS NULL
         AND (
           full_name ILIKE '%query%'
           OR social_name ILIKE '%query%'
           OR email ILIKE '%query%'
           OR phone ILIKE '%query%'
         )
       ORDER BY full_name;
       ```
       Expected: Index Scan using `patients_workspaceId_deletedAt_idx` (existing) with BitmapOr on text search
       ```

       Nota: Como o executor pode não ter acesso direto ao banco de produção, este arquivo serve como documentação das queries a serem verificadas manualmente pelo desenvolvedor.
  </action>
  <verify>
    <automated>pnpm test && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(vault)/actions/search.ts usa `patientRepo.searchByName(workspaceId, query)` em vez de `listActive + listArchived`
    - `searchAllAction` não carrega mais todos os pacientes ativos e arquivados em memória
    - `src/lib/db/QUERY_PLANS.md` existe e contém 3 queries EXPLAIN ANALYZE documentadas
    - QUERY_PLANS.md menciona os índices esperados por nome
    - `pnpm test` passa
    - `pnpm build` completa sem erros
  </acceptance_criteria>
  <done>Search action otimizado para usar database-level search e query plans documentados para validação manual.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| src/lib/db.ts → console | Query logs may expose query structure in dev |
| search action → patient data | Search results must exclude sensitive fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03-01 | Information Disclosure | Query logging | accept | Logs only in development (NODE_ENV check). No query parameters logged — only model, operation, and duration. |
| T-27-03-02 | Information Disclosure | searchByName | mitigate | LIST_SELECT explicitly excludes importantObservations. Mode insensitive search does not bypass workspace scoping. |
| T-27-03-03 | Denial of Service | searchByName with short query | mitigate | Empty/whitespace query returns [] immediately without hitting database. |
</threat_model>

<verification>
- `pnpm test` passa (407 tests)
- `pnpm build` completa sem erros
- Query logging middleware compila sem erros de tipo
- searchByName existe em repository.ts, repository.prisma.ts e in-memory implementation
- searchAllAction usa searchByName (não listActive + listArchived)
- QUERY_PLANS.md documenta EXPLAIN ANALYZE para 3 queries críticas
</verification>

<success_criteria>
- PrismaClient loga queries >500ms no console em desenvolvimento
- PatientRepository tem método searchByName com database-level LIKE search
- searchByName usa LIST_SELECT (importantObservations excluído)
- searchAllAction usa searchByName em vez de carregar todos os pacientes em memória
- Documentação de query plans criada em src/lib/db/QUERY_PLANS.md
</success_criteria>

<output>
After completion, create `.planning/phases/27-diagn-stico-e-funda-o-de-dados/27-03-SUMMARY.md`
</output>
