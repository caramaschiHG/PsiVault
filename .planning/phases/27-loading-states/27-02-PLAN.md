# Phase 27-02: Migrar Todos os loading.tsx para `<Skeleton>` + Adicionar Faltantes

## Goal
Unificar todos os loading states sob o componente `<Skeleton>` e adicionar loading.tsx para rotas que não têm.

## Tasks

### Task 1: Migrar loading.tsx existentes (7 arquivos)
Arquivos a migrar de inline styles para `<Skeleton>`:
- `src/app/(vault)/agenda/loading.tsx` — usa skeleton-pulse inline
- `src/app/(vault)/inicio/loading.tsx` — já usa shimmer inline
- `src/app/(vault)/patients/loading.tsx` — já usa shimmer inline
- `src/app/(vault)/patients/[patientId]/loading.tsx` — usa skeleton-pulse className
- `src/app/(vault)/financeiro/loading.tsx` — já usa shimmer inline
- `src/app/(vault)/prontuario/loading.tsx` — usa skeleton-pulse className
- `src/app/(vault)/prontuario/[patientId]/loading.tsx` — usa skeleton-pulse className

Para cada um: substituir divs com style inline por `<Skeleton>` com props adequadas.

### Task 2: Criar loading.tsx faltantes
- `src/app/(vault)/settings/loading.tsx` — skeleton simples com card de perfil
- `src/app/(vault)/settings/profile/loading.tsx` — skeleton de formulário de perfil
- `src/app/(vault)/settings/dados-e-privacidade/loading.tsx` — skeleton de formulário
- `src/app/(vault)/vault-plus/loading.tsx` — skeleton de card de apresentação

### Task 3: Verificar consistência visual
- Todos os skeletons devem ter delays escalonados (staggered)
- Estrutura deve espelhar a página real (cards, listas, headings)

## Files Changed
- 7 loading.tsx existentes (edit)
- 4 loading.tsx novos (new)
- Total: 11 files

## Files
- 11 files
