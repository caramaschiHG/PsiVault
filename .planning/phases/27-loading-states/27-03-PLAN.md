# Phase 27-03: Limpar CSS Morto

## Goal
Remover keyframes e classes CSS que não são usados em nenhum lugar do produto.

## Tasks

### Task 1: Identificar CSS morto
Keyframes confirmados como não usados:
- `@keyframes auth-progress-bar` — definido em globals.css, nenhum uso encontrado
- `@keyframes progress-slide` — definido em globals.css, nenhum uso encontrado

### Task 2: Remover skeleton-pulse
Após migração de todos os loading.tsx para shimmer (Phase 27-02):
- Remover `@keyframes skeleton-pulse` de globals.css
- Buscar por qualquer `skeleton-pulse` restante no código e migrar

### Task 3: Buscar outros CSS morto
- Verificar se há classes definidas mas nunca usadas
- Verificar se há variáveis CSS órfãs

### Task 4: Verificar `prefers-reduced-motion`
- Confirmar que o bloco `@media (prefers-reduced-motion: reduce)` continua funcional
- Testar que todas as animações são reduzidas corretamente

## Files Changed
- `src/app/globals.css` (edit — remover keyframes mortos)

## Files
- 1 file
