---
phase: 28-streaming-suspense
plan: 04
type: execute
wave: 3
depends_on:
  - 28-02
  - 28-03
files_modified:
  - tests/streaming/integration.test.tsx
autonomous: false
requirements:
  - STREAM-01
  - STREAM-03
  - STREAM-05
must_haves:
  truths:
    - Skeletons renderizam e são substituídos por conteúdo sem layout shift
    - Charts transicionam suavemente de skeleton para dados
    - Seções com erro não quebram a página inteira
  artifacts:
    - path: tests/streaming/integration.test.tsx
      provides: Teste de integração do padrão AsyncBoundary + async section
      exports: []
  key_links:
    - from: integration.test.tsx
      to: AsyncBoundary
      via: render com MockSection async
      pattern: "<AsyncBoundary fallback={<SectionSkeleton>}><MockSection /></AsyncBoundary>"
---

<objective>
Validar o comportamento de streaming end-to-end com teste de integração e verificação humana de qualidade visual.

Purpose: Garantir que a implementação técnica se traduz em experiência perceptível para o usuário — sem layout shift, com transições suaves.
Output: 1 arquivo de teste + relatório de verificação visual.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/28-streaming-suspense/28-CONTEXT.md
@.planning/phases/28-streaming-suspense/28-02-PLAN.md
@.planning/phases/28-streaming-suspense/28-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration smoke test for streaming pattern</name>
  <files>tests/streaming/integration.test.tsx</files>
  <action>
Create `tests/streaming/integration.test.tsx`:

```typescript
import { describe, it, expect } from "vitest";
import { render, screen } from "@testing-library/react";
import { Suspense } from "react";
import { AsyncBoundary } from "@/components/streaming/async-boundary";
import { SectionSkeleton } from "@/components/streaming/section-skeleton";
import { ChartSkeleton } from "@/components/streaming/chart-skeleton";
import { StatCardSkeleton } from "@/components/streaming/stat-card-skeleton";

// Mock async section
async function MockAsyncSection() {
  // Simulate async work
  await new Promise((resolve) => setTimeout(resolve, 10));
  return <div data-testid="async-section">Section loaded</div>;
}

// Mock error section
async function MockErrorSection() {
  throw new Error("Section failed");
}

describe("Streaming integration", () => {
  it("renders async section through AsyncBoundary with skeleton fallback", async () => {
    render(
      <AsyncBoundary fallback={<div data-testid="section-skeleton">Skeleton</div>}>
        <MockAsyncSection />
      </AsyncBoundary>
    );
    // Should eventually show content
    expect(await screen.findByTestId("async-section")).toHaveTextContent("Section loaded");
  });

  it("renders ChartSkeleton with correct structure", () => {
    render(<ChartSkeleton height={120} barCount={6} />);
    const skeleton = screen.getByRole("generic"); // or query by class
    expect(skeleton).toBeInTheDocument();
  });

  it("renders StatCardSkeleton with label and value placeholders", () => {
    render(<StatCardSkeleton />);
    // ChartSkeleton renders 2 skeleton divs inside
    const skeletons = document.querySelectorAll(".skeleton-shimmer");
    expect(skeletons.length).toBeGreaterThanOrEqual(2);
  });

  it("catches section error with AsyncBoundary and shows retry", async () => {
    render(
      <AsyncBoundary fallback={<div>Loading</div>}>
        <MockErrorSection />
      </AsyncBoundary>
    );
    expect(await screen.findByText(/Erro ao carregar esta seção/i)).toBeInTheDocument();
    expect(screen.getByRole("button", { name: /Tentar novamente/i })).toBeInTheDocument();
  });

  it("all skeleton components have skeleton-shimmer class", () => {
    render(
      <>
        <SectionSkeleton />
        <StatCardSkeleton />
        <ChartSkeleton />
      </>
    );
    const shimmerElements = document.querySelectorAll(".skeleton-shimmer");
    expect(shimmerElements.length).toBeGreaterThanOrEqual(5);
  });
});
```

Adjust selectors if needed to match actual DOM output of skeleton components. The key assertions are:
1. AsyncBoundary renders async content after resolution
2. ChartSkeleton has the expected bar count
3. Error boundaries catch errors and show retry
4. All skeletons use the `skeleton-shimmer` class for consistent styling
  </action>
  <verify>
    <automated>pnpm test -- tests/streaming/integration.test.tsx</automated>
  </verify>
  <done>Integration tests pass. All 4+ test cases succeed.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Visual verification of streaming and zero CLS</name>
  <files></files>
  <action>
Human verifies the streaming implementation visually and functionally. No automated action — this is a pure verification checkpoint.
  </action>
  <what-built>
/financeiro e /inicio foram refatorados com Suspense granular. Cada seção renderiza independentemente com skeleton correspondente. RemindersSection usa React 19 use API. Charts têm transição suave.
  </what-built>
  <how-to-verify>
1. **Navegue para `/financeiro`**:
   - Observe se o skeleton aparece IMEDIATAMENTE (sem tela em branco)
   - Verifique se o layout do skeleton corresponde à página final (header, cards, chart, lista, seções)
   - Observe se o chart aparece suavemente (fade-in) após o skeleton
   - Verifique se há layout shift (elementos pulando) quando as seções carregam

2. **Navegue para `/inicio`**:
   - Observe se cada seção (Hoje, Lembretes, Resumo, A receber) aparece progressivamente
   - Verifique se o skeleton de /inicio corresponde à estrutura final
   - Teste se a seção de Lembretes funciona normalmente (adicionar, concluir)

3. **Teste de erro**:
   - Verifique se uma seção com erro (simule desconectando a rede brevemente ou force um erro) não quebra a página inteira

4. **Métrica visual**:
   - A página deve parecer "mais rápida" porque o conteúdo principal aparece antes das seções analíticas
  </how-to-verify>
  <verify>
    <automated>N/A — human verification checkpoint</automated>
  </verify>
  <done>Human confirms zero CLS, smooth transitions, progressive rendering, and no functional regressions.</done>
  <resume-signal>Digite "approved" se a experiência visual está correta, ou descreva issues encontradas</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human verification | Visual/functional confirmation of streaming behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-08 | Repudiation | Visual verification | accept | Human checkpoint captures subjective quality. No security impact. |
</threat_model>

<verification>
- `pnpm test -- tests/streaming/integration.test.tsx` passes
- Human confirms zero visible layout shift on /financeiro and /inicio
- Human confirms smooth chart transition
- Human confirms error boundaries work section-by-section
</verification>

<success_criteria>
- Integration tests validate AsyncBoundary + skeleton rendering + error handling
- Human verification confirms immediate skeleton display, progressive rendering, zero CLS, and smooth chart transitions
- No regressions in existing functionality (charge list filters, reminder CRUD, etc.)
</success_criteria>

<output>
After completion, create `.planning/phases/28-streaming-suspense/28-04-SUMMARY.md`
</output>
