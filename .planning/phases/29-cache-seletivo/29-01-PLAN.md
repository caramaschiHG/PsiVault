---
phase: 29-cache-seletivo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/cache/tags.ts
  - src/lib/setup/profile.ts
  - src/lib/expense-categories/repository.prisma.ts
autonomous: true
requirements:
  - CACHE-01
  - CACHE-03
must_haves:
  truths:
    - "Practice profile carrega do cache em visitas repetidas ao mesmo workspace"
    - "Categorias de despesa ativas carregam do cache em visitas repetidas"
    - "Nenhuma chave de cache é gerada sem workspaceId como argumento"
  artifacts:
    - path: "src/lib/cache/tags.ts"
      provides: "Constantes de cache tags e key builder"
      exports: ["CACHE_TAGS", "buildCacheKey"]
    - path: "src/lib/setup/profile.ts"
      provides: "Practice profile com unstable_cache wrapper"
      contains: "unstable_cache"
    - path: "src/lib/expense-categories/repository.prisma.ts"
      provides: "Expense categories com unstable_cache wrapper"
      contains: "unstable_cache"
  key_links:
    - from: "src/lib/setup/profile.ts"
      to: "db.practiceProfile.findUnique"
      via: "unstable_cache wrapper"
      pattern: "unstable_cache.*async.*workspaceId"
    - from: "src/lib/expense-categories/repository.prisma.ts"
      to: "db.expenseCategory.findMany"
      via: "unstable_cache wrapper"
      pattern: "unstable_cache.*async.*workspaceId"
---

<objective>
Implementar cache cross-request para dados de leitura intensa: practice profile e categorias de despesa. Garantir que todas as chaves de cache incluam workspaceId para prevenir vazamento cross-tenant.

Purpose: Reduzir queries redundantes ao banco em dados que raramente mudam mas são lidos em quase toda navegação.
Output: Tags de cache centralizadas, practice profile cacheado, expense categories cacheadas.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-cache-seletivo/29-CONTEXT.md

## Interfaces relevantes

De `src/lib/setup/profile.ts`:
```typescript
export async function getPracticeProfileSnapshot(
  _accountId?: string,
  workspaceId?: string,
) {
  if (!workspaceId) {
    return { ...DEFAULT_PROFILE_SNAPSHOT };
  }
  const profile = await db.practiceProfile.findUnique({
    where: { workspaceId },
    include: { signatureAsset: true },
  });
  return mapProfileRowToSnapshot(profile);
}
```

De `src/lib/expense-categories/repository.prisma.ts`:
```typescript
export function createPrismaExpenseCategoryRepository(): ExpenseCategoryRepository {
  return {
    async findById(workspaceId: string, id: string): Promise<ExpenseCategory | null> { ... },
    async findByWorkspace(workspaceId: string): Promise<ExpenseCategory[]> { ... },
    async findActiveByWorkspace(workspaceId: string): Promise<ExpenseCategory[]> { ... },
    async create(category: ExpenseCategory): Promise<ExpenseCategory> { ... },
    async update(workspaceId: string, id: string, patch: Partial<ExpenseCategory>): Promise<ExpenseCategory> { ... },
    async softDelete(workspaceId: string, id: string, actorId: string, now: Date): Promise<void> { ... },
  };
}
```

De `src/lib/expense-categories/repository.ts`:
```typescript
export interface ExpenseCategoryRepository {
  findById(workspaceId: string, id: string): Promise<ExpenseCategory | null>;
  findByWorkspace(workspaceId: string): Promise<ExpenseCategory[]>;
  findActiveByWorkspace(workspaceId: string): Promise<ExpenseCategory[]>;
  create(category: ExpenseCategory): Promise<ExpenseCategory>;
  update(workspaceId: string, id: string, patch: Partial<ExpenseCategory>): Promise<ExpenseCategory>;
  softDelete(workspaceId: string, id: string, actorId: string, now: Date): Promise<void>;
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar central de tags de cache</name>
  <files>src/lib/cache/tags.ts</files>
  <action>Criar `src/lib/cache/tags.ts` com:
1. Constante `CACHE_TAGS` exportada contendo tags de domínio:
   - `practiceProfile: "practice-profile"`
   - `expenseCategories: "expense-categories"`
   - `expenses: "expenses"`
   - `financeCharges: "finance-charges"`
   - `appointments: "appointments"`
   - `patients: "patients"`
   - `reminders: "reminders"`
   - `notifications: "notifications"`
2. Função auxiliar `buildCacheKey(domain: string, workspaceId: string, ...parts: string[]): string[]` que retorna array no formato `['domain', workspaceId, ...parts]` para uso como key prefix em `unstable_cache`.
3. Documentar no topo do arquivo que workspaceId é obrigatório em toda chave — violação = risco de cross-tenant leak.

Não criar testes para este arquivo — é puramente configuração.</action>
  <verify>
    <automated>grep -n "CACHE_TAGS\|buildCacheKey\|workspaceId" src/lib/cache/tags.ts | head -20</automated>
  </verify>
  <done>Arquivo existe, exporta CACHE_TAGS e buildCacheKey, workspaceId aparece em toda assinatura de key</done>
</task>

<task type="auto">
  <name>Task 2: Cachear practice profile com unstable_cache</name>
  <files>src/lib/setup/profile.ts</files>
  <action>Modificar `src/lib/setup/profile.ts` para cachear o acesso ao practice profile:

1. Adicionar import: `import { unstable_cache } from "next/cache";`
2. Criar a função cacheada privada `_getCachedPracticeProfile` usando `unstable_cache`:
   - Função recebe `workspaceId: string`
   - Executa `db.practiceProfile.findUnique({ where: { workspaceId }, include: { signatureAsset: true } })`
   - Retorna `mapProfileRowToSnapshot(profile)`
   - Key prefix: `buildCacheKey('practice-profile', workspaceId)` (importar de `src/lib/cache/tags.ts`)
   - Options: `{ revalidate: 3600, tags: [CACHE_TAGS.practiceProfile] }`
3. Modificar `getPracticeProfileSnapshot` para chamar `_getCachedPracticeProfile(workspaceId)` quando `workspaceId` está definido, mantendo o early return para `!workspaceId`.

**CRITICAL**: A função cacheada deve receber `workspaceId` como argumento posicional. O `unstable_cache` serializa os argumentos da função na chave de cache — sem isso, o cache seria compartilhado cross-tenant.

Não alterar assinatura pública de `getPracticeProfileSnapshot` para não quebrar callers existentes.</action>
  <verify>
    <automated>grep -n "unstable_cache\|_getCachedPracticeProfile\|CACHE_TAGS" src/lib/setup/profile.ts</automated>
  </verify>
  <done>getPracticeProfileSnapshot chama função cacheada quando workspaceId existe; workspaceId é argumento da função cacheada; revalidate = 3600</done>
</task>

<task type="auto">
  <name>Task 3: Cachear expense categories com unstable_cache</name>
  <files>src/lib/expense-categories/repository.prisma.ts</files>
  <action>Modificar `src/lib/expense-categories/repository.prisma.ts` para cachear métodos de leitura:

1. Adicionar imports: `import { unstable_cache } from "next/cache";` e `import { CACHE_TAGS, buildCacheKey } from "../cache/tags";`
2. Dentro de `createPrismaExpenseCategoryRepository()`, criar funções cacheadas:
   - `cachedFindByWorkspace = unstable_cache(async (workspaceId) => { ...existing findByWorkspace logic... }, ['expense-categories'], { revalidate: 3600, tags: [CACHE_TAGS.expenseCategories] })`
   - `cachedFindActiveByWorkspace = unstable_cache(async (workspaceId) => { ...existing findActiveByWorkspace logic... }, ['expense-categories', 'active'], { revalidate: 3600, tags: [CACHE_TAGS.expenseCategories] })`
3. Substituir implementações de `findByWorkspace` e `findActiveByWorkspace` no objeto retornado para delegar às funções cacheadas: `findByWorkspace: (workspaceId) => cachedFindByWorkspace(workspaceId)`, etc.
4. Manter `findById`, `create`, `update`, `softDelete` sem cache — são mutations ou lookups pontuais.

**CRITICAL**: As funções cacheadas devem receber `workspaceId` como primeiro argumento. O `unstable_cache` inclui os argumentos serializados na chave de cache. Isso garante CACHE-03 (workspaceId na chave).</action>
  <verify>
    <automated>grep -n "unstable_cache\|cachedFindByWorkspace\|cachedFindActiveByWorkspace" src/lib/expense-categories/repository.prisma.ts</automated>
  </verify>
  <done>findByWorkspace e findActiveByWorkspace delegam para unstable_cache; workspaceId é argumento das funções cacheadas; métodos de mutação não foram cacheados</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Component → unstable_cache | Dados de tenant A não devem vazar para tenant B via cache compartilhado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Information Disclosure | `unstable_cache` sem workspaceId na chave | mitigate | workspaceId é argumento obrigatório da função cacheada; unstable_cache serializa args na chave |
| T-29-02 | Information Disclosure | `unstable_cache` com workspaceId undefined | mitigate | Guard `if (!workspaceId)` retorna default snapshot antes de chamar cache; nunca chama cache com workspaceId vazio |
| T-29-03 | Tampering | Cache poisoning via tag invalidation | accept | Tags são estáticas por domínio; over-invalidation é preferível a under-invalidation nesta escala |
</threat_model>

<verification>
1. `grep -r "unstable_cache" src/lib/setup/profile.ts src/lib/expense-categories/repository.prisma.ts` — confirma presença
2. `grep -n "workspaceId" src/lib/cache/tags.ts` — confirma que key builder exige workspaceId
3. Build passa: `pnpm build` sem TypeScript errors
4. Testes passam: `pnpm test` — 407 tests existentes não quebram
</verification>

<success_criteria>
- Practice profile é servido do cache em segundo acesso ao mesmo workspace (verificar via query logging ou teste de integração)
- Expense categories ativas são servidas do cache em segundo acesso
- workspaceId aparece como argumento em toda função envolvida com `unstable_cache`
- Nenhum `unstable_cache` é aplicado a método de mutação (create/update/delete)
</success_criteria>

<output>
After completion, create `.planning/phases/29-cache-seletivo/29-01-SUMMARY.md`
</output>
