---
phase: 29-cache-seletivo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(vault)/setup/actions.ts
  - src/app/(vault)/settings/notificacoes/actions.ts
  - src/app/(vault)/appointments/actions.ts
  - src/app/(vault)/actions/reminders.ts
autonomous: true
requirements:
  - CACHE-04
must_haves:
  truths:
    - "Zero chamadas bare revalidatePath('/path') permanecem na codebase"
    - "Toda chamada revalidatePath especifica 'page' como segundo argumento"
    - "Chamadas condicionais (if (...) revalidatePath(...)) também usam 'page'"
  artifacts:
    - path: "src/app/(vault)/setup/actions.ts"
      provides: "3 revalidatePath calls com escopo 'page'"
      contains: "revalidatePath(\"/settings/profile\", \"page\")"
    - path: "src/app/(vault)/settings/notificacoes/actions.ts"
      provides: "1 revalidatePath call com escopo 'page'"
      contains: "revalidatePath(\"/settings/notificacoes\", \"page\")"
    - path: "src/app/(vault)/appointments/actions.ts"
      provides: "13 revalidatePath calls com escopo 'page'"
      contains: "revalidatePath(\"/agenda\", \"page\")"
    - path: "src/app/(vault)/actions/reminders.ts"
      provides: "4 revalidatePath calls com escopo 'page'"
      contains: "revalidatePath(\"/inicio\", \"page\")"
  key_links:
    - from: "grep revalidatePath"
      to: "zero matches without 'page'"
      via: "audit automatizado"
      pattern: "revalidatePath\\([^,]+\\);"
---

<objective>
Auditar e corrigir TODAS as chamadas revalidatePath na codebase que nao especificam escopo 'page', prevenindo revalidacao desnecessaria de layouts compartilhados (cache stampede).

Purpose: Em v1.3, 13 chamadas em /financeiro foram corrigidas. Restam chamadas bare em setup, notificacoes, agenda e lembretes. O escopo default de revalidatePath revalida o layout — em rotas aninhadas do vault, isso revalida o layout raiz em toda mutacao.
Output: Zero chamadas bare revalidatePath na codebase vault.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-cache-seletivo/29-CONTEXT.md

## Estado atual das chamadas revalidatePath

Arquivos com chamadas bare (sem segundo argumento):
- src/app/(vault)/setup/actions.ts — 3 chamadas: revalidatePath("/settings/profile")
- src/app/(vault)/settings/notificacoes/actions.ts — 1 chamada: revalidatePath("/settings/notificacoes")
- src/app/(vault)/appointments/actions.ts — 13 chamadas: revalidatePath("/agenda") (8x), revalidatePath com template string para /patients/ (4x), revalidatePath com template string para /sessions/.../note (1x)
- src/app/(vault)/actions/reminders.ts — 4 chamadas: revalidatePath("/inicio") (2x), revalidatePath com template string para /patients/ (2x)

Arquivos ja corretos (nao modificar):
- src/app/(vault)/financeiro/actions.ts — 13 chamadas ja usam "page"
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auditar todas as chamadas revalidatePath</name>
  <files>src/app/(vault)</files>
  <action>Executar audit completo de todas as chamadas revalidatePath em src/app/(vault):

1. Rodar: grep -rn "revalidatePath" src/app/(vault) --include="*.ts"
2. Para cada match, classificar:
   - Ja com "page": manter
   - Bare (sem segundo argumento): marcar para correcao
   - Com outro escopo (ex: "layout"): flag para revisao
3. Documentar contagem por arquivo no SUMMARY.

Nao modificar arquivos nesta task — apenas coletar dados.</action>
  <verify>
    <automated>grep -rn "revalidatePath" src/app/(vault) --include="*.ts" | grep -v '"page"' | grep -v "from \"next/cache\"" | wc -l</automated>
  </verify>
  <done>Lista completa de chamadas bare identificada com arquivo e numero de linha</done>
</task>

<task type="auto">
  <name>Task 2: Corrigir escopo em todas as chamadas bare</name>
  <files>src/app/(vault)/setup/actions.ts, src/app/(vault)/settings/notificacoes/actions.ts, src/app/(vault)/appointments/actions.ts, src/app/(vault)/actions/reminders.ts</files>
  <action>Adicionar "page" como segundo argumento em TODAS as chamadas bare revalidatePath:

1. setup/actions.ts:
   - revalidatePath("/settings/profile") -> revalidatePath("/settings/profile", "page") (3 ocorrencias)

2. settings/notificacoes/actions.ts:
   - revalidatePath("/settings/notificacoes") -> revalidatePath("/settings/notificacoes", "page") (1 ocorrencia)

3. appointments/actions.ts:
   - revalidatePath("/agenda") -> revalidatePath("/agenda", "page") (8 ocorrencias)
   - revalidatePath com template string para /patients/ -> adicionar "page" como segundo arg (4 ocorrencias condicionais)
   - revalidatePath com template string para /sessions/.../note -> adicionar "page" como segundo arg (1 ocorrencia)
   
   Atencao nas chamadas condicionais: if (patientIdForRevalidate) revalidatePath(...) deve ficar if (patientIdForRevalidate) revalidatePath(..., "page").

4. actions/reminders.ts:
   - revalidatePath("/inicio") -> revalidatePath("/inicio", "page") (2 ocorrencias)
   - revalidatePath com template string para /patients/ -> adicionar "page" como segundo arg (2 ocorrencias condicionais)

IMPORTANTE: Nao modificar src/app/(vault)/financeiro/actions.ts — ja esta correto.</action>
  <verify>
    <automated>grep -rn "revalidatePath" src/app/(vault) --include="*.ts" | grep -v '"page"' | grep -v "from \"next/cache\"" | wc -l</automated>
  </verify>
  <done>Zero chamadas bare revalidatePath permanecem em src/app/(vault); todas incluem "page" como segundo argumento</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mutation -> revalidatePath | Escopo incorreto causa revalidacao em excesso de layouts compartilhados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04 | Denial of Service | revalidatePath sem escopo | mitigate | Escopo "page" limita revalidacao a pagina, nao propaga ao layout raiz; evita cache stampede sob carga |
| T-29-05 | Information Disclosure | revalidatePath("/agenda") revalida layout | accept | Layout raiz usa resolveSession() que eh dinamico por request; revalidacao de layout nao serve dados de outro tenant, apenas recria o cache do layout |
</threat_model>

<verification>
1. grep -rn 'revalidatePath' src/app/(vault) --include='*.ts' | grep -v '"page"' | grep -v 'from "next/cache"' — deve retornar vazio
2. Build passa: pnpm build sem TypeScript errors
3. Testes passam: pnpm test — 407 tests existentes nao quebram
</verification>

<success_criteria>
- Zero ocorrencias de revalidatePath("/path") sem segundo argumento em todo src/app/(vault)
- Todas as chamadas condicionais (if (...) revalidatePath(...)) tambem incluem "page"
- Nenhuma chamada em financeiro/actions.ts foi alterada (ja estava correta)
</success_criteria>

<output>
After completion, create .planning/phases/29-cache-seletivo/29-02-SUMMARY.md
</output>
