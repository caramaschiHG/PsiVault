---
phase: 30-otimiza-o-de-assets-e-bundle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(vault)/agenda/page.tsx
  - src/app/(vault)/settings/profile/components/signature-crop-modal.tsx
  - src/app/(vault)/patients/[patientId]/documents/[documentId]/page.tsx
  - next.config.ts
autonomous: true
requirements:
  - ASSET-01
  - ASSET-02
  - ASSET-03
  - ASSET-04
  - ASSET-05
must_haves:
  truths:
    - "react-day-picker e @dnd-kit não são mais carregados no bundle inicial da agenda"
    - "Todas as tags <img> raw na codebase possuem comentário documentando a exceção"
    - "optimizePackageImports em next.config.ts cobre date-fns, react-day-picker, @dnd-kit/core e @dnd-kit/utilities"
    - "web-vitals permanece no bundle inicial (~3KB gzipped)"
  artifacts:
    - path: "src/app/(vault)/agenda/page.tsx"
      provides: "Lazy loading de MiniCalendar, CalendarGrid e WeekCalendarGrid"
      contains: "next/dynamic"
    - path: "src/app/(vault)/settings/profile/components/signature-crop-modal.tsx"
      provides: "Justificativa documentada para raw <img> de object URLs"
      contains: "object URL efêmero"
    - path: "src/app/(vault)/patients/[patientId]/documents/[documentId]/page.tsx"
      provides: "Justificativa documentada para raw <img> de signed URLs"
      contains: "signed URL do Supabase"
  key_links:
    - from: "src/app/(vault)/agenda/page.tsx"
      to: "next/dynamic"
      via: "import dynamic from 'next/dynamic'"
      pattern: "dynamic\\(\\(\\) => import"
---

<objective>
Reduzir bytes carregados no bundle inicial da aplicação através de lazy loading de componentes pesados da agenda e documentar exceções de assets que não se beneficiam de otimização automática.

Purpose: Cumprir os critérios de sucesso da Phase 30 — carregar menos bytes e renderizar assets de forma eficiente — sem reduzir funcionalidade.
Output: Componentes da agenda carregando sob demanda, exceções de <img> raw documentadas, configuração de bundle verificada.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-otimiza-o-de-assets-e-bundle/30-CONTEXT.md

## Interfaces e Patterns do Código

De `src/lib/documents/pdf.tsx` — pattern de lazy loading a replicar:
```typescript
let pdfModulePromise: Promise<typeof import("@react-pdf/renderer")> | null = null;
function loadPdfModule() {
  if (!pdfModulePromise) {
    pdfModulePromise = import("@react-pdf/renderer");
  }
  return pdfModulePromise;
}
```

De `src/app/(vault)/settings/profile/components/signature-upload.tsx` — pattern de `next/dynamic` a replicar:
```typescript
import dynamic from "next/dynamic";
const SignatureCropModal = dynamic(
  () => import("./signature-crop-modal").then((m) => m.SignatureCropModal),
  { ssr: false },
);
```

Configuração atual de `next.config.ts`:
```typescript
experimental: {
  optimizePackageImports: [
    "date-fns",
    "react-day-picker",
    "@dnd-kit/core",
    "@dnd-kit/utilities",
  ],
}
```

## Decisões Travadas (D-XX)

- **D-04:** Aplicar `next/dynamic` com `ssr: false` para `react-day-picker` (mini-calendar)
- **D-05:** Aplicar `next/dynamic` com `ssr: false` para `@dnd-kit/core` e `@dnd-kit/utilities` (drag-and-drop da agenda)
- **D-06:** Aplicar `next/dynamic` com `ssr: false` para `react-easy-crop` — **JÁ IMPLEMENTADO** em `signature-upload.tsx`
- **D-07:** Manter `optimizePackageImports` no `next.config.ts` como camada adicional de tree-shaking
- **D-08/D-09:** Manter `web-vitals` no bundle inicial (não lazy-load)
- **D-01/D-02/D-03:** Manter tags `<img>` raw para imagens dinâmicas (object URLs e signed URLs) com justificativa documentada no código

## Notas sobre Requisitos

- **ASSET-04 (fontes → next/font):** Deferido para Phase 31 por decisão do usuário em CONTEXT.md.
- **ASSET-05 (scripts de terceiros):** Nenhum script de terceiro existe no projeto atualmente. Requisito satisfeito pelo estado atual.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lazy loading de componentes pesados da agenda</name>
  <files>src/app/(vault)/agenda/page.tsx</files>
  <read_first>
    - src/app/(vault)/agenda/page.tsx (estado atual dos imports)
    - src/app/(vault)/agenda/components/mini-calendar.tsx (props do MiniCalendar)
    - src/app/(vault)/agenda/components/calendar-grid.tsx (props do CalendarGrid)
    - src/app/(vault)/agenda/components/week-calendar-grid.tsx (props do WeekCalendarGrid)
    - src/app/(vault)/settings/profile/components/signature-upload.tsx (pattern de dynamic import existente)
  </read_first>
  <action>
    Em `src/app/(vault)/agenda/page.tsx`, converter os imports estáticos de `MiniCalendar`, `CalendarGrid` e `WeekCalendarGrid` para `next/dynamic` com `ssr: false`.

    1. Adicionar `import dynamic from "next/dynamic";` no topo do arquivo.
    2. Substituir:
       ```typescript
       import { CalendarGrid } from "./components/calendar-grid";
       import { WeekCalendarGrid } from "./components/week-calendar-grid";
       import { MiniCalendar } from "./components/mini-calendar";
       ```
       Por:
       ```typescript
       const MiniCalendar = dynamic(
         () => import("./components/mini-calendar").then((m) => m.MiniCalendar),
         { ssr: false, loading: () => <div style={{ height: "280px", background: "var(--color-surface-1)", borderRadius: "var(--radius-lg)" }} /> }
       );
       const CalendarGrid = dynamic(
         () => import("./components/calendar-grid").then((m) => m.CalendarGrid),
         { ssr: false, loading: () => <div style={{ height: "400px", background: "var(--color-surface-1)", borderRadius: "var(--radius-lg)" }} /> }
       );
       const WeekCalendarGrid = dynamic(
         () => import("./components/week-calendar-grid").then((m) => m.WeekCalendarGrid),
         { ssr: false, loading: () => <div style={{ height: "400px", background: "var(--color-surface-1)", borderRadius: "var(--radius-lg)" }} /> }
       );
       ```
    3. Certificar-se de que os JSX existentes que usam `<MiniCalendar .../>`, `<CalendarGrid .../>` e `<WeekCalendarGrid .../>` permaneçam inalterados — apenas os imports mudam.
    4. NÃO alterar os componentes filhos (`mini-calendar.tsx`, `calendar-grid.tsx`, `week-calendar-grid.tsx`, `appointment-block.tsx`). Eles continuam importando `react-day-picker` e `@dnd-kit` estaticamente internamente; o lazy loading no page.tsx é que move o código dessas bibliotecas para chunks separados.

    NOTA: `react-easy-crop` já está lazy-loaded via `signature-upload.tsx` (D-06 já implementado). `@react-pdf/renderer` já está lazy-loaded via `pdf.tsx`. Nenhuma ação necessária para estes.
  </action>
  <verify>
    <automated>grep -n "next/dynamic" src/app/(vault)/agenda/page.tsx && grep -n "MiniCalendar = dynamic" src/app/(vault)/agenda/page.tsx && grep -n "CalendarGrid = dynamic" src/app/(vault)/agenda/page.tsx && grep -n "WeekCalendarGrid = dynamic" src/app/(vault)/agenda/page.tsx</automated>
  </verify>
  <done>
    - `src/app/(vault)/agenda/page.tsx` importa `dynamic` de `next/dynamic`
    - `MiniCalendar`, `CalendarGrid` e `WeekCalendarGrid` são definidos via `dynamic()` com `ssr: false`
    - Cada dynamic tem um `loading` fallback com div de altura apropriada e estilo inline
    - Nenhum import estático desses três componentes permanece no arquivo
  </done>
</task>

<task type="auto">
  <name>Task 2: Documentar exceções de raw <img> com justificativas</name>
  <files>src/app/(vault)/settings/profile/components/signature-crop-modal.tsx, src/app/(vault)/patients/[patientId]/documents/[documentId]/page.tsx</files>
  <read_first>
    - src/app/(vault)/settings/profile/components/signature-crop-modal.tsx (linhas dos <img> raw)
    - src/app/(vault)/patients/[patientId]/documents/[documentId]/page.tsx (linha do <img> raw)
    - CLAUDE.md (seção de anti-padrões visuais, para consistência de tom)
  </read_first>
  <action>
    Adicionar comentários explicativos detalhados imediatamente ANTES de cada tag `<img>` raw, documentando por que `next/image` não é apropriado. Os comentários devem ser em português (pt-BR), concisos e técnicos.

    1. Em `signature-crop-modal.tsx`, substituir os comentários eslint existentes:
       ```tsx
       {/* eslint-disable-next-line @next/next/no-img-element */}
       {beforeUrl && (
         <img src={beforeUrl} alt="Recorte original da foto" style={resultImgStyle} />
       )}
       ```
       Por:
       ```tsx
       {/* next/image não se aplica: object URL efêmero gerado por URL.createObjectURL()
           durante o crop. A URL dura segundos e não se beneficia do otimizador do Next.js. */}
       {/* eslint-disable-next-line @next/next/no-img-element */}
       {beforeUrl && (
         <img src={beforeUrl} alt="Recorte original da foto" style={resultImgStyle} />
       )}
       ```
       E o segundo `<img>` (resultUrl):
       ```tsx
       {/* next/image não se aplica: object URL efêmero do resultado processado.
           Gerado por URL.createObjectURL() — não há domínio estável para configurar em images.remotePatterns. */}
       {/* eslint-disable-next-line @next/next/no-img-element */}
       {resultUrl && (
         <img src={resultUrl} alt="Assinatura digitalizada com fundo removido" style={resultImgStyle} />
       )}
       ```

    2. Em `documentId/page.tsx`, substituir:
       ```tsx
       {/* eslint-disable-next-line @next/next/no-img-element */}
       <img
         src={signatureImageUrl}
         alt="Assinatura profissional"
         style={signatureImgStyle}
       />
       ```
       Por:
       ```tsx
       {/* next/image não se aplica: signed URL do Supabase Storage com expiração de 1h.
           O Supabase já serve imagens otimizadas; adicionar o domínio em images.remotePatterns
           aumentaria a superfície de configuração sem ganho proporcional. */}
       {/* eslint-disable-next-line @next/next/no-img-element */}
       <img
         src={signatureImageUrl}
         alt="Assinatura profissional"
         style={signatureImgStyle}
       />
       ```
  </action>
  <verify>
    <automated>grep -n "next/image não se aplica" src/app/(vault)/settings/profile/components/signature-crop-modal.tsx && grep -n "next/image não se aplica" src/app/(vault)/patients/[patientId]/documents/[documentId]/page.tsx</automated>
  </verify>
  <done>
    - `signature-crop-modal.tsx` possui 2 blocos `<img>` com comentários explicando object URLs efêmeros
    - `documentId/page.tsx` possui 1 bloco `<img>` com comentário explicando signed URL do Supabase
    - Todos os comentários preservam a linha `eslint-disable-next-line @next/next/no-img-element`
  </done>
</task>

<task type="auto">
  <name>Task 3: Verificar configuração de bundle e validar separação de chunks</name>
  <files>next.config.ts</files>
  <read_first>
    - next.config.ts (configuração atual de optimizePackageImports)
    - package.json (lista de dependências para confirmar quais bibliotecas estão presentes)
  </read_first>
  <action>
    1. Verificar que `next.config.ts` já contém `optimizePackageImports` com os valores corretos. Se faltar alguma biblioteca pesada que é carregada estaticamente em algum ponto (além das já listadas), adicionar.
       - Atual lista: `date-fns`, `react-day-picker`, `@dnd-kit/core`, `@dnd-kit/utilities`
       - `react-easy-crop` NÃO precisa ser adicionado porque já é lazy-loaded via dynamic import em `signature-upload.tsx`
       - `@react-pdf/renderer` NÃO precisa ser adicionado porque já é lazy-loaded via `import()` em `pdf.tsx`
       - Nenhuma outra biblioteca pesada está presente no package.json que justifique adição
       Se a lista atual estiver correta, NÃO modificar o arquivo.

    2. Executar build com bundle analyzer para confirmar que `react-day-picker` e `@dnd-kit` não estão no bundle principal:
       ```bash
       set ANALYZE=true && pnpm build
       ```
       (No Windows/PowerShell: `$env:ANALYZE="true"; pnpm build`)

    3. Inspecionar o output do bundle analyzer (gerado em `.next/analyze/`) ou o stdout do build para confirmar que:
       - `@dnd-kit/core` e `@dnd-kit/utilities` aparecem em um chunk separado (não no main)
       - `react-day-picker` aparece em um chunk separado (não no main)
       - `web-vitals` permanece no bundle inicial (esperado e desejado por D-08)

    4. Se o build falhar por causa do dynamic import, corrigir erros de tipagem (ex: garantir que as props dos componentes dynamic ainda satisfaçam os tipos esperados).
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>
    - `next.config.ts` contém `optimizePackageImports: ["date-fns", "react-day-picker", "@dnd-kit/core", "@dnd-kit/utilities"]`
    - `pnpm build` completa sem erros
    - Bundle analysis (via `ANALYZE=true pnpm build`) mostra `react-day-picker` e `@dnd-kit` em chunks separados do main bundle
    - `web-vitals` ainda presente no bundle inicial
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Server | Nenhuma mudança nos contratos de API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Denial of Service | Lazy loading de componentes da agenda | accept | Falha no carregamento do chunk mostra fallback vazio; não bloqueia funcionalidade crítica. O usuário pode recarregar a página. |
| T-30-02 | Information Disclosure | Comentários em <img> raw | accept | Comentários descrevem apenas decisões técnicas de arquitetura; não expõem dados sensíveis, chaves ou credenciais. |
</threat_model>

<verification>
1. `grep -n "next/dynamic" src/app/(vault)/agenda/page.tsx` — confirma dynamic imports
2. `grep -n "MiniCalendar = dynamic" src/app/(vault)/agenda/page.tsx` — confirma lazy load do calendário
3. `grep -n "CalendarGrid = dynamic" src/app/(vault)/agenda/page.tsx` — confirma lazy load do grid diário
4. `grep -n "WeekCalendarGrid = dynamic" src/app/(vault)/agenda/page.tsx` — confirma lazy load do grid semanal
5. `grep -n "object URL efêmero" src/app/(vault)/settings/profile/components/signature-crop-modal.tsx` — confirma documentação
6. `grep -n "signed URL do Supabase" src/app/(vault)/patients/[patientId]/documents/[documentId]/page.tsx` — confirma documentação
7. `grep -n "optimizePackageImports" next.config.ts` — confirma configuração de tree-shaking
8. `pnpm build` — build passa sem erros de TypeScript ou de bundle
</verification>

<success_criteria>
- Bundle inicial da rota `/agenda` não contém `react-day-picker` nem `@dnd-kit`
- Todas as tags `<img>` raw na codebase têm comentário justificando a exceção
- `optimizePackageImports` cobre todas as bibliotecas pesadas carregadas estaticamente
- Build completo sem regressões (`pnpm build` passa)
- `web-vitals` não foi removido do bundle inicial
</success_criteria>

<output>
After completion, create `.planning/phases/30-otimiza-o-de-assets-e-bundle/30-01-SUMMARY.md`
</output>
