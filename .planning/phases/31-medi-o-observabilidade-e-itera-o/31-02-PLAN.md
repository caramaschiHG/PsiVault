---
phase: 31-medi-o-observabilidade-e-itera-o
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/metrics/model.ts
  - src/lib/metrics/repository.ts
  - src/lib/metrics/repository.prisma.ts
  - src/app/(vault)/admin/performance/page.tsx
  - scripts/cleanup-metrics.mjs
autonomous: true
requirements:
  - OBS-02

must_haves:
  truths:
    - "Métricas agregadas p75 de LCP, INP, CLS são consultáveis por página nos últimos 7 dias"
    - "Dashboard /admin/performance só é acessível quando ENABLE_PERF_DASHBOARD=1"
    - "Métricas com mais de 30 dias são removidas automaticamente via script"
  artifacts:
    - path: "src/lib/metrics/repository.ts"
      provides: "Interface com métodos de agregação e cleanup"
      exports: ["MetricsRepository"]
      contains:
        - "getP75ByPagePath"
        - "cleanupOldMetrics"
    - path: "src/lib/metrics/repository.prisma.ts"
      provides: "Implementação Prisma com percentile_cont e deleteMany"
      exports: ["createPrismaMetricsRepository"]
      contains: "$queryRaw"
    - path: "src/app/(vault)/admin/performance/page.tsx"
      provides: "Dashboard RUM com tabelas de p75 por página"
      contains: "ENABLE_PERF_DASHBOARD"
    - path: "scripts/cleanup-metrics.mjs"
      provides: "Script de limpeza de métricas antigas"
      contains: "cleanupOldMetrics"
  key_links:
    - from: "src/app/(vault)/admin/performance/page.tsx"
      to: "src/lib/metrics/repository.prisma.ts"
      via: "getMetricsRepository()"
      pattern: "getMetricsRepository"
    - from: "src/lib/metrics/repository.prisma.ts"
      to: "prisma/schema.prisma (PerformanceMetric)"
      via: "db.performanceMetric / db.$queryRaw"
      pattern: "performance_metric"
    - from: "scripts/cleanup-metrics.mjs"
      to: "src/lib/metrics/repository.prisma.ts"
      via: "PrismaClient direto"
      pattern: "PrismaClient"
---

<objective>
Construir dashboard interno de Real User Monitoring (RUM) em `/admin/performance` com métricas agregadas (percentil 75) de CWV por página, protegido por feature flag, e estabelecer rotina de limpeza automática de métricas antigas (retenção 30 dias).

Purpose: Time de desenvolvimento tem visibilidade contínua de performance real dos usuários sem depender de serviços externos.
Output: Página `/admin/performance` funcional, repository com queries agregadas, script de cleanup.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/31-medi-o-observabilidade-e-itera-o/31-CONTEXT.md

## Interfaces relevantes do codebase

From src/lib/metrics/model.ts:
```typescript
export interface PerformanceMetric {
  id: string;
  metricName: string;
  value: number;
  rating: string | null;
  pagePath: string;
  workspaceId: string | null;
  sessionId: string | null;
  createdAt: Date;
}
```

From src/lib/metrics/repository.ts:
```typescript
import type { PerformanceMetric } from "./model";

export interface MetricsRepository {
  save(metric: PerformanceMetric): Promise<PerformanceMetric>;
}
```

From src/lib/metrics/repository.prisma.ts:
```typescript
import { db } from "../db";
import type { PerformanceMetric } from "./model";
import type { MetricsRepository } from "./repository";

export function createPrismaMetricsRepository(): MetricsRepository {
  return {
    async save(metric: PerformanceMetric): Promise<PerformanceMetric> {
      await db.performanceMetric.create({ data: { ...metric } });
      return metric;
    },
  };
}
```

From src/lib/metrics/store.ts:
```typescript
export function getMetricsRepository(): MetricsRepository {
  return globalThis.__psivaultMetricsRepo__ ?? createPrismaMetricsRepository();
}
```

From prisma/schema.prisma (PerformanceMetric model):
```prisma
model PerformanceMetric {
  id          String   @id @default(uuid())
  metricName  String   @map("metric_name")
  value       Float
  rating      String?
  pagePath    String   @map("page_path")
  workspaceId String?  @map("workspace_id")
  sessionId   String?  @map("session_id")
  createdAt   DateTime @default(now()) @map("created_at")

  @@index([workspaceId, metricName, createdAt])
  @@index([createdAt])
  @@map("performance_metrics")
}
```

From src/lib/db.ts:
```typescript
export const db = globalThis.__psivaultPrisma__ ?? new PrismaClient({...});
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estender MetricsRepository com agregações p75 e cleanup</name>
  <files>src/lib/metrics/model.ts, src/lib/metrics/repository.ts, src/lib/metrics/repository.prisma.ts, scripts/cleanup-metrics.mjs</files>
  <read_first>
    - src/lib/metrics/model.ts
    - src/lib/metrics/repository.ts
    - src/lib/metrics/repository.prisma.ts
    - src/lib/db.ts (para padrão PrismaClient)
  </read_first>
  <action>
    1. Estender `src/lib/metrics/model.ts` com interface de agregação:
       ```typescript
       export interface P75Metric {
         pagePath: string;
         metricName: string;
         p75: number;
       }
       ```

    2. Estender `src/lib/metrics/repository.ts`:
       ```typescript
       import type { PerformanceMetric, P75Metric } from "./model";

       export interface MetricsRepository {
         save(metric: PerformanceMetric): Promise<PerformanceMetric>;
         getP75ByPagePath(days: number): Promise<P75Metric[]>;
         cleanupOldMetrics(retentionDays: number): Promise<number>;
       }
       ```

    3. Estender `src/lib/metrics/repository.prisma.ts`:
       ```typescript
       import { db } from "../db";
       import type { PerformanceMetric, P75Metric } from "./model";
       import type { MetricsRepository } from "./repository";

       export function createPrismaMetricsRepository(): MetricsRepository {
         return {
           async save(metric: PerformanceMetric): Promise<PerformanceMetric> {
             await db.performanceMetric.create({ data: { ...metric } });
             return metric;
           },
           async getP75ByPagePath(days: number): Promise<P75Metric[]> {
             const start = new Date(Date.now() - days * 86400000);
             const result = await db.$queryRaw<P75Metric[]>`
               SELECT
                 page_path as "pagePath",
                 metric_name as "metricName",
                 percentile_cont(0.75) WITHIN GROUP (ORDER BY value) as p75
               FROM performance_metrics
               WHERE created_at >= ${start}
               GROUP BY page_path, metric_name
               ORDER BY page_path, metric_name
             `;
             return result;
           },
           async cleanupOldMetrics(retentionDays: number): Promise<number> {
             const cutoff = new Date(Date.now() - retentionDays * 86400000);
             const result = await db.performanceMetric.deleteMany({
               where: { createdAt: { lt: cutoff } },
             });
             return result.count;
           },
         };
       }
       ```

    4. Criar `scripts/cleanup-metrics.mjs`:
       ```js
       import { PrismaClient } from "@prisma/client";

       const prisma = new PrismaClient();
       const RETENTION_DAYS = 30;

       async function main() {
         const cutoff = new Date(Date.now() - RETENTION_DAYS * 86400000);
         const result = await prisma.performanceMetric.deleteMany({
           where: { createdAt: { lt: cutoff } },
         });
         console.log(`Removidas ${result.count} métricas antigas (>${RETENTION_DAYS} dias).`);
       }

       main()
         .catch((e) => { console.error(e); process.exit(1); })
         .finally(() => prisma.$disconnect());
       ```

    5. Adicionar script em package.json:
       `"metrics:cleanup": "node scripts/cleanup-metrics.mjs"`

    IMPORTANTE: Preservar o método `save` existente sem alterações. Usar `db.$queryRaw` apenas para percentile_cont (Prisma não suporta nativamente). Usar `deleteMany` tipado para cleanup — mais seguro que raw SQL.
  </action>
  <verify>
    <automated>pnpm build && pnpm test -- --filter=metrics</automated>
  </verify>
  <done>Repository estendido com agregação p75 e cleanup, script de limpeza criado.</done>
</task>

<task type="auto">
  <name>Task 2: Criar página /admin/performance com dashboard RUM</name>
  <files>src/app/(vault)/admin/performance/page.tsx</files>
  <read_first>
    - src/app/(vault)/layout.tsx (para confirmar que Server Components são o padrão)
    - src/lib/metrics/store.ts (para padrão de acesso ao repository)
    - src/app/(vault)/inicio/page.tsx (exemplo de Server Component com dados)
  </read_first>
  <action>
    1. Criar diretório `src/app/(vault)/admin/performance/` se não existir.

    2. Criar `src/app/(vault)/admin/performance/page.tsx`:
       ```tsx
       import { getMetricsRepository } from "@/lib/metrics/store";
       import { notFound } from "next/navigation";
       import type React from "react";

       export default async function PerformanceDashboardPage() {
         if (process.env.ENABLE_PERF_DASHBOARD !== "1") {
           notFound();
         }

         const repo = getMetricsRepository();
         const rows = await repo.getP75ByPagePath(7);

         const byPage = rows.reduce<Record<string, typeof rows>>((acc, row) => {
           if (!acc[row.pagePath]) acc[row.pagePath] = [];
           acc[row.pagePath].push(row);
           return acc;
         }, {});

         return (
           <div style={{ padding: "2rem", maxWidth: "960px" }}>
             <h1 style={{ fontSize: "1.25rem", fontWeight: 700, marginBottom: "1.5rem" }}>
               Observabilidade de Performance
             </h1>
             <p style={{ fontSize: "0.875rem", color: "var(--color-text-secondary)", marginBottom: "2rem" }}>
               Percentil 75 (p75) das Core Web Vitals — últimos 7 dias
             </p>

             {Object.entries(byPage).map(([page, metrics]) => (
               <section key={page} style={{ marginBottom: "2rem" }}>
                 <h2 style={{ fontSize: "1rem", fontWeight: 600, marginBottom: "0.75rem" }}>
                   {page}
                 </h2>
                 <div style={{ overflowX: "auto" as const }}>
                   <table style={{ width: "100%", borderCollapse: "collapse", fontSize: "0.875rem" }}>
                     <thead>
                       <tr>
                         <th style={thStyle}>Métrica</th>
                         <th style={{ ...thStyle, textAlign: "right" }}>p75</th>
                       </tr>
                     </thead>
                     <tbody>
                       {metrics.map((m) => (
                         <tr key={m.metricName}>
                           <td style={tdStyle}>{m.metricName}</td>
                           <td style={{ ...tdStyle, textAlign: "right", fontVariantNumeric: "tabular-nums" }}>
                             {typeof m.p75 === "number" ? m.p75.toFixed(3) : String(m.p75)}
                           </td>
                         </tr>
                       ))}
                     </tbody>
                   </table>
                 </div>
               </section>
             ))}

             {rows.length === 0 && (
               <p style={{ fontSize: "0.875rem", color: "var(--color-text-secondary)" }}>
                 Nenhuma métrica coletada nos últimos 7 dias. Verifique se o CwvCollector está ativo.
               </p>
             )}
           </div>
         );
       }

       const thStyle: React.CSSProperties = {
         textAlign: "left",
         padding: "0.5rem 0.75rem",
         borderBottom: "1px solid var(--color-border)",
         fontWeight: 600,
       };

       const tdStyle: React.CSSProperties = {
         padding: "0.5rem 0.75rem",
         borderBottom: "1px solid var(--color-border-subtle)",
       };
       ```

    3. Executar `pnpm build` para garantir que a nova rota compila sem erros.

    IMPORTANTE: A página DEVE ser Server Component (sem "use client"). A validação do feature flag DEVE usar `process.env.ENABLE_PERF_DASHBOARD` e chamar `notFound()` se não estiver ativo. Não usar biblioteca de charts externa — apenas tabelas HTML com CSS inline usando tokens existentes.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>Página /admin/performance criada com tabelas de p75 e proteção por feature flag.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /admin/performance | Dashboard pode expor métricas agregadas |
| /admin/performance → PostgreSQL | Queries agregadas no banco |
| scripts/cleanup-metrics.mjs → PostgreSQL | Script destrutivo (DELETE) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04 | Information Disclosure | /admin/performance | mitigate | Feature flag `ENABLE_PERF_DASHBOARD=1` obrigatória; `notFound()` quando desativado. Nenhum link público aponta para esta rota. |
| T-31-05 | Denial of Service | getP75ByPagePath | mitigate | Query usa índice `@@index([createdAt])` e filtro por `created_at >= ${start}`. Sem joins ou subqueries. |
| T-31-06 | Denial of Service | cleanupOldMetrics | mitigate | Usa `deleteMany` tipado do Prisma com `where: { createdAt: { lt: cutoff } }` — impossível de deletar métricas recentes sem alterar `RETENTION_DAYS`. |
</threat_model>

<verification>
- `src/lib/metrics/repository.ts` contém `getP75ByPagePath` e `cleanupOldMetrics`
- `src/lib/metrics/repository.prisma.ts` implementa ambos os métodos
- `scripts/cleanup-metrics.mjs` existe e usa `PrismaClient`
- `pnpm build` completa sem erros
- `ENABLE_PERF_DASHBOARD=1` ativa a rota; sem a variável a rota retorna 404
- `pnpm metrics:cleanup` executa e reporta contagem de métricas removidas
</verification>

<success_criteria>
- Repository retorna p75 de LCP/INP/CLS por página via query PostgreSQL com `percentile_cont`
- Dashboard `/admin/performance` renderiza tabelas com métricas dos últimos 7 dias
- Dashboard inacessível sem `ENABLE_PERF_DASHBOARD=1`
- Script `pnpm metrics:cleanup` remove métricas com mais de 30 dias
- Nenhum método ou tipo existente do domínio de métricas foi quebrado
</success_criteria>

<output>
After completion, create `.planning/phases/31-medi-o-observabilidade-e-itera-o/31-02-SUMMARY.md`
</output>
