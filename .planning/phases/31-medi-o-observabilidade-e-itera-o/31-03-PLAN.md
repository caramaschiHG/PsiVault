---
phase: 31-medi-o-observabilidade-e-itera-o
plan: 03
type: execute
wave: 2
depends_on:
  - 31-01-PLAN.md
files_modified:
  - package.json
  - scripts/generate-performance-report.mjs
  - .planning/milestones/v1.4-PERFORMANCE-REPORT.md
autonomous: true
requirements:
  - OBS-05

must_haves:
  truths:
    - "Script `pnpm performance:report` gera relatório Markdown consolidado"
    - "Relatório inclui dados RUM (p75), status Lighthouse e referência a memlab"
    - "Relatório é versionado em `.planning/milestones/v1.4-PERFORMANCE-REPORT.md`"
  artifacts:
    - path: "scripts/generate-performance-report.mjs"
      provides: "Script gerador de relatório de observabilidade"
      contains:
        - "PrismaClient"
        - "v1.4-PERFORMANCE-REPORT.md"
    - path: "package.json"
      provides: "Script npm `performance:report`"
      contains: "performance:report"
    - path: ".planning/milestones/v1.4-PERFORMANCE-REPORT.md"
      provides: "Relatório gerado com dados atuais"
      contains: "Performance Profunda"
  key_links:
    - from: "scripts/generate-performance-report.mjs"
      to: "prisma/PerformanceMetric"
      via: "PrismaClient"
      pattern: "performanceMetric"
    - from: "scripts/generate-performance-report.mjs"
      to: ".planning/performance/lighthouse"
      via: "fs.readFileSync"
      pattern: "lighthouse"
---

<objective>
Criar script gerador de relatório de observabilidade que consolida dados RUM do banco, resultados do Lighthouse CI e execução do memlab em um único documento Markdown versionado no repositório.

Purpose: Documentar infraestrutura de observabilidade instalada na Phase 31 e fornecer baseline para futuras comparações.
Output: Script `pnpm performance:report` e arquivo `.planning/milestones/v1.4-PERFORMANCE-REPORT.md` gerado automaticamente.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/31-medi-o-observabilidade-e-itera-o/31-CONTEXT.md

## Interfaces relevantes do codebase

From package.json (scripts adicionados no plan 31-01):
```json
{
  "scripts": {
    "lighthouse": "lhci autorun --config=lighthouserc.js",
    "memlab": "memlab run --scenario tests/memory/patient-navigation.scenario.js"
  }
}
```

From prisma/schema.prisma (PerformanceMetric):
```prisma
model PerformanceMetric {
  id          String   @id @default(uuid())
  metricName  String   @map("metric_name")
  value       Float
  rating      String?
  pagePath    String   @map("page_path")
  workspaceId String?  @map("workspace_id")
  sessionId   String?  @map("session_id")
  createdAt   DateTime @default(now()) @map("created_at")
  @@map("performance_metrics")
}
```

From .planning/ROADMAP.md (success criteria da fase):
- Relatório publicado mostra métricas before/after com próximos passos acionáveis

From 31-CONTEXT.md:
- D-15: Formato Markdown no repo + script gerador (`pnpm performance:report`)
- D-16: Arquivo `.planning/milestones/v1.4-PERFORMANCE-REPORT.md`
- D-17: Escopo do relatório foca na fase 31 (observabilidade) — documentar infraestrutura instalada, thresholds definidos, próximos passos
- D-18: Script lê métricas do banco (RUM), resultados do Lighthouse, e memlab
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar script gerador de relatório de performance</name>
  <files>scripts/generate-performance-report.mjs</files>
  <read_first>
    - package.json (para confirmar scripts de lighthouse e memlab)
    - src/lib/metrics/model.ts (para entender schema de métricas)
  </read_first>
  <action>
    1. Criar diretório `scripts/` se não existir.

    2. Criar `scripts/generate-performance-report.mjs`:
       ```js
       import { PrismaClient } from "@prisma/client";
       import fs from "fs";
       import path from "path";
       import { fileURLToPath } from "url";

       const __dirname = path.dirname(fileURLToPath(import.meta.url));
       const prisma = new PrismaClient();

       const REPORT_PATH = path.join(process.cwd(), ".planning", "milestones", "v1.4-PERFORMANCE-REPORT.md");
       const LIGHTHOUSE_DIR = path.join(process.cwd(), ".planning", "performance", "lighthouse");

       async function fetchRum() {
         const rows = await prisma.$queryRaw`
           SELECT
             page_path,
             metric_name,
             percentile_cont(0.75) WITHIN GROUP (ORDER BY value) as p75,
             COUNT(*)::int as samples
           FROM performance_metrics
           WHERE created_at >= NOW() - INTERVAL '30 days'
           GROUP BY page_path, metric_name
           ORDER BY page_path, metric_name
         `;
         return rows;
       }

       async function fetchLighthouse() {
         try {
           const entries = fs.readdirSync(LIGHTHOUSE_DIR);
           const jsonFiles = entries.filter((f) => f.endsWith(".json"));
           if (jsonFiles.length === 0) return null;
           jsonFiles.sort();
           const latest = jsonFiles[jsonFiles.length - 1];
           const content = fs.readFileSync(path.join(LIGHTHOUSE_DIR, latest), "utf-8");
           return { file: latest, data: JSON.parse(content) };
         } catch {
           return null;
         }
       }

       function formatRumTable(rows) {
         if (!rows || rows.length === 0) {
           return ["_Nenhuma métrica RUM coletada nos últimos 30 dias._", ""];
         }
         const lines = [
           "| Página | Métrica | p75 | Amostras |",
           "|--------|---------|-----|----------|",
         ];
         for (const row of rows) {
           const p75 = typeof row.p75 === "number" ? row.p75.toFixed(3) : String(row.p75);
           lines.push(`| ${row.page_path} | ${row.metric_name} | ${p75} | ${row.samples} |`);
         }
         lines.push("");
         return lines;
       }

       function formatLighthouse(lighthouse) {
         if (!lighthouse) {
           return [
             "_Nenhum relatório Lighthouse encontrado._",
             "",
             "Execute `pnpm lighthouse` para gerar um relatório.",
             "",
           ];
         }
         const lines = [
           `**Arquivo:** \`${lighthouse.file}\``,
           "",
         ];
         const runs = Array.isArray(lighthouse.data) ? lighthouse.data : [lighthouse.data];
         for (const run of runs.slice(0, 3)) {
           const url = run?.requestedUrl || run?.finalUrl || "—";
           const lhr = run?.lhr || run;
           const perf = lhr?.categories?.performance?.score;
           const perfLabel = typeof perf === "number" ? `${Math.round(perf * 100)}` : "N/A";
           const lcp = lhr?.audits?.["largest-contentful-paint"]?.numericValue;
           const lcpLabel = typeof lcp === "number" ? `${(lcp / 1000).toFixed(2)}s` : "N/A";
           const cls = lhr?.audits?.["cumulative-layout-shift"]?.numericValue;
           const clsLabel = typeof cls === "number" ? cls.toFixed(3) : "N/A";
           lines.push(`- **URL:** ${url}`);
           lines.push(`  - Performance score: ${perfLabel}`);
           lines.push(`  - LCP: ${lcpLabel}`);
           lines.push(`  - CLS: ${clsLabel}`);
           lines.push("");
         }
         return lines;
       }

       async function main() {
         const [rumRows, lighthouse] = await Promise.all([fetchRum(), fetchLighthouse()]);

         const lines = [
           "# v1.4 Performance Profunda — Relatório de Observabilidade",
           "",
           `Gerado em: ${new Date().toISOString()}`,
           "",
           "## Sobre este relatório",
           "",
           "Este documento consolida a infraestrutura de observabilidade instalada na **Phase 31** do milestone v1.4 Performance Profunda.",
           "Ele é gerado automaticamente pelo script `pnpm performance:report` e deve ser versionado no repositório.",
           "",
           "## Infraestrutura instalada",
           "",
           "- **Lighthouse CI**: auditoria automatizada de CWV em builds locais (`pnpm lighthouse`).",
           "- **RUM (Real User Monitoring)**: coleta contínua de LCP, INP, CLS, TTFB, FCP via `CwvCollector`.",
           "- **memlab**: detecção de memory leaks em fluxo de navegação (`pnpm memlab`).",
           "- **react-scan**: overlay de re-renders em desenvolvimento (dev-only).",
           "",
           "## Thresholds definidos",
           "",
           "| Métrica | Threshold | Tipo |",
           "|---------|-----------|------|",
           "| LCP | < 2.5s | error (falha build) |",
           "| CLS | < 0.1 | error (falha build) |",
           "| INP | monitorado | advisory |",
           "| TTFB | monitorado | advisory |",
           "",
           "## RUM — Últimos 30 dias",
           "",
           ...formatRumTable(rumRows),
           "## Lighthouse CI — Última execução",
           "",
           ...formatLighthouse(lighthouse),
           "## memlab",
           "",
           "Execute `pnpm memlab` e verifique o relatório no terminal.",
           "Se forem detectados leaks, documente-os nesta seção manualmente.",
           "",
           "## Próximos passos acionáveis",
           "",
           "- [ ] Revisar métricas RUM semanalmente via `/admin/performance`.",
           "- [ ] Executar `pnpm lighthouse` após mudanças significativas no bundle.",
           "- [ ] Executar `pnpm memlab` a cada release para detectar regressões de memória.",
           "- [ ] Revisar e ajustar thresholds de CWV conforme baseline se estabiliza.",
           "",
           "---",
           "*Relatório gerado automaticamente. Não edite manualmente — suas alterações serão sobrescritas.*",
           "",
         ];

         fs.mkdirSync(path.dirname(REPORT_PATH), { recursive: true });
         fs.writeFileSync(REPORT_PATH, lines.join("\n"), "utf-8");
         console.log(`Relatório gerado: ${REPORT_PATH}`);
       }

       main()
         .catch((e) => { console.error(e); process.exit(1); })
         .finally(() => prisma.$disconnect());
       ```

    3. Garantir que o script trata gracefully ausência de dados (tabelas vazias, arquivos inexistentes).

    IMPORTANTE: Não assumir que dados existem — o relatório deve ser gerável mesmo em ambiente sem métricas coletadas. Usar `fs.mkdirSync` com `recursive: true` para criar diretórios ausentes.
  </action>
  <verify>
    <automated>node scripts/generate-performance-report.mjs</automated>
  </verify>
  <done>Script gerador criado e executado com sucesso, produzindo relatório Markdown.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar script npm e gerar relatório inicial</name>
  <files>package.json, .planning/milestones/v1.4-PERFORMANCE-REPORT.md</files>
  <read_first>
    - package.json (scripts atuais, incluindo scripts adicionados nos plans anteriores)
  </read_first>
  <action>
    1. Adicionar script em package.json:
       `"performance:report": "node scripts/generate-performance-report.mjs"`

    2. Executar `pnpm performance:report` para gerar o relatório inicial.

    3. Verificar que `.planning/milestones/v1.4-PERFORMANCE-REPORT.md` foi criado e contém:
       - Título "v1.4 Performance Profunda — Relatório de Observabilidade"
       - Seção "Infraestrutura instalada"
       - Seção "Thresholds definidos"
       - Seção "RUM — Últimos 30 dias"
       - Seção "Lighthouse CI — Última execução"
       - Seção "Próximos passos acionáveis"

    4. Executar `pnpm build` para garantir que nenhuma modificação em package.json quebrou a build.

    IMPORTANTE: Preservar TODOS os scripts existentes. O novo script deve ser adicionado sem remover `lighthouse`, `memlab`, `metrics:cleanup`, `dev`, `build`, `start`, `test`, etc.
  </action>
  <verify>
    <automated>grep -n "performance:report" package.json && cat .planning/milestones/v1.4-PERFORMANCE-REPORT.md | head -20</automated>
  </verify>
  <done>Script npm adicionado, relatório inicial gerado e build verificada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script → PostgreSQL | Script acessa banco para ler métricas |
| script → filesystem | Gera arquivo Markdown no repositório |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-07 | Information Disclosure | v1.4-PERFORMANCE-REPORT.md | accept | Relatório contém apenas métricas agregadas de timing (LCP, CLS, INP) e page paths. Nenhum dado pessoal ou clínico. Arquivo versionado no repo. |
| T-31-08 | Tampering | generate-performance-report.mjs | accept | Script é determinístico — mesmas entradas produzem mesma saída. Versionado no repo. |
| T-31-09 | Denial of Service | generate-performance-report.mjs | mitigate | Query RUM usa índice em `createdAt` e agregação GROUP BY. Leitura apenas. Timeout implícito do PrismaClient. |
</threat_model>

<verification>
- `grep "performance:report" package.json` retorna match
- `.planning/milestones/v1.4-PERFORMANCE-REPORT.md` existe e contém todas as seções esperadas
- `pnpm build` completa sem erros
- `pnpm performance:report` executa sem erros mesmo sem dados RUM ou Lighthouse
</verification>

<success_criteria>
- Script `pnpm performance:report` gera `.planning/milestones/v1.4-PERFORMANCE-REPORT.md`
- Relatório inclui infraestrutura instalada, thresholds, RUM p75, Lighthouse e próximos passos
- Script funciona corretamente mesmo quando não há dados coletados
- Nenhum script existente foi removido ou alterado indevidamente
</success_criteria>

<output>
After completion, create `.planning/phases/31-medi-o-observabilidade-e-itera-o/31-03-SUMMARY.md`
</output>
