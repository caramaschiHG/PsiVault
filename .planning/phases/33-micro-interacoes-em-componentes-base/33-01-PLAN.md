---
phase: 33-micro-interacoes-em-componentes-base
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/styles/motion.css
  - src/components/ui/card.tsx
autonomous: true
requirements:
  - MICR-01
  - MICR-02
  - MICR-03
  - MICR-04
  - MICR-05
user_setup: []
must_haves:
  truths:
    - "Focus rings use box-shadow instead of outline on all focusable elements"
    - "Card hover transitions use motion tokens with ease-out"
    - "Button active states include scale(0.97) for all variants"
    - "Input error shake keyframe exists in motion.css"
    - "Floating label CSS supports both input-field and auth-input"
    - "Sidebar nav transitions use duration-300 for hover"
  artifacts:
    - path: "src/app/globals.css"
      provides: "All micro-interaction CSS classes"
      contains: "box-shadow focus ring system"
    - path: "src/styles/motion.css"
      provides: "inputShake keyframe and reduced motion coverage"
      contains: "@keyframes inputShake"
    - path: "src/components/ui/card.tsx"
      provides: "Auto card-hover class application"
      contains: "asLink prop"
  key_links:
    - from: "src/styles/motion.css"
      to: "src/app/globals.css"
      via: "@import"
      pattern: "@import \"../styles/motion.css\""
    - from: "src/components/ui/card.tsx"
      to: "src/app/globals.css"
      via: "card-hover class"
      pattern: "card-hover"
---

<objective>
Establish the complete CSS foundation for micro-interactions: focus ring system, card hover, button active states, input error shake, floating labels, and sidebar nav transitions. Update the Card component to auto-apply hover classes.

Purpose: All subsequent plans (auth forms, vault forms) depend on these CSS classes and component changes being in place first.
Output: globals.css and motion.css extended with Phase 33 contracts; card.tsx updated with asLink prop and auto card-hover.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-micro-interacoes-em-componentes-base/33-CONTEXT.md
@.planning/phases/33-micro-interacoes-em-componentes-base/33-UI-SPEC.md
@src/app/globals.css
@src/styles/motion.css
@src/components/ui/card.tsx

<interfaces>
From src/components/ui/card.tsx:
```typescript
interface CardProps {
  variant?: CardVariant;
  padding?: "sm" | "md" | "lg";
  className?: string;
  style?: CSSProperties;
  children: React.ReactNode;
  onClick?: () => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate and extend globals.css with micro-interaction system</name>
  <files>src/app/globals.css</files>
  <action>
    Read globals.css fully. Apply the following changes derived from the UI-SPEC.md CSS Contracts section. Every change MUST use `--duration-*` and `--ease-*` tokens. Do NOT use generic time values.

    **A. Replace global `:focus-visible` rule (around line 582)**
    Replace the existing:
    ```css
    :focus-visible {
      outline: 2px solid var(--color-accent);
      outline-offset: 2px;
      border-radius: inherit;
    }
    ```
    With:
    ```css
    :focus-visible {
      outline: none;
      box-shadow: 0 0 0 2px var(--color-bg), 0 0 0 4px var(--color-accent);
      border-radius: inherit;
      transition: box-shadow var(--duration-100) var(--ease-out);
    }
    ```

    **B. Refine input focus-visible rules (around line 588)**
    Replace:
    ```css
    .input-field:focus-visible,
    .auth-input:focus-visible {
      outline: none;
      box-shadow: 0 0 0 3px rgba(154, 52, 18, 0.25);
    }
    ```
    With:
    ```css
    .input-field:focus-visible,
    .auth-input:focus-visible,
    .auth-otp-digit:focus-visible {
      outline: none;
      box-shadow: 0 0 0 3px rgba(154, 52, 18, 0.1);
      border-color: var(--color-accent);
      transition: border-color var(--duration-100) var(--ease-out), box-shadow var(--duration-100) var(--ease-out);
    }
    ```

    **C. Add sidebar focus ring override (after existing `.vault-sidebar .nav-link` rules, around line 535)**
    Add:
    ```css
    .vault-sidebar .nav-link:focus-visible {
      outline: none;
      box-shadow: 0 0 0 2px var(--color-sidebar-bg), 0 0 0 4px rgba(253, 186, 116, 0.5);
      border-radius: var(--radius-md);
      transition: box-shadow var(--duration-100) var(--ease-out);
    }
    ```

    **D. Replace tab focus ring (around line 1298)**
    Replace:
    ```css
    .tab:focus-visible {
      outline: 2px solid var(--color-accent);
      outline-offset: -2px;
      border-radius: inherit;
    }
    ```
    With:
    ```css
    .tab:focus-visible {
      outline: none;
      box-shadow: inset 0 0 0 2px var(--color-accent);
      border-radius: inherit;
      transition: box-shadow var(--duration-100) var(--ease-out);
    }
    ```

    **E. Add bottom nav focus ring (after `.bottom-nav-item` rules, around line 636)**
    Add:
    ```css
    .bottom-nav-item:focus-visible {
      outline: none;
      box-shadow: 0 0 0 2px var(--color-surface-0), 0 0 0 4px var(--color-accent);
      border-radius: var(--radius-md);
      transition: box-shadow var(--duration-100) var(--ease-out);
    }
    ```

    **F. Update card hover base rule (around line 436)**
    Replace:
    ```css
    .card-hover {
      transition: box-shadow var(--duration-200), transform var(--duration-200);
    }
    ```
    With:
    ```css
    .card-hover {
      transition: box-shadow var(--duration-200) var(--ease-out), transform var(--duration-200) var(--ease-out);
    }
    .card-hover:hover {
      box-shadow: var(--shadow-md);
      transform: translateY(-2px);
    }
    ```

    **G. Remove duplicate legacy blocks**
    Delete the entire `/* ─── Micro-interactions & Polish ───────────────────────────────────────────── */` section at the bottom of globals.css (lines ~1225-1257). This section contains duplicates of `.card-hover:hover`, `.btn-primary:active`, etc. that will be consolidated above.
    Also delete the duplicate `.btn-secondary:active, .btn-ghost:active, .btn-danger:active` block around line 570.

    **H. Add consolidated button active states (after existing button disabled rules)**
    Ensure the following exists exactly once (consolidating any remnants):
    ```css
    .btn-primary:active,
    .btn-secondary:active,
    .btn-ghost:active,
    .btn-danger:active {
      transform: scale(0.97);
      transition: transform 60ms var(--ease-out);
    }
    ```

    **I. Add input error states**
    Add:
    ```css
    .input-error {
      border-color: var(--color-error-border) !important;
    }
    .input-error-shake {
      animation: inputShake var(--duration-200) var(--ease-in-out) 1;
    }
    ```

    **J. Add floating label system**
    Add:
    ```css
    .input-floating-label-wrap {
      position: relative;
      padding-top: 1rem;
    }
    .input-floating-label {
      position: absolute;
      left: 0.75rem;
      top: 1.5rem;
      font-size: var(--font-size-meta);
      color: var(--color-text-2);
      font-weight: 500;
      pointer-events: none;
      transform-origin: left top;
      transition: transform var(--duration-200) var(--ease-out), color var(--duration-200) var(--ease-out);
    }
    .input-field:focus ~ .input-floating-label,
    .input-field:not(:placeholder-shown) ~ .input-floating-label,
    .auth-input:focus ~ .input-floating-label,
    .auth-input:not(:placeholder-shown) ~ .input-floating-label,
    .select-has-value ~ .input-floating-label {
      transform: translateY(-1.25rem) scale(0.85);
      color: var(--color-accent);
    }
    ```
    Also add auth-specific floating label override if the `top: 1.5rem` value does not visually align with `.auth-input` text (padding: 0.875rem 1rem). If misaligned after testing visually, add:
    ```css
    .input-floating-label-wrap .auth-input ~ .input-floating-label {
      top: 1.875rem;
    }
    ```

    **K. Refine sidebar nav transitions (around line 518)**
    Replace the existing `.vault-sidebar .nav-link` base and hover rules with:
    ```css
    .vault-sidebar .nav-link {
      color: rgba(255, 255, 255, 0.75);
      transition: background-color var(--duration-200) var(--ease-out),
                  color var(--duration-200) var(--ease-out),
                  box-shadow var(--duration-200) var(--ease-out);
      line-height: 1.4;
      letter-spacing: 0.005em;
    }
    .vault-sidebar .nav-link:hover {
      color: rgba(255, 255, 255, 0.95);
      background-color: rgba(255, 255, 255, 0.08);
      transition: background-color var(--duration-300) var(--ease-out),
                  color var(--duration-300) var(--ease-out);
    }
    .vault-sidebar .nav-link.active {
      color: rgba(255, 255, 255, 0.98);
      background-color: rgba(255, 255, 255, 0.12);
      font-weight: 600;
      box-shadow: inset 2px 0 0 rgba(253, 186, 116, 0.7);
    }
    ```

    **L. Verify `prefers-reduced-motion` catch-all remains intact at the bottom of globals.css.**
  </action>
  <verify>
    <automated>grep -n "box-shadow: 0 0 0 2px var(--color-bg), 0 0 0 4px var(--color-accent)" src/app/globals.css && grep -n "@keyframes inputShake" src/styles/motion.css && grep -n "input-error-shake" src/styles/motion.css && grep -n "card-hover" src/components/ui/card.tsx</automated>
  </verify>
  <done>
    - globals.css contains box-shadow focus ring, input error states, floating labels, refined sidebar nav transitions
    - Duplicate legacy CSS blocks removed
    - motion.css contains inputShake keyframe
    - card.tsx contains asLink prop and auto card-hover logic
  </done>
</task>

<task type="auto">
  <name>Task 2: Add inputShake keyframe and reduced-motion coverage to motion.css</name>
  <files>src/styles/motion.css</files>
  <action>
    Read motion.css fully.

    **A. Add `inputShake` keyframe before the reduced-motion media query.**
    Add:
    ```css
    @keyframes inputShake {
      0%   { transform: translateX(0); }
      25%  { transform: translateX(-2px); }
      75%  { transform: translateX(2px); }
      100% { transform: translateX(0); }
    }
    ```

    **B. Add `.input-error-shake` to the reduced-motion override list.**
    In the `@media (prefers-reduced-motion: reduce)` block, add `.input-error-shake` to the selector list. The block should read:
    ```css
    @media (prefers-reduced-motion: reduce) {
      .motion-fade-in,
      .motion-fade-out,
      .motion-slide-up,
      .motion-slide-down,
      .motion-scale-in,
      .motion-stagger > *,
      .vault-page-transition,
      .vault-page-enter,
      .toast-enter,
      .fab-enter,
      .search-dropdown-enter,
      .side-panel-overlay,
      .side-panel-drawer,
      .notif-dropdown,
      .tab-panel,
      .kbd-overlay,
      .skeleton-shimmer,
      .input-error-shake {
        animation: none !important;
        opacity: 1 !important;
        transform: none !important;
      }
    }
    ```
  </action>
  <verify>
    <automated>grep -n "@keyframes inputShake" src/styles/motion.css && grep -n "input-error-shake" src/styles/motion.css</automated>
  </verify>
  <done>
    - motion.css contains `@keyframes inputShake` with the exact 4 keyframes
    - `.input-error-shake` appears in the `@media (prefers-reduced-motion: reduce)` selector list
  </done>
</task>

<task type="auto">
  <name>Task 3: Update Card component with asLink prop and auto card-hover class</name>
  <files>src/components/ui/card.tsx</files>
  <action>
    Read card.tsx fully.

    **A. Add `asLink` prop to CardProps interface.**
    Change:
    ```typescript
    interface CardProps {
      variant?: CardVariant;
      padding?: "sm" | "md" | "lg";
      className?: string;
      style?: CSSProperties;
      children: React.ReactNode;
      onClick?: () => void;
    }
    ```
    To:
    ```typescript
    interface CardProps {
      variant?: CardVariant;
      padding?: "sm" | "md" | "lg";
      className?: string;
      style?: CSSProperties;
      children: React.ReactNode;
      onClick?: () => void;
      asLink?: boolean;
    }
    ```

    **B. Update the Card function to auto-apply `card-hover` class.**
    Change the destructuring to include `asLink`:
    ```typescript
    export function Card({
      variant = "default",
      padding = "md",
      className,
      style,
      children,
      onClick,
      asLink,
    }: CardProps) {
    ```

    Update the `combinedClassName` construction:
    ```typescript
    const isInteractive = !!onClick || !!asLink;
    const combinedClassName = [
      className,
      isInteractive ? "card-hover" : "",
    ].filter(Boolean).join(" ");
    ```

    **C. Remove inline transition from `interactive` variant style.**
    In `variantStyles.interactive`, remove the `transition` and `cursor` properties. The hover effect will come entirely from the `.card-hover` CSS class.
    Change:
    ```typescript
    interactive: {
      background: "var(--surface-base)",
      border: "1px solid var(--color-border)",
      boxShadow: "var(--shadow-xs)",
      cursor: "pointer",
      transition: "box-shadow var(--transition-normal), transform var(--transition-normal)",
    },
    ```
    To:
    ```typescript
    interactive: {
      background: "var(--surface-base)",
      border: "1px solid var(--color-border)",
      boxShadow: "var(--shadow-xs)",
    },
    ```
    Note: `var(--transition-normal)` does not exist in the token system. Removing it is correct.

    **D. Render with `className` always applied.**
    Ensure the returned element uses `combinedClassName`:
    ```tsx
    <Component
      className={combinedClassName}
      style={combinedStyle}
      onClick={onClick}
      {...(onClick ? { type: "button" as const } : {})}
    >
    ```
  </action>
  <verify>
    <automated>grep -n "asLink" src/components/ui/card.tsx && grep -n "card-hover" src/components/ui/card.tsx && grep -n "isInteractive" src/components/ui/card.tsx</automated>
  </verify>
  <done>
    - CardProps interface contains `asLink?: boolean`
    - Card auto-applies `"card-hover"` class when `onClick` or `asLink` is truthy
    - `interactive` variant no longer has inline `transition` or `cursor`
    - Component renders with `combinedClassName`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS → browser rendering | Styles are static, no user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Denial of Service | globals.css animations | mitigate | All animations use GPU-accelerated properties only (transform, opacity, box-shadow). No layout-triggering animations (width, height, margin). |
| T-33-02 | Tampering | globals.css | accept | No user input is rendered into CSS. Styles are static declarations. |
| T-33-03 | Information Disclosure | focus ring styling | accept | Focus ring colors are public design tokens. No sensitive data leakage. |
| T-33-04 | Accessibility Bypass | :focus-visible migration | mitigate | Never apply `outline: none` without a `box-shadow` substitute. Verify focus rings are visible on keyboard navigation. Use `:focus-visible` exclusively — never `:focus`. |
</threat_model>

<verification>
- `grep -c "box-shadow: 0 0 0 2px var(--color-bg), 0 0 0 4px var(--color-accent)" src/app/globals.css` returns ≥1
- `grep -c "@keyframes inputShake" src/styles/motion.css` returns 1
- `grep -c "input-error-shake" src/styles/motion.css` returns 1
- `grep -c "asLink" src/components/ui/card.tsx` returns ≥1
- Build passes: `npm run build` (or `next build`) exits 0
- Tests pass: `pnpm test` exits 0
</verification>

<success_criteria>
- globals.css contains all UI-SPEC CSS contracts for focus rings, card hover, button active, input errors, floating labels, and sidebar nav transitions
- motion.css contains `@keyframes inputShake` and `.input-error-shake` in reduced-motion block
- card.tsx auto-applies `card-hover` class when interactive
- Build and tests pass with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/33-micro-interacoes-em-componentes-base/33-01-SUMMARY.md`
</output>
