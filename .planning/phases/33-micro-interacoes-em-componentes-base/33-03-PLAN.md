---
phase: 33-micro-interacoes-em-componentes-base
plan: 03
type: execute
wave: 2
depends_on:
  - 33-01-PLAN.md
files_modified:
  - src/app/(vault)/financeiro/components/expense-filters.tsx
  - src/app/(vault)/financeiro/components/expense-side-panel.tsx
  - src/app/(vault)/financeiro/components/expense-category-modal.tsx
  - src/app/(vault)/inicio/components/reminders-section.tsx
  - src/app/(vault)/components/search-bar.tsx
autonomous: true
requirements:
  - MICR-04
  - MICR-06
  - MICR-07
user_setup: []
must_haves:
  truths:
    - "Vault inputs display floating labels that animate on focus and when filled"
    - "Vault forms with validation show error shake on invalid fields"
    - "All interactive elements have appropriate cursor"
    - "Build passes and all tests pass"
  artifacts:
    - path: "src/app/(vault)/financeiro/components/expense-filters.tsx"
      provides: "Expense filters with floating labels"
      contains: "input-floating-label-wrap"
    - path: "src/app/(vault)/financeiro/components/expense-side-panel.tsx"
      provides: "Expense form with floating labels and error shake"
      contains: "input-error-shake"
  key_links:
    - from: "vault form components"
      to: "src/app/globals.css"
      via: "input-floating-label-wrap and input-error-shake classes"
      pattern: "input-floating-label"
    - from: "expense-side-panel.tsx"
      to: "src/styles/motion.css"
      via: "inputShake keyframe"
      pattern: "input-error-shake"
---

<objective>
Restructure all vault inputs with floating labels. Add error shake to vault forms that have validation. Verify cursor appropriateness across all interactive elements and confirm build/tests pass.

Purpose: Complete the micro-interaction coverage for the vault (protected) area of the app. Vault forms include expense filters, expense entry, category modal, reminders, and search.
Output: All vault inputs use floating labels; validation errors trigger shake; cursor audit verified; build and tests green.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-micro-interacoes-em-componentes-base/33-CONTEXT.md
@.planning/phases/33-micro-interacoes-em-componentes-base/33-UI-SPEC.md
@src/app/globals.css
@src/app/(vault)/financeiro/components/expense-filters.tsx
@src/app/(vault)/financeiro/components/expense-side-panel.tsx
@src/app/(vault)/financeiro/components/expense-category-modal.tsx
@src/app/(vault)/inicio/components/reminders-section.tsx
@src/app/(vault)/components/search-bar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add floating labels to expense-filters, reminders-section, and search-bar</name>
  <files>src/app/(vault)/financeiro/components/expense-filters.tsx, src/app/(vault)/inicio/components/reminders-section.tsx, src/app/(vault)/components/search-bar.tsx</files>
  <action>
    Read all three files fully.

    **Restructure every `.input-field` input** from the current `<label htmlFor="id">Label <input id="id" className="input-field" /></label>` or separate label+input pattern to the floating label pattern:
    ```tsx
    <div style={{ position: "relative" }} className="input-floating-label-wrap">
      <input
        id="id"
        className="input-field"
        placeholder=" "
        ...
      />
      <label htmlFor="id" className="input-floating-label">Label Text</label>
    </div>
    ```

    **For expense-filters.tsx specifically:**
    - The current `fieldStyle` is `{ display: "flex", flexDirection: "column", gap: "var(--space-1)" }`. When converting to `.input-floating-label-wrap`, keep the flex column gap for spacing between the wrap and any adjacent elements. The wrap itself provides `padding-top: 1rem` via CSS.
    - For the `<select className="input-field">` (category filter): conditionally add `select-has-value` class when `value !== ""`:
      ```tsx
      <select
        id="expense-filter-category"
        className={`input-field ${category ? "select-has-value" : ""}`}
        ...
      >
      ```
    - Remove the old `<label htmlFor="..." style={labelStyle}>` elements — their text becomes the `.input-floating-label` content.
    - Verify the filter row still aligns properly with `alignItems: flex-end`. The `.input-floating-label-wrap` padding-top should not break the row height; if it does, adjust `fieldStyle` or the row style minimally.

    **For reminders-section.tsx specifically:**
    - Identify the `.input-field` inputs (grep showed 2 matches)
    - Wrap each with `.input-floating-label-wrap` and add `.input-floating-label` sibling labels.
    - Remove any existing label elements.

    **For search-bar.tsx specifically:**
    - Identify the `.input-field` input (grep showed 1 match)
    - Wrap with `.input-floating-label-wrap` and add `.input-floating-label` sibling label.
    - If the search input has no visible label (common for search), consider adding an `aria-label` or a visually hidden label for accessibility. The floating label can say "Buscar" or remain hidden if the search icon provides sufficient context. Use discretion — if adding a visible label breaks the search bar design, use `sr-only` class or `aria-label` instead, but still wrap in `.input-floating-label-wrap` to maintain consistency.

    **Accessibility:** Every input MUST retain a label association. Use `<label htmlFor={id}>` matching the input's `id`.
  </action>
  <verify>
    <automated>grep -c "input-floating-label-wrap" src/app/(vault)/financeiro/components/expense-filters.tsx && grep -c "input-floating-label-wrap" src/app/(vault)/inicio/components/reminders-section.tsx && grep -c "input-floating-label-wrap" src/app/(vault)/components/search-bar.tsx && grep -c "select-has-value" src/app/(vault)/financeiro/components/expense-filters.tsx</automated>
  </verify>
  <done>
    - expense-filters.tsx has floating labels on all 5 inputs, with `select-has-value` on category select
    - reminders-section.tsx has floating labels on all inputs
    - search-bar.tsx has floating label or accessible alternative
    - No old `<label>` wrappers remain around inputs
  </done>
</task>

<task type="auto">
  <name>Task 2: Add floating labels and error shake to expense-side-panel and expense-category-modal</name>
  <files>src/app/(vault)/financeiro/components/expense-side-panel.tsx, src/app/(vault)/financeiro/components/expense-category-modal.tsx</files>
  <action>
    Read both files fully.

    **A. Add floating labels to all inputs in both files.**
    Apply the same floating label restructure as Task 1 to every `.input-field` input in these components.
    - Wrap each input in `.input-floating-label-wrap`
    - Add `<label htmlFor={id} className="input-floating-label">` as a sibling after the input
    - For `<select>` elements, add `select-has-value` class when the select has a non-empty value
    - Remove old label wrappers

    **B. Wire `.input-error-shake` for validation errors.**
    In `expense-side-panel.tsx`:
    - The component has `formError` (general string) and `uploadError`. If the server action returns field-specific errors, map them to individual fields and apply `.input-error-shake` to those fields.
    - If only a general `formError` exists (no per-field mapping), add `.input-error-shake` to ALL form inputs when `formError` is set. This provides visible feedback that something is wrong without requiring server-side field mapping changes.
    - Use `onAnimationEnd` on each input to remove the `.input-error-shake` class after the `inputShake` animation completes, preventing replay on re-render:
      ```tsx
      onAnimationEnd={(e) => {
        if (e.animationName === "inputShake") {
          e.currentTarget.classList.remove("input-error-shake");
        }
      }}
      ```
    - Also apply `.input-error` class to keep the red border after the shake finishes.

    In `expense-category-modal.tsx`:
    - Identify how validation errors are handled (likely similar pattern with server actions)
    - Apply the same shake logic: add `.input-error input-error-shake` classes to fields with errors, remove shake class on `animationend`.

    **C. Maintain form functionality.**
    Do NOT change form submission logic, server action calls, or state management. Only modify the input markup, label structure, and error class application.
  </action>
  <verify>
    <automated>grep -c "input-floating-label-wrap" src/app/(vault)/financeiro/components/expense-side-panel.tsx && grep -c "input-floating-label-wrap" src/app/(vault)/financeiro/components/expense-category-modal.tsx && grep -c "input-error-shake" src/app/(vault)/financeiro/components/expense-side-panel.tsx && grep -c "input-error-shake" src/app/(vault)/financeiro/components/expense-category-modal.tsx</automated>
  </verify>
  <done>
    - expense-side-panel.tsx has floating labels on all inputs
    - expense-category-modal.tsx has floating labels on all inputs
    - Both components apply `.input-error-shake` on validation errors
    - Shake class is removed after animation completes via `onAnimationEnd`
    - Form submission logic is unchanged
  </done>
</task>

<task type="auto">
  <name>Task 3: Cursor audit, build verification, and reduced-motion check</name>
  <files>src/app/globals.css</files>
  <action>
    **A. Cursor Audit**
    Read the UI-SPEC.md "Cursor Audit Requirements" section (section 6). Verify the following are already covered by existing CSS in globals.css or by browser defaults. If ANY are missing, add the appropriate `cursor` declaration:

    | Element | Cursor | Status Check |
    |---------|--------|--------------|
    | Botões (`<button>`, `[role="button"]`) | `pointer` | Covered by catch-all: `a, button, [role="button"], label[for], .nav-link, summary { cursor: pointer; }` |
    | Links (`<a>`, `<Link>`) | `pointer` | Covered by catch-all |
    | Cards clicáveis | `pointer` | Card renders as `<button>` when `onClick` present; covered by catch-all |
    | `.row-interactive` | `pointer` | Already defined in globals.css |
    | Inputs | `text` | Browser default; verify NOT overridden by catch-all |
    | Inputs desabilitados | `not-allowed` | Already defined for `.btn-*:disabled` |
    | FABs (`.fab-mobile`) | `pointer` | Verify `.fab-mobile` has `cursor: pointer` or inherits from button |
    | Tabs (`.tab`) | `pointer` | Already defined in globals.css |
    | Notif bell (`.notif-bell`) | `pointer` | Already defined in globals.css |
    | Side panel close (`.side-panel-close`) | `pointer` | Already defined in globals.css |
    | Mini calendar days (`.mini-cal-day`) | `pointer` | Already defined in globals.css |
    | Month calendar bars (`.month-cal-bar`) | `pointer` | Already defined in globals.css |
    | Select inputs | `pointer` | Add if missing: `select.input-field { cursor: pointer; }` |
    | Template cards (`.template-card`) | `pointer` | Verify; add if missing |

    **If any cursor declaration is missing, add it to globals.css in the existing interactivity section (around line 1612).**

    **B. Reduced Motion Verification**
    Confirm that the catch-all at the bottom of globals.css still exists:
    ```css
    @media (prefers-reduced-motion: reduce) {
      *, *::before, *::after {
        animation-duration: 0.01ms !important;
        animation-iteration-count: 1 !important;
        transition-duration: 0.01ms !important;
        scroll-behavior: auto !important;
      }
    }
    ```
    This ensures floating labels, error shake, and all new transitions are instantly disabled when the user prefers reduced motion.

    **C. Build and Test**
    Run the build and test suite:
    ```bash
    pnpm test
    ```
    If any tests fail due to DOM structure changes (e.g., tests looking for `.auth-label` or specific label markup), update the tests to match the new floating label structure.
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <done>
    - All cursor declarations from the UI-SPEC audit table are present in globals.css
    - `prefers-reduced-motion` catch-all exists and covers all new animations
    - `pnpm test` exits 0 with all tests passing
    - `next build` exits 0 with zero TypeScript errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vault form → browser | Form markup is static; no new data handling introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-08 | Information Disclosure | form error states | accept | Error messages are already user-facing. Shake animation does not expose new information. |
| T-33-09 | Accessibility Bypass | floating label markup | mitigate | Every input retains a `<label htmlFor>` association. No placeholder-only labels. |
| T-33-10 | Denial of Service | error shake animation | mitigate | Shake bounded to 2px/200ms/1 iteration. Disabled by `prefers-reduced-motion` catch-all. |
| T-33-11 | Accessibility Bypass | cursor audit | mitigate | All interactive elements have explicit `cursor` declarations. No invisible clickable areas. |
</threat_model>

<verification>
- `grep -c "input-floating-label-wrap" src/app/(vault)/financeiro/components/expense-filters.tsx` returns ≥5
- `grep -c "input-floating-label-wrap" src/app/(vault)/financeiro/components/expense-side-panel.tsx` returns ≥5
- `grep -c "input-floating-label-wrap" src/app/(vault)/financeiro/components/expense-category-modal.tsx` returns ≥2
- `grep -c "input-error-shake" src/app/(vault)/financeiro/components/expense-side-panel.tsx` returns ≥1
- `grep -c "cursor: pointer" src/app/globals.css` returns ≥2 (catch-all exists)
- `pnpm test` exits 0
- `next build` exits 0
</verification>

<success_criteria>
- All vault inputs are wrapped in `.input-floating-label-wrap` with accessible `<label>` siblings
- Vault forms with validation apply `.input-error-shake` and remove it after animation
- Cursor audit confirms all interactive elements have appropriate `cursor` declarations
- Build and all 419+ tests pass with zero errors
- `prefers-reduced-motion` catch-all disables all new animations
</success_criteria>

<output>
After completion, create `.planning/phases/33-micro-interacoes-em-componentes-base/33-03-SUMMARY.md`
</output>
