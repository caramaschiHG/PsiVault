---
phase: 34-feedback-de-acao-e-loading
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/form-submit-button.tsx
  - src/app/(vault)/patients/[patientId]/documents/new/components/document-composer-form.tsx
  - src/app/(vault)/patients/[patientId]/documents/[documentId]/edit/page.tsx
  - src/app/(vault)/patients/[patientId]/archive-confirm/page.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "FormSubmitButton component exists and uses useFormStatus from react-dom"
    - "Document composer form shows loading state on submit"
    - "Document edit form shows loading state on submit"
    - "Archive confirm forms show loading state on submit"
  artifacts:
    - path: "src/components/ui/form-submit-button.tsx"
      provides: "Reusable submit button with loading feedback"
      exports: ["FormSubmitButton"]
    - path: "src/app/(vault)/patients/[patientId]/documents/new/components/document-composer-form.tsx"
      provides: "Document creation form with loading submit button"
      imports: ["FormSubmitButton"]
    - path: "src/app/(vault)/patients/[patientId]/documents/[documentId]/edit/page.tsx"
      provides: "Document edit form with loading submit button"
      imports: ["FormSubmitButton"]
    - path: "src/app/(vault)/patients/[patientId]/archive-confirm/page.tsx"
      provides: "Archive confirmation forms with loading submit buttons"
      imports: ["FormSubmitButton"]
  key_links:
    - from: "FormSubmitButton"
      to: "react-dom/useFormStatus"
      via: "import { useFormStatus }"
      pattern: "useFormStatus"
---

<objective>
Implementar feedback visual de loading em botões de submit de formulários críticos do vault.

Purpose: Garantir que o usuário receba feedback imediato e calmo sobre o estado de suas ações de submit, prevenindo cliques duplos e reduzindo ansiedade.
Output: Componente FormSubmitButton reutilizável e integrado em todos os formulários de alta visibilidade identificados.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-feedback-de-acao-e-loading/34-CONTEXT.md

## Interfaces

From src/components/ui/spinner.tsx:
```typescript
export function Spinner({
  size = "sm",
  className = "",
  "aria-label": ariaLabel = "Carregando...",
}: SpinnerProps)
```

From src/components/ui/button.tsx:
```typescript
export const Button = forwardRef<HTMLButtonElement | HTMLAnchorElement, ButtonProps>(
  function Button({ variant = "primary", size = "md", isLoading = false, loadingLabel, children, className = "", as = "button", disabled, ...rest })
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar componente FormSubmitButton com useFormStatus</name>
  <files>src/components/ui/form-submit-button.tsx</files>
  <read_first>
    - src/components/ui/spinner.tsx (Spinner API e props)
    - src/components/ui/button.tsx (padrão de componente UI do vault)
  </read_first>
  <action>Criar client component `"use client"` que importa `useFormStatus` de `react-dom` e `Spinner` de `@/components/ui/spinner`.

Interface:
```typescript
interface FormSubmitButtonProps {
  label: string;
  pendingLabel: string;
  style?: React.CSSProperties;
  className?: string;
}
```

Comportamento:
- Usar `const { pending } = useFormStatus();`
- Renderizar `<button type="submit" disabled={pending}>`
- Aplicar `opacity: pending ? 0.7 : 1` e `cursor: pending ? "wait" : "pointer"`
- Mostrar `<Spinner size="sm" aria-hidden="true" />` quando `pending === true`
- Mostrar `pendingLabel` quando pendente, senão `label`
- Usar `display: "flex", alignItems: "center", gap: "0.5rem"` no botão

NÃO usar classes CSS globais — usar estilos inline com `React.CSSProperties` para manter consistência com o restante do vault.
</action>
  <verify>
    <automated>grep -n "useFormStatus" src/components/ui/form-submit-button.tsx && grep -n "Spinner" src/components/ui/form-submit-button.tsx</automated>
  </verify>
  <done>Arquivo existe, exporta FormSubmitButton, usa useFormStatus corretamente, mostra Spinner durante pending state.</done>
</task>

<task type="auto">
  <name>Task 2: Wire FormSubmitButton em formulários críticos do vault</name>
  <files>
    src/app/(vault)/patients/[patientId]/documents/new/components/document-composer-form.tsx
    src/app/(vault)/patients/[patientId]/documents/[documentId]/edit/page.tsx
    src/app/(vault)/patients/[patientId]/archive-confirm/page.tsx
  </files>
  <read_first>
    - src/components/ui/form-submit-button.tsx (componente criado na Task 1)
    - src/app/(vault)/patients/[patientId]/documents/new/components/document-composer-form.tsx (estado atual do botão)
    - src/app/(vault)/patients/[patientId]/documents/[documentId]/edit/page.tsx (estado atual do botão)
    - src/app/(vault)/patients/[patientId]/archive-confirm/page.tsx (estado atual dos botões)
  </read_first>
  <action>Substituir botões de submit brutos (`<button type="submit">`) por `<FormSubmitButton>` nos três arquivos identificados.

### document-composer-form.tsx
- Importar: `import { FormSubmitButton } from "@/components/ui/form-submit-button";`
- Substituir o botão de submit existente por:
```tsx
<FormSubmitButton
  label="Salvar documento"
  pendingLabel="Salvando..."
  style={submitButtonStyle}
/>
```

### documents/[documentId]/edit/page.tsx
- Importar: `import { FormSubmitButton } from "@/components/ui/form-submit-button";`
- Substituir o botão de submit existente por:
```tsx
<FormSubmitButton
  label="Salvar alterações"
  pendingLabel="Salvando..."
  style={submitButtonStyle}
/>
```

### archive-confirm/page.tsx
- Importar: `import { FormSubmitButton } from "@/components/ui/form-submit-button";`
- Substituir os 3 botões de submit brutos por FormSubmitButton com labels apropriados:
  - "Cancelar todas e arquivar" / "Arquivando..."
  - "Manter sessões e arquivar" / "Arquivando..."
  - "Arquivar paciente" / "Arquivando..."

Preservar os estilos inline existentes (`style={...}`) passando-os via prop `style` do FormSubmitButton.
</action>
  <verify>
    <automated>grep -n "FormSubmitButton" src/app/(vault)/patients/[patientId]/documents/new/components/document-composer-form.tsx && grep -n "FormSubmitButton" src/app/(vault)/patients/[patientId]/documents/[documentId]/edit/page.tsx && grep -n "FormSubmitButton" src/app/(vault)/patients/[patientId]/archive-confirm/page.tsx</automated>
  </verify>
  <done>Todos os três arquivos importam e usam FormSubmitButton; nenhum `<button type="submit">` bruto permanece nos arquivos modificados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client component → Form action | FormSubmitButton é Client Component que interage com Server Actions via formulários HTML. Nenhum dado sensível atravessa o componente em si. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Denial of Service | FormSubmitButton | accept | `disabled={pending}` previne cliques duplos, mas não protege contra spam de requisições fora do browser. Mitigação aceita pois Server Actions já validam sessão e workspace no servidor. |
| T-34-02 | Information Disclosure | Spinner aria-label | mitigate | Spinner usa `aria-hidden="true"` quando dentro do botão pendente; o botão desabilitado já comunica estado a leitores de tela via `disabled`. |
</threat_model>

<verification>
- `pnpm test` — todos os testes passam
- `pnpm build` — zero erros de TypeScript
- Verificar que `FormSubmitButton` é importado em todos os 3 arquivos alvo
- Verificar que nenhum `<button type="submit">` bruto permanece nos arquivos alvo
</verification>

<success_criteria>
- [ ] FormSubmitButton existe em `src/components/ui/form-submit-button.tsx`
- [ ] Componente usa `useFormStatus` de `react-dom`
- [ ] Componente mostra Spinner + label pendente durante submit
- [ ] document-composer-form usa FormSubmitButton
- [ ] document edit page usa FormSubmitButton
- [ ] archive-confirm page usa FormSubmitButton em todos os 3 botões
- [ ] Build passa sem erros
</success_criteria>

<output>
After completion, create `.planning/phases/34-feedback-de-acao-e-loading/34-01-SUMMARY.md`
</output>
