---
phase: 34-feedback-de-acao-e-loading
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/motion.css
  - src/components/ui/toast-provider.tsx
autonomous: true
requirements:
  - FEED-01
must_haves:
  truths:
    - Toasts entram com slide+fade de 200ms
    - Toasts saem com fade de 300ms
    - Toast permanece 3000ms antes de iniciar saída
    - prefers-reduced-motion desativa animações de toast
  artifacts:
    - path: src/styles/motion.css
      provides: toastFadeOut keyframe, .toast-exit class, reduced-motion fallback
      contains: "@keyframes toastFadeOut", ".toast-exit"
    - path: src/components/ui/toast-provider.tsx
      provides: Toast exit animation via CSS class, synced timeouts
      contains: "toast-exit", "3300"
  key_links:
    - from: src/components/ui/toast-provider.tsx
      to: .toast-exit
      via: className={t.fading ? "toast-exit" : "toast-enter"}
---

<objective>
Refinar o sistema de toast para animações de entrada e saída consistentes com os tokens de motion do projeto — ≤300ms, CSS-first, com fallback para reduced motion.

Purpose: Garantir que toasts forneçam feedback visual calmo e imediato sem bloquear interação, alinhado ao tom discreto do PsiVault.
Output: `toast-provider.tsx` e `motion.css` atualizados com animações tokenizadas.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/34-feedback-de-acao-e-loading/34-CONTEXT.md
@.planning/phases/34-feedback-de-acao-e-loading/34-RESEARCH.md
@.planning/phases/34-feedback-de-acao-e-loading/34-PATTERNS.md
@.planning/phases/34-feedback-de-acao-e-loading/34-UI-SPEC.md

# Decisões travadas
- D-01: CSS-first — sem Framer Motion
- D-02: Entrada slide+fade ≤300ms (usar `toastSlideIn` existente)
- D-03: Saída fade puro ≤300ms, manter estado `fading` no React
- D-04: Toast visível por ~3000ms antes de sair
- D-05: Não animar reposicionamento do stack
- D-06: Max 4 toasts, posição bottom-right fixa

# Estado atual do toast-provider.tsx
- Usa inline `transition: "opacity 500ms ease"` para saída (hardcoded, fora dos tokens)
- Timeout de remoção é 3500ms (500ms após início do fade = desalinhado)
- Não tem classe CSS para saída — usa inline style que bypassa `prefers-reduced-motion`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar keyframe e classe de saída do toast em motion.css</name>
  <files>src/styles/motion.css</files>
  <read_first>
    - src/styles/motion.css
    - src/components/ui/toast-provider.tsx
  </read_first>
  <action>
    Adicionar ao final de `src/styles/motion.css` (antes da media query `prefers-reduced-motion`):

    1. Keyframe `toastFadeOut`:
    ```css
    @keyframes toastFadeOut {
      from { opacity: 1; }
      to   { opacity: 0; }
    }
    ```

    2. Classe `.toast-exit`:
    ```css
    .toast-exit {
      animation: toastFadeOut var(--duration-300) var(--ease-out) forwards;
    }
    ```

    3. Dentro da media query `@media (prefers-reduced-motion: reduce)`, adicionar `.toast-exit` à lista de seletores existentes (já cobre `.toast-enter`, `.skeleton-shimmer`, etc.). Se `.toast-exit` não estiver na lista, adicioná-lo:
    ```css
    @media (prefers-reduced-motion: reduce) {
      .motion-fade-in,
      .motion-fade-out,
      .motion-slide-up,
      .motion-slide-down,
      .motion-scale-in,
      .motion-stagger > *,
      .vault-page-transition,
      .vault-page-enter,
      .toast-enter,
      .toast-exit,
      .fab-enter,
      .search-dropdown-enter,
      .side-panel-overlay,
      .side-panel-drawer,
      .notif-dropdown,
      .tab-panel,
      .kbd-overlay,
      .skeleton-shimmer,
      .input-error-shake {
        animation: none !important;
        opacity: 1 !important;
        transform: none !important;
      }
    }
    ```

    Nota: a regra para `.toast-exit` em reduced-motion deve definir `opacity: 0 !important` em vez de `opacity: 1 !important` para que o toast desapareça instantaneamente. Adicionar uma regra específica logo após o bloco geral:
    ```css
    @media (prefers-reduced-motion: reduce) {
      .toast-exit {
        opacity: 0 !important;
      }
    }
    ```
  </action>
  <verify>
    <automated>grep -n "toastFadeOut\|\.toast-exit" src/styles/motion.css</automated>
  </verify>
  <done>O keyframe `toastFadeOut`, a classe `.toast-exit` e o fallback `prefers-reduced-motion` existem em `motion.css`</done>
  <acceptance_criteria>
    - `src/styles/motion.css` contém `@keyframes toastFadeOut`
    - `src/styles/motion.css` contém `.toast-exit { animation: toastFadeOut var(--duration-300) var(--ease-out) forwards; }`
    - A media query `prefers-reduced-motion: reduce` em `motion.css` inclui `.toast-exit` na lista de seletores
    - Existe regra específica `.toast-exit { opacity: 0 !important; }` dentro de `prefers-reduced-motion`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Atualizar toast-provider.tsx para usar .toast-exit e sincronizar timeouts</name>
  <files>src/components/ui/toast-provider.tsx</files>
  <read_first>
    - src/components/ui/toast-provider.tsx
    - src/styles/motion.css
  </read_first>
  <action>
    Editar `src/components/ui/toast-provider.tsx`:

    1. Na função `toast`, alinhar o timeout de remoção do DOM:
       - Manter `setTimeout(() => { setToasts(...) /* fading=true */ }, 3000);`
       - Alterar `setTimeout(() => { setToasts(...) /* filter */ }, 3500);` para `3300` (3000ms visível + 300ms fade = 3300ms total)

    2. No render do toast item, substituir o inline transition por uso da classe `.toast-exit`:
       - Alterar `className={t.fading ? "" : "toast-enter"}` para `className={t.fading ? "toast-exit" : "toast-enter"}`
       - Remover a propriedade inline `transition` do style (linha 71: `transition: t.fading ? "opacity 500ms ease" : "none"`)
       - Manter `opacity: t.fading ? 0 : 1` como fallback (a classe CSS fará a animação, mas o inline opacity garante o estado final)

    3. Remover a propriedade `transition` do `toastStyle` constante (linha 115: `transition: "opacity 500ms ease"`) — este estilo é compartilhado por todos os toasts e não deve ter transição base.

    Resultado esperado do bloco de render do toast:
    ```tsx
    <div
      key={t.id}
      className={t.fading ? "toast-exit" : "toast-enter"}
      style={{
        ...toastStyle,
        borderLeftColor: borderColors[t.type],
        opacity: t.fading ? 0 : 1,
        position: "relative",
        pointerEvents: "auto",
      }}
      role="status"
    >
    ```
  </action>
  <verify>
    <automated>grep -n "toast-exit\|3300" src/components/ui/toast-provider.tsx</automated>
  </verify>
  <done>O toast-provider usa `.toast-exit` para saída, o timeout de remoção é 3300ms, e não há inline transition hardcoded</done>
  <acceptance_criteria>
    - `src/components/ui/toast-provider.tsx` contém `className={t.fading ? "toast-exit" : "toast-enter"}`
    - `src/components/ui/toast-provider.tsx` contém `3500` substituído por `3300` no setTimeout de remoção
    - `src/components/ui/toast-provider.tsx` NÃO contém `transition: t.fading ? "opacity 500ms ease" : "none"`
    - `src/components/ui/toast-provider.tsx` NÃO contém `transition: "opacity 500ms ease"` em `toastStyle`
    - `pnpm test` passa (testes existentes não quebram)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client→DOM | Toast é renderizado via createPortal no document.body — conteúdo é controlado pelo cliente |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Tampering | toast-provider.tsx | accept | Toast messages são gerados pelo código cliente; não há input de usuário não-sanitizado no conteúdo do toast |
| T-34-02 | Information Disclosure | toast dismiss button | accept | aria-label já existe; nenhuma mudança de segurança nesta fase |
</threat_model>

<verification>
- Verificar que `motion.css` contém `toastFadeOut` e `.toast-exit`
- Verificar que `toast-provider.tsx` usa `3300ms` e classe `.toast-exit`
- Verificar que `prefers-reduced-motion: reduce` cobre `.toast-exit`
- Executar `pnpm test` — todos os 407+ testes devem passar
</verification>

<success_criteria>
- [ ] Toasts entram com slide+fade de 200ms (`toastSlideIn`)
- [ ] Toasts saem com fade de 300ms (`toastFadeOut` via `.toast-exit`)
- [ ] Toast permanece visível por 3000ms antes de iniciar saída
- [ ] Remoção do DOM ocorre em 3300ms (3000ms + 300ms fade)
- [ ] `prefers-reduced-motion: reduce` desativa animações de toast (entrada e saída)
- [ ] Nenhum inline transition hardcoded permanece no toast-provider
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/34-feedback-de-acao-e-loading/34-01-SUMMARY.md`
</output>
