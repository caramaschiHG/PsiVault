---
phase: 34-feedback-de-acao-e-loading
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/spinner.tsx
  - src/components/ui/button.tsx
  - src/app/globals.css
autonomous: true
requirements:
  - FEED-03
  - FEED-04
must_haves:
  truths:
    - Componente Spinner standalone existe e é importável
    - Button usa Spinner do componente standalone
    - Skeleton shimmer usa gradiente orgânico com duração 1.6s
    - prefers-reduced-motion desativa shimmer e spinner
  artifacts:
    - path: src/components/ui/spinner.tsx
      provides: Componente Spinner reutilizável com variantes de tamanho
      exports: ["Spinner"]
    - path: src/components/ui/button.tsx
      provides: Button importando Spinner de spinner.tsx
      contains: "import { Spinner }"
    - path: src/app/globals.css
      provides: Estilos refinados de skeleton-shimmer
      contains: "animation: skeleton-shimmer 1.6s"
  key_links:
    - from: src/components/ui/button.tsx
      to: src/components/ui/spinner.tsx
      via: import { Spinner }
---

<objective>
Criar componente Spinner standalone reutilizável, atualizar Button para usá-lo, e refinar o shimmer do Skeleton para um gradiente orgânico mais calmo.

Purpose: Consistência visual de estados de loading em toda a aplicação, com componentes leves (SVG + CSS, sem bibliotecas externas) e shimmer sutil que não pareça mecânico.
Output: `spinner.tsx` criado, `button.tsx` atualizado, `.skeleton-shimmer` refinado em `globals.css`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/34-feedback-de-acao-e-loading/34-CONTEXT.md
@.planning/phases/34-feedback-de-acao-e-loading/34-RESEARCH.md
@.planning/phases/34-feedback-de-acao-e-loading/34-PATTERNS.md
@.planning/phases/34-feedback-de-acao-e-loading/34-UI-SPEC.md

# Decisões travadas
- D-08: Skeleton shimmer — executor avalia se gradiente atual é "orgânico" o suficiente
- D-09: Spinner — executor decide se extrai SpinnerIcon de button.tsx para standalone

# Estado atual
- `button.tsx` contém `SpinnerIcon` inline (função interna, não exportada)
- `globals.css` tem `.skeleton-shimmer` com duração 1.4s e cores hardcoded `#f0ebe2`, `#e8e2d9`
- `motion.css` tem media query `prefers-reduced-motion` que já cobre `.skeleton-shimmer` e `[style*="animation: spin"]`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar componente Spinner standalone</name>
  <files>src/components/ui/spinner.tsx</files>
  <read_first>
    - src/components/ui/button.tsx
    - src/styles/motion.css
  </read_first>
  <action>
    Criar `src/components/ui/spinner.tsx` com o seguinte conteúdo exato:

    ```tsx
    "use client";

    interface SpinnerProps {
      size?: "sm" | "md" | "lg";
      className?: string;
      "aria-label"?: string;
    }

    const sizeMap = {
      sm: 14,
      md: 20,
      lg: 28,
    };

    export function Spinner({
      size = "sm",
      className = "",
      "aria-label": ariaLabel = "Carregando...",
    }: SpinnerProps) {
      const px = sizeMap[size];
      return (
        <svg
          width={px}
          height={px}
          viewBox="0 0 14 14"
          fill="none"
          style={{ animation: "spin 0.6s linear infinite", flexShrink: 0 }}
          className={className}
          aria-hidden={ariaLabel ? undefined : "true"}
          aria-label={ariaLabel}
          role="status"
        >
          <circle
            cx="7"
            cy="7"
            r="5.5"
            stroke="currentColor"
            strokeWidth="2"
            strokeDasharray="22"
            strokeDashoffset="8"
            strokeLinecap="round"
          />
        </svg>
      );
    }
    ```

    O componente deve ser baseado no `SpinnerIcon` existente em `button.tsx` (linhas 45-67), mas com:
    - Prop `size` com valores "sm" (14px), "md" (20px), "lg" (28px)
    - Prop `className` opcional
    - `role="status"` e `aria-label` para acessibilidade quando usado standalone
    - `aria-hidden` condicional (omitido quando `aria-label` presente)
  </action>
  <verify>
    <automated>grep -n "export function Spinner\|sizeMap\|role=\"status\"" src/components/ui/spinner.tsx</automated>
  </verify>
  <done>Arquivo `spinner.tsx` criado com componente Spinner exportado, 3 tamanhos, e atributos de acessibilidade</done>
  <acceptance_criteria>
    - `src/components/ui/spinner.tsx` existe e contém `export function Spinner(`
    - O componente aceita props `size` ("sm" | "md" | "lg"), `className`, e `aria-label`
    - `sizeMap` define sm=14, md=20, lg=28
    - O SVG tem `viewBox="0 0 14 14"`, `strokeDasharray="22"`, `strokeDashoffset="8"`
    - O SVG tem `role="status"` e `aria-label` com default "Carregando..."
    - O SVG tem `style={{ animation: "spin 0.6s linear infinite", flexShrink: 0 }}`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Atualizar Button para importar Spinner de spinner.tsx</name>
  <files>src/components/ui/button.tsx</files>
  <read_first>
    - src/components/ui/button.tsx
    - src/components/ui/spinner.tsx
  </read_first>
  <action>
    Editar `src/components/ui/button.tsx`:

    1. Adicionar import no topo:
       ```tsx
       import { Spinner } from "./spinner";
       ```

    2. Remover a função interna `SpinnerIcon()` inteira (linhas 45-67)

    3. Substituir todas as ocorrências de `<SpinnerIcon />` por `<Spinner size="sm" aria-hidden="true" />`
       - Existem 2 ocorrências (linhas 99 e 115 aproximadamente)

    O restante do componente Button (props, variantes, lógica de disabled, aria-busy) permanece inalterado.
  </action>
  <verify>
    <automated>grep -n "SpinnerIcon\|import { Spinner }" src/components/ui/button.tsx</automated>
  </verify>
  <done>Button importa Spinner de `spinner.tsx` e não contém mais `SpinnerIcon` inline</done>
  <acceptance_criteria>
    - `src/components/ui/button.tsx` contém `import { Spinner } from "./spinner";`
    - `src/components/ui/button.tsx` NÃO contém `function SpinnerIcon(`
    - `src/components/ui/button.tsx` contém `<Spinner size="sm" aria-hidden="true" />`
    - `pnpm test` passa
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Refinar skeleton-shimmer em globals.css</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css
    - src/styles/motion.css
  </read_first>
  <action>
    Editar a regra `.skeleton-shimmer` em `src/app/globals.css` (aproximadamente linha 532):

    Substituir:
    ```css
    .skeleton-shimmer {
      background: linear-gradient(90deg, #f0ebe2 25%, #e8e2d9 50%, #f0ebe2 75%);
      background-size: 200% 100%;
      animation: skeleton-shimmer 1.4s ease infinite;
      border-radius: var(--radius-sm);
    }
    ```

    Por:
    ```css
    .skeleton-shimmer {
      background: linear-gradient(90deg, #f0ebe2 25%, #e8e2d9 50%, #f0ebe2 75%);
      background-size: 200% 100%;
      animation: skeleton-shimmer 1.6s ease-in-out infinite;
      border-radius: var(--radius-sm);
    }
    ```

    Mudanças:
    - Duração: `1.4s` → `1.6s` (mais orgânico, menos mecânico)
    - Easing: `ease` → `ease-in-out` (transição mais suave nos dois sentidos)
    - Cores permanecem as mesmas (`#f0ebe2`, `#e8e2d9`) — já são próximas ao fundo do app

    Nota: não tokenizar as cores do gradiente para CSS vars a menos que `var(--color-surface-3)` e `var(--color-bg)` já estejam definidos e sejam visualmente equivalentes. Manter hex para evitar regressão visual.
  </action>
  <verify>
    <automated>grep -n "skeleton-shimmer" src/app/globals.css</automated>
  </verify>
  <done>A regra `.skeleton-shimmer` em globals.css usa duração 1.6s e easing ease-in-out</done>
  <acceptance_criteria>
    - `src/app/globals.css` contém `animation: skeleton-shimmer 1.6s ease-in-out infinite;`
    - `src/app/globals.css` contém o gradiente `linear-gradient(90deg, #f0ebe2 25%, #e8e2d9 50%, #f0ebe2 75%)`
    - `src/app/globals.css` contém `background-size: 200% 100%;`
    - `src/app/globals.css` contém `border-radius: var(--radius-sm);`
    - `pnpm test` passa
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client→DOM | Spinner é SVG decorativo; Skeleton é div sem conteúdo — nenhum dado sensível |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-03 | Information Disclosure | spinner.tsx | accept | Spinner é elemento decorativo; `aria-label` default "Carregando..." não expõe dados |
| T-34-04 | Denial of Service | skeleton-shimmer CSS | accept | Animação CSS pura, sem JS loop; não consome recursos significativos |
</threat_model>

<verification>
- Verificar que `spinner.tsx` existe e exporta `Spinner`
- Verificar que `button.tsx` importa `Spinner` e não tem `SpinnerIcon` inline
- Verificar que `.skeleton-shimmer` usa `1.6s ease-in-out`
- Executar `pnpm test` — todos os testes devem passar
</verification>

<success_criteria>
- [ ] Componente `Spinner` standalone existe em `src/components/ui/spinner.tsx` com 3 tamanhos
- [ ] `Button` importa `Spinner` de `spinner.tsx` — nenhum `SpinnerIcon` inline permanece
- [ ] `.skeleton-shimmer` usa duração 1.6s e easing ease-in-out
- [ ] `prefers-reduced-motion: reduce` desativa shimmer (já coberto por media query existente)
- [ ] Todos os 407+ testes existentes continuam passando
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/34-feedback-de-acao-e-loading/34-02-SUMMARY.md`
</output>
