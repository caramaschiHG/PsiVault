---
phase: 34-feedback-de-acao-e-loading
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(vault)/agenda/components/calendar-grid.tsx
  - src/app/(vault)/agenda/components/week-calendar-grid.tsx
  - src/app/(vault)/agenda/components/quick-create-wrapper.tsx
  - src/app/(vault)/settings/dados-e-privacidade/components/workspace-backup-button.tsx
  - src/app/(vault)/patients/[patientId]/components/reminders-section.tsx
  - src/app/(vault)/patients/[patientId]/components/export-section.tsx
  - src/app/(vault)/financeiro/page-client.tsx
  - src/components/ui/button.tsx
autonomous: true
requirements:
  - FEED-02
must_haves:
  truths:
    - Todo componente que usa useTransition para Server Action exibe feedback visual de loading
    - Botões em loading mostram opacity reduzida (0.7) + spinner
    - Calendar navigation exibe cursor: wait ou opacity dip durante isPending
    - prefers-reduced-motion desativa spinner durante loading
  artifacts:
    - path: src/app/(vault)/agenda/components/calendar-grid.tsx
      provides: isPending wired to visual feedback on month navigation
      contains: "isPending"
    - path: src/app/(vault)/agenda/components/week-calendar-grid.tsx
      provides: isPending wired to visual feedback on week navigation
      contains: "isPending"
    - path: src/app/(vault)/agenda/components/quick-create-wrapper.tsx
      provides: isPending passed to QuickCreatePopover or submit trigger
      contains: "isPending"
    - path: src/app/(vault)/settings/dados-e-privacidade/components/workspace-backup-button.tsx
      provides: isPending wired to Button isLoading
      contains: "isLoading={isPending}"
    - path: src/app/(vault)/patients/[patientId]/components/reminders-section.tsx
      provides: isPending wired to visual feedback
      contains: "isPending"
    - path: src/app/(vault)/patients/[patientId]/components/export-section.tsx
      provides: isPending wired to visual feedback
      contains: "isPending"
  key_links:
    - from: "~10 components using useTransition"
      to: "Button[isLoading] or opacity/cursor feedback"
      via: "isPending state from useTransition"
      pattern: "const \\[isPending, startTransition\\] = useTransition()"
---

<objective>
Auditar todos os componentes que usam `useTransition` para disparar Server Actions e garantir que `isPending` esteja conectado a feedback visual de loading (opacity + spinner via `Button[isLoading]` ou equivalente).

Purpose: Eliminar botões "mudos" — onde o usuário clica e nada acontece visualmente enquanto a Server Action executa. Consistência de feedback de loading em toda a aplicação.
Output: Componentes auditados e padronizados com `isPending` wired a estado visual de loading.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/34-feedback-de-acao-e-loading/34-CONTEXT.md
@.planning/phases/34-feedback-de-acao-e-loading/34-RESEARCH.md
@.planning/phases/34-feedback-de-acao-e-loading/34-PATTERNS.md
@.planning/phases/34-feedback-de-acao-e-loading/34-UI-SPEC.md

# Decisões travadas
- D-07: Executor decide se padroniza ~10 componentes com useTransition manual para exibir isLoading, ou cria wrapper ActionButton. Recomendação da pesquisa: padronizar manualmente (mais simples, anti-bloat).

# Estado atual
- `Button` já suporta `isLoading` + spinner interno (ou `Spinner` standalone após 34-02)
- `SubmitButton` já conecta `useFormStatus` — formulários estão cobertos
- Componentes fora de formulários usam `useTransition` manualmente e muitos descartam `isPending`
- `financeiro/page-client.tsx` é o padrão correto a copiar

# Componentes conhecidos a auditar
- `src/app/(vault)/agenda/components/calendar-grid.tsx` — `const [, startTransition] = useTransition()` (descarta isPending)
- `src/app/(vault)/agenda/components/week-calendar-grid.tsx` — `const [, startTransition] = useTransition()` (descarta isPending)
- `src/app/(vault)/agenda/components/quick-create-wrapper.tsx` — captura `isPending` mas não propaga
- `src/app/(vault)/settings/dados-e-privacidade/components/workspace-backup-button.tsx` — audit
- `src/app/(vault)/patients/[patientId]/components/reminders-section.tsx` — audit
- `src/app/(vault)/patients/[patientId]/components/export-section.tsx` — audit
- Possíveis outros: grep por `useTransition` no diretório `src/app/(vault)/`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep e mapear todos os callers de useTransition</name>
  <files>src/app/(vault)/</files>
  <read_first>
    - src/app/(vault)/agenda/components/calendar-grid.tsx
    - src/app/(vault)/agenda/components/week-calendar-grid.tsx
    - src/app/(vault)/agenda/components/quick-create-wrapper.tsx
    - src/app/(vault)/settings/dados-e-privacidade/components/workspace-backup-button.tsx
    - src/app/(vault)/patients/[patientId]/components/reminders-section.tsx
    - src/app/(vault)/patients/[patientId]/components/export-section.tsx
    - src/app/(vault)/financeiro/page-client.tsx
  </read_first>
  <action>
    1. Executar grep para encontrar TODOS os arquivos em `src/app/(vault)/` que usam `useTransition`:
       ```bash
       grep -rn "useTransition" src/app/(vault)/ --include="*.tsx" --include="*.ts"
       ```

    2. Para cada arquivo encontrado, classificar:
       - **Já wired**: Já passa `isPending` para `Button[isLoading]` ou outro feedback visual → nada a fazer
       - **Descarta isPending**: Usa `const [, startTransition] = useTransition()` → precisa capturar `isPending`
       - **Captura mas não usa**: Usa `const [isPending, startTransition] = useTransition()` mas não propaga → precisa propagar
       - **Dentro de formulário**: Usa `useFormStatus` via `SubmitButton` → já coberto, ignorar

    3. Listar todos os arquivos que precisam de modificação e o padrão de correção para cada um.

    O padrão de correção para cada componente é:
    ```tsx
    const [isPending, startTransition] = useTransition();
    // ...
    <Button variant="primary" isLoading={isPending} loadingLabel="Carregando...">
      {isPending ? "Carregando..." : "Ação"}
    </Button>
    ```

    Para navegação de calendário (onde a ação é rápida), usar abordagem mais sutil:
    ```tsx
    const [isPending, startTransition] = useTransition();
    // ...
    <div style={{ opacity: isPending ? 0.6 : 1, transition: "opacity var(--duration-100) ease-out", cursor: isPending ? "wait" : "default" }}>
      {/* grid content */}
    </div>
    ```
  </action>
  <verify>
    <automated>grep -rn "useTransition" src/app/(vault)/ --include="*.tsx" --include="*.ts" | wc -l</automated>
  </verify>
  <done>Lista completa de arquivos com useTransition classificada por status (wired / descarta / captura-sem-usar)</done>
  <acceptance_criteria>
    - Grep retorna todos os arquivos com `useTransition` no vault
    - Cada arquivo está classificado em uma das categorias
    - Lista de arquivos que precisam modificação está definida
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire isPending em componentes de agenda (calendar-grid, week-calendar-grid, quick-create-wrapper)</name>
  <files>
    src/app/(vault)/agenda/components/calendar-grid.tsx
    src/app/(vault)/agenda/components/week-calendar-grid.tsx
    src/app/(vault)/agenda/components/quick-create-wrapper.tsx
  </files>
  <read_first>
    - src/app/(vault)/agenda/components/calendar-grid.tsx
    - src/app/(vault)/agenda/components/week-calendar-grid.tsx
    - src/app/(vault)/agenda/components/quick-create-wrapper.tsx
    - src/components/ui/button.tsx
  </read_first>
  <action>
    Editar cada arquivo de agenda conforme o padrão encontrado:

    **calendar-grid.tsx:**
    1. Alterar `const [, startTransition] = useTransition();` para `const [isPending, startTransition] = useTransition();`
    2. No container do grid de dias (ou nos botões de navegação de mês), adicionar:
       ```tsx
       style={{
         opacity: isPending ? 0.6 : 1,
         transition: "opacity var(--duration-100) ease-out",
         cursor: isPending ? "wait" : "default",
       }}
       ```
       Ou, se houver um botão de ação específico que dispara a transição, passar `isLoading={isPending}` para o `<Button>`.

    **week-calendar-grid.tsx:**
    1. Alterar `const [, startTransition] = useTransition();` para `const [isPending, startTransition] = useTransition();`
    2. Aplicar a mesma abordagem sutil de opacity/cursor no container do grid ou no botão de ação.

    **quick-create-wrapper.tsx:**
    1. Este arquivo já captura `isPending` (linha 25: `const [isPending, startTransition] = useTransition();`)
    2. Verificar se `QuickCreatePopover` ou o botão de submit dentro dele recebe `isPending`.
    3. Se o botão de submit for um `<Button>` ou `<SubmitButton>`, passar `isLoading={isPending}` e label apropriada (ex: "Criando...").
    4. Se o componente filho não aceita `isPending` prop, adicionar a prop e propagar.
  </action>
  <verify>
    <automated>grep -n "isPending\|isLoading" src/app/(vault)/agenda/components/calendar-grid.tsx src/app/(vault)/agenda/components/week-calendar-grid.tsx src/app/(vault)/agenda/components/quick-create-wrapper.tsx</automated>
  </verify>
  <done>Todos os componentes de agenda capturam `isPending` e o conectam a feedback visual (opacity/cursor ou Button isLoading)</done>
  <acceptance_criteria>
    - `calendar-grid.tsx` contém `const [isPending, startTransition] = useTransition()`
    - `calendar-grid.tsx` aplica `opacity` ou `isLoading` baseado em `isPending`
    - `week-calendar-grid.tsx` contém `const [isPending, startTransition] = useTransition()`
    - `week-calendar-grid.tsx` aplica `opacity` ou `isLoading` baseado em `isPending`
    - `quick-create-wrapper.tsx` propaga `isPending` para o botão de ação (via `isLoading` ou prop)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wire isPending em settings, patients e demais componentes</name>
  <files>
    src/app/(vault)/settings/dados-e-privacidade/components/workspace-backup-button.tsx
    src/app/(vault)/patients/[patientId]/components/reminders-section.tsx
    src/app/(vault)/patients/[patientId]/components/export-section.tsx
  </files>
  <read_first>
    - src/app/(vault)/settings/dados-e-privacidade/components/workspace-backup-button.tsx
    - src/app/(vault)/patients/[patientId]/components/reminders-section.tsx
    - src/app/(vault)/patients/[patientId]/components/export-section.tsx
    - src/components/ui/button.tsx
  </read_first>
  <action>
    Para cada um dos arquivos listados (e quaisquer outros encontrados no grep da Task 1 que ainda não foram tratados):

    1. Verificar se o componente usa `useTransition`.
    2. Se usar `const [, startTransition] = useTransition()`, alterar para capturar `isPending`.
    3. No botão ou elemento interativo que dispara a Server Action, adicionar feedback visual:
       - Se for `<Button>`: adicionar `isLoading={isPending}` e `loadingLabel` apropriado
       - Se for outro elemento: aplicar `opacity` transition ou `cursor: wait`

    Labels sugeridos (pt-BR, conforme UI-SPEC):
    - Backup: "Exportando..."
    - Lembrete: "Salvando..."
    - Exportação de prontuário: "Exportando..."

    Exemplo de padrão para cada arquivo:
    ```tsx
    const [isPending, startTransition] = useTransition();
    // ...
    <Button
      variant="primary"
      onClick={() => startTransition(() => action())}
      isLoading={isPending}
      loadingLabel="Exportando..."
    >
      {isPending ? "Exportando..." : "Exportar Backup"}
    </Button>
    ```

    Nota: se algum arquivo já estiver correto (já passa `isPending` para `Button[isLoading]`), apenas verificar e não modificar.
  </action>
  <verify>
    <automated>grep -rn "isLoading={isPending}\|isPending" src/app/(vault)/settings/ src/app/(vault)/patients/ --include="*.tsx" | grep -v node_modules</automated>
  </verify>
  <done>Todos os componentes de settings e patients capturam `isPending` e o conectam a `Button[isLoading]` ou feedback visual equivalente</done>
  <acceptance_criteria>
    - `workspace-backup-button.tsx` usa `isLoading={isPending}` ou equivalente
    - `reminders-section.tsx` usa `isLoading={isPending}` ou equivalente
    - `export-section.tsx` usa `isLoading={isPending}` ou equivalente
    - Nenhum arquivo no vault usa `const [, startTransition]` sem capturar `isPending` (verificar via grep)
    - `pnpm test` passa
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client→Server Action | `useTransition` é client-side; Server Action executa no servidor |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-05 | Denial of Service | Button with isLoading | mitigate | `disabled={isLoading}` já é comportamento padrão do Button — previne double-submit |
| T-34-06 | Information Disclosure | Loading labels | accept | Labels genéricas ("Salvando...", "Exportando...") não expõem dados do usuário |
</threat_model>

<verification>
- Verificar via grep que nenhum arquivo em `src/app/(vault)/` usa `const [, startTransition]` (isPending descartado)
- Verificar que todos os `useTransition` callers conectam `isPending` a algum feedback visual
- Executar `pnpm test` — todos os testes devem passar
</verification>

<success_criteria>
- [ ] Todos os componentes que usam `useTransition` para Server Actions exibem feedback visual de loading
- [ ] Nenhum `const [, startTransition]` permanece no código do vault
- [ ] Botões de ação usam `Button[isLoading]` com label apropriado em pt-BR
- [ ] Navegação de calendário aplica opacity dip ou cursor: wait durante `isPending`
- [ ] Todos os 407+ testes existentes continuam passando
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/34-feedback-de-acao-e-loading/34-03-SUMMARY.md`
</output>
