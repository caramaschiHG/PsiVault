---
phase: 34-feedback-de-acao-e-loading
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/cadastro/page.tsx
  - src/app/(auth)/recuperar-senha/page.tsx
  - src/app/(vault)/patients/components/patient-form.tsx
  - src/app/(vault)/patients/[patientId]/components/patient-edit-form.tsx
  - src/app/(vault)/agenda/components/appointment-form.tsx
  - src/app/(vault)/settings/components/settings-form.tsx
  - src/app/(vault)/settings/dados-e-privacidade/components/workspace-delete-form.tsx
  - src/app/(vault)/financeiro/components/expense-side-panel.tsx
  - src/app/globals.css
autonomous: true
requirements:
  - FEED-05
must_haves:
  truths:
    - Todo formulário do app aplica .input-error-shake em campos com erro de validação
    - Shake é removido após animationend para permitir re-trigger
    - Borda do input transiciona para cor de erro com duração var(--duration-100)
    - prefers-reduced-motion desativa shake (mantém border color transition)
  artifacts:
    - path: src/app/(auth)/login/page.tsx
      provides: Shake class em inputs de erro + onAnimationEnd handler
      contains: "input-error-shake"
    - path: src/app/(auth)/cadastro/page.tsx
      provides: Shake class em inputs de erro + onAnimationEnd handler
      contains: "input-error-shake"
    - path: src/app/(vault)/patients/components/patient-form.tsx
      provides: Shake class em inputs de erro + onAnimationEnd handler
      contains: "input-error-shake"
    - path: src/app/(vault)/agenda/components/appointment-form.tsx
      provides: Shake class em inputs de erro + onAnimationEnd handler
      contains: "input-error-shake"
    - path: src/app/(vault)/settings/components/settings-form.tsx
      provides: Shake class em inputs de erro + onAnimationEnd handler
      contains: "input-error-shake"
    - path: src/app/(vault)/financeiro/components/expense-side-panel.tsx
      provides: Shake class em inputs de erro (já existente — servir como referência)
      contains: "input-error-shake"
  key_links:
    - from: "Form components"
      to: ".input-error-shake"
      via: "conditional className + onAnimationEnd"
      pattern: "onAnimationEnd=.*inputShake"
---

<objective>
Auditar todos os formulários do aplicativo e garantir que campos com erro de validação apliquem a classe `.input-error-shake` com remoção via `onAnimationEnd`, além da transição de cor de borda para estado de erro.

Purpose: Feedback visual imediato e consistente em todos os formulários — o usuário percebe instantaneamente que algo precisa de atenção, sem depender apenas de mensagens textuais.
Output: Todos os formulários do app com `.input-error-shake` wired corretamente e transição de borda aplicada.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/34-feedback-de-acao-e-loading/34-CONTEXT.md
@.planning/phases/34-feedback-de-acao-e-loading/34-RESEARCH.md
@.planning/phases/34-feedback-de-acao-e-loading/34-PATTERNS.md
@.planning/phases/34-feedback-de-acao-e-loading/34-UI-SPEC.md

# Decisões travadas
- D-10: Executor verifica se `.input-error-shake` está wired em todos os formulários e completa onde faltar. Phase 33 já definiu shake de 2px lateral + border glow em erro.

# Estado atual
- `.input-error-shake` keyframe existe em `src/styles/motion.css`
- `expense-side-panel.tsx` já implementa o padrão completo (reference pattern)
- Auth pages (login, cadastro, recuperar-senha) podem já ter shake — precisam ser verificados
- Formulários de pacientes, atendimentos, settings, e financeiro precisam de audit
- A media query `prefers-reduced-motion` já cobre `.input-error-shake` em `motion.css`

# Padrão de referência (expense-side-panel.tsx)
```tsx
const shakeClass = formError ? "input-error input-error-shake" : "";
<input
  className={`input-field ${shakeClass}`}
  onAnimationEnd={(e) => {
    if (e.animationName === "inputShake") {
      e.currentTarget.classList.remove("input-error-shake");
    }
  }}
/>
```

# Formulários a auditar (lista inicial)
- Auth: `src/app/(auth)/login/page.tsx`, `src/app/(auth)/cadastro/page.tsx`, `src/app/(auth)/recuperar-senha/page.tsx`
- Pacientes: `src/app/(vault)/patients/components/patient-form.tsx`, `src/app/(vault)/patients/[patientId]/components/patient-edit-form.tsx`
- Agenda: `src/app/(vault)/agenda/components/appointment-form.tsx`
- Settings: `src/app/(vault)/settings/components/settings-form.tsx`, `src/app/(vault)/settings/dados-e-privacidade/components/workspace-delete-form.tsx`
- Financeiro: `src/app/(vault)/financeiro/components/expense-side-panel.tsx` (reference, já feito)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep e mapear todos os formulários com inputs e estado de erro</name>
  <files>src/app/(auth)/, src/app/(vault)/</files>
  <read_first>
    - src/app/(vault)/financeiro/components/expense-side-panel.tsx
    - src/styles/motion.css
    - src/app/globals.css
  </read_first>
  <action>
    1. Executar grep para encontrar todos os formulários com inputs que possam ter estado de erro:
       ```bash
       grep -rln "input-field\|auth-input" src/app/(auth)/ src/app/(vault)/ --include="*.tsx" --include="*.ts"
       ```

    2. Para cada arquivo encontrado, executar:
       ```bash
       grep -n "input-error-shake\|onAnimationEnd\|formError\|error" {arquivo}
       ```

    3. Classificar cada arquivo:
       - **Já wired**: Já aplica `.input-error-shake` e tem `onAnimationEnd` handler → verificar se está correto
       - **Faltando shake**: Tem estado de erro (`formError`, `error`, `state?.error`) mas não aplica `.input-error-shake`
       - **Faltando onAnimationEnd**: Aplica `.input-error-shake` mas não remove a classe após animação (não permite re-trigger)
       - **Sem erro state**: Não tem validação com estado de erro → fora do escopo desta fase

    4. Listar todos os arquivos que precisam de modificação e o padrão exato de correção.

    O padrão de correção para cada input com erro:
    ```tsx
    const shakeClass = hasError ? "input-error input-error-shake" : "";
    // ...
    <input
      className={`input-field ${shakeClass}`}
      onAnimationEnd={(e) => {
        if (e.animationName === "inputShake") {
          e.currentTarget.classList.remove("input-error-shake");
        }
      }}
    />
    ```

    Nota: se o arquivo usa `.auth-input` em vez de `.input-field`, aplicar o shake class da mesma forma:
    ```tsx
    <input className={`auth-input ${shakeClass}`} ... />
    ```

    5. Verificar também `.select-has-value` e outros campos de formulário (textarea, select) que possam ter estado de erro.
  </action>
  <verify>
    <automated>grep -rln "input-field\|auth-input" src/app/(auth)/ src/app/(vault)/ --include="*.tsx" | wc -l</automated>
  </verify>
  <done>Lista completa de formulários classificada por status de wiring do .input-error-shake</done>
  <acceptance_criteria>
    - Todos os arquivos com `input-field` ou `auth-input` foram listados
    - Cada arquivo está classificado em uma das categorias (já wired / faltando shake / faltando onAnimationEnd / sem erro state)
    - Lista de arquivos que precisam modificação está definida
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Aplicar .input-error-shake nos formulários de auth</name>
  <files>
    src/app/(auth)/login/page.tsx
    src/app/(auth)/cadastro/page.tsx
    src/app/(auth)/recuperar-senha/page.tsx
  </files>
  <read_first>
    - src/app/(auth)/login/page.tsx
    - src/app/(auth)/cadastro/page.tsx
    - src/app/(auth)/recuperar-senha/page.tsx
    - src/app/(vault)/financeiro/components/expense-side-panel.tsx
  </read_first>
  <action>
    Para cada página de auth:

    1. Identificar o estado de erro. Geralmente é `state?.error`, `formError`, ou similar.
    2. Para cada `<input>` com classe `.auth-input` (ou `.input-field`) que pode ter erro:
       - Adicionar `const shakeClass = errorState ? "input-error input-error-shake" : "";` (ou equivalente)
       - Alterar `className` para incluir `${shakeClass}`
       - Adicionar `onAnimationEnd` handler para remover `.input-error-shake` após animação:
         ```tsx
         onAnimationEnd={(e) => {
           if (e.animationName === "inputShake") {
             e.currentTarget.classList.remove("input-error-shake");
           }
         }}
         ```

    3. Se o formulário já tiver `.input-error-shake` aplicado, verificar:
       - Se `onAnimationEnd` está presente e correto (remove a classe)
       - Se a classe é aplicada condicionalmente (não permanentemente)
       - Se `.input-error` (estilo de borda vermelha) é aplicado junto com `.input-error-shake`

    4. Verificar se `.auth-input` já tem `transition` de border-color em `globals.css`. Se não tiver, garantir que `.input-error` aplica a transição:
       ```css
       .input-error {
         border-color: var(--color-error-border);
         transition: border-color var(--duration-100) ease-out;
       }
       ```

    Nota: Não modificar a lógica de validação ou mensagens de erro — apenas o feedback visual.
  </action>
  <verify>
    <automated>grep -n "input-error-shake\|onAnimationEnd" src/app/(auth)/login/page.tsx src/app/(auth)/cadastro/page.tsx src/app/(auth)/recuperar-senha/page.tsx</automated>
  </verify>
  <done>Todos os formulários de auth aplicam `.input-error-shake` condicionalmente e removem a classe via `onAnimationEnd`</done>
  <acceptance_criteria>
    - Cada formulário de auth com inputs validáveis contém `input-error-shake` aplicado condicionalmente
    - Cada input com shake possui `onAnimationEnd` handler que remove `input-error-shake`
    - `.input-error` aplica `border-color` com `transition` de `var(--duration-100)`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Aplicar .input-error-shake nos formulários do vault (patients, agenda, settings, finance)</name>
  <files>
    src/app/(vault)/patients/components/patient-form.tsx
    src/app/(vault)/patients/[patientId]/components/patient-edit-form.tsx
    src/app/(vault)/agenda/components/appointment-form.tsx
    src/app/(vault)/settings/components/settings-form.tsx
    src/app/(vault)/settings/dados-e-privacidade/components/workspace-delete-form.tsx
  </files>
  <read_first>
    - src/app/(vault)/patients/components/patient-form.tsx
    - src/app/(vault)/patients/[patientId]/components/patient-edit-form.tsx
    - src/app/(vault)/agenda/components/appointment-form.tsx
    - src/app/(vault)/settings/components/settings-form.tsx
    - src/app/(vault)/settings/dados-e-privacidade/components/workspace-delete-form.tsx
    - src/app/(vault)/financeiro/components/expense-side-panel.tsx
  </read_first>
  <action>
    Para cada formulário do vault listado (e quaisquer outros descobertos na Task 1):

    1. Localizar o estado de erro (pode ser `formError`, `state?.error`, `errorField`, `errors`, etc.)
    2. Para cada input, select, ou textarea com classe `.input-field` (ou similar) que possa falhar na validação:
       - Aplicar o padrão de shake:
         ```tsx
         const shakeClass = hasError ? "input-error input-error-shake" : "";
         <input
           className={`input-field ${shakeClass}`}
           onAnimationEnd={(e) => {
             if (e.animationName === "inputShake") {
               e.currentTarget.classList.remove("input-error-shake");
             }
           }}
         />
         ```

    3. **Cuidados específicos:**
       - **Selects**: se usar `.select-has-value`, aplicar shake da mesma forma. Verificar se o select é um `<select>` nativo ou um componente customizado — se for customizado, garantir que `onAnimationEnd` possa ser propagado.
       - **Textareas**: mesmo padrão de input.
       - **Campos múltiplos com erro**: se houver um objeto de erros (ex: `{ name: "obrigatório", email: "inválido" }`), aplicar shake individualmente em cada campo que tenha erro.

    4. Para `expense-side-panel.tsx` (reference pattern):
       - Verificar se o padrão já está completo e correto
       - Se estiver correto, usar como modelo para os outros
       - Se houver alguma imperfeição (ex: `onAnimationEnd` faltando em algum campo), corrigir

    5. Verificar `globals.css` para garantir que `.input-error` tem transição de border-color:
       ```css
       .input-error {
         border-color: var(--color-error-border);
         transition: border-color var(--duration-100) ease-out;
       }
       ```
       Se não existir, adicionar ao `globals.css`.
  </action>
  <verify>
    <automated>grep -rn "input-error-shake" src/app/(vault)/patients/ src/app/(vault)/agenda/ src/app/(vault)/settings/ src/app/(vault)/financeiro/ --include="*.tsx" | grep -v node_modules</automated>
  </verify>
  <done>Todos os formulários do vault aplicam `.input-error-shake` condicionalmente com remoção via `onAnimationEnd`</done>
  <acceptance_criteria>
    - `patient-form.tsx` aplica `.input-error-shake` em campos com erro
    - `patient-edit-form.tsx` aplica `.input-error-shake` em campos com erro
    - `appointment-form.tsx` aplica `.input-error-shake` em campos com erro
    - `settings-form.tsx` aplica `.input-error-shake` em campos com erro
    - `workspace-delete-form.tsx` aplica `.input-error-shake` se tiver inputs validáveis
    - Todos os inputs com shake possuem `onAnimationEnd` que remove `input-error-shake`
    - `globals.css` contém `.input-error` com `transition: border-color var(--duration-100) ease-out`
    - `pnpm test` passa
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client→DOM | Shake é puramente visual; não afeta dados enviados ao servidor |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-07 | Denial of Service | .input-error-shake animation | accept | Animação CSS de 200ms, single iteration — não causa seizure nem consome recursos |
| T-34-08 | Information Disclosure | Error border color | accept | Cor de erro é padrão visual da aplicação; não expõe dados sensíveis |
</threat_model>

<verification>
- Verificar via grep que todos os arquivos com `input-field` ou `auth-input` e estado de erro aplicam `.input-error-shake`
- Verificar que `onAnimationEnd` handler existe em todos os inputs com shake
- Verificar que `globals.css` tem `.input-error` com `transition: border-color var(--duration-100) ease-out`
- Verificar que `prefers-reduced-motion: reduce` desativa `.input-error-shake` (já coberto em `motion.css`)
- Executar `pnpm test` — todos os testes devem passar
</verification>

<success_criteria>
- [ ] Todos os formulários de auth (login, cadastro, recuperar-senha) aplicam `.input-error-shake` em inputs com erro
- [ ] Todos os formulários do vault (patients, agenda, settings, finance) aplicam `.input-error-shake` em inputs com erro
- [ ] Todos os inputs com shake possuem `onAnimationEnd` handler que remove a classe após animação
- [ ] `.input-error` em `globals.css` aplica `border-color` com `transition: var(--duration-100)`
- [ ] `prefers-reduced-motion: reduce` desativa shake (mantém border color)
- [ ] Todos os 407+ testes existentes continuam passando
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/34-feedback-de-acao-e-loading/34-04-SUMMARY.md`
</output>
