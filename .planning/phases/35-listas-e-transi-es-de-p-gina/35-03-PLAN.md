---
phase: 35-listas-e-transi-es-de-p-gina
plan: 03
type: execute
wave: 2
depends_on:
  - 35-01-PLAN.md
files_modified:
  - src/app/(vault)/agenda/page.tsx
  - src/app/(vault)/prontuario/[patientId]/page.tsx
autonomous: true
requirements:
  - LIST-03
must_haves:
  truths:
    - "Agenda 'Problemas de conexão' details expand with smooth height animation"
    - "Prontuário patient page 'Ver mais' and 'Dispensados' details expand smoothly"
    - "All details elements use CSS-only animation (grid-template-rows technique)"
  artifacts:
    - path: "src/app/(vault)/agenda/page.tsx"
      provides: "Animated details for remote issues"
      contains: "details-animated, details-animated-content"
    - path: "src/app/(vault)/prontuario/[patientId]/page.tsx"
      provides: "Animated details for hidden appointments"
      contains: "details-animated, details-animated-content"
  key_links:
    - from: "agenda/page.tsx details"
      to: "motion.css .details-animated"
      via: "className attribute"
    - from: "prontuario/[patientId]/page.tsx details"
      to: "motion.css .details-animated-content"
      via: "wrapper div className"
---

<objective>
Apply smooth expand/collapse animation to all `<details>` elements in the agenda and prontuário patient pages.

Purpose: Implements LIST-03 for pages that contain native `<details>`/`summary` expandables.
Output: All details elements animate height smoothly without layout jump.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-listas-e-transi-es-de-p-gina/35-CONTEXT.md
@.planning/phases/35-listas-e-transi-es-de-p-gina/35-01-PLAN.md

## Existing Code

From `src/app/(vault)/agenda/page.tsx` (around line 343):
```tsx
<details style={detailsStyle}>
  <summary style={detailsSummaryStyle}>Problemas de conexão</summary>
  {appt.remoteIssueNote && (
    <p style={remoteIssueNoteStyle}>{appt.remoteIssueNote}</p>
  )}
  <RemoteIssueForm
    appointmentId={appt.id}
    meetingLink={appt.meetingLink ?? null}
    urlInputStyle={urlInputStyle}
    inlineFormStyle={inlineFormStyle}
  />
</details>
```

From `src/app/(vault)/prontuario/[patientId]/page.tsx` (around lines 139 and 169):
```tsx
<details style={detailsStyle}>
  <summary style={summaryStyle}>Ver mais ({completedHidden.length} atendimento...)</summary>
  <div style={detailsBodyStyle}>
    {completedHidden.map((appt, idx) => (
      <div key={appt.id} style={entryStyle}>...</div>
    ))}
  </div>
</details>

<details style={detailsStyle}>
  <summary style={summaryStyle}>{dismissedAll.length} atendimento... dispensado...</summary>
  <div style={detailsBodyStyle}>
    {dismissedAll.map((appt) => (
      <div key={appt.id} style={dismissedEntryStyle}>...</div>
    ))}
  </div>
</details>
```

## CSS Foundation (from Plan 01)
```css
.details-animated {
  overflow: hidden;
}
.details-animated-content {
  display: grid;
  grid-template-rows: 0fr;
  transition: grid-template-rows var(--duration-300) var(--ease-out);
}
.details-animated[open] .details-animated-content {
  grid-template-rows: 1fr;
}
.details-animated-content > * {
  overflow: hidden;
  min-height: 0;
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Animate agenda page details element</name>
  <read_first>src/app/(vault)/agenda/page.tsx</read_first>
  <files>src/app/(vault)/agenda/page.tsx</files>
  <action>
1. Locate the `<details>` element around line 343 that contains "Problemas de conexão".

2. Add `className="details-animated"` to the `<details>` element:
   ```tsx
   <details style={detailsStyle} className="details-animated">
   ```

3. Wrap ALL content inside the `<details>` (everything after `<summary>`) in a `<div className="details-animated-content">`:
   ```tsx
   <details style={detailsStyle} className="details-animated">
     <summary style={detailsSummaryStyle}>Problemas de conexão</summary>
     <div className="details-animated-content">
       {appt.remoteIssueNote && (
         <p style={remoteIssueNoteStyle}>{appt.remoteIssueNote}</p>
       )}
       <RemoteIssueForm
         appointmentId={appt.id}
         meetingLink={appt.meetingLink ?? null}
         urlInputStyle={urlInputStyle}
         inlineFormStyle={inlineFormStyle}
       />
     </div>
   </details>
   ```
   Important: Only the content AFTER `<summary>` should be wrapped. The `<summary>` itself must remain a direct child of `<details>`.
  </action>
  <acceptance_criteria>
    - `src/app/(vault)/agenda/page.tsx` contains `<details ... className="details-animated">`
    - `src/app/(vault)/agenda/page.tsx` contains `<div className="details-animated-content">` inside the details element
    - The `<summary>` element remains a direct child of `<details>` (not inside the wrapper div)
  </acceptance_criteria>
  <verify>
    <automated>
      grep -n "details-animated" src/app/(vault)/agenda/page.tsx &&
      grep -n "details-animated-content" src/app/(vault)/agenda/page.tsx
    </automated>
  </verify>
  <done>
    Agenda remote issues details element expands and collapses with smooth height animation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Animate prontuário patient page details elements</name>
  <read_first>src/app/(vault)/prontuario/[patientId]/page.tsx</read_first>
  <files>src/app/(vault)/prontuario/[patientId]/page.tsx</files>
  <action>
1. Locate the first `<details>` element around line 139 ("Ver mais" / completed hidden appointments).

2. Add `className="details-animated"` to the `<details>` element:
   ```tsx
   <details style={detailsStyle} className="details-animated">
   ```

3. Add `className="details-animated-content"` to the existing `<div style={detailsBodyStyle}>` wrapper:
   ```tsx
   <div style={detailsBodyStyle} className="details-animated-content">
   ```
   Note: This page already wraps the content in a `<div>` — just add the className.

4. Repeat steps 2-3 for the second `<details>` element around line 169 ("Dispensados"):
   ```tsx
   <details style={detailsStyle} className="details-animated">
     <summary style={summaryStyle}>...</summary>
     <div style={detailsBodyStyle} className="details-animated-content">
       {dismissedAll.map((appt) => (
         ...
       ))}
     </div>
   </details>
   ```
  </action>
  <acceptance_criteria>
    - `src/app/(vault)/prontuario/[patientId]/page.tsx` contains 2 instances of `className="details-animated"` on `<details>` elements
    - `src/app/(vault)/prontuario/[patientId]/page.tsx` contains 2 instances of `className="details-animated-content"` on `<div>` elements inside details
    - Each `<summary>` remains a direct child of its `<details>`
  </acceptance_criteria>
  <verify>
    <automated>
      grep -c "details-animated" src/app/(vault)/prontuario/\[patientId\]/page.tsx &&
      grep -c "details-animated-content" src/app/(vault)/prontuario/\[patientId\]/page.tsx
    </automated>
  </verify>
  <done>
    Both prontuário patient page details elements expand and collapse with smooth height animation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | No trust boundaries crossed — CSS class additions only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-03 | N/A | CSS classes | accept | No security impact from adding CSS class names to HTML elements. |
</threat_model>

<verification>
After both tasks:
1. Run `pnpm build` — must pass with zero TypeScript errors
2. Run `pnpm test` — all 419 tests must pass
3. Grep verify all files contain `details-animated` and `details-animated-content`
</verification>

<success_criteria>
- Agenda page details element has `details-animated` class and content wrapped in `details-animated-content`
- Prontuário patient page has 2 details elements with `details-animated` and `details-animated-content`
- All `<summary>` elements remain direct children of `<details>`
- Build passes, tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/35-listas-e-transi-es-de-p-gina/35-03-SUMMARY.md`
</output>
