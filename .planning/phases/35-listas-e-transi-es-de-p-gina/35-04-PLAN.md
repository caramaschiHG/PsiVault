---
phase: 35-listas-e-transi-es-de-p-gina
plan: 04
type: execute
wave: 2
depends_on:
  - 35-01-PLAN.md
files_modified:
  - src/app/(vault)/financeiro/components/expense-filters.tsx
  - src/app/(vault)/patients/[patientId]/components/finance-section.tsx
autonomous: true
requirements:
  - LIST-03
must_haves:
  truths:
    - "Financeiro mobile filters expand with smooth height animation"
    - "Patient finance-section 'Editar cobrança' details expand smoothly"
    - "All details elements use CSS-only animation"
  artifacts:
    - path: "src/app/(vault)/financeiro/components/expense-filters.tsx"
      provides: "Animated mobile filter details"
      contains: "details-animated, details-animated-content"
    - path: "src/app/(vault)/patients/[patientId]/components/finance-section.tsx"
      provides: "Animated charge edit form details"
      contains: "details-animated, details-animated-content"
  key_links:
    - from: "expense-filters.tsx details"
      to: "motion.css .details-animated"
      via: "className attribute"
    - from: "finance-section.tsx details"
      to: "motion.css .details-animated-content"
      via: "wrapper div className"
---

<objective>
Apply smooth expand/collapse animation to the remaining `<details>` elements in financeiro and patient detail pages.

Purpose: Completes LIST-03 coverage across the entire app.
Output: Mobile filter drawer and inline charge edit forms animate height smoothly.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-listas-e-transi-es-de-p-gina/35-CONTEXT.md
@.planning/phases/35-listas-e-transi-es-de-p-gina/35-01-PLAN.md

## Existing Code

From `src/app/(vault)/financeiro/components/expense-filters.tsx`:
```tsx
<details style={mobileWrapStyle}>
  <summary style={summaryStyle}>Filtros</summary>
  <div style={mobileContentStyle}>{filterContent}</div>
</details>
```

From `src/app/(vault)/patients/[patientId]/components/finance-section.tsx`:
```tsx
<details style={detailsStyle}>
  <summary style={summaryStyle}>Editar cobrança</summary>
  <form action={updateChargeAction} style={formStyle}>
    <input type="hidden" name="chargeId" value={charge.id} />
    <div style={fieldGroupStyle}>...</div>
    <div style={fieldGroupStyle}>...</div>
    <div style={fieldGroupStyle}>...</div>
    <button type="submit" style={submitStyle}>Salvar</button>
  </form>
</details>
```

## CSS Foundation (from Plan 01)
```css
.details-animated {
  overflow: hidden;
}
.details-animated-content {
  display: grid;
  grid-template-rows: 0fr;
  transition: grid-template-rows var(--duration-300) var(--ease-out);
}
.details-animated[open] .details-animated-content {
  grid-template-rows: 1fr;
}
.details-animated-content > * {
  overflow: hidden;
  min-height: 0;
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Animate financeiro mobile filter details</name>
  <read_first>src/app/(vault)/financeiro/components/expense-filters.tsx</read_first>
  <files>src/app/(vault)/financeiro/components/expense-filters.tsx</files>
  <action>
1. Locate the `<details>` element around line 151.

2. Add `className="details-animated"` to the `<details>` element:
   ```tsx
   <details style={mobileWrapStyle} className="details-animated">
   ```

3. Add `className="details-animated-content"` to the existing `<div style={mobileContentStyle}>`:
   ```tsx
   <div style={mobileContentStyle} className="details-animated-content">{filterContent}</div>
   ```
   Note: This file already wraps the content in a `<div>` — just add the className.
  </action>
  <acceptance_criteria>
    - `src/app/(vault)/financeiro/components/expense-filters.tsx` contains `className="details-animated"` on `<details>`
    - `src/app/(vault)/financeiro/components/expense-filters.tsx` contains `className="details-animated-content"` on the `<div>` inside details
    - The `<summary>` remains a direct child of `<details>`
  </acceptance_criteria>
  <verify>
    <automated>grep -n "details-animated" src/app/(vault)/financeiro/components/expense-filters.tsx</automated>
  </verify>
  <done>
    Financeiro mobile filter drawer expands and collapses with smooth height animation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Animate patient finance-section charge edit details</name>
  <read_first>src/app/(vault)/patients/[patientId]/components/finance-section.tsx</read_first>
  <files>src/app/(vault)/patients/[patientId]/components/finance-section.tsx</files>
  <action>
1. Locate the `<details>` element around line 102 that contains "Editar cobrança".

2. Add `className="details-animated"` to the `<details>` element:
   ```tsx
   <details style={detailsStyle} className="details-animated">
   ```

3. Wrap the `<form>` element (and all its contents) in a `<div className="details-animated-content">`:
   ```tsx
   <details style={detailsStyle} className="details-animated">
     <summary style={summaryStyle}>Editar cobrança</summary>
     <div className="details-animated-content">
       <form action={updateChargeAction} style={formStyle}>
         <input type="hidden" name="chargeId" value={charge.id} />
         <div style={fieldGroupStyle}>...</div>
         <div style={fieldGroupStyle}>...</div>
         <div style={fieldGroupStyle}>...</div>
         <button type="submit" style={submitStyle}>Salvar</button>
       </form>
     </div>
   </details>
   ```
   Important: The `<summary>` must remain a direct child of `<details>`. Only the `<form>` and its contents go inside the wrapper div.
  </action>
  <acceptance_criteria>
    - `src/app/(vault)/patients/[patientId]/components/finance-section.tsx` contains `className="details-animated"` on `<details>`
    - `src/app/(vault)/patients/[patientId]/components/finance-section.tsx` contains `<div className="details-animated-content">` wrapping the form inside details
    - The `<summary>` remains a direct child of `<details>` (not inside the wrapper div)
  </acceptance_criteria>
  <verify>
    <automated>grep -n "details-animated" src/app/(vault)/patients/\[patientId\]/components/finance-section.tsx</automated>
  </verify>
  <done>
    Patient finance-section charge edit form expands and collapses with smooth height animation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | No trust boundaries crossed — CSS class additions only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-04 | N/A | CSS classes | accept | No security impact from adding CSS class names. |
</threat_model>

<verification>
After both tasks:
1. Run `pnpm build` — must pass with zero TypeScript errors
2. Run `pnpm test` — all 419 tests must pass
3. Grep verify both files contain `details-animated` and `details-animated-content`
</verification>

<success_criteria>
- Expense-filters mobile details has `details-animated` and `details-animated-content`
- Finance-section charge edit details has `details-animated` and content wrapped in `details-animated-content`
- All `<summary>` elements remain direct children of `<details>`
- Build passes, tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/35-listas-e-transi-es-de-p-gina/35-04-SUMMARY.md`
</output>
