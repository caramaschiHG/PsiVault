---
phase: 40-integracao-atendimentos
plan: 02
type: execute
wave: 2
depends_on:
  - 40-01
files_modified:
  - src/app/(vault)/patients/[patientId]/components/document-timeline.tsx
  - src/app/(vault)/patients/[patientId]/documents/[documentId]/page.tsx
  - src/app/(vault)/agenda/page.tsx
autonomous: true
requirements:
  - APPT-01
  - APPT-02
  - APPT-05

must_haves:
  truths:
    - Documentos vinculados a atendimento exibem badge discreto na timeline com data/hora
    - Badge de atendimento na timeline é clicável e leva para a agenda
    - Visualização A4 do documento mostra meta-info do atendimento acima do papel
    - Atendimentos COMPLETED na agenda exibem quick action "Criar declaração"
    - Fluxo sessão→nota→documento preserva parâmetro `from`
  artifacts:
    - path: src/app/(vault)/patients/[patientId]/components/document-timeline.tsx
      provides: Timeline com badge de atendimento discreto
      contains: "appointmentId"
    - path: src/app/(vault)/patients/[patientId]/documents/[documentId]/page.tsx
      provides: View page com meta-info de atendimento acima do A4
      contains: "appointmentId"
    - path: src/app/(vault)/agenda/page.tsx
      provides: Agenda com quick action de criar documento em atendimentos completed
      contains: "Criar declaração\|documents/new"
  key_links:
    - from: document-timeline.tsx
      to: /agenda
      via: Link no badge de atendimento
      pattern: "href=\"/agenda\""
    - from: [documentId]/page.tsx
      to: DocumentPaperScaler
      via: meta-info renderizada acima do scaler
      pattern: "DocumentPaperScaler"
    - from: agenda/page.tsx
      to: /patients/[patientId]/documents/new
      via: Link com query params
      pattern: "/documents/new"
---

<objective>
Exibir o vínculo com atendimento nos documentos e permitir criação rápida de documento diretamente da agenda.

Purpose: Usuários veem contexto clínico nos documentos e iniciam documentos sem sair do fluxo de atendimento.
Output: Timeline com badge, view page com meta-info, e agenda com quick action.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-integracao-atendimentos/40-CONTEXT.md
@.planning/phases/40-integracao-atendimentos/40-RESEARCH.md
@.planning/phases/40-integracao-atendimentos/40-01-PLAN.md

<interfaces>
From src/lib/documents/model.ts:
```ts
export interface PracticeDocument {
  id: string;
  workspaceId: string;
  patientId: string;
  type: DocumentType;
  content: string;
  createdByAccountId: string;
  createdByName: string;
  createdAt: Date;
  updatedAt: Date;
  editedAt: Date | null;
  archivedAt: Date | null;
  archivedByAccountId: string | null;
  status: DocumentStatus;
  appointmentId: string | null;
  signedAt: Date | null;
  signedByAccountId: string | null;
  deliveredAt: Date | null;
  deliveredTo: string | null;
  deliveredVia: string | null;
}
```

From src/lib/appointments/model.ts:
```ts
export interface Appointment {
  id: string;
  workspaceId: string;
  patientId: string;
  startsAt: Date;
  endsAt: Date;
  durationMinutes: number;
  careMode: AppointmentCareMode;
  status: AppointmentStatus;
}
```

From src/app/(vault)/patients/[patientId]/components/document-timeline.tsx:
```ts
interface DocumentTimelineProps {
  documents: PracticeDocument[];
  patientId: string;
  patientName: string;
}
```

From src/app/(vault)/patients/[patientId]/documents/[documentId]/page.tsx:
```tsx
export default async function DocumentViewPage({ params }: DocumentViewPageProps) {
  const doc = await docRepo.findById(documentId, workspaceId);
  // ... render DocumentPaperScaler + DocumentPaperView
}
```

From src/app/(vault)/agenda/page.tsx (relevant snippet):
```tsx
nextSessionActions[appt.id] = (
  <>
    <CompletedAppointmentNextSessionAction appointmentId={appt.id} defaults={defaults} />
    {hasNote ? (
      <Link href={`/sessions/${appt.id}/note`} style={viewNoteStyle}>Ver prontuário</Link>
    ) : (
      <Link href={`/sessions/${appt.id}/note`} style={registerNoteStyle}>Registrar prontuário</Link>
    )}
    {onlineSection}
    {comunicacaoSection}
  </>
);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DocumentTimeline exibe badge discreto de atendimento</name>
  <files>src/app/(vault)/patients/[patientId]/components/document-timeline.tsx</files>
  <read_first>
    - src/app/(vault)/patients/[patientId]/components/document-timeline.tsx
    - src/lib/documents/model.ts
    - src/lib/appointments/repository.ts
  </read_first>
  <action>
    1. O componente `DocumentTimeline` é Client Component ("use client"). Como ele recebe `documents: PracticeDocument[]` e cada documento tem `appointmentId: string | null`, precisamos buscar os dados do atendimento. Como é Client Component, temos duas opções:
       - **Opção A (escolhida):** Passar `appointments: Record<string, Appointment>` como nova prop do Server Component pai (`PatientDocumentosTab` → `DocumentTimeline`).
       - **Opção B:** Fazer fetch no client. Não escolher — evita waterfall e mantém dados já carregados.

    2. Adicionar prop opcional à interface:
       ```ts
       interface DocumentTimelineProps {
         documents: PracticeDocument[];
         patientId: string;
         patientName: string;
         appointments?: Record<string, { startsAt: Date; careMode: AppointmentCareMode }>;
       }
       ```

    3. Criar componente `AppointmentBadge` dentro do mesmo arquivo:
       ```tsx
       function AppointmentBadge({ doc, appointments }: { doc: PracticeDocument; appointments?: Record<string, { startsAt: Date; careMode: AppointmentCareMode }> }) {
         if (!doc.appointmentId) return null;
         const appt = appointments?.[doc.appointmentId];
         if (!appt) {
           return (
             <span style={{ fontSize: "0.72rem", color: "var(--color-text-4)", fontWeight: 500 }}>
               Atendimento removido
             </span>
           );
         }
         const dateLabel = shortDateFormatter.format(appt.startsAt);
         const timeLabel = timeFormatter.format(appt.startsAt);
         return (
           <Link
             href={`/agenda`} // deep-link por dia será Fase 41; por agora, agenda geral
             style={{ fontSize: "0.72rem", color: "var(--color-text-3)", fontWeight: 500, textDecoration: "none" }}
             title={`Ver atendimento de ${dateLabel}, ${timeLabel}`}
           >
             Atendimento de {dateLabel}, {timeLabel}
           </Link>
         );
       }
       ```

    4. Renderizar `AppointmentBadge` no header de cada item da timeline, ao lado do `StatusBadge`:
       - Posição: após `StatusBadge`, separado por `·` (middot)
       - Estilo: discreto, não competir com status badge (cor text-3, sem background)

    5. No `PatientDocumentosTab` (arquivo separado, mas este plano NÃO o modifica diretamente — a prop será passada pelo executor se necessário, ou fica como responsabilidade do Tab):
       - Na verdade, `PatientDocumentosTab` recebe `documents` e passa para `DocumentTimeline`. Precisamos também passar `appointments`.
       - Como `PatientDocumentosTab` é Client Component (verificar), ele precisa receber os appointments do Server Component (`page.tsx`).
       - **Ação:** Modificar `src/app/(vault)/patients/[patientId]/components/patient-documentos-tab.tsx` (se existir) para receber e passar `appointments`. Se não existir como arquivo separado, modificar `PatientProfileTabs` para passar a prop.

    6. Para buscar os appointments no Server Component `page.tsx`:
       - Já existe `appointments = await appointmentRepo.listByPatient(patient.id, workspaceId)`
       - Criar um map: `const appointmentMap = Object.fromEntries(appointments.map(a => [a.id, { startsAt: a.startsAt, careMode: a.careMode }]))`
       - Passar para `PatientProfileTabs` como prop `appointments={appointmentMap}`

    7. Modificar `PatientProfileTabs` para receber e repassar `appointments` para `PatientDocumentosTab`

    8. Modificar `PatientDocumentosTab` para receber e repassar `appointments` para `DocumentTimeline`
  </action>
  <verify>
    <automated>grep -n "AppointmentBadge\|appointmentId\|appointments" src/app/(vault)/patients/[patientId]/components/document-timeline.tsx</automated>
  </verify>
  <done>
    - DocumentTimeline exibe badge "Atendimento de {date}, {time}" quando doc.appointmentId presente e appointment existe no map
    - Quando appointment não existe no map, exibe "Atendimento removido" em cor neutra
    - Badge é clicável (Link para /agenda)
    - Badge estilizado discretamente (0.72rem, text-3, sem background)
  </done>
</task>

<task type="auto">
  <name>Task 2: Document view page exibe meta-info de atendimento acima do A4</name>
  <files>src/app/(vault)/patients/[patientId]/documents/[documentId]/page.tsx</files>
  <read_first>
    - src/app/(vault)/patients/[patientId]/documents/[documentId]/page.tsx
    - src/lib/appointments/repository.ts
    - src/lib/appointments/model.ts
  </read_first>
  <action>
    1. Após buscar `doc`, verificar `doc.appointmentId`:
       ```ts
       let appointment: Appointment | null = null;
       if (doc.appointmentId) {
         const apptRepo = getAppointmentRepository();
         appointment = await apptRepo.findById(doc.appointmentId, workspaceId);
       }
       ```

    2. Criar componente inline `AppointmentMeta` acima do `DocumentPaperScaler`:
       ```tsx
       {appointment && (
         <div style={metaStyle}>
           <span style={metaLabelStyle}>Atendimento de{" "}</span>
           <span style={metaValueStyle}>
             {new Intl.DateTimeFormat("pt-BR", {
               day: "2-digit", month: "2-digit", year: "numeric",
               hour: "2-digit", minute: "2-digit", timeZone: "UTC",
             }).format(appointment.startsAt)}
             {" · "}
             {appointment.careMode === "IN_PERSON" ? "Presencial" : "Online"}
           </span>
         </div>
       )}
       {doc.appointmentId && !appointment && (
         <div style={metaStyle}>
           <span style={metaRemovedStyle}>Atendimento removido</span>
         </div>
       )}
       ```

    3. Posicionar este bloco entre o breadcrumb e o `DocumentPaperScaler`:
       ```tsx
       {/* Breadcrumb */}
       <nav>...</nav>

       {/* Appointment meta-info */}
       {appointmentMeta}

       {/* A4 Paper View */}
       <DocumentPaperScaler>...</DocumentPaperScaler>
       ```

    4. Estilos do meta-info (fora do papel A4, discreto):
       ```ts
       const metaStyle: React.CSSProperties = {
         padding: "0.5rem 0.75rem",
         background: "rgba(248, 246, 243, 0.6)",
         borderRadius: "var(--radius-md)",
         border: "1px solid rgba(146, 64, 14, 0.08)",
         fontSize: "0.78rem",
       };
       const metaLabelStyle: React.CSSProperties = {
         color: "var(--color-text-3)",
         fontWeight: 500,
       };
       const metaValueStyle: React.CSSProperties = {
         color: "var(--color-text-2)",
         fontWeight: 600,
       };
       const metaRemovedStyle: React.CSSProperties = {
         color: "var(--color-text-4)",
         fontWeight: 500,
         fontStyle: "italic",
       };
       ```

    5. Garantir que a meta-info NÃO está dentro de `DocumentPaperScaler` (não deve aparecer no PDF)
  </action>
  <verify>
    <automated>grep -n "appointmentId\|appointment\|Atendimento de\|metaStyle" src/app/(vault)/patients/[patientId]/documents/[documentId]/page.tsx</automated>
  </verify>
  <done>
    - View page busca appointment quando doc.appointmentId presente
    - Meta-info renderizada entre breadcrumb e DocumentPaperScaler
    - Meta-info mostra data, hora e modalidade do atendimento
    - Quando appointment removido, mostra "Atendimento removido" em estilo itálico apagado
    - Meta-info NÃO está dentro do DocumentPaperView (não aparece no PDF)
  </done>
</task>

<task type="auto">
  <name>Task 3: Agenda exibe quick action "Criar declaração" em atendimentos COMPLETED</name>
  <files>src/app/(vault)/agenda/page.tsx</files>
  <read_first>
    - src/app/(vault)/agenda/page.tsx
    - src/app/(vault)/agenda/components/completed-appointment-next-session-action.tsx
    - src/lib/documents/model.ts
  </read_first>
  <action>
    1. No loop que monta `nextSessionActions[appt.id]` para appointments COMPLETED (linha ~360), adicionar um novo Link antes do `CompletedAppointmentNextSessionAction`:
       ```tsx
       <Link
         href={`/patients/${appt.patientId}/documents/new?type=declaration_of_attendance&appointmentId=${appt.id}&from=/agenda`}
         style={createDocStyle}
       >
         Criar declaração
       </Link>
       ```

    2. Adicionar também um link para "Criar nota" (session_record):
       ```tsx
       <Link
         href={`/patients/${appt.patientId}/documents/new?type=session_record&appointmentId=${appt.id}&from=/agenda`}
         style={createDocStyleSecondary}
       >
         Criar nota
       </Link>
       ```

    3. Estilos:
       ```ts
       const createDocStyle: React.CSSProperties = {
         display: "inline-flex",
         alignItems: "center",
         gap: "0.4rem",
         padding: "0.48rem 1rem",
         borderRadius: "var(--radius-md)",
         background: "var(--color-accent)",
         color: "var(--color-surface-0)",
         textDecoration: "none",
         fontWeight: 600,
         fontSize: "0.85rem",
       };
       const createDocStyleSecondary: React.CSSProperties = {
         display: "inline-flex",
         alignItems: "center",
         gap: "0.4rem",
         padding: "0.48rem 1rem",
         borderRadius: "var(--radius-md)",
         background: "rgba(255, 247, 237, 0.9)",
         border: "1px solid rgba(146, 64, 14, 0.2)",
         color: "var(--color-warning-text)",
         textDecoration: "none",
         fontWeight: 600,
         fontSize: "0.85rem",
       };
       ```

    4. Garantir que os links incluem:
       - `type` correto (`declaration_of_attendance` ou `session_record`)
       - `appointmentId` do atendimento
       - `from=/agenda` para navegação de volta
       - `patientId` correto na rota

    5. Verificar se `Link` está importado no topo do arquivo (já está, via Next.js Link)
  </action>
  <verify>
    <automated>grep -n "Criar declaração\|Criar nota\|declaration_of_attendance\|session_record\|from=/agenda" src/app/(vault)/agenda/page.tsx</automated>
  </verify>
  <done>
    - Atendimentos COMPLETED na agenda exibem link "Criar declaração" (primário) e "Criar nota" (secundário)
    - Links levam para `/patients/[id]/documents/new` com type, appointmentId e from=/agenda
    - Links aparecem na seção nextSessionActions do card
    - Estilos consistentes com existing action links
  </done>
</task>

<task type="auto">
  <name>Task 4: Passar appointments map da page até DocumentTimeline</name>
  <files>
    - src/app/(vault)/patients/[patientId]/page.tsx
    - src/app/(vault)/patients/[patientId]/components/patient-profile-tabs.tsx
    - src/app/(vault)/patients/[patientId]/components/patient-documentos-tab.tsx
  </files>
  <read_first>
    - src/app/(vault)/patients/[patientId]/page.tsx
    - src/app/(vault)/patients/[patientId]/components/patient-profile-tabs.tsx
    - src/app/(vault)/patients/[patientId]/components/patient-documentos-tab.tsx
    - src/app/(vault)/patients/[patientId]/components/document-timeline.tsx
  </read_first>
  <action>
    1. Em `src/app/(vault)/patients/[patientId]/page.tsx`:
       - Já existe `appointments = await appointmentRepo.listByPatient(patient.id, workspaceId)`
       - Criar map simplificado:
         ```ts
         const appointmentMap = Object.fromEntries(
           appointments.map((a) => [
             a.id,
             { startsAt: a.startsAt, careMode: a.careMode },
           ]),
         );
         ```
       - Passar para `PatientProfileTabs`: `appointmentMap={appointmentMap}`

    2. Em `src/app/(vault)/patients/[patientId]/components/patient-profile-tabs.tsx`:
       - Adicionar `appointmentMap?: Record<string, { startsAt: Date; careMode: AppointmentCareMode }>` à interface
       - Repassar para `PatientDocumentosTab`: `appointmentMap={appointmentMap}`

    3. Em `src/app/(vault)/patients/[patientId]/components/patient-documentos-tab.tsx`:
       - Adicionar `appointmentMap` à interface
       - Repassar para `DocumentTimeline`: `appointments={appointmentMap}`

    4. Se `patient-documentos-tab.tsx` não existir (DocumentTimeline é usado diretamente em outro lugar), verificar e adaptar.
  </action>
  <verify>
    <automated>grep -n "appointmentMap\|appointments" src/app/(vault)/patients/[patientId]/page.tsx src/app/(vault)/patients/[patientId]/components/patient-profile-tabs.tsx src/app/(vault)/patients/[patientId]/components/patient-documentos-tab.tsx 2>/dev/null</automated>
  </verify>
  <done>
    - page.tsx cria appointmentMap e passa para PatientProfileTabs
    - PatientProfileTabs repassa para PatientDocumentosTab
    - PatientDocumentosTab repassa para DocumentTimeline como prop `appointments`
    - TypeScript compila sem erros
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Server Component | searchParams e params são input não-confiável |
| Server → Database | Appointment lookup cruz boundary de persistência |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-07 | Information Disclosure | document-timeline.tsx | mitigate | Appointment data é passada via prop do Server Component (já filtrada por workspace); nunca fetched no client por ID arbitrário |
| T-40-08 | Information Disclosure | [documentId]/page.tsx | mitigate | `findById(doc.appointmentId, workspaceId)` garante workspace scoping |
| T-40-09 | Tampering | agenda/page.tsx | accept | Links da agenda são hardcoded com patientId/appt.id do próprio loop; não há input do usuário |
| T-40-10 | Repudiation | agenda/page.tsx | accept | Quick action é apenas um Link; audit trail da criação do documento cobre a ação |
</threat_model>

<verification>
- `pnpm test` passa (453 testes)
- `pnpm build` passa sem erros TypeScript
- Timeline mostra badge de atendimento para documentos vinculados
- View page mostra meta-info acima do papel A4
- Agenda mostra "Criar declaração" em atendimentos completed
</verification>

<success_criteria>
- DocumentTimeline exibe badge discreto com data/hora do atendimento quando vinculado
- Badge é clicável e leva para /agenda
- "Atendimento removido" aparece em cor neutra quando appointment foi deletado
- Document view page mostra meta-info do atendimento acima da área A4
- Meta-info NÃO aparece no conteúdo do documento (não vai para PDF)
- Agenda exibe quick actions "Criar declaração" e "Criar nota" em atendimentos COMPLETED
- Links da agenda incluem appointmentId e from=/agenda
</success_criteria>

<output>
After completion, create `.planning/phases/40-integracao-atendimentos/40-02-SUMMARY.md`
</output>
