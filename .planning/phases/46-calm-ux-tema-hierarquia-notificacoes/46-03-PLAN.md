---
phase: 46-calm-ux-tema-hierarquia-notificacoes
plan: 03
type: execute
wave: 3
depends_on:
  - 46-01
  - 46-02
files_modified:
  - package.json
  - tests/axe-wcag-audit.test.ts
  - src/app/globals.css
autonomous: true
requirements:
  - CALM-05
user_setup: []
must_haves:
  truths:
    - "axe-core runs automated WCAG 2.1 AA audit on both light and dark themes"
    - "Hover states pass contrast requirements in both themes"
    - "Placeholder text passes contrast requirements in both themes"
    - "Disabled elements pass contrast requirements in both themes"
  artifacts:
    - path: "tests/axe-wcag-audit.test.ts"
      provides: "Automated WCAG audit for both themes"
      min_lines: 50
    - path: "package.json"
      provides: "axe-core devDependency"
      contains: "axe-core"
    - path: "src/app/globals.css"
      provides: "Fixed contrast violations"
      pattern: "contrast|color"
  key_links:
    - from: "tests/axe-wcag-audit.test.ts"
      to: "src/app/globals.css"
      via: "CSS variable values under test"
    - from: "tests/axe-wcag-audit.test.ts"
      to: "axe-core"
      via: "npm dependency"
---

<objective>
Implementar auditoria automatizada WCAG 2.1 AA com axe-core em ambos os temas (light e dark), garantindo que hover states, placeholders e elementos desabilitados atendam aos critérios de contraste.

Purpose: Garantir acessibilidade visual consistente independentemente do tema escolhido.
Output: Testes axe-core automatizados para ambos os temas, correções de CSS aplicadas.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-calm-ux-tema-hierarquia-notificacoes/46-CONTEXT.md

## Key decisions from CONTEXT.md (MUST implement)
- **D-11:** WCAG 2.1 AA audit covers the entire app, not just components affected by this phase
- **D-12:** Automated with axe-core run on both themes (light and dark)
- **D-13:** Priority to hover states, placeholders, and disabled elements mentioned in CALM-05
- axe-core must run on both themes by alternating `data-theme`

## Interface context

From `src/app/globals.css` (theme transition):
```css
html[data-theme="dark"],
html[data-theme="dark"] body,
html[data-theme="dark"] * {
  transition-property: background-color, border-color, color, fill, stroke, box-shadow;
  transition-duration: 200ms;
  transition-timing-function: ease;
}
```

From `DESIGN.md` (contrast table):
```
| Papel | Token | Light | Dark | Contraste |
|-------|-------|-------|------|-----------|
| Primário | --color-text-1 | #1c1917 | #f0ebe2 | AAA |
| Secundário | --color-text-2 | #57534e | #b8b0a4 | AA |
| Terciário | --color-text-3 | #57534e | #a8a095 | AA |
| Quaternário | --color-text-4 | #78716c | #8a8278 | AA |
```

From `vitest.config.ts`:
```typescript
export default defineConfig({
  test: {
    environment: "node",
    include: ["tests/**/*.test.ts", "tests/**/*.test.tsx"],
    globals: true,
    setupFiles: ["tests/setup.ts"],
  },
});
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install axe-core and create dual-theme test helper</name>
  <files>package.json, tests/axe-wcag-audit.test.ts, tests/setup.ts</files>
  <read_first>
    - package.json
    - vitest.config.ts
    - tests/setup.ts
    - src/app/globals.css (for understanding CSS structure)
  </read_first>
  <action>
    1. Add `axe-core` as devDependency in `package.json` (use exact latest stable version, e.g. `"axe-core": "^4.10.0"`).
    2. Create `tests/axe-wcag-audit.test.ts` with:
       - Helper `runAxeOnHtml(html: string, theme: "light" | "dark")` that:
         a. Creates a JSDOM document from the HTML string
         b. Sets `document.documentElement.setAttribute("data-theme", theme)`
         c. Runs `axe.run(document)` with rules focused on color-contrast
         d. Returns violations array
       - Test cases covering critical UI patterns:
         * Button hover states (simulate hover class)
         * Input placeholders
         * Disabled buttons and inputs
         * Sidebar nav links
         * Notification badge
       - Each test runs twice: once with `data-theme="light"`, once with `data-theme="dark"`
       - Use `expect(violations).toHaveLength(0)` or filter to only `color-contrast` rule violations
    3. If `jsdom` environment doesn't support `axe.run()` directly, mock or use `@testing-library/jest-dom` patterns. If axe-core requires a real DOM, consider using `jsdom` + manual element creation.
    4. Update `tests/setup.ts` if needed to import axe-core accessibility matchers (optional).
  </action>
  <verify>
    <automated>grep -n "axe-core" package.json</automated>
    <automated>test -f tests/axe-wcag-audit.test.ts && echo "EXISTS" || echo "MISSING"</automated>
    <automated>grep -n "runAxeOnHtml\|data-theme" tests/axe-wcag-audit.test.ts | head -20</automated>
  </verify>
  <done>
    - axe-core is in devDependencies
    - Test file exists with dual-theme helper
    - Tests cover hover states, placeholders, disabled elements
    - pnpm install completed
  </done>
</task>

<task type="auto">
  <name>Task 2: Run audit and fix contrast violations</name>
  <files>tests/axe-wcag-audit.test.ts, src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (focus on dark mode color overrides and hover states)
    - DESIGN.md §2 (Paleta e Papéis Semânticos)
  </read_first>
  <action>
    1. Run the audit: `pnpm test tests/axe-wcag-audit.test.ts`
    2. For each violation found:
       - Identify the CSS selector and property causing the failure
       - Cross-reference with DESIGN.md tokens to find the correct replacement
       - Apply the fix in `src/app/globals.css` (or component inline styles if appropriate)
       - Common issues to check:
         * Hover states on dark mode: `html[data-theme="dark"] .nav-link:hover` uses `rgba(247,243,237,0.06)` which might be too subtle
         * Placeholder colors in dark mode: ensure `--color-text-4` in dark is at least AA against input background
         * Disabled elements: ensure disabled buttons/inputs have sufficient contrast in both themes
         * Focus rings: `rgba(154,52,18,0.25)` in light and `rgba(194,106,74,0.35)` in dark
    3. Re-run tests after each fix batch until all tests pass
    4. Run full test suite `pnpm test` to ensure no regressions
    5. Run `pnpm build` to ensure no TypeScript errors
  </action>
  <verify>
    <automated>pnpm test tests/axe-wcag-audit.test.ts --reporter=verbose 2>&1 | tail -30</automated>
    <automated>pnpm test 2>&1 | tail -10</automated>
    <automated>pnpm build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - All axe-core tests pass in both light and dark themes
    - Full test suite passes with no regressions
    - Build succeeds
    - CSS fixes are minimal and token-aligned per DESIGN.md
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test→CSS | Tests validate visual contrast but do not affect runtime security |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-07 | Information Disclosure | axe-core test output | accept | Test output is local/dev only; no sensitive data exposed |
| T-46-08 | Denial of Service | axe-core slows test suite | accept | Tests are dev-only; can be run selectively if needed |
</threat_model>

<verification>
1. `pnpm test tests/axe-wcag-audit.test.ts` — all tests pass
2. `pnpm test` — full suite passes with no regressions
3. `pnpm build` — succeeds without TypeScript errors
4. axe-core tests cover both `data-theme="light"` and `data-theme="dark"`
</verification>

<success_criteria>
- axe-core installed and configured
- Automated WCAG 2.1 AA tests exist for both themes
- Hover states, placeholders, and disabled elements pass contrast checks
- Any violations found are fixed in globals.css using DESIGN.md tokens
- Full test suite and build remain green
</success_criteria>

<output>
After completion, create `.planning/phases/46-calm-ux-tema-hierarquia-notificacoes/46-03-SUMMARY.md`
</output>
