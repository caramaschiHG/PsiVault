describe("audit contract baseline", () => {
  it("keeps audit coverage reserved in Wave 0", () => {
    expect(true).toBe(true);
  });

  it.todo("audit event contract");
  it.todo("privacy-safe surfaces");
});
